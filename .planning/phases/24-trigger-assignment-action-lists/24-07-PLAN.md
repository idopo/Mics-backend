---
phase: 24-trigger-assignment-action-lists
plan: 07
type: execute
wave: 4
depends_on: [06, 08]
files_modified:
  - .planning/phases/24-trigger-assignment-action-lists/24-07-SUMMARY.md
autonomous: false
requirements: [TRIGA-11a]

must_haves:
  truths:
    - "A researcher assembles the detector write in the task editor on a sourceless toolkit, saves it, reloads the page, and gets back exactly what they built"
    - "An invalid trigger assignment is rejected at save time with a readable message, not at session start"
    - "Touching each of the four MPR121 electrodes updates the matching LICKER{n} view tracker, every write carrying a pi_timestamp"
    - "Touching electrode 2 never moves LICKER0, LICKER1 or LICKER3"
    - "None of this requires any Python callback, any learning_cage, or any detector-specific code in the Pi trigger runtime"
  artifacts:
    - path: ".planning/phases/24-trigger-assignment-action-lists/24-07-SUMMARY.md"
      provides: "the rig evidence: run id, per-electrode ES counts, the cross-talk window result, the 8 negative-case responses, and the stored FDA JSON that produced them"
      min_lines: 60
  key_links:
    - from: "task editor (DetectorWriteWidget)"
      to: "task_definitions.fda_json"
      via: "autosave PUT through the existing debounced path, 422-gated"
      pattern: "source_ref"
    - from: "task_definitions.fda_json"
      to: "the Pi's self.triggers['TOUCH_INT']"
      via: "orchestrator dispatch spec -> apply_trigger_assignments -> _build_trigger_action_list"
      pattern: "trigger_assignments"
    - from: "MPR121.detect_change() return"
      to: "event_log_v2 Tracker events for LICKER0..3"
      via: "output capture -> if guard -> view action with a {device_name} template"
      pattern: "LICKER"
---

<objective>
Prove TRIGA-11a on the real rig: lick detection, assembled entirely in the UI, running on a
backend-authored toolkit, writing the right tracker for the right electrode.

Purpose: waves 1–2 were proven on hardware (run 475), but with a stand-in action list — an LED
driven from `{"trigger":"level"}`. This plan proves the real thing: four electrodes, four
trackers, the level taken from `detect_change`'s return, and the cross-talk negative that the
constrained editor affordance exists to guarantee.

The ordering is evidence-backed, not preference: **the UI round-trip checkpoint runs FIRST.**
Five of the seven post-execution defects in waves 1–2 lived in the UI↔API seam and surfaced only
by using the editor — none were catchable by unit tests or `tsc`.

Output: a signed-off rig result recorded in `24-07-SUMMARY.md`, or a defect list.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-trigger-assignment-action-lists/24-CONTEXT.md
@.planning/phases/24-trigger-assignment-action-lists/24-HARDWARE-VALIDATION.md
@.planning/phases/24-trigger-assignment-action-lists/24-06-PLAN.md
@.planning/phases/24-trigger-assignment-action-lists/24-08-PLAN.md
@.claude/skills/pi-deploy/SKILL.md
@CLAUDE.md

<hard_constraints>
1. **NEVER run git in `/home/ido/pi-mirror`** — not even `status`. Byte-identity is proved with
   `diff <(ssh -i ~/.ssh/pi_mics pi@132.77.72.28 'cat ~/Apps/.../f.py') /home/ido/pi-mirror/.../f.py`.
2. **Never start or stop the pilot. Never run Python on the Pi.** Hand the user the command and
   wait for their output. Pi tests run from `~/Apps/mice_interactive_home_cage` **on the Pi**.
3. **This is a verification plan — no source file is edited.** If it uncovers a defect, record
   it and stop. The fix is a separate plan, not an in-flight patch.
4. **`api`/`web_ui` have no bind mounts.** Any container the preflight finds stale must be
   rebuilt with `docker compose up --build -d <svc>`, never plain `up -d`.
5. **Do not assert anything ES cannot show.** `@log_action` records kwargs only, so
   `hardware.set(x)` never logs `x`, and a `set` event's envelope `level` is post-call hardware
   state. Only `Tracker.set` logs a `value` — which is exactly why this checkpoint is
   observable at all. **No acceptance criterion may take the form "confirm the action passed
   value N" against a `hardware` action.**
</hard_constraints>

<environment_facts>
- Live ES: **`132.77.73.217:9200`**, index `event_log_v2`. (`.125` is a different cluster
  holding `restored-event_log_v2` — do not query it.)
- Document shape, verified 2026-07-27 against a real historical lick event:
```json
{"pilot":"...","subject":"...","session":68,"run_id":475,"timestamp":"...",
 "event":{"event_type":"Tracker",
          "event_data":{"id":"LICKER0","value":1,"result":null,
                        "pi_timestamp":"2025-10-22T14:00:55.495279+03:00","func_name":"set"}}}
```
  Query fields: `run_id`, `event.event_type`, `event.event_data.id`, `event.event_data.value`,
  `event.event_data.pi_timestamp`. `event.event_type` is the Python class name (`Tracker`) for
  tracker writes and the hardware group (`Modules`) for hardware calls.
- Pilot `pilot_raspberry_lior` at `132.77.72.28`; live state via
  `curl -s http://localhost:9000/pilots/live`.
- Toolkit **100** `source_less_toolkit`, `hardware_module_ids = [1,2,3,5,6,7,8]`; module 7 =
  `MPR121` (`Touch_Detector`; pilot-1 config `{num_detectors: 4, device_name: "LICKER"}`),
  module 8 = `TOUCH_INT` (`Digital_In`, pin 8, trigger `"B"` = either-edge).
- Known pilot-1 config collisions — harmless here, do not be surprised: `Left_LED`/`Right_LED`
  both pin 11; `Mid_LED`/`Solenoid` both pin 12.
- Task definition **186** holds the run-475 smoke-test list (`MPR121.detect_change` +
  `Mid_LED.set({trigger:"level"})`). Useful as a reference; **not** the artifact under test.
- Placeholders below: `{ID}` = the task definition id from Task 1, `{RUN}` = the run_id from
  Task 3, `{PHASE_B}` = the phase-B start timestamp.
</environment_facts>

<expected_payload>
What the editor must produce in checkpoint 1 and what runs on the rig in checkpoint 3:

```json
{
  "variables": {"pin_number": {}, "level": {}},
  "trigger_assignments": [{
    "trigger_name": "TOUCH_INT",
    "actions": [
      {"type": "hardware", "ref": "MPR121", "method": "detect_change",
       "args": [], "output": ["pin_number", "level"]},
      {"type": "if",
       "condition": {"left": {"flag": "pin_number"}, "op": "!=", "right": null},
       "then": [
         {"type": "view", "key_template": "{device_name}{pin_number}",
          "source_ref": "MPR121", "value": {"flag": "level"}}
       ]}
    ]
  }]
}
```

No `{"trigger":"level"}`. No `kwargs`. **No literal `LICKER` anywhere in the stored JSON** — the
name is resolved on the Pi from the device's own `device_name` (TRIGA-18), which is what keeps
the definition pilot-agnostic.
</expected_payload>

<out_of_scope>
- **Legacy `trigger_assignments` rows in task definitions 181 and 185.** Both are inert
  handler-format entries. **185 belongs to another user (Gili) — ask before touching it**, and
  re-run the query rather than trusting the recorded finding (TRIGA-06's equivalent claim was
  wrong once already). Cleaning them is not part of this proof.
- The duplicate-`set` observation from run 475 (140 `Mid_LED` calls for 47 firings, ~3×). It
  must be understood before real data collection, but it does not block this proof. If the
  LICKER counts show the same multiplier, **record the ratio** — new evidence for a later
  investigation, not a failure of this plan.
- Transitions on `LICKER2` and detector keys in the editor's operand pickers — Phase 25 (DVK).
</out_of_scope>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Checkpoint 1 — UI round-trip on a sourceless toolkit</name>
  <files>none (checkpoint — no file is edited by this task)</files>
  <action>
Agent side, before pausing: run every command in `<verify>` below and report the results as part
of presenting the checkpoint. Do not present the checkpoint against stale code — if any md5
differs, `i2c.py` is not identical, the pilot is disconnected, `trigger_sources`/`detector_refs`
are absent from `GET /api/toolkits/100`, or a container predates plan 24-08, resolve that first
(`docker compose up --build -d api web_ui`; ask the user to restart the pilot). Then hand over
the numbered steps in `<how-to-verify>` and wait.
  </action>
  <what-built>
Plans 24-06 and 24-08 are complete: the Pi discovers detectors by capability and resolves
`{device_name}` at run time; the editor offers a `trigger_name` dropdown, declared variables in
the operand pickers, and a one-pick detector write; the backend 422s a method-less hardware
action.

Before pausing, the agent runs the readiness commands in `<verify>` and reports: the Pi is
running 24-06's `mics_task.py`/`fda_vocabulary.py` (md5 match), `i2c.py` is byte-identical to
the mirror, the pilot is connected, `GET /api/toolkits/100` returns the derived
`trigger_sources`/`detector_refs`, and `api`/`web_ui` are serving rebuilt images. If any of
those fail, fix that first — this checkpoint is meaningless against stale code.
  </what-built>
  <how-to-verify>
In the browser, on the task editor for toolkit 100 (`source_less_toolkit`):

1. Create a NEW task definition on toolkit 100 named `TRIGA-11a lick proof`. Use a new one, not
   186 — the round-trip is part of what is being proven.
2. In the trigger panel, add an assignment. **Trigger name** must be a dropdown, not a text
   box. Confirm it lists `TOUCH_INT` under **Inputs** and the four LED/solenoid devices under
   **Outputs** (`Digital_Out` really does set `is_trigger`, and `init_hardware` really does call
   `assign_cb` on them — the list is truthful, not filtered). `MPR121` and `TIMER` must NOT
   appear. Pick `TOUCH_INT`.
3. Use the **Read detector** control. It must offer exactly one pick — the device (`MPR121`) —
   and no key template, no pin field, no value field. Select `MPR121`.
4. Confirm `pin_number` and `level` now appear in the Variables panel (the widget declares them;
   a hardware action's `output` slot must be declared or the save 422s).
5. Confirm the raw action editor is still reachable (`Edit as raw actions`), and that what it
   shows is two ordinary actions — a `hardware` call and an `if` wrapping a `view` — nothing
   detector-specific.
6. Wait for the autosave status line to show saved. Then **hard-reload the page**
   (Ctrl-Shift-R) and confirm the assignment comes back identical: same trigger, same device,
   still rendered by the one-pick widget.
7. **Negative, in the UI:** open any hardware action (state body or trigger), clear its
   **method**, and try to save. Confirm you get a readable error naming the device — not
   `[object Object]`, and not a silent 200.
8. Report the task definition **id** (from the URL) and paste the status-line text from step 7.
  </how-to-verify>
  <verify>
    <automated>ssh -i ~/.ssh/pi_mics pi@132.77.72.28 'cd ~/Apps/mice_interactive_home_cage &amp;&amp; md5sum autopilot/autopilot/tasks/mics_task.py autopilot/autopilot/tasks/fda_vocabulary.py' &amp;&amp; cd /home/ido/pi-mirror &amp;&amp; md5sum autopilot/autopilot/tasks/mics_task.py autopilot/autopilot/tasks/fda_vocabulary.py</automated>
    <automated>diff &lt;(ssh -i ~/.ssh/pi_mics pi@132.77.72.28 'cat ~/Apps/mice_interactive_home_cage/autopilot/autopilot/hardware/i2c.py') /home/ido/pi-mirror/autopilot/autopilot/hardware/i2c.py &amp;&amp; echo I2C_IDENTICAL</automated>
    <automated>curl -s http://localhost:9000/pilots/live | python3 -c "import sys,json; d=json.load(sys.stdin); p=d.get('pilot_raspberry_lior',{}); print('connected',p.get('connected'),'state',p.get('state'))"</automated>
    <automated>curl -s -H "Authorization: Bearer $MICS_API_TOKEN" http://localhost:8000/api/toolkits/100 | python3 -c "import sys,json; d=json.load(sys.stdin); print('TRIGGERS',[t['hw_id'] for t in d.get('trigger_sources',[])]); print('DETECTORS',d.get('detector_refs'))"</automated>
    <automated>docker ps --format '{{.Names}} {{.Status}}' | grep -E 'mics_api|mics_web_ui'</automated>
  </verify>
  <resume-signal>Reply with the new task definition id and the step-7 error text, then type "approved" — or describe what went wrong.</resume-signal>
  <done>Preflight is green (Pi md5s match, `i2c.py` identical, pilot connected, derived `trigger_sources`/`detector_refs` served, containers current) and the user has confirmed the round-trip: dropdown trigger pick, one-pick detector write, variables auto-declared, identical after hard reload, and a readable save-time error for a method-less action. The task definition id is recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Checkpoint 2 — the saved JSON is canonical, and invalid ones 422</name>
  <files>none (verification only)</files>
  <action>
**2a — read back what the editor actually stored** for `{ID}`:
```bash
docker exec mics_db psql -U mics_user -d mics_db -t -A -c \
  "SELECT jsonb_pretty(fda_json) FROM task_definitions WHERE id = {ID};"
```
Assert, field by field, against `<expected_payload>`:
- `trigger_assignments[0].trigger_name == "TOUCH_INT"`.
- action[0]: `type=hardware`, `ref=MPR121`, `method=detect_change`,
  `output == ["pin_number","level"]`.
- action[1]: `type=if`, condition `{"flag":"pin_number"} != null`, whose single `then` entry is
  `type=view` with `key_template == "{device_name}{pin_number}"`, `source_ref == "MPR121"`,
  `value == {"flag":"level"}`.
- `variables` declares `pin_number` and `level`.
- **The string `LICKER` appears nowhere in the stored JSON**, and neither does
  `{"trigger": "level"}`. Both greps are load-bearing: the first proves the definition is
  pilot-agnostic (TRIGA-18), the second that the level comes from the capture (TRIGA-19).
- No `handler` and no `config` key anywhere.

**2b — the negative suite.** Against toolkit 100, POST each of the following to
`/api/task-definitions` and record the status and `detail.errors[0]`. Every one must be **422**:
1. `trigger_name: "TOCH_INT"` (typo) with an otherwise valid action list — the silent-no-op case.
2. `trigger_name: ""`.
3. A trigger assignment with `actions: []`.
4. `{"type":"hardware","ref":"MPR121","method":""}` — the TRIGA-16 case.
5. `{"type":"hardware","ref":"NOPE","method":"set"}` — unknown ref.
6. A `view` action with `key_template: "{device_name}{pin_number}"` and **no** `source_ref`.
7. `output: ["pin_number","level"]` with `variables: {}` — undeclared slots.
8. A legacy `{"handler":"touch_detector","trigger_name":"TOUCH_INT"}` entry with no `actions`.

Then POST the full `<expected_payload>` and assert **201**. Delete every scratch definition this
task created (`DELETE /api/task-definitions/{id}`) and confirm they are gone — do not leave
probes in a researcher's list. **Do not touch definitions 181, 185, 186, or `{ID}`.**

**2c — record** the 8 status codes and their messages verbatim in the summary. A 422 whose
message is unreadable is itself a defect: defect 3 in waves 1–2 was exactly that
(`[object Object]`, because `apiFetch` assigned `json.detail` straight to `new Error()`).
  </action>
  <verify>
    <automated>docker exec mics_db psql -U mics_user -d mics_db -t -A -c "SELECT fda_json::text FROM task_definitions WHERE id = {ID};" &gt; /tmp/claude-1000/proof_fda.json; python3 -c "
import json
s=open('/tmp/claude-1000/proof_fda.json').read()
d=json.loads(s)
ta=d['trigger_assignments'][0]; a0,a1=ta['actions'][0],ta['actions'][1]; v=a1['then'][0]
assert ta['trigger_name']=='TOUCH_INT', ta
assert a0['method']=='detect_change' and a0['output']==['pin_number','level'], a0
assert v['type']=='view' and v['key_template']=='{device_name}{pin_number}', v
assert v['source_ref']=='MPR121' and v['value']=={'flag':'level'}, v
assert 'LICKER' not in s, 'pilot-specific name baked in'
assert 'trigger' not in json.dumps(a1), 'trigger context used for the level'
assert {'pin_number','level'} &lt;= set(d.get('variables',{})), d.get('variables')
assert 'handler' not in s and 'config' not in s
print('CANONICAL_OK')"</automated>
    <automated>docker exec mics_db psql -U mics_user -d mics_db -t -A -c "SELECT id, display_name FROM task_definitions WHERE display_name ILIKE '%probe%';"</automated>
  </verify>
  <done>The stored JSON matches `&lt;expected_payload&gt;` field for field, with no `LICKER` literal and no `{"trigger":"level"}`; all 8 negative cases return 422 with a human-readable message; the canonical payload returns 201; every scratch definition is deleted and 181/185/186 are untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint 3 — four electrodes on the rig</name>
  <files>none (checkpoint — no file is edited by this task)</files>
  <action>
Agent side: confirm the pilot is still connected and report the current run list, then hand over
the numbered steps in `<how-to-verify>` and wait. The agent does **not** restart the pilot, start
the session, or run any Python on the Pi. Record the user's reported `run_id`, phase-B start
time, and phase-A electrode order verbatim — Task 4's queries are parameterised on all three.
  </action>
  <what-built>
The task definition from Task 1 is stored in canonical form and the negative gate is proven. The
Pi is running capability-based detector discovery, so `LICKER0…LICKER3` now exist in the view of
a sourceless task, and the `view` action resolves `{device_name}` from the MPR121 object's own
`device_name` (`"LICKER"` on pilot 1).

The agent cannot start a session, restart the pilot, or run Python on the Pi. This step is
yours.
  </what-built>
  <how-to-verify>
1. If the pilot has not been restarted since 24-06 was deployed, restart it now. (The agent has
   confirmed the md5s match, but a running process holds the old module in memory.)
2. Point a protocol step at the `TRIGA-11a lick proof` task definition and start a session on
   `pilot_raspberry_lior` — the same route used for run 475.
3. **Phase A — one touch per electrode, in order, ~5 s apart:** electrode 0, then 1, then 2,
   then 3. Note the order actually used if it differs.
4. Pause ~10 s.
5. **Phase B — the cross-talk negative:** note the wall-clock time, then touch **only electrode
   2**, five times over roughly 15 s. Touch nothing else.
6. Wait ~10 s, then stop the run.
7. Report: the **run_id**, the phase-B start time (HH:MM:SS local), and the phase-A electrode
   order you actually used.

If nothing appears at all, do not retry blindly — say so, and the agent will first check whether
`TOUCH_INT` fired (a `Modules` event with `id: TOUCH_INT`, dispatched unconditionally and
independently of the callbacks). That distinguishes "the trigger never fired" from "the trigger
fired and the action list did nothing" — different defects, different fixes.
  </how-to-verify>
  <verify>
    <automated>curl -s http://localhost:9000/pilots/live | python3 -c "import sys,json; d=json.load(sys.stdin); p=d.get('pilot_raspberry_lior',{}); print('connected',p.get('connected'),'state',p.get('state'),'active_run',p.get('active_run'))"</automated>
  </verify>
  <resume-signal>Reply with run_id, phase-B start time, and the phase-A electrode order — or describe what happened instead.</resume-signal>
  <done>The user has run the session and reported `run_id`, the phase-B start time, and the phase-A electrode order; all three are recorded for Task 4. The agent started/stopped nothing on the Pi.</done>
</task>

<task type="auto">
  <name>Task 4: verify the run in ES — per-electrode writes and the cross-talk negative</name>
  <files>none (verification only)</files>
  <action>
**4a — did the trigger fire at all** (diagnostic baseline, run this first):
```bash
curl -s "http://132.77.73.217:9200/event_log_v2/_search" -H 'Content-Type: application/json' -d '
{"size":0,"query":{"term":{"run_id":{RUN}}},
 "aggs":{"types":{"terms":{"field":"event.event_type.keyword","size":20}},
         "ids":{"terms":{"field":"event.event_data.id.keyword","size":20}}}}'
```
Expect a `TOUCH_INT` id bucket (the unconditional `Hardware_Event` dispatch) and a `Tracker`
type bucket. If `TOUCH_INT` is present but `Tracker` is not, the trigger fired and the action
list did nothing — record that distinction; it is the single most useful diagnostic here.

**4b — per-electrode writes (the positive).** For each of `LICKER0`…`LICKER3`: at least one
document with `event.event_type == "Tracker"`, `event.event_data.func_name == "set"`, a `value`
in `{0,1}`, and a **non-null `event.event_data.pi_timestamp`**. The timestamp is what proves the
trigger's tick reached the write silently — no UI control, no JSON kwarg.

Report the ordered `(id, value, pi_timestamp)` sequence for phase A and confirm the ids follow
the electrode order the user reported. A mismatch between the touched electrode and the written
tracker is exactly the failure the constrained widget exists to prevent — a blocking defect, not
a rounding error.

**4c — the cross-talk negative (the point of the checkpoint).** Restrict to
`timestamp >= {PHASE_B}` and assert:
- `LICKER2` document count ≥ 3, **and**
- `LICKER0` + `LICKER1` + `LICKER3` document count == **0**.

State it in the summary as an explicit pass/fail line. It must be asserted, not assumed.

**4d — record, do not chase:** the `Tracker` document count per electrode versus the number of
physical touches. Run 475 showed ~3 hardware calls per trigger pair; if the same multiplier
appears here it is new evidence for the deferred duplicate-`set` question
(`24-CONTEXT.md` &lt;deferred&gt;), not a failure of this plan. Write down the ratio and move on.

**4e — closing gates:**
- `diff` `i2c.py` and `task.py` against the Pi via SSH (never git in `pi-mirror`) → identical.
- `grep -c "Touch_Detector" /home/ido/pi-mirror/autopilot/autopilot/tasks/mics_task.py` → `0`.
- The run's `task_type` is `mics_task` and its toolkit is backend-authored — no `learning_cage`
  involvement. There is deliberately **no** grep for "no Python callback registered for
  TOUCH_INT": TRIGA-11 was dropped as vacuous on a sourceless toolkit, which never had one.
  </action>
  <verify>
    <automated>curl -s "http://132.77.73.217:9200/event_log_v2/_search" -H 'Content-Type: application/json' -d '{"size":0,"query":{"bool":{"must":[{"term":{"run_id":{RUN}}},{"term":{"event.event_type.keyword":"Tracker"}}]}},"aggs":{"ids":{"terms":{"field":"event.event_data.id.keyword","size":20}}}}' | python3 -m json.tool | tail -30</automated>
    <automated>curl -s "http://132.77.73.217:9200/event_log_v2/_search" -H 'Content-Type: application/json' -d '{"size":50,"_source":["timestamp","event.event_data"],"query":{"bool":{"must":[{"term":{"run_id":{RUN}}},{"term":{"event.event_type.keyword":"Tracker"}},{"prefix":{"event.event_data.id.keyword":"LICKER"}}]}},"sort":[{"timestamp":{"order":"asc"}}]}' | python3 -c "import sys,json; h=json.load(sys.stdin)['hits']['hits']; [print(x['_source']['timestamp'], x['_source']['event']['event_data'].get('id'), x['_source']['event']['event_data'].get('value'), x['_source']['event']['event_data'].get('pi_timestamp')) for x in h]"</automated>
    <automated>curl -s "http://132.77.73.217:9200/event_log_v2/_search" -H 'Content-Type: application/json' -d '{"size":0,"query":{"bool":{"must":[{"term":{"run_id":{RUN}}},{"term":{"event.event_type.keyword":"Tracker"}},{"prefix":{"event.event_data.id.keyword":"LICKER"}},{"range":{"timestamp":{"gte":"{PHASE_B}"}}}]}},"aggs":{"ids":{"terms":{"field":"event.event_data.id.keyword","size":10}}}}' | python3 -m json.tool | tail -25</automated>
    <automated>diff &lt;(ssh -i ~/.ssh/pi_mics pi@132.77.72.28 'cat ~/Apps/mice_interactive_home_cage/autopilot/autopilot/tasks/task.py') /home/ido/pi-mirror/autopilot/autopilot/tasks/task.py &amp;&amp; echo TASK_PY_IDENTICAL</automated>
    <automated>test $(grep -c "Touch_Detector" /home/ido/pi-mirror/autopilot/autopilot/tasks/mics_task.py) -eq 0 &amp;&amp; echo NO_IDENTITY_MATCH</automated>
  </verify>
  <done>All four `LICKER{n}` trackers show at least one `Tracker.set` with a value in {0,1} and a non-null `pi_timestamp`; the phase-A order matches the touch order the user reported; in the phase-B window `LICKER2` has ≥3 documents and `LICKER0/1/3` have exactly 0; the writes-per-touch ratio is recorded; `i2c.py`/`task.py` byte-identical and `Touch_Detector` absent from `mics_task.py`.</done>
</task>

</tasks>

<verification>
1. Preflight (Task 1 `<verify>`): Pi md5s match the mirror, `i2c.py` identical, pilot connected,
   `GET /api/toolkits/100` returns derived `trigger_sources`/`detector_refs`, containers current.
2. Stored FDA for `{ID}` matches `<expected_payload>` exactly; no `LICKER` literal, no
   `{"trigger":"level"}`, no `handler`/`config`.
3. All 8 negative POSTs → 422 with readable messages; the canonical payload → 201; scratch rows
   deleted.
4. Run `{RUN}` in `event_log_v2`: `LICKER0…3` each written with a `pi_timestamp`; phase-A order
   matches the touch order.
5. Cross-talk negative: phase-B window contains `LICKER2` only.
6. No git command was run in `/home/ido/pi-mirror`; the agent never started/stopped the pilot and
   never ran Python on the Pi.
</verification>

<success_criteria>
TRIGA-11a is met when, on a **sourceless** toolkit:
- the researcher builds the detector write in the editor, it survives save + hard reload, and an
  invalid variant is refused at save time with a message they can act on;
- touching each of the four MPR121 electrodes updates the matching `LICKER{n}` view tracker with
  a `pi_timestamp`;
- touching electrode 2 never moves `LICKER0/1/3`;
- and none of it involves a Python callback, `learning_cage`, or any detector-specific code in
  the Pi's trigger runtime.

If any of these fails, the plan's output is the defect record — not a patch.
</success_criteria>

<output>
After completion, create
`.planning/phases/24-trigger-assignment-action-lists/24-07-SUMMARY.md`.

Record explicitly:
- The task definition id, and its stored `fda_json` verbatim.
- The 8 negative-case status codes and messages.
- `{RUN}`, the phase-A `(id, value, pi_timestamp)` sequence, the phase-B bucket counts, and the
  pass/fail line for the cross-talk negative.
- The `Tracker` writes-per-physical-touch ratio (evidence for the deferred duplicate-`set`
  question — record only).
- Anything that had to be retried, and why. Waves 1–2 produced seven defects that only running
  the system revealed; this file is where the next equivalent gets caught.
</output>
</content>
