---
phase: 31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - /home/ido/mics_core/autopilot/autopilot/tasks/task.py
  - /home/ido/mics_core/autopilot/autopilot/core/pilot.py
  - /home/ido/mics_core/autopilot/autopilot/hardware/gpio.py
  - /home/ido/mics_core/autopilot/autopilot/prefs.py
  - /home/ido/mics_core/autopilot/autopilot/__init__.py
  - /home/ido/mics_core/autopilot/autopilot/setup/
  - /home/ido/mics_core/autopilot/setup.py
  - /home/ido/mics_core/pilot/prefs.json
  - /home/ido/mics_core/tools/tree_integrity/final_checks.py
  - /home/ido/mics_core/conftest.py
  - /home/ido/mics_core/tests/test_shed_absences.py
  - /home/ido/mics_core/tools/rebaseline_pytest.py
  - /home/ido/mics-backend/.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/31-PYTEST-BASELINE.json
autonomous: true
requirements: [PLAT-01]
must_haves:
  truths:
    - "Importing `autopilot` no longer pulls a GUI toolkit, so the test suite runs without npyscreen"
    - "No code path can reach an HDF5 write or a TrialData row"
    - "No code path can compute a valve duration from a volume lookup"
    - "The tree-integrity guard still passes and its PORT_CALIBRATION assertion was updated deliberately, not silently"
    - "The baseline cannot absorb a new failure without someone typing a reason for it"
  artifacts:
    - path: "/home/ido/mics_core/tests/test_shed_absences.py"
      provides: "Static gate: the shed subsystems cannot come back"
      min_lines: 30
    - path: "/home/ido/mics_core/tools/tree_integrity/final_checks.py"
      provides: "F2 prefs gate, updated for the post-shed key set"
      contains: "PORT_CALIBRATION"
    - path: "/home/ido/mics_core/tools/rebaseline_pytest.py"
      provides: "Subset-asserting re-baseline; refuses to freeze an unexplained new failure"
      min_lines: 40
  key_links:
    - from: "autopilot/autopilot/__init__.py"
      to: "autopilot.setup"
      via: "the import that must be GONE"
      pattern: "from autopilot.setup"
    - from: "autopilot/autopilot/core/pilot.py"
      to: "task.TrialData"
      via: "the hasattr branch that must be GONE"
      pattern: "TrialData"
---

<objective>
Stage 1, part 1. Delete the subsystems that are already dead, in one change, with the guard
updated in the same commit.

Purpose: this is the cheapest large win in the phase and it unblocks everything after it. Deleting
`autopilot/setup/` removes the `npyscreen` import at `autopilot/__init__.py:4` that currently
poisons **179 of 381** test results. Deleting HDF5/`TrialData` drops `tables`, `blosc`, `numexpr`
and `pandas`; deleting port calibration drops `scipy` (its only use was `linregress` inside the
calibration function — no numpy-polyfit rewrite is needed). Dropping `PySide2` is what makes
Python 3.11 possible at all: PySide2 5.15 cannot run there.

Output: a materially smaller tree, a green `--strict`, and a static test that stops the deleted
subsystems reappearing.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/CONTEXT.md
@.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/31-RESEARCH.md
@.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/31-01-SUMMARY.md

<repo_boundary>
ALL code changes land in `/home/ido/mics_core` on branch **`phase-31-modern-pi-platform`**.

<branch_guard>
**Assert the branch before committing anything.** All Phase 31 code lands on
`phase-31-modern-pi-platform` in `/home/ido/mics_core`, published to `origin` by plan 01. Before the
first commit of this plan, run `git branch --show-current` and confirm it is that branch — if it is
not, STOP and do not commit. Never `git checkout main`, never merge into `main`, never force-push,
and never push `main`. Integration to `main` is the user's decision after the C4 acceptance gate.
</branch_guard>

NEVER modify `/home/ido/mics-backend` source.
</repo_boundary>

<pi_rules>
ABSOLUTE:
- **NEVER run git on the Pi.** No `rsync --delete`.
- **NEVER start or stop the pilot. NEVER run any Python file on the Pi.**
- `Message` and `hardware_state` classes are **off limits** to this sweep. Do not touch them even
  if they look like they belong to a deleted subsystem.
- RTK-proxied grep can render a matching line blank or compress output. **Never claim a symbol is
  unused from it alone** — re-verify every deletion with a direct `grep -rn`. This exact failure
  nearly deleted the `Camera` base class during the Phase 30 audit.
</pi_rules>

<integrity_gate>
`cd /home/ido/mics_core && python3 tools/check_tree_integrity.py --strict` must exit 0 after EVERY
task in this plan. **NEVER `--rebaseline`.**
None of the files this plan deletes are in the `baseline_sha256` protected manifest, so no sha
edit should be needed. If one IS needed, edit the single entry by hand and justify it in the
commit message.
Note `--final` is a separate, stricter gate and is expected to fail until plan C3 retires F3.
</integrity_gate>

<verified_deletion_map>
Verified by direct `grep -n` (CONTEXT §3, re-confirmed 2026-08-16). Line numbers are pre-edit.

**HDF5 / TrialData** — only four survivors, and the single consumer is a guarded branch:
- `autopilot/autopilot/tasks/task.py:7` `import tables`
- `autopilot/autopilot/tasks/task.py:75, :103` `class TrialData(tables.IsDescription)`
- `autopilot/autopilot/core/pilot.py:34` `tables.NaturalNameWarning` warnings filter
- `autopilot/autopilot/core/pilot.py:1105` `if hasattr(self.task, 'TrialData')` — deleting the
  class means this branch is simply never taken; delete the branch too.
- `mics_task.py` (the real MICS task base, 76 KB) references `tables`/`pandas`/`scipy` **zero**
  times. `pilot/plugins/` is empty in both the repo and `~/pi-mirror`.

**Port calibration** — doubly dead: `Solenoid_mics.__init__` defaults `vol=None`, and every
`VALVE*`/`ODOR*`/`AIR_PUF*` prefs entry sets an explicit `duration` (200 ms / 40 ms) and never
`vol`; the lookup already falls back to a default LUT on exception.
- `autopilot/autopilot/hardware/gpio.py:1582` `Solenoid.dur_from_vol()`
- `autopilot/autopilot/core/pilot.py:699-760` `calibrate_port`
- `autopilot/autopilot/prefs.py:610-628` and `:700-743` `compute_calibration`
- Guard to update in the SAME commit: `tools/tree_integrity/final_checks.py:108` — the
  `for key in ("SUBJECT", "PORT_CALIBRATION")` row inside `f2_prefs`.

**Setup wizard** — `PySide2`, `shiboken2`, `pyqtgraph`, `npyscreen` appear **only** under
`autopilot/setup/` (`setup.py`, `setup_autopilot.py`, `forms.py`) and never in the pilot import
path. The one live reference outside that directory is `autopilot/autopilot/__init__.py:4`
`from autopilot.setup import setup_autopilot`.
- `autopilot/autopilot/core/pilot.py:58` mentions `setup_autopilot.py` in an argparse HELP STRING.
  `autopilot/autopilot/prefs.py:15` and `hardware/gpio.py:4` mention it in DOCSTRINGS. Those are
  prose, not imports — update the wording, do not treat them as code references.
- `autopilot/autopilot/external/__init__.py:33` raises `ImportError('the pigpiod daemon was not
  found! use autopilot.setup.')` — a string, not an import. Reword it; `external/` itself is
  not deleted in this phase at all (PLAT-16 is deferred), and certainly not here.
- `tests/test_tree_integrity.py:151,157` reference `autopilot.setup.request_helpers` inside a
  `tmp_path` SYNTHETIC fixture, not the real tree. It is PROTECTED (sha256 baseline) and must NOT
  be edited. It will keep passing.

**Dead audio** — no HiFiBerry overlay exists on the rig and `prefs.json` has
`"AUDIOSERVER": false`, so `JACKDSTRING`, `sndrpihifiberry` and the 192 kHz config are dead
weight. Delete rather than port. `pilot/prefs.json` currently carries `ALSA_NPERIODS`,
`AUDIOSERVER`, `FS`, `JACKDSTRING`, `NCHANNELS`, `OUTCHANNELS`, `SOUNDDIR` — decide each on
evidence, not on the word "audio": `SOUNDDIR` backs `pilot/sounds/*.wav` which the tree-integrity
bulk budget already exempts, so leave the sound files alone.
</verified_deletion_map>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete the HDF5/TrialData path and the port-calibration path, and update the F2 guard</name>
  <files>
    /home/ido/mics_core/autopilot/autopilot/tasks/task.py
    /home/ido/mics_core/autopilot/autopilot/core/pilot.py
    /home/ido/mics_core/autopilot/autopilot/hardware/gpio.py
    /home/ido/mics_core/autopilot/autopilot/prefs.py
    /home/ido/mics_core/tools/tree_integrity/final_checks.py
    /home/ido/mics_core/tests/test_shed_absences.py
  </files>
  <behavior>
    - `tests/test_shed_absences.py` greps the tree (excluding `tools/tree_integrity/`,
      `tools/check_tree_integrity.py`, `tests/test_tree_integrity.py` and this test itself — the
      guard's own `SELF_PATHS` set) and fails if any of these reappear:
      `import tables`, `class TrialData`, `NaturalNameWarning`, `dur_from_vol`, `calibrate_port`,
      `compute_calibration`, `import scipy`, `from scipy`, `import blosc`, `import numexpr`.
    - Importing `autopilot.tasks.task` succeeds with `tables` absent from the environment.
    - `f2_prefs` still refuses a prefs file containing a top-level `PORT_CALIBRATION` key.
  </behavior>
  <action>
    1. Write `tests/test_shed_absences.py` FIRST and watch it fail. It is the permanent gate; the
       deletions are what make it pass.

    2. Delete, using the line references in `<verified_deletion_map>` as a starting point and
       `grep -rn` to confirm each before and after:
       - `task.py`: the `tables` import and both `TrialData` class definitions. If `task.py` has
         nothing left that any live module imports, say so in the summary — but do NOT delete the
         module in this plan; the closure check in `--strict` is the arbiter and a surprise module
         removal belongs in its own change.
       - `pilot.py:34`: the `NaturalNameWarning` filter. `pilot.py:1105`: the whole
         `if hasattr(self.task, 'TrialData')` branch.
       - `pilot.py:699-760`: `calibrate_port`, plus any dispatch entry that routes to it.
       - `gpio.py:1582`: `Solenoid.dur_from_vol` and the `vol`-driven branch in
         `Solenoid_mics.__init__` that calls it. **Leave `duration` handling exactly as it is** —
         that is the live path.
       - `prefs.py:610-628` and `:700-743`: `compute_calibration` and its helper.

    3. Update `tools/tree_integrity/final_checks.py`'s `f2_prefs` in the SAME commit. The
       `PORT_CALIBRATION` row is an ABSENCE assertion on `pilot/prefs.json`, and after this change
       `PORT_CALIBRATION` no longer exists anywhere in the code either. Keep the row — an absence
       assertion that can never fire is still cheap and still correct — and add a one-line comment
       above it recording that Phase 31 plan 02 deleted the producing code, so a future reader
       knows the row is now belt-and-braces rather than load-bearing. Do NOT delete the row and do
       NOT weaken the SUBJECT row beside it.
       (The prefs PATH this guard reads changes in plan 04, when `prefs.json` becomes
       `prefs.template.json`. Do not pre-empt that here.)

    4. `scipy` disappears from the runtime set as a consequence. Do NOT rewrite `linregress` with
       numpy — it had exactly one caller and that caller is gone.
  </action>
  <verify>
    <automated>cd /home/ido/mics_core && /home/ido/.venvs/mics_core_dev/bin/python -m pytest -q tests/test_shed_absences.py --tb=short && /home/ido/.venvs/mics_core_dev/bin/python tools/pytest_delta.py && python3 tools/check_tree_integrity.py --strict</automated>
  </verify>
  <done>`test_shed_absences.py` passes; the pytest delta reports no NEW failures (and should report several `fixed:` lines); `--strict` exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete the setup wizard and the dead audio configuration</name>
  <files>
    /home/ido/mics_core/autopilot/autopilot/__init__.py
    /home/ido/mics_core/autopilot/autopilot/setup/
    /home/ido/mics_core/autopilot/setup.py
    /home/ido/mics_core/autopilot/autopilot/core/pilot.py
    /home/ido/mics_core/autopilot/autopilot/prefs.py
    /home/ido/mics_core/pilot/prefs.json
    /home/ido/mics_core/conftest.py
    /home/ido/mics_core/tests/test_shed_absences.py
  </files>
  <behavior>
    - `import autopilot` succeeds in an environment with **no** `npyscreen`, `PySide2`, `shiboken2`
      or `pyqtgraph` installed.
    - `test_shed_absences.py` additionally fails on reappearance of: `import npyscreen`,
      `from PySide2`, `import shiboken2`, `import pyqtgraph`, `autopilot/setup/` as a directory,
      `JACKDSTRING`, `sndrpihifiberry`.
    - The two modules currently in `conftest.py`'s `collect_ignore` (`tests/test_compute_ops.py`,
      `tests/test_log_action_values.py`) now COLLECT and run on the dev host.
  </behavior>
  <action>
    1. Extend `tests/test_shed_absences.py` with the new absence set and watch the new cases fail.

    2. Delete `autopilot/autopilot/setup/` (whole directory) and `autopilot/setup.py`. Remove
       `from autopilot.setup import setup_autopilot` at `autopilot/autopilot/__init__.py:4`.
       Verify the other three names that line's module re-exported (`get`, `get_task`,
       `get_hardware`, `get_names` from `utils.registry`; `dehydrate`, `hydrate` from
       `utils.hydration`) are untouched — those imports stay.

    3. Reword the three PROSE references so they do not point at a deleted module:
       `core/pilot.py:58` argparse help, `prefs.py:15` docstring, `hardware/gpio.py:4` docstring.
       Also reword `external/__init__.py:33`'s ImportError message. These are strings; changing
       them is cosmetic and must not change behaviour.

    4. Delete the dead audio config. Evidence, not the word "audio", decides each item:
       - `pilot/prefs.json`: remove `JACKDSTRING`, `ALSA_NPERIODS`, `NCHANNELS`, `OUTCHANNELS`,
         `FS` **only if** `grep -rn` shows no live consumer once `AUDIOSERVER` is false. Keep
         `SOUNDDIR` — `pilot/sounds/*.wav` stays and the integrity bulk budget already exempts it.
         Keep `AUDIOSERVER: false` itself unless every reader is gone.
       - Remove any `sndrpihifiberry` / 192 kHz handling you find. `grep -rn sndrpihifiberry` and
         `grep -rn 192000` first; if there are zero hits in the tree, say so in the summary
         instead of inventing a deletion.
       - Do NOT delete `hardware/mixer.py` or the `stim/sound/` tree in this plan. They are a
         separate call and plan 03 decides their fate as part of the dependency audit.

    5. Update `conftest.py`: remove the two `collect_ignore` entries and the comment block that
       justifies them, since the npyscreen chain they described is gone. If either module still
       fails to collect for a DIFFERENT reason, keep it ignored and rewrite the comment to name
       the new reason honestly. Do not leave a stale justification in place.

    6. `pilot/prefs.json` is currently tracked and is read by `f2_prefs`. Editing it is fine; the
       RENAME to `prefs.template.json` is plan 04's job. Do not rename it here.
  </action>
  <verify>
    <automated>cd /home/ido/mics_core && /home/ido/.venvs/mics_core_dev/bin/python -m pytest -q tests/test_shed_absences.py --tb=short && /home/ido/.venvs/mics_core_dev/bin/python -c "import sys; sys.path.insert(0,'autopilot'); import autopilot; print('import ok')" && /home/ido/.venvs/mics_core_dev/bin/python tools/pytest_delta.py && python3 tools/check_tree_integrity.py --strict</automated>
  </verify>
  <done>`import autopilot` works with no GUI toolkit present; `test_shed_absences.py` passes on the extended set; `--strict` exits 0; `conftest.py` carries either no `collect_ignore` or an honest, newly-written reason.</done>
</task>

<task type="auto">
  <name>Task 3: Re-baseline the suite under a subset assertion, and record what the shed bought</name>
  <files>
    /home/ido/mics_core/tools/rebaseline_pytest.py
    /home/ido/mics-backend/.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/31-PYTEST-BASELINE.json
  </files>
  <action>
    The shed changes the failing set on purpose. Re-capturing the baseline is therefore necessary —
    and it is also **the single most dangerous operation in the phase**, because a freshly written
    baseline makes `pytest_delta.py` trivially green. If plan 02 introduced a genuine new failure,
    a wholesale rewrite would freeze that failure in as "expected" and every later plan would
    inherit a gate that can no longer see it. Prose asking the executor to be careful is not a
    control. This task builds the control.

    Two modules make this a live risk rather than a theoretical one: Task 2 un-`collect_ignore`s
    `tests/test_compute_ops.py` and `tests/test_log_action_values.py`, both **protected** and
    **never executed anywhere**. Whatever they do on first run is unknown, and "unknown" must not
    be silently promoted to "expected".

    1. Write `tools/rebaseline_pytest.py`. It is the only sanctioned way to write the baseline:
       - Runs the suite, collects the current `FAILED` node-id set.
       - Loads the existing baseline. Computes `added = now - previous` and `fixed = previous - now`.
       - **If `added` is non-empty, it REFUSES to write** and exits non-zero, printing each new node
         id — unless every one is named in an `--allow-new` argument **with a reason string**, which
         it then records in the baseline under an `accepted_new_failures` list as
         `{node_id, reason, accepted_on}`.
       - Preserves the previous capture verbatim under `superseded` (never deletes it).
       - Refuses to run at all on a collection error.
       This makes "I froze a regression into the baseline" an action someone had to type a reason
       for, rather than a side effect of running a script.

    2. Re-baseline with it. **Expect `added` to be empty and `fixed` to be large.** If anything is
       in `added`, do not reach for `--allow-new` first — read the failure. The two newly-collected
       protected modules failing on first execution is a real finding about unexecuted test debt and
       belongs in the summary either way; only accept it into the baseline if it is genuinely
       pre-existing debt rather than damage the shed caused.

    3. In the summary state four numbers explicitly: failures before plan 01, after plan 01, after
       plan 02, and the count of `fixed:` node ids. If the drop is smaller than expected, say so and
       name the remaining causes — do not round the story up.

    4. Re-run `--strict` and record its exact output line. The closure member count will have
       dropped from 40; that is expected and is not a violation. The protected-file count (30) and
       the known-dangling count (1) must be unchanged.
  </action>
  <verify>
    <automated>cd /home/ido/mics_core && /home/ido/.venvs/mics_core_dev/bin/python tools/rebaseline_pytest.py --check-only && /home/ido/.venvs/mics_core_dev/bin/python tools/pytest_delta.py && python3 tools/check_tree_integrity.py --strict && python3 -c "
import json
d=json.load(open('/home/ido/mics-backend/.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/31-PYTEST-BASELINE.json'))
assert 'superseded' in d, 'superseded block missing'
prev=set(d['superseded']['failing_node_ids']); now=set(d['failing_node_ids'])
accepted={a['node_id'] for a in d.get('accepted_new_failures',[])}
added=now-prev-accepted
assert not added, 'UNACCEPTED new failures frozen into the baseline: %s' % sorted(added)
print('baseline ok:', d['failed'],'failed /',d['passed'],'passed; fixed',len(prev-now),'; accepted_new',len(accepted))
"</automated>
  </verify>
  <done>`tools/rebaseline_pytest.py` exists and refuses to write when the failing set grows without an explicit reasoned allow-entry; the new `failing_node_ids` is a subset of the previous set plus any recorded `accepted_new_failures`; the `superseded` block carries the plan-01 numbers; `--strict` exits 0 with 30 protected files and 1 exemption.</done>
</task>

</tasks>

<verification>
1. `python3 tools/check_tree_integrity.py --strict` -> exit 0
2. `/home/ido/.venvs/mics_core_dev/bin/python -m pytest -q tests/ --tb=no | tail -1` -> failure count
   dramatically lower than 179
3. `grep -rn "import tables\|class TrialData\|dur_from_vol\|calibrate_port\|compute_calibration" --include=*.py /home/ido/mics_core/autopilot /home/ido/mics_core/pilot` -> no hits
4. `ls /home/ido/mics_core/autopilot/autopilot/setup` -> no such directory
5. `git -C /home/ido/mics_core log --oneline phase-31-modern-pi-platform | head -3` -> the shed commits
</verification>

<success_criteria>
- `import autopilot` needs no GUI toolkit.
- `tables`, `blosc`, `numexpr`, `pandas`, `scipy`, `npyscreen`, `PySide2`, `shiboken2`, `pyqtgraph`
  are unreachable from the pilot import path.
- The F2 `PORT_CALIBRATION` assertion was updated deliberately, with a comment recording why.
- `--strict` green; a permanent static test stops all of it coming back.
</success_criteria>

<output>
After completion, create `31-02-SUMMARY.md` in the phase directory. Include the three failure
counts (pre-01, post-01, post-02), the exact `--strict` output line before and after, and any
deletion in `<verified_deletion_map>` that turned out to have zero hits — that is a correction to
CONTEXT.md and must be recorded, not quietly dropped.
</output>
