---
phase: 30-pi-repo-cleanup
plan: 02
type: execute
wave: 1
depends_on: ["30-01"]
files_modified:
  - /home/ido/pi-mirror/autopilot/code_2023.deb
  - /home/ido/pi-mirror/autopilot/docs/
  - /home/ido/pi-mirror/autopilot/examples/
  - /home/ido/pi-mirror/autopilot/tests/
  - /home/ido/pi-mirror/autopilot/src/
  - /home/ido/pi-mirror/autopilot/.gitmodules
  - /home/ido/pi-mirror/autopilot/home/
  - /home/ido/pi-mirror/autopilot/~/
  - /home/ido/pi-mirror/autopilot/auto_pi_lot.egg-info/
  - /home/ido/pi-mirror/autopilot/Cow.wav
  - /home/ido/pi-mirror/autopilot/Dying Light Bulb-SoundBible.com-742005847.wav
  - /home/ido/pi-mirror/autopilot/Testing_stepper_motor_Hat/
  - /home/ido/pi-mirror/autopilot/.travis.yml
  - /home/ido/pi-mirror/autopilot/.coveralls.yml
  - /home/ido/pi-mirror/autopilot/.readthedocs.yml
  - /home/ido/pi-mirror/autopilot/CNAME
  - /home/ido/pi-mirror/autopilot/hardware.gpio
  - /home/ido/pi-mirror/autopilot/networking.node
  - /home/ido/pi-mirror/autopilot/networking.station_02
  - /home/ido/pi-mirror/autopilot/.pilot
  - /home/ido/pi-mirror/autopilot/plugins
  - /home/ido/pi-mirror/autopilot/plugins.YoShiTask
  - /home/ido/pi-mirror/autopilot/registry
  - /home/ido/pi-mirror/output.txt
  - /home/ido/pi-mirror/environment.yml.save
  - /home/ido/pi-mirror/blip.wav
  - /home/ido/pi-mirror/alt_blip8.wav
  - /home/ido/pi-mirror/.gitignore
  - /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-02-ledger.md
autonomous: true
requirements: [HYG-08]
must_haves:
  truths:
    - "The tree contains no vendored installer, no pre-built documentation output, and no uninitialised submodule"
    - "No surviving file references anything this plan removed"
    - "The guard's three Wave-1 scan_skip paths no longer exist, so the skip becomes moot the moment this plan lands"
    - "LICENSE (MPL-2.0) is still present"
    - "The only blip.wav / alt_blip8.wav in the tree are the reachable pilot/sounds/ copies"
  artifacts:
    - path: "/home/ido/pi-mirror/autopilot/LICENSE"
      provides: "MPL-2.0 licence — mandatory, must survive"
    - path: "/home/ido/pi-mirror/.gitignore"
      provides: "Excludes __pycache__, .pyc, .pytest_cache, egg-info from the new repo"
      contains: "__pycache__"
  key_links:
    - from: "autopilot/autopilot/stim/sounds/mixer.py"
      to: "pilot/sounds/blip.wav"
      via: "bare-filename resolution through SOUNDDIR"
      pattern: "SOUNDDIR"
---

<objective>
Remove the vendored and generated bulk (HYG-08) — ~93 MB of installer, pre-built Sphinx output,
an upstream test suite, uninitialised submodules, tilde-literal directories, build artifacts and
byte-duplicate wavs.

Purpose: this is the single largest size reduction in the phase and it carries the lowest import
risk, so it lands first and independently. It touches nothing in the runtime import closure.
Output: a tree with no vendored bulk, a hardened `.gitignore`, and every removal recorded in the
§4 removal ledger against the four criteria.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-pi-repo-cleanup/30-CONTEXT.md
@.planning/phases/30-pi-repo-cleanup/30-VALIDATION.md
@.planning/phases/30-pi-repo-cleanup/30-01-SUMMARY.md

<pi_rules>
ABSOLUTE (STATE.md:1727-1745). Override anything below:
- Edit ONLY `/home/ido/pi-mirror`. **No git command in that directory — not even `status`.**
  Deletions are plain `rm -rf`, never `git rm`, never `git clean`.
- Never `rsync --delete`. This plan does not rsync.
- Never start/stop the pilot. Never run Python on the Pi.
- `autopilot` cannot be imported on this host; `compileall` is the verification ceiling.
</pi_rules>

<removal_criteria>
From 30-CONTEXT.md. A path is removable only when ALL four hold. Record the verdict for each
path in §4 of `30-HARDWARE-VALIDATION.md`:
1. Not in the static import closure of `python3 -m autopilot.core.pilot`.
2. Not reachable dynamically — `PLUGINDIR` sweep, `autopilot/tasks/` AST sweep, `importlib`,
   or a string-keyed dispatch table.
3. Not named by the backend — no literal filename, module path or class-name string in
   `mics-backend`, and no row in `hardware_libs`, `hardware_modules`, `task_toolkits` or
   `available_locked_states` resolving to it.
4. Not reserved by a pending phase (Phase 26: `hardware/openephys_client.py`,
   `tests/test_openephys_client.py`, `tests/test_openephys_markers.py`).
</removal_criteria>

<verified_sizes>
Measured 2026-08-10:
`autopilot/code_2023.deb` 87,013,118 B · `autopilot/docs/` 9,095,683 B · `autopilot/examples/`
44 KB · `autopilot/tests/` 68 KB · `autopilot/src/` 12 KB · `autopilot/home/` 144 KB ·
`autopilot/~/` 48 KB · `autopilot/auto_pi_lot.egg-info/` 32 KB.
Whole tree excluding `.git`: 202,275,257 B. `.git`: ~193 MB. `pilot/sounds/`: 2,326,388 B.

**The HYG-08 budget is ≤ 8 MB excluding `.git` AND excluding `pilot/sounds/`.** `pilot/sounds/`
is reachable rig audio that `mixer.py` resolves by bare filename through `SOUNDDIR`; static
analysis cannot prove any wav in it dead, no plan removes it, and budgeting it as bulk would make
the exit gate unpassable for a reason that has nothing to do with the sweep. Report the three
numbers separately in the ledger so plan 08 can compute the budget without re-measuring.
</verified_sizes>

<this_plan_retires_the_guards_wave_1_blind_spot>
`tools/tree_protect_list.json` carries a `scan_skip` list — `autopilot/tests/`,
`autopilot/examples/`, `autopilot/docs/` — added in plan 01 for one reason only:

```
autopilot/tests/test_terminal.py:9   import autopilot.core.terminal
```

is a **genuine import** of a module plan 03 deletes, and plan 03 runs in the same wave as this
one. Without the skip, whichever of 02/03 lands second determines whether the guard goes red, for
no real defect.

**This plan is what makes the skip moot**, because it deletes all three directories. Plan 08's
`--final` F1 asserts they are gone, so the exemption can never harden into a blind spot. Say so
explicitly in the ledger, listing `autopilot/tests/test_terminal.py:9` and
`autopilot/tests/test_registry.py:14-16` (which imports `autopilot.hardware.cameras`, removed
later by plan 05) as the two references the skip was covering.
</this_plan_retires_the_guards_wave_1_blind_spot>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove vendored, generated and duplicate bulk</name>
  <files>
    /home/ido/pi-mirror/autopilot/code_2023.deb,
    /home/ido/pi-mirror/autopilot/docs/,
    /home/ido/pi-mirror/autopilot/examples/,
    /home/ido/pi-mirror/autopilot/tests/,
    /home/ido/pi-mirror/autopilot/src/,
    /home/ido/pi-mirror/autopilot/.gitmodules,
    /home/ido/pi-mirror/autopilot/home/,
    /home/ido/pi-mirror/autopilot/~/,
    /home/ido/pi-mirror/autopilot/auto_pi_lot.egg-info/,
    /home/ido/pi-mirror/autopilot/Cow.wav,
    /home/ido/pi-mirror/autopilot/Dying Light Bulb-SoundBible.com-742005847.wav,
    /home/ido/pi-mirror/autopilot/Testing_stepper_motor_Hat/,
    /home/ido/pi-mirror/autopilot/.travis.yml,
    /home/ido/pi-mirror/autopilot/.coveralls.yml,
    /home/ido/pi-mirror/autopilot/.readthedocs.yml,
    /home/ido/pi-mirror/autopilot/CNAME,
    /home/ido/pi-mirror/output.txt,
    /home/ido/pi-mirror/environment.yml.save,
    /home/ido/pi-mirror/blip.wav,
    /home/ido/pi-mirror/alt_blip8.wav
  </files>
  <action>
    Plain `rm` / `rm -rf`. **No git.** Work from `/home/ido/pi-mirror`.

    **A. Enumerated removals (HYG-08 names these explicitly):**
    - `autopilot/code_2023.deb` — 83 MB VS Code installer, zero references.
    - `autopilot/docs/` — 9.1 MB of pre-built Sphinx *output*. (Note: this is build output, not
      source. Confirm there is no `docs/source/` you would be discarding — if there is, still
      remove: nothing builds it and nothing links it.)
    - `autopilot/examples/` — upstream examples. Note `examples/tasks/blink.py:64` names
      `autopilot.core.subject` **in a class docstring**; that is Sphinx prose, not a reference,
      and the guard's 2(b) skips docstrings by design. It is removed here regardless.
    - `autopilot/tests/` — the **upstream** suite. It needs PySide2 and is unmaintained here.
      It holds the two references the guard's Wave-1 `scan_skip` was covering:
      `test_terminal.py:9` (`import autopilot.core.terminal`) and `test_registry.py:14-16`
      (`autopilot.hardware.cameras`). Record both in the ledger.
      **Do not confuse it with `/home/ido/pi-mirror/tests/`**, the lab's 21 protected root test
      modules, which are on the HYG-13 protect-list and must not be touched.
    - `autopilot/src/` — two uninitialised submodules. Remove `autopilot/.gitmodules` with them
      (the file, not any git operation).
    - `autopilot/home/` and `autopilot/~/` — the tilde was never expanded; these are accidental
      literal directories.
    - `autopilot/auto_pi_lot.egg-info/` — build artifact.
    - `autopilot/Cow.wav` and `autopilot/Dying Light Bulb-SoundBible.com-742005847.wav` — both
      unreferenced; the latter is third-party audio.
    - Root `blip.wav` and `alt_blip8.wav` — byte-identical duplicates of the `pilot/sounds/`
      copies. Only the `pilot/sounds/` copies are reachable, because `mixer.py:18` resolves by
      bare filename through `SOUNDDIR`. **Prove byte-identity before deleting**
      (`md5sum blip.wav pilot/sounds/blip.wav` etc.) and record both md5s in the ledger. If they
      are NOT identical, stop and report — the root copies would then be a distinct asset.
      **`pilot/sounds/` is untouched by this phase.**
    - The seven zero-byte logger-name files at `autopilot/` root: `hardware.gpio`,
      `networking.node`, `networking.station_02`, `.pilot`, `plugins`, `plugins.YoShiTask`,
      `registry`. Verify each is 0 bytes before removing.
      **Careful:** `autopilot/plugins` is a zero-byte FILE, not a directory, and is unrelated to
      `pilot/plugins/` (plan 04's target). Confirm with `test -f` + size 0.
    - Upstream CI dotfiles: `autopilot/.travis.yml`, `autopilot/.coveralls.yml`,
      `autopilot/.readthedocs.yml`, `autopilot/CNAME`.
    - `output.txt` (repo root, 16 KB of stray output).
    - `environment.yml.save` — a nano emergency-save differing from `environment.yml` by one
      leading tab, i.e. the invalid copy. Confirm with `diff environment.yml environment.yml.save`
      before removing; keep `environment.yml`.

    **B. Residue not enumerated by HYG-08 — apply the four criteria and record the verdict.**
    `autopilot/Testing_stepper_motor_Hat/` exists in the tree and appears in no requirement.
    Run the criteria on it: grep the surviving tree and `mics-backend` for its name and for any
    class it defines; query `hardware_libs`/`hardware_modules` for a matching `filename`. If all
    four criteria hold, remove it and record the four verdicts in §4. If ANY criterion fails,
    keep it and record why. Do the same for any other unenumerated path you find at
    `autopilot/` root. **Do not remove anything whose criteria you cannot state in the ledger** —
    that is the difference between a reproducible sweep and a hand-list.

    **C. Explicitly KEEP:** `autopilot/LICENSE` (MPL-2.0, mandatory), `autopilot/setup.py`,
    `autopilot/requirements.txt`, `autopilot/requirements/`, `autopilot/pyproject.toml`,
    `autopilot/README.md`, `autopilot/CITATION.cff`, `autopilot/MANIFEST.in`,
    `autopilot/CODE_OF_CONDUCT.md`, `autopilot/CONTRIBUTING.md`, `autopilot/.gitattributes`,
    `autopilot/.gitignore`, `environment.yml`, `requirements.txt`, `run_pilot.sh`,
    `scripts/dev/extlink_smoke.py` (a Phase 18 dev tool), `pilot/sounds/`.

    **D. Do NOT purge `__pycache__` or `.pyc` here.** Plan 03 runs in this same wave and its
    `compileall` regenerates them, so a purge-then-assert in this plan is a race against a
    sibling. The cache purge moved to plan 08's final gate, where nothing runs `compileall`
    afterwards. `.gitignore` (Task 2) already keeps them out of the new repo in the meantime.
  </action>
  <verify>
    <automated>cd /home/ido/pi-mirror &amp;&amp; test -f autopilot/LICENSE &amp;&amp; test ! -e autopilot/code_2023.deb &amp;&amp; test ! -e autopilot/docs &amp;&amp; test ! -e autopilot/tests &amp;&amp; test ! -e autopilot/examples &amp;&amp; test -d tests &amp;&amp; test "$(ls tests/*.py | wc -l)" -ge 21 &amp;&amp; test -f pilot/sounds/blip.wav &amp;&amp; test ! -e blip.wav &amp;&amp; python3 tools/check_tree_integrity.py --strict &amp;&amp; python3 -m compileall -q autopilot/autopilot tools tests</automated>
  </verify>
  <done>
    Every enumerated path is gone, including all three of the guard's `scan_skip` directories.
    `autopilot/LICENSE`, `/home/ido/pi-mirror/tests/` (21 modules) and `pilot/sounds/` are intact.
    `check_tree_integrity.py --strict` exits 0 — no surviving file names anything removed.
    `compileall` over `autopilot/autopilot`, `tools`, `tests` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Harden .gitignore and record the ledger</name>
  <files>
    /home/ido/pi-mirror/.gitignore,
    /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-02-ledger.md
  </files>
  <action>
    **A. Harden `/home/ido/pi-mirror/.gitignore`.** Current contents:
    `pilot/logs/.pilot.log.*`, `*.log`, `*.log.*`, `*.pyc`, `*.csv`, `*.h5`. Append:
    ```
    __pycache__/
    .pytest_cache/
    *.egg-info/
    *.deb
    ```
    Do not remove existing lines. `pilot/data/` and `pilot/logs/` content exclusions are plan
    07's job (HYG-10) — leave them alone here.

    **B. Note the deferred cache purge.** The actual `__pycache__` / `*.pyc` / `.pytest_cache`
    deletion happens in plan 08 Task 1, immediately before the manifest diff and the size
    measurement, because plan 03 (same wave as this one) runs `compileall` and would recreate
    them. Record that hand-off in the ledger in one line so plan 08 does not have to rediscover
    it. Also record, from 30-CONTEXT.md, that the Pi's `.cpython-37` bytecode was used by the
    audit as evidence of what actually loads and that this evidence has **already been extracted**
    into 30-CONTEXT.md — so deleting the caches later costs nothing.

    **C. Record.** Write
    `.planning/phases/30-pi-repo-cleanup/ledger/30-02-ledger.md` (create the `ledger/` directory
    if absent). **Do not edit `30-HARDWARE-VALIDATION.md` directly** — plan 03 runs in parallel
    with this one and would conflict; plan 08 merges every fragment into §4 at the phase gate.

    The fragment contains:
    - one ledger row per path removed by Task 1, columns
      `Path | C1 not in static closure | C2 not dynamically reached | C3 not named by backend |
      C4 not reserved | Evidence`, with an explicit verdict in each criterion column;
    - the `blip.wav` / `alt_blip8.wav` md5 pairs proving byte-identity with the `pilot/sounds/`
      copies;
    - the `environment.yml` vs `environment.yml.save` diff result;
    - the four-criteria verdict for `autopilot/Testing_stepper_motor_Hat/` and any other
      unenumerated path, kept or removed;
    - the **`scan_skip` retirement note** (see `<this_plan_retires_the_guards_wave_1_blind_spot>`):
      the three directories are gone, the two references they were covering
      (`autopilot/tests/test_terminal.py:9`, `autopilot/tests/test_registry.py:14-16`), and a
      pointer to plan 08's F1 assertion that they stay gone;
    - **three separate size numbers** — `du -sb --exclude=.git .`, `du -sb .git`, and
      `du -sb pilot/sounds` — before and after, against the §1 baseline, so plan 08 can compute
      the "≤ 8 MB excluding `.git` and `pilot/sounds/`" budget directly;
    - a proposed verdict line: `HYG-08 | PROVEN | <evidence>`, which plan 08 transcribes.
  </action>
  <verify>
    <automated>cd /home/ido/pi-mirror &amp;&amp; grep -q '__pycache__/' .gitignore &amp;&amp; grep -q '.pytest_cache/' .gitignore &amp;&amp; grep -q '\*.deb' .gitignore &amp;&amp; python3 tools/check_tree_integrity.py --strict &amp;&amp; test -f /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-02-ledger.md &amp;&amp; grep -q 'scan_skip' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-02-ledger.md &amp;&amp; grep -q 'HYG-08 | PROVEN' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-02-ledger.md</automated>
  </verify>
  <done>
    `.gitignore` covers `__pycache__/`, `.pytest_cache/`, `*.egg-info/`, `*.deb`.
    `ledger/30-02-ledger.md` has a row for every path this plan removed, each with four criterion
    verdicts, plus the `scan_skip` retirement note, the three separate size measurements, the
    cache-purge hand-off to plan 08, and the proposed HYG-08 PROVEN line.
  </done>
</task>

</tasks>

<verification>
- `python3 /home/ido/pi-mirror/tools/check_tree_integrity.py --strict` exits 0 (1 known-dangling
  exemption held, 0 violations).
- `python3 -m compileall -q /home/ido/pi-mirror/autopilot/autopilot /home/ido/pi-mirror/tools /home/ido/pi-mirror/tests` exits 0.
- **Pi suite: run the delta command from `30-01-SUMMARY.md`**, not a bare `pytest`. It exits 0
  when no node id fails that was not already failing in `30-PYTEST-BASELINE.json`. A bare
  `python3 -m pytest -q` cannot be chained with `&&` here — the recorded baseline is 179 failed /
  181 passed, so its exit code is 1 on a perfectly healthy tree.
- `du -sb --exclude=.git /home/ido/pi-mirror` is ≥ 90 MB smaller than the §1 baseline.
- No git command was run in `/home/ido/pi-mirror`.
</verification>

<success_criteria>
1. Tree excluding `.git` drops by at least 90 MB.
2. `autopilot/LICENSE` present; `/home/ido/pi-mirror/tests/` still has its 21 modules;
   `pilot/sounds/` untouched.
3. All three `scan_skip` directories (`autopilot/{tests,examples,docs}/`) are gone, retiring the
   guard's Wave-1 blind spot before it can outlive the wave.
4. The guard passes `--strict` — nothing surviving names a removed path.
5. Every removal has a four-criteria ledger row in `ledger/30-02-ledger.md`, with three separate
   size measurements and the cache-purge hand-off to plan 08.
6. The fragment proposes `HYG-08 | PROVEN` for plan 08 to transcribe.
</success_criteria>

<output>
After completion, create
`.planning/phases/30-pi-repo-cleanup/30-02-SUMMARY.md`.
</output>
