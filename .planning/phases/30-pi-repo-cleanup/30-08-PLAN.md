---
phase: 30-pi-repo-cleanup
plan: 08
type: execute
wave: 5
depends_on: ["30-02", "30-03", "30-05", "30-06", "30-07"]
files_modified:
  - /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-HARDWARE-VALIDATION.md
  - /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-PUBLISH.md
  - /home/ido/pi-mirror/README.md
  - /home/ido/pi-mirror/**/__pycache__/   # final cache purge, moved here from plan 02
  - /home/ido/pi-mirror/.pytest_cache/
autonomous: true
requirements: [HYG-01, HYG-13, HYG-07, HYG-08, HYG-11]
must_haves:
  truths:
    - "One command proves the whole tree is internally consistent: check_tree_integrity.py --final exits 0"
    - "The protected file set is byte-identical to its pre-sweep baseline, proven by manifest diff"
    - "Every removed path in the phase has a four-criteria ledger row"
    - "No __pycache__, .pyc or .pytest_cache enters the new repo, purged after the last compileall in the phase"
    - "The user has an exact, copy-pasteable publication procedure that the agent could not run itself"
  artifacts:
    - path: "/home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-HARDWARE-VALIDATION.md"
      provides: "Consolidated evidence log: 14-row verdict table, merged removal ledger, manifest diff"
    - path: "/home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-PUBLISH.md"
      provides: "User-run publication procedure: revoke, fresh git init, HYG-01 history proof, branch cut"
  key_links:
    - from: "30-PUBLISH.md"
      to: "/home/ido/.hyg01-probe.txt"
      via: "git log -p | grep -c -F -f <probe> → expect 0"
      pattern: "hyg01-probe"
---

<objective>
Close the sweep: run the full gate, prove the survival manifest, merge every ledger fragment into
one evidence log, and hand the user an exact publication procedure.

Purpose: everything from here is the user's, because the standing rule forbids the agent running
git in `pi-mirror` and the branch lives on the user's machine. The agent's job ends at "a
verified-clean tree plus the exact commands". Getting the handover wrong is how a destructive
phase ends with a repo nobody trusts.
Output: `--final` green, a manifest diff with zero drift, a consolidated
`30-HARDWARE-VALIDATION.md`, and `30-PUBLISH.md`.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-pi-repo-cleanup/30-CONTEXT.md
@.planning/phases/30-pi-repo-cleanup/30-VALIDATION.md
@.planning/phases/30-pi-repo-cleanup/30-01-SUMMARY.md
@.planning/phases/30-pi-repo-cleanup/30-02-SUMMARY.md
@.planning/phases/30-pi-repo-cleanup/30-03-SUMMARY.md
@.planning/phases/30-pi-repo-cleanup/30-04-SUMMARY.md
@.planning/phases/30-pi-repo-cleanup/30-05-SUMMARY.md
@.planning/phases/30-pi-repo-cleanup/30-06-SUMMARY.md
@.planning/phases/30-pi-repo-cleanup/30-07-SUMMARY.md

<pi_rules>
ABSOLUTE (STATE.md:1727-1745). Override anything below:
- Edit ONLY `/home/ido/pi-mirror`. **No git command in that directory — not even `status`.**
  This plan WRITES git commands into a document for the user to run. It never runs them.
- Never `rsync --delete`. This plan does not rsync. **This plan does not deploy anything.**
- Never start/stop the pilot. Never run Python on the Pi.
</pi_rules>

<what_the_agent_may_not_do>
Four things in this phase are the user's, recorded in 30-VALIDATION.md "Manual-Only":

1. **Revoking the Gmail app password** — an action in a third-party account. Must happen
   **before** publication, not after.
2. **Creating the new repository** — a fresh `git init` over the pruned tree. Never a clone,
   never a filtered history: the credential is in the mirror's 193 MB `.git`, and a rewrite
   leaves reflogs and packed objects. The mirror's history has no other value here.
3. **Cutting the `.28` branch** on the old repo. The branch lives on the user's machine, not on
   the device.
4. **Running the Pi test suite** and the live session (plan 09).

The agent's deliverable is a verified-clean tree plus `30-PUBLISH.md`. Nothing more.

**Explicitly out of scope, do not plan or write procedure for:**
- rsync-based device reconciliation. `tools/sync_pi.sh` syncs only `autopilot/` and passes no
  `--delete`, so rsync could never remove the deleted files from `.28` — which is precisely why
  the topology is "branch checkout on `.28`, fresh image on new units".
- Provisioning or imaging a new unit (venv, `pigpio`/`jackd`, systemd). The user handles imaging.
- Converting `.28` to the new repo in place. Explicitly deferred ("not for now").
</what_the_agent_may_not_do>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full gate and prove the survival manifest</name>
  <files>
    /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-HARDWARE-VALIDATION.md
  </files>
  <action>
    0. **Cache purge — ordering matters, and it is NOT only first.** Plan 02 deliberately
       deferred the purge: it shares Wave 1 with plan 03, whose `compileall` regenerates
       `__pycache__`, so a purge-then-assert there would have been a race against a sibling plan.

       **This plan still runs `compileall` and `pytest` itself** (step 3), and both regenerate
       `__pycache__` and `.pytest_cache`. A purge that runs only here as step 0 is therefore
       undone minutes later, and `<done>` / success criterion 1b would be false the moment the
       plan finished.

       **So the purge runs TWICE, and the second one is the authoritative one:**
       - **here (step 0)** — a preliminary purge, so step 1's F1 size measurement reflects what
         will actually reach the repo rather than counting bytes that never will;
       - **as step 3b, after step 3's `compileall` and test runs *and* after
         `check_tree_integrity.py --final` have finished** — the identical commands again. This
         is the run `<done>` describes, and it must be the last thing in the task that touches
         the tree. Re-measure the F1 size afterwards and record the final figure.

       **`--final` must precede the authoritative purge, not follow it.** F4 (HYG-06) proves
       the task tree compiles by shelling out to `python3 -m compileall -q
       autopilot/autopilot/tasks`, which *creates* `autopilot/autopilot/tasks/__pycache__`.
       Purging before `--final` therefore leaves a cache directory behind and makes `<done>`
       and criterion 1b false at plan end. The verify chain below is ordered accordingly:
       `compileall → backend pytest → tree-absence tests → --final → purge → assert clean → du`.
       The cost is that F1's internal size measurement counts bytecode; harmless, since the
       measured budget is ~1.0 MiB against an 8 MiB limit and the final `du` runs post-purge.
       ```
       find /home/ido/pi-mirror -name '__pycache__' -type d -not -path '*/.git/*' -prune -exec rm -rf {} +
       find /home/ido/pi-mirror -name '*.pyc' -not -path '*/.git/*' -delete
       rm -rf /home/ido/pi-mirror/.pytest_cache
       ```
       Plain `rm`. **No git.** These are the Pi's `.cpython-37` files; the audit used *which* pycs
       exist as evidence of what actually loads, and that evidence was already extracted into
       30-CONTEXT.md, so removing them costs nothing. Record **both** purges in §6 and mark the
       second as authoritative.

    1. **`--final`.** Run `python3 /home/ido/pi-mirror/tools/check_tree_integrity.py --final`.
       This is the first time the completion checks F1–F5 are expected to pass:
       - **F1** — tree ≤ **8 MB** excluding **both** `.git` and `pilot/sounds/` (the latter is
         2.3 MB of reachable rig audio that `mixer.py` resolves by bare filename through
         `SOUNDDIR`; no plan removes it, and budgeting it as bulk would make this gate
         unpassable for a reason unrelated to the sweep) — **and** `autopilot/tests`,
         `autopilot/examples`, `autopilot/docs` and `terminal/` absent, which is what stops the
         guard's Wave-1 `scan_skip` (all four prefixes) from having outlived its wave.
       - **F2** — prefs carry no rig config.
       - **F3** — restorations applied and the three holds resolved, asserted as **call forms**
         (`set_cdc_manual(0x3f)`, `self.triggers['IR1']`, `pulse_and_notify(`…`OG_TRIGGER`…`)`),
         **never as bare tokens**. `pilot/prefs.json` keeps `HARDWARE.GPIO.IR1` and
         `HARDWARE.GPIO.OG_TRIGGER` as live pin declarations by design.
       - **F4** — `tasks/` compiles.
       - **F5** — no camera code.
       If any fails, fix it here if the fix is within an already-landed plan's scope, otherwise
       report which plan owns it. **Do not weaken an assertion to make it pass** — and equally,
       **do not satisfy one by deleting live code.** If F3 ever appears to demand removing an
       `IR1` or `OG_TRIGGER` entry from `prefs.json`, the assertion has regressed to a bare-token
       form: stop and report. Plan 01's `<F3_must_assert_call_forms_not_tokens>` spells out why
       that specific failure is the most dangerous one available at this point in the phase.

    2. **Manifest diff (HYG-13).** Recompute `md5sum` for all 30 protected paths and diff against
       the §1 pre-sweep manifest from plan 01. Expected drift: **zero** for the five
       `external_hardware*` files, the 21 `tests/*.py` modules, `fda_vocabulary.py`,
       `Event_Dispatcher.py`, `log_value.py` and `autopilot/LICENSE`.
       Any non-zero drift is a finding, not a rounding error — report the file and the plan that
       touched it.
       Confirm separately that the three Phase 26 `reserved_absent` names are still absent and
       were never reported as strays.

    3. **Full suite.**
       - **Pi suite — a delta, never a bare exit code.** Run the `delta_command` recorded in
         `30-PYTEST-BASELINE.json` (also quoted in `30-01-SUMMARY.md`). It exits 0 when no node id
         fails that was not already failing at Wave 0, and exits 1 loudly on a *collection* error,
         which is a break rather than a delta. The Wave 0 baseline is **179 failed / 181 passed**
         over **19** collectable modules, with `tests/test_compute_ops.py` and
         `tests/test_log_action_values.py` `collect_ignore`d as Pi-only (they import `autopilot`
         at top level → `setup_autopilot` → `npyscreen`, absent here). Those 179 are pre-existing
         debt predating Phase 30 — the suite has never been run to green anywhere. **Do not chain
         a bare `python3 -m pytest -q` with `&&`**: it exits 1 on a perfectly healthy tree, and
         "fixing" it would mean either editing protected test modules or deleting sweep work.
         Record both the baseline and the current numbers in §1 so the delta is auditable.
       - `python3 -m compileall -q /home/ido/pi-mirror/autopilot/autopilot /home/ido/pi-mirror/tools /home/ido/pi-mirror/tests`
       - `docker compose exec -T api python -m pytest -q tests/` — 352 pass. This one **is** a
         bare-exit-code gate; the backend suite is green.

    4. **Merge the ledgers.** Read every `ledger/30-0N-ledger.md` fragment and merge into §4 of
       `30-HARDWARE-VALIDATION.md` as one table sorted by path, preserving each row's four
       criterion verdicts and its owning plan. Then flip the 14-row verdict table using each
       fragment's proposed verdict line. Requirements this plan can mark PROVEN from evidence
       already in hand: HYG-03, HYG-04, HYG-05, HYG-06, HYG-07, HYG-08, HYG-09, HYG-10, HYG-11,
       HYG-12, HYG-13. **HYG-01 stays UNPROVEN until the user publishes and runs the history
       grep; HYG-02 stays UNPROVEN until the rig session in plan 09.** Do not mark either
       PROVEN here.

    5. **Add §6, "Findings and corrections"**, carrying forward from the fragments at minimum:
       - the **HYG-05 correction, recorded as CLOSED** — `Camera` is the base class of `MLX90640`
         at `i2c.py:580`, so the earlier "never used… only docstring prose" reading was wrong and
         the edit needed a third part. **The amendment has already landed**: REQUIREMENTS.md
         HYG-05 carries it in the row itself and 30-CONTEXT.md carries it as an inline
         `> **CORRECTION, 2026-08-10 (planning).**` block. Write this up as confirmed-and-closed,
         not as an outstanding action.
       - the **`OG_TRIGGER` inventory correction, still OPEN against 30-CONTEXT.md** — its claim
         that "no orphan `OG_TRIGGER` declaration survives" because
         `mics_cage_task.py:97`/`learning_cage.py:82` both go with HYG-03 misses two sites:
         `tasks/RecordingBox.py:53` (removed by plan **05**, not 04) and `pilot/prefs.json:241,243`
         (a **live pin declaration that survives the phase by design**, alongside
         `HARDWARE.GPIO.IR1` at `:278,282`). Merge the three halves recorded by plans 04, 05 and
         07 into one entry. This is what forced F3 to assert call forms rather than bare tokens.
       - the two **instrument-level exemptions** seeded in §6 by plan 01: the `known_dangling`
         entry for `mics_task.py:1589` (`autopilot.autopilot.core.pilot`, deferred by name in
         30-CONTEXT.md and asserted to be **still** dangling so this phase cannot silently repair
         it), and the `scan_skip` of `autopilot/{tests,examples,docs}/` — now retired by plan 02
         and re-asserted absent by F1 — plus `terminal/`, the fourth `scan_skip` prefix, removed
         by plan 03 and likewise re-asserted absent by F1 — and the `runtime_generated` exemption
         for `pilot/plugin_db.json`, generated on the device and never present in the mirror while
         `pilot/prefs.json` keeps its `PLUGIN_DB` key by design.
       - the **test-module count reconciliation**, all three numbers:
         `23 (HYG-13) = 21 present + 2 Phase 26 reserved-absent`, and
         `21 present = 19 collectable on this host + 2 Pi-only (npyscreen)`.
       - the **pytest baseline and delta**: 179 failed / 181 passed at Wave 0, the current
         numbers, and the explicit statement that the 179 are pre-existing debt predating this
         phase.
       - the characterised **6-byte `hardware_libs` drift** and its reconciliation decision.
       - every kept-as-undecided commented line and prefs pin value, and the list of files plan 06
         edited beyond its declared `files_modified`.
       - the final size: `du -sb --exclude=.git` and `du -sb pilot/sounds` before vs after, plus
         the computed HYG-08 budget figure (tree minus `.git` minus `pilot/sounds/` ≤ 8 MB).
  </action>
  <verify>
    <automated>cd /home/ido/pi-mirror &amp;&amp; python3 -m compileall -q autopilot/autopilot tools tests &amp;&amp; docker compose -f /home/ido/mics-backend/docker-compose.yml exec -T api python -m pytest -q tests/ &amp;&amp; test ! -e autopilot/tests &amp;&amp; test ! -e autopilot/examples &amp;&amp; test ! -e autopilot/docs &amp;&amp; test ! -e terminal &amp;&amp; python3 tools/check_tree_integrity.py --final &amp;&amp; { find . -name '__pycache__' -type d -not -path './.git/*' -prune -exec rm -rf {} + ; find . -name '*.pyc' -not -path './.git/*' -delete ; rm -rf .pytest_cache ; } &amp;&amp; test -z "$(find . -name '__pycache__' -type d -not -path './.git/*')" &amp;&amp; test -z "$(find . -name '*.pyc' -not -path './.git/*')" &amp;&amp; test ! -e .pytest_cache &amp;&amp; python3 -c "
import subprocess
tot=int(subprocess.run(['du','-sb','--exclude=.git','.'],capture_output=True,text=True).stdout.split()[0])
snd=int(subprocess.run(['du','-sb','pilot/sounds'],capture_output=True,text=True).stdout.split()[0])
budget=tot-snd
assert budget &lt;= 8*1024*1024, f'HYG-08 budget {budget} B &gt; 8 MB (tree-minus-git {tot}, sounds {snd})'
print(f'HYG-08 OK: {budget} B excluding .git and pilot/sounds/')" &amp;&amp; grep -c 'PROVEN' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-HARDWARE-VALIDATION.md &amp;&amp; grep -q 'MLX90640' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-HARDWARE-VALIDATION.md &amp;&amp; grep -q 'known_dangling' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-HARDWARE-VALIDATION.md</automated>
  </verify>
  <done>
    No `__pycache__`, `.pyc` or `.pytest_cache` survives outside `.git` **after the final purge,
    which runs after everything in this plan that can create one — this plan's own `compileall`
    and pytest, and `--final` itself, whose F4 shells out to
    `python3 -m compileall -q autopilot/autopilot/tasks` and would otherwise regenerate
    `autopilot/autopilot/tasks/__pycache__` after the "authoritative" purge had already run**, and
    `autopilot/{tests,examples,docs}` and `terminal/` are absent. `check_tree_integrity.py --final` exits 0 with
    every F1–F5 assertion intact and none weakened; the HYG-08 budget (tree minus `.git` minus
    `pilot/sounds/`) is ≤ 8 MB. The 30-path manifest diff shows zero drift. `compileall` exits 0,
    the Pi suite has **no new failing node ids** against `30-PYTEST-BASELINE.json` (its 179
    baseline failures remain, as expected), and the backend suite is green at 352 pass.
    `30-HARDWARE-VALIDATION.md` has a merged §4 ledger, 12 PROVEN rows (HYG-03..HYG-13 plus
    HYG-09), HYG-01 and HYG-02 still UNPROVEN, and a §6 carrying the closed HYG-05 correction,
    the open `OG_TRIGGER` inventory correction, and both instrument-level exemptions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the publication handover (HYG-01 repo-hygiene half)</name>
  <files>
    /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-PUBLISH.md,
    /home/ido/pi-mirror/README.md
  </files>
  <action>
    **A. `pi-mirror/README.md`** is currently 116 bytes. Rewrite it as the new repo's front door:
    what the tree is (the pilot agent for a MICS behavioural rig), what it is not (the Terminal
    agent is gone — the orchestrator replaced it), how to start the pilot
    (`python3 -m autopilot.core.pilot -f pilot/prefs.json`), that `pilot/prefs.json` is a
    **template** requiring `TERMINALIP` and `NAME` before first boot, how to run the tests
    (`python3 -m pytest -q` from the repo root), and what `tools/check_tree_integrity.py` is for
    (run it after any tree change; it is a permanent artifact, not phase scaffolding).
    Keep it short. Do not paste the phase history into it.

    **B. Write `.planning/phases/30-pi-repo-cleanup/30-PUBLISH.md`** — the user's procedure, in
    order, every command copy-pasteable and every one to be run **by the user**:

    **Step 1 — Revoke the credential (do this FIRST).**
    Google account → Security → App passwords → revoke the app password used by
    `send_email()` in the deleted `AssociationLearning.py`. Record the revocation date; it
    belongs in `30-HARDWARE-VALIDATION.md` §3. State plainly why order matters: deleting the file
    does not revoke anything, and the credential is live in the mirror's `.git` until revoked.

    **Step 2 — Create the new repository as a fresh `git init`.**
    Give the exact sequence: copy the cleaned tree to a new directory **excluding `.git`**
    (`rsync -a --exclude='.git' /home/ido/pi-mirror/ /home/ido/<new-repo>/` — note this is a
    local copy, not a push to the Pi, and carries no `--delete`), then `git init`,
    `git add -A`, first commit, `git remote add origin <same git account>`, `git push -u origin
    main`. Spell out, in these words so the gate below can find them: **never `git clone`, never
    `git filter-repo`/`filter-branch`.** A rewrite leaves reflogs and packed objects, and the
    mirror's 193 MB history has no other value here.

    **Note on the verify gate:** it asserts these warnings are **present** (`grep -q
    'never .*git clone'` and `grep -q 'never .*filter-repo'`), not absent. The credential lives in
    the mirror's 193 MB `.git`, so this is the single most important line in the procedure — a
    gate that forbade the words would forbid the warning itself.

    **Step 3 — Prove HYG-01.**
    ```
    git -C /home/ido/<new-repo> log -p --all | grep -c -F -f /home/ido/.hyg01-probe.txt
    ```
    Expect **0**. Explain that `/home/ido/.hyg01-probe.txt` was captured in Wave 0 and lives
    outside every repository deliberately, and that its sha256 is recorded in
    `30-HARDWARE-VALIDATION.md` §3 while its contents are not. Tell the user to paste the `0`
    into §3 and flip HYG-01 to PROVEN.
    Add a second, cheaper check for reassurance:
    `git -C /home/ido/<new-repo> log --oneline | wc -l` should be **1** — a fresh init has
    exactly one commit, which is itself the strongest evidence that no old history came along.

    **Step 4 — Cut the `.28` branch on the old repo.**
    On the **user's machine**, from the old repository, create a branch holding the cleaned tree
    so `.28` can check it out later. Note explicitly: `.28` is **not** migrated by this phase and
    is **not** reimaged; converting it in place is deferred. New units get a fresh OS image and
    the new repo — **the user handles imaging; do not script it.**

    **Step 5 — Before the rig proof (plan 09 blocker).**
    Point at plan 09: the `ExtlinkDemo` fixture must come off pilot 1 first, or the HYG-02
    session fails for reasons unrelated to this phase.

    **C. Add a "What the agent verified vs what remains" table** at the top of `30-PUBLISH.md`,
    so the user can see at a glance which of the 14 requirements are already PROVEN by automated
    evidence and which two depend on their actions.
  </action>
  <verify>
    <automated>test -f /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-PUBLISH.md &amp;&amp; grep -q 'hyg01-probe' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-PUBLISH.md &amp;&amp; grep -q 'git init' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-PUBLISH.md &amp;&amp; grep -q 'never .*git clone' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-PUBLISH.md &amp;&amp; grep -q 'never .*filter-repo' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-PUBLISH.md &amp;&amp; grep -q 'git init' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-PUBLISH.md &amp;&amp; grep -q 'CHANGE_ME\|template' /home/ido/pi-mirror/README.md &amp;&amp; test -f /home/ido/.hyg01-probe.txt</automated>
  </verify>
  <done>
    `30-PUBLISH.md` exists with the five ordered steps, the fresh-`git init` sequence, the
    `grep -c -F -f` history proof against `/home/ido/.hyg01-probe.txt`, the one-commit sanity
    check, and a verified-vs-remaining table. It explicitly **warns against** `git clone` and
    `git filter-repo`/`filter-branch` (asserted positively by the verify gate) and recommends
    neither anywhere. `pi-mirror/README.md` documents the tree, the prefs template and the integrity
    guard. The probe file still exists.
  </done>
</task>

</tasks>

<verification>
- `python3 /home/ido/pi-mirror/tools/check_tree_integrity.py --final` exits 0, with F3 asserting
  the three **call forms** and not the bare tokens, and F1 additionally asserting
  `autopilot/{tests,examples,docs}` and `terminal/` are gone.
- 30-path md5 manifest diff against the plan 01 baseline: zero drift.
- **Pi suite: the `delta_command` from `30-PYTEST-BASELINE.json`** — zero new failing node ids
  against the Wave 0 baseline of 179 failed / 181 passed over 19 collectable modules. A bare
  `python3 -m pytest -q` is not a valid gate here and must not be chained with `&&`.
- `docker compose exec -T api python -m pytest -q tests/` — 352 pass.
- `du -sb --exclude=.git /home/ido/pi-mirror` **minus** `du -sb /home/ido/pi-mirror/pilot/sounds`
  is ≤ 8 MB. `pilot/sounds/` (2.3 MB) is reachable rig audio and is excluded from the budget by
  design.
- `find /home/ido/pi-mirror -name '__pycache__' -not -path '*/.git/*'` returns nothing.
- `grep -n 'OG_TRIGGER\|IR1' /home/ido/pi-mirror/pilot/prefs.json` still returns the live
  `HARDWARE.GPIO` pin declarations. That is correct and must not be "fixed".
- No git command was run in `/home/ido/pi-mirror`. Nothing was deployed to the Pi.
</verification>

<success_criteria>
1. `--final` green; every completion assertion (F1–F5) passes without being weakened **and
   without any live code being deleted to satisfy one** — in particular `pilot/prefs.json`'s
   `HARDWARE.GPIO.IR1` and `HARDWARE.GPIO.OG_TRIGGER` are untouched.
1b. The cache purge deferred from plan 02 has landed **and was run after this plan's own final
   `compileall`/pytest *and* after `--final`, whose F4 `compileall` would otherwise regenerate
   it**, so nothing regenerated it: no `__pycache__`, `.pyc` or `.pytest_cache`
   outside `.git`, and `autopilot/{tests,examples,docs}` and `terminal/` absent.
2. HYG-13 proven by a zero-drift manifest diff over all 30 protected paths, with the three
   reserved-absent names still absent and unflagged.
3. `30-HARDWARE-VALIDATION.md` consolidated: 14-row verdict table, merged §4 removal ledger,
   §6 findings including the **closed** HYG-05 correction, the **open** `OG_TRIGGER` inventory
   correction against 30-CONTEXT.md, both instrument-level exemptions (`known_dangling`,
   `scan_skip`), the three-number test-count reconciliation, and the pytest baseline/delta.
4. HYG-01 and HYG-02 remain UNPROVEN — they depend on user actions.
5. `30-PUBLISH.md` gives an exact, agent-free publication procedure with the history proof.
</success_criteria>

<output>
After completion, create
`.planning/phases/30-pi-repo-cleanup/30-08-SUMMARY.md`.
</output>
