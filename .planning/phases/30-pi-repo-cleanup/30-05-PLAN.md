---
phase: 30-pi-repo-cleanup
plan: 05
type: execute
wave: 3
depends_on: ["30-01", "30-04"]
files_modified:
  - /home/ido/pi-mirror/autopilot/autopilot/tasks/children.py
  - /home/ido/pi-mirror/autopilot/autopilot/tasks/nafc.py
  - /home/ido/pi-mirror/autopilot/autopilot/tasks/gonogo.py
  - /home/ido/pi-mirror/autopilot/autopilot/tasks/free_water.py
  - /home/ido/pi-mirror/autopilot/autopilot/tasks/test.py
  - /home/ido/pi-mirror/autopilot/autopilot/tasks/RecordingBox.py
  - /home/ido/pi-mirror/autopilot/autopilot/tasks/protocol_scripts.py
  - /home/ido/pi-mirror/autopilot/autopilot/utils/registry.py
  - /home/ido/pi-mirror/autopilot/autopilot/hardware/cameras.py
  - /home/ido/pi-mirror/autopilot/autopilot/hardware/usb.py
  - /home/ido/pi-mirror/autopilot/autopilot/hardware/i2c.py
  - /home/ido/pi-mirror/autopilot/autopilot/setup/setup_mlx90640.sh
  - /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-05-ledger.md
autonomous: true
requirements: [HYG-05, HYG-06]
must_haves:
  truths:
    - "Every file in autopilot/tasks/ compiles — a syntactically broken file there ships `tasks: []` in every handshake"
    - "cameras.py is gone and nothing left in the tree references Camera or MLX90640"
    - "The hardware_libs copy of i2c.py that the Pi exec()s carries the identical edits as the disk copy"
    - "Touch_Detector / MPR121 still exist in i2c.py — the lick path is untouched"
    - "The last surviving set_cdc_manual(0x3f) toggle goes with RecordingBox.py, completing HYG-14's third DELETE"
    - "The REGISTRIES dispatch table no longer names a deleted module — the one string-keyed reference the AST guard cannot see"
  artifacts:
    - path: "/home/ido/pi-mirror/autopilot/autopilot/hardware/i2c.py"
      provides: "MPR121 + Touch_Detector + Motor_Shield_Hat, minus the camera subtree"
      contains: "class Touch_Detector"
    - path: "/home/ido/pi-mirror/autopilot/autopilot/tasks/mics_task.py"
      provides: "The live sourceless task base — imports i2c at line 4, must still import cleanly"
  key_links:
    - from: "autopilot/autopilot/tasks/mics_task.py"
      to: "autopilot/autopilot/hardware/i2c.py"
      via: "import at mics_task.py:4"
      pattern: "hardware\\.i2c|from autopilot.hardware import"
    - from: "hardware_libs id 9 (active version)"
      to: "autopilot.hardware.* on the Pi"
      via: "source_code exec()'d during toolkit hardware-class resolution"
      pattern: "hardware-libs/9"
---

<objective>
Remove the registry sweep collateral (HYG-06) and the camera subtree (HYG-05), reconcile the
`hardware_libs` copy of `i2c.py` that the Pi `exec()`s, and retire the phase's last surviving
`0x3f` toggle.

Purpose: `registry.get_task()` AST-sweeps `autopilot/tasks/` at every boot, dragging in
`cameras.py`, `usb.py` and the whole `transform/` and `stim/` trees — roughly 200 KB of import
work per start. Deleting the sweep collateral removes that. But `autopilot/tasks/` is the one
directory with **no per-file guard** (`common.py:47-67`'s `list_classes`), so a syntactically
broken file there makes `discover_tasks_metadata`'s blanket handler ship `tasks: []` in every
handshake. `compileall` over the surviving `tasks/` is a gate, not a nicety.
Output: a lean `tasks/` that compiles, no camera code on either side of the DB boundary, a
recorded reconciliation of the 6-byte `hardware_libs` drift, and `set_cdc_manual(0x3f)` gone from
the tree.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-pi-repo-cleanup/30-CONTEXT.md
@.planning/phases/30-pi-repo-cleanup/30-VALIDATION.md
@.planning/phases/30-pi-repo-cleanup/30-01-SUMMARY.md
@.planning/phases/30-pi-repo-cleanup/30-04-SUMMARY.md
@api/routers/hardware_libs.py

<pi_rules>
ABSOLUTE (STATE.md:1727-1745). Override anything below:
- Edit ONLY `/home/ido/pi-mirror`. **No git command in that directory — not even `status`.**
- Never `rsync --delete`. This plan does not rsync. **This plan does not deploy anything.**
- Never start/stop the pilot. Never run Python on the Pi.
- `autopilot` cannot be imported on this host; `compileall` is the verification ceiling.
</pi_rules>

<HYG_05_is_a_THREE_part_edit>
## The requirement has been amended. This block records what the amendment says and why.

**Status, 2026-08-10: the correction has LANDED.** `REQUIREMENTS.md` HYG-05 and
`.planning/phases/30-pi-repo-cleanup/30-CONTEXT.md` both now carry the full `Camera` / `MLX90640`
correction — REQUIREMENTS.md in the row itself ("**Corrected 2026-08-10 during planning** … the
edit is therefore **three-part**"), 30-CONTEXT.md as an inline `> **CORRECTION, 2026-08-10
(planning).**` block under the `cameras.py` conflict. **Neither file still carries the false
text.** Do not go looking for wording to fix; do not re-open the finding. It remains recorded here
because it is the reason the edit has three parts and not two, and because getting it wrong kills
the pilot silently.

**The finding.** An earlier reading of HYG-05 claimed `Camera` was "**never used** in `i2c.py`;
the only other matches are docstring prose", and therefore that removing the file plus the
`i2c.py:8` import was sufficient. That was **false**. Direct inspection of `/home/ido/pi-mirror`:

```
autopilot/autopilot/hardware/i2c.py:8    from autopilot.hardware.cameras import Camera
autopilot/autopilot/hardware/i2c.py:580  class MLX90640(Camera):
autopilot/autopilot/hardware/i2c.py:632      super(MLX90640, self).__init__(fps, **kwargs)
```

`Camera` is the **base class of `MLX90640`**, which spans `i2c.py:580` through `i2c.py:798`
(the next class, `MPR121`, begins at `:799`). Removing only the import leaves
`class MLX90640(Camera):` evaluating an undefined name at module import time → `NameError` →
`mics_task.py:4` fails → the pilot dies at import. This is exactly the failure mode HYG-02
exists to catch, and it would present silently (`pilot.py:609` reports nothing).

Root cause worth remembering, recorded in 30-CONTEXT.md: on this host `grep` output passes
through a compressing proxy that rendered line 580 as blank. **Any load-bearing "symbol X is
unused" claim must be re-checked with unfiltered output** before it is acted on.

**Reachability verdict on `MLX90640` itself** (the four criteria, all verified):
1. Not in the pilot's static closure other than via `i2c.py` itself.
2. Not dynamically reachable — no `PLUGINDIR` or `tasks/` sweep hit; the only self-references
   are inside `i2c.py` (`import MLX90640 as mlx_cam` at `:33` guards an optional third-party
   library that is not installed, setting `MLX90640_LIB = False`).
3. Not named by the backend — `grep -rn 'MLX90640\|cameras' /home/ido/mics-backend/api
   /home/ido/mics-backend/orchestrator` returns **zero** hits; no `hardware_libs` /
   `hardware_modules` row resolves to it; `pilot/prefs.json` contains no `MLX` key.
4. Not reserved by any pending phase.

**So the correct HYG-05 edit is three parts:** delete `cameras.py`, delete the `i2c.py:8` import,
**and delete the `MLX90640` class body (`i2c.py:580`–`:798`)** — in BOTH the Pi copy and the
`hardware_libs` copy. Record the confirmation in the ledger fragment (plan 08 carries it into §6
as a closed finding, not an open one).

`i2c.py` is otherwise off-limits under TRIGA-12. This plan's edits are scoped to exactly the
import line, the `MLX90640` class and its `mlx_cam` guard. **Do not touch `MPR121` (`:799`),
`Motor_Shield_Hat` (`:892`), `Motor_Shield_Hat_extend` (`:949`) or `Touch_Detector` (`:973`)**,
and do not "fix" `i2c.py:819`'s `except(e):` — that defect is deferred by name in 30-CONTEXT.md.
</HYG_05_is_a_THREE_part_edit>

<the_last_0x3f_toggle_lives_here>
`tasks/RecordingBox.py`, removed by Task 1 of this plan, carries the **final** two sites of the
corrected toggle inventory that plan 04 handed off:

```
autopilot/autopilot/tasks/RecordingBox.py:53    'OG_TRIGGER': {        ← third HARDWARE-dict declaration
autopilot/autopilot/tasks/RecordingBox.py:104   # self.hardware['I2C']['MPR121'].set_cdc_manual(0x3f)
```

30-CONTEXT.md's claim that the `OG_TRIGGER` declaration exists only at `mics_cage_task.py:97` and
`learning_cage.py:82` misses this one (and misses `pilot/prefs.json`, which keeps its live pin
declaration on purpose). Plan 04's `<CORRECTED_toggle_inventory>` holds the full table; the
coordinator owns the 30-CONTEXT.md amendment.

**Consequence for this plan:** after Task 1, `set_cdc_manual(0x3f)` is absent tree-wide and
HYG-14's third DELETE toggle is complete. Assert that here — it is the plan that earns it.

**Still do not assert bare `OG_TRIGGER`, bare `IR1` or bare `0x3f` tree-wide.** `pilot/prefs.json`
carries `HARDWARE.GPIO.OG_TRIGGER` and `HARDWARE.GPIO.IR1` as live pin declarations that survive
the phase by design; HYG-10 deliberately leaves the `GPIO`, `I2C`, `Mixer`, `Timers` and `Modules`
groups alone. Assert the **call form**, never the token. Plan 01's
`<F3_must_assert_call_forms_not_tokens>` explains what a bare-token assertion would force at the
exit gate.
</the_last_0x3f_toggle_lives_here>

<the_string_keyed_reference_the_guard_cannot_see>
## `utils/registry.py:42` names `tasks/children.py`. Task 1 owns it.

```
autopilot/autopilot/utils/registry.py:42   CHILDREN = "autopilot.tasks.children.Child"
autopilot/autopilot/core/pilot.py:591      task_class = autopilot.get('children', value['task_type'])
```

`REGISTRIES` is exactly the **string-keyed dispatch table** named in the phase's removal
criterion 2, and `pilot.py:591` is its only live consumer — inside `l_start`, the START path,
whose handler at `:607-609` sets `IDLE` and reports nothing to the orchestrator. So a break here
presents as "the run just didn't start", with no error anywhere.

The guard **cannot** catch this on its own: after plan 01's B1 fix, check 2(b) matches invocation
context (`-m`/`import`/`from` + module), and `"autopilot.tasks.children.Child"` is a bare enum
value with no invocation form. It was found by hand, and post-sweep it is one of exactly **two**
string-level danglers tree-wide — the other, `setup_autopilot.py:198`, is owned by plan 03. This
plan owns this one.

**Four-criteria verdict on the `CHILDREN` enum member** (record all four in the ledger):
1. Not in the pilot's static closure — `registry.py` survives, but the enum *value* names
   `tasks/children.py`, deleted by Task 1 of this plan.
2. Dynamically reached only via `autopilot.get('children', …)` at `pilot.py:591`, which is
   guarded by `if 'child' in value.keys()`. **Verified: no MICS START message ever carries a
   `child` key** — `grep -rn "'child'" /home/ido/mics-backend/orchestrator /home/ido/mics-backend/api`
   returns zero hits. Re-run that grep and record the result; do not take it on trust.
3. Not named by the backend — same grep.
4. Not reserved by any pending phase.

**Decision on the `pilot.py:591` branch, taken explicitly rather than left as a mitigation:**
the whole `if 'child' in value.keys():` / `else:` construct at `pilot.py:589-592` is **removed
too**, collapsing to the unconditional `task_class = autopilot.get_task(value['task_type'])`.
Rationale: leaving it would mean this phase knowingly ships a dispatch call into a registry entry
it just deleted — a latent break on the START path that the guard is structurally unable to see,
which would need a *second* `known_dangling`-style exemption to be honest about. Removing it is a
four-line deletion of a branch that is unreachable for MICS by criterion 2 above, and plan 09's
live rig session exercises START end-to-end, so it is the one behavioural change in this phase
that actually has a proof scoped to it.

**`pilot.py` is plan 06's file, not this one's.** This plan removes only the enum member; **plan
06 Task 1 removes the branch**, and plan 06 already `depends_on: ["30-05"]` (Wave 4 after Wave 3),
so the ordering is enum-member-then-consumer within a single wave boundary. Hand the verdict to
plan 06 through the ledger.
</the_string_keyed_reference_the_guard_cannot_see>

<verified_db_state>
Queried 2026-08-10 against the live Postgres:

| lib id | name | filename | active_version_id | version_number | len(source_code) |
|---|---|---|---|---|---|
| 9 | I2C Driver | i2c.py | 26 | 2 | 35928 |

- Disk `i2c.py` is **35934** bytes → the 6-byte drift 30-CONTEXT.md records, confirmed.
- Version 26's `source_code` contains `from autopilot.hardware.cameras import Camera`
  (offset 189) **and** `class MLX90640` (offset 21345). Both edits must reach it.
- `PUT /api/hardware-libs/{lib_id}` (`api/routers/hardware_libs.py:409-461`) is the correct
  mechanism: it creates a **new version row**, sets `active_version_id`, re-runs the AST
  extraction, and returns an `impact` block listing `removed_methods` and
  `affected_definition_ids`. It does not mutate version 26 — history is preserved, which is what
  makes the reconciliation auditable.
- The `_validate_compute_lib` gate at `:421-422` applies only to `kind == "compute"`; lib 9 is
  `kind = "hardware"`, so it is not run.
- `_diff_removed_methods` + `_flag_broken_task_defs` (`:444-445`) will flag task definitions that
  reference methods removed by the edit. `MLX90640`'s methods are referenced by no task
  definition, so `impact.affected_definition_ids` is expected to come back **empty** — assert it.
</verified_db_state>

<verified_reachability>
Confirmed by grep on `/home/ido/pi-mirror` 2026-08-10:
- `hardware/cameras.py` importers: `tasks/children.py:18,23`, `core/terminal.py:951` (docstring),
  `core/gui.py:3184`, `autopilot/tests/test_registry.py:14-16`, `hardware/i2c.py:8`, **and
  `tasks/test.py:18`** — the last written `from autopilot.hardware import gpio, cameras`, a
  submodule-via-package form that the guard only sees under
  `<from_package_import_submodule_must_resolve>` in plan 01 (re-measured 2026-08-10: removing
  `cameras.py` yields 5 AST violations, and this is one of them).
  Plans 02 and 03 already removed `core/gui.py`, `core/terminal.py` and `autopilot/tests/`. That
  leaves `tasks/children.py` **and `tasks/test.py`** (both removed by Task 1 here, items 1 and 5)
  and `i2c.py:8` (Task 2 here). Clean cut — and note the ordering is load-bearing: Task 1 must
  land before Task 2, or Task 2's `--strict` fails on `tasks/test.py` and `tasks/children.py`.
- `hardware/usb.py` importers: `tasks/children.py` and the already-removed
  `autopilot/tests/test_registry.py`. It goes with `children.py`.
- `setup/setup_mlx90640.sh` exists and provisions the third-party MLX90640 library for the class
  this plan deletes. Its only in-tree mentions are `i2c.py:606` (docstring) and `i2c.py:630`
  (the `ImportError` message inside the `MLX90640` class) — both removed by Task 2. It is
  **residue, not a dangling reference**, so the guard would not catch it; Task 2 applies the four
  criteria and records a verdict, in the same way plan 02 handled `Testing_stepper_motor_Hat/`.
  (Plan 03 removed the sibling `install_pyspin.sh` because that one *did* dangle.)
- After plan 04, `autopilot/tasks/` holds: `children.py`, `fda_vocabulary.py`, `free_water.py`,
  `gonogo.py`, `graduation.py`, `__init__.py`, `mics_task.py`, `nafc.py`, `protocol_scripts.py`,
  `RecordingBox.py`, `task.py`, `test.py`.
  **Survivors after this plan:** `fda_vocabulary.py`, `graduation.py`, `__init__.py`,
  `mics_task.py`, `task.py`. `fda_vocabulary.py` is on the HYG-13 protect-list (zero top-level
  classes, so the AST sweep never sees it; it survives on one import at `mics_task.py:18`).
</verified_reachability>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove registry sweep collateral from autopilot/tasks/ (HYG-06) and close the 0x3f toggle</name>
  <files>
    /home/ido/pi-mirror/autopilot/autopilot/tasks/children.py,
    /home/ido/pi-mirror/autopilot/autopilot/tasks/nafc.py,
    /home/ido/pi-mirror/autopilot/autopilot/tasks/gonogo.py,
    /home/ido/pi-mirror/autopilot/autopilot/tasks/free_water.py,
    /home/ido/pi-mirror/autopilot/autopilot/tasks/test.py,
    /home/ido/pi-mirror/autopilot/autopilot/tasks/RecordingBox.py,
    /home/ido/pi-mirror/autopilot/autopilot/tasks/protocol_scripts.py,
    /home/ido/pi-mirror/autopilot/autopilot/utils/registry.py
  </files>
  <action>
    Plain `rm`. **No git.**

    Delete, in this order (importers before imported — `children.py` first, since it is the one
    that drags in `cameras.py` and `usb.py`):
    1. `autopilot/autopilot/tasks/children.py`
    2. `autopilot/autopilot/tasks/nafc.py`
    3. `autopilot/autopilot/tasks/gonogo.py`
    4. `autopilot/autopilot/tasks/free_water.py`
    5. `autopilot/autopilot/tasks/test.py`
    6. `autopilot/autopilot/tasks/RecordingBox.py`
    7. `autopilot/autopilot/tasks/protocol_scripts.py`

    **Do NOT delete** `mics_task.py`, `task.py`, `graduation.py`, `fda_vocabulary.py`,
    `__init__.py`.

    Then check `autopilot/autopilot/tasks/__init__.py` (37 bytes) — if it names any removed
    module, edit it. A broken `__init__.py` in this directory is the worst outcome available:
    `common.py:47-67`'s `list_classes` has no per-file guard, so `discover_tasks_metadata`'s
    blanket handler would ship `tasks: []` in every handshake with no error anywhere.

    **Then edit `autopilot/autopilot/utils/registry.py` — the one string-keyed reference the
    guard cannot see.** Read `<the_string_keyed_reference_the_guard_cannot_see>` first.
    - Delete the single enum member `CHILDREN = "autopilot.tasks.children.Child"` (line 42) from
      `class REGISTRIES`. **Leave `HARDWARE`, `TASK`, `GRADUATION`, `TRANSFORM` and `SOUND`
      alone**, and change nothing else in the file — `registry.py` survives the phase.
    - Note `TRANSFORM = "autopilot.transform.transforms.Transform"` and
      `SOUND = "autopilot.stim.sound.sounds.BASE_CLASS"` **stay**: `transform/` and `stim/` are
      not removed by this phase. Only `CHILDREN` names a deleted module.
    - Re-run the criterion-2/3 grep and record its output verbatim:
      `grep -rn "'child'" /home/ido/mics-backend/orchestrator /home/ido/mics-backend/api`
      → expected **zero** hits. **If it returns anything, stop and report** — a MICS START
      message carrying a `child` key would make `pilot.py:591` live code and invalidate the
      decision recorded in that block.
    - `python3 -m py_compile autopilot/autopilot/utils/registry.py`.
    - Record the four-criteria verdict for the `CHILDREN` member in the ledger, **and record the
      hand-off to plan 06**: plan 06 Task 1 removes the now-orphaned
      `if 'child' in value.keys():` branch at `pilot.py:589-592`.

    Also grep the surviving tree for `tasks.children`, `tasks.nafc`, `tasks.gonogo`,
    `tasks.free_water`, `tasks.RecordingBox`, `tasks.protocol_scripts` and `Nafc`, `GoNoGo`,
    `Free_Water`, `Children` as *string literals* (`registry.get_task()` resolves by name, so a
    task-name string in `pilot/protocols/*.json` is a live reference). Handle hits by location —
    **every location has an instruction, none is left to judgement:**
    - `pilot/protocols/*.json` → record in the ledger as a legacy protocol row that is not run;
      **do not** delete the protocol file.
    - a surviving file under `autopilot/` → this is a dangling reference in a survivor. The only
      one known to exist is `utils/registry.py:42`, handled above. **If the grep finds any
      other, stop and report** rather than improvising a removal: a survivor naming a deleted
      task is either a second dispatch table (which needs its own four-criteria verdict) or
      evidence that a deletion in this task was wrong.
    - `mics-backend` → **stop and report.**
    Record the full grep output in the ledger either way, including the empty case.

    **Close the toggle handed off by plan 04.** `RecordingBox.py` held the last
    `set_cdc_manual(0x3f)` (`:104`) and a third `OG_TRIGGER` HARDWARE-dict declaration (`:53`).
    After the deletions above, assert over surviving `.py` files:
    - `set_cdc_manual(0x3f)` — **zero** occurrences tree-wide. HYG-14's third DELETE toggle is now
      complete; say so in the ledger.
    - `OG_TRIGGER` — the **only** remaining occurrences are in `pilot/prefs.json`
      (`HARDWARE.GPIO.OG_TRIGGER`, lines ~241/243) and, until plan 07, `pilot/prefs_wsl*.json`.
      **Zero** occurrences in any `.py`. Do not touch `prefs.json`; that pin declaration is live
      and survives the phase.
    Exclude `tools/check_tree_integrity.py`, `tools/tree_integrity/` and
    `tests/test_tree_integrity.py` from both scans — the guard contains these literals *because*
    it searches for them (plan 01 keeps them in a `SELF_PATHS` set for the same reason).
    **Use `--exclude-dir=tree_integrity` alongside the two `--exclude=` filters, or do the scan
    in Python with plan 04's `SELF` prefix-tuple shape.** `grep --exclude=` matches **basenames
    only**, so if plan 01 split the guard into `tools/tree_integrity/` (which plan 01 explicitly
    permits, and the 300-line project standard makes likely), helper modules there would carry
    the very literals being searched for and would fail this scan for the wrong reason.

    Record both results and the corrected inventory in the ledger.

    **Record.** Write `.planning/phases/30-pi-repo-cleanup/ledger/30-05-ledger.md` (create
    `ledger/` if absent) with a row per removed path, four criterion verdicts, plus the
    `__init__.py` and protocol-string findings, the toggle-closure results, plus a proposed
    `HYG-06 | PROVEN | <evidence>`.
  </action>
  <verify>
    <automated>cd /home/ido/pi-mirror &amp;&amp; test ! -e autopilot/autopilot/tasks/children.py &amp;&amp; test ! -e autopilot/autopilot/tasks/nafc.py &amp;&amp; test ! -e autopilot/autopilot/tasks/RecordingBox.py &amp;&amp; test -f autopilot/autopilot/tasks/mics_task.py &amp;&amp; test -f autopilot/autopilot/tasks/task.py &amp;&amp; test -f autopilot/autopilot/tasks/graduation.py &amp;&amp; test -f autopilot/autopilot/tasks/fda_vocabulary.py &amp;&amp; test -f autopilot/autopilot/utils/registry.py &amp;&amp; ! grep -q 'autopilot.tasks.children' autopilot/autopilot/utils/registry.py &amp;&amp; grep -q 'TASK = ' autopilot/autopilot/utils/registry.py &amp;&amp; grep -q 'GRADUATION = ' autopilot/autopilot/utils/registry.py &amp;&amp; python3 -m py_compile autopilot/autopilot/utils/registry.py &amp;&amp; ! grep -rq 'set_cdc_manual(0x3f)' --include=*.py --exclude=check_tree_integrity.py --exclude=test_tree_integrity.py --exclude-dir=tree_integrity . &amp;&amp; ! grep -rq 'OG_TRIGGER' --include=*.py --exclude=check_tree_integrity.py --exclude=test_tree_integrity.py --exclude-dir=tree_integrity . &amp;&amp; grep -q 'OG_TRIGGER' pilot/prefs.json &amp;&amp; python3 -m compileall -q autopilot/autopilot/tasks &amp;&amp; python3 tools/check_tree_integrity.py --strict</automated>
  </verify>
  <done>
    The seven collateral modules are gone; `mics_task.py`, `task.py`, `graduation.py`,
    `fda_vocabulary.py` and `__init__.py` remain. `utils/registry.py` survives, compiles, and no
    longer carries the `CHILDREN` enum member, while `TASK`, `GRADUATION`, `HARDWARE`,
    `TRANSFORM` and `SOUND` are intact; the four-criteria verdict and the plan-06 hand-off for
    `pilot.py:589-592` are in the ledger. `compileall` over
    `autopilot/autopilot/tasks/` exits 0 (the HYG-06 gate). `set_cdc_manual(0x3f)` and
    `OG_TRIGGER` appear in no surviving `.py`; `pilot/prefs.json` still carries its live
    `HARDWARE.GPIO.OG_TRIGGER` pin declaration, untouched. Guard `--strict` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove cameras.py, usb.py, setup_mlx90640.sh, the i2c import and the MLX90640 class (HYG-05, Pi copy)</name>
  <files>
    /home/ido/pi-mirror/autopilot/autopilot/hardware/cameras.py,
    /home/ido/pi-mirror/autopilot/autopilot/hardware/usb.py,
    /home/ido/pi-mirror/autopilot/autopilot/hardware/i2c.py,
    /home/ido/pi-mirror/autopilot/autopilot/setup/setup_mlx90640.sh
  </files>
  <action>
    Read `<HYG_05_is_a_THREE_part_edit>` above before starting. This is three edits to the camera
    subtree, not two, plus one residue removal.

    1. **Capture the "before" copy.** `cp autopilot/autopilot/hardware/i2c.py
       /tmp/i2c.before.py` and record `md5sum` + byte size of the original (expected 35934).
       You need the before/after pair for the ledger and for the DB reconciliation in Task 3.

    2. `rm autopilot/autopilot/hardware/cameras.py` (67.7 KB).
    3. `rm autopilot/autopilot/hardware/usb.py` (10.3 KB) — its only surviving importer,
       `tasks/children.py`, went in Task 1.
    4. Edit `autopilot/autopilot/hardware/i2c.py`:
       - Delete line 8, `from autopilot.hardware.cameras import Camera`.
       - Delete the entire `MLX90640` class: from `class MLX90640(Camera):` (line 580 before the
         import deletion) through the last line before `class MPR121(Hardware):`. Locate the
         boundaries by class statement, not by hardcoded line number — the import deletion shifts
         everything by one. Remove any now-orphaned module-level guard that exists solely for
         this class: the `try: import MLX90640 as mlx_cam / MLX90640_LIB = True / except:
         MLX90640_LIB = False` block around lines 33-36, and any `MLX90640`-only constant.
       - **Leave everything else byte-identical.** Do not reformat, do not reorder imports, do
         not fix `except(e):` at what is currently `:819`, do not touch `MPR121`,
         `Motor_Shield_Hat`, `Motor_Shield_Hat_extend` or `Touch_Detector`.

    5. **`setup/setup_mlx90640.sh` — residue, apply the four criteria and record the verdict.**
       It provisions the third-party MLX90640 library for the class just deleted. Its only in-tree
       mentions were `i2c.py:606` (docstring) and `i2c.py:630` (the `ImportError` message inside
       the removed class), both gone as of step 4, so it now names nothing and nothing names it.
       Criteria to check and record: (1) not in the pilot's static closure — it is a shell script,
       never imported; (2) not dynamically reachable — grep the surviving tree for
       `setup_mlx90640`; (3) not named by the backend — grep `mics-backend` for `setup_mlx90640`
       and `MLX`; (4) not reserved by a pending phase. If all four hold, `rm` it and record the
       verdicts. If any fails, keep it and record why. **Do not remove it on vibes** — this is the
       same standard plan 02 applied to `Testing_stepper_motor_Hat/`.

    6. **Verify no residue:** `grep -n 'Camera\|MLX90640\|mlx_cam' autopilot/autopilot/hardware/i2c.py`
       must return nothing except, if present, unrelated prose. Any *code* hit means the class
       was not fully removed.

    7. Record `md5sum` + byte size of the edited file. Keep `/tmp/i2c.before.py` for Task 3.
  </action>
  <verify>
    <automated>cd /home/ido/pi-mirror &amp;&amp; test ! -e autopilot/autopilot/hardware/cameras.py &amp;&amp; test ! -e autopilot/autopilot/hardware/usb.py &amp;&amp; ! grep -q 'class MLX90640' autopilot/autopilot/hardware/i2c.py &amp;&amp; ! grep -q 'mlx_cam' autopilot/autopilot/hardware/i2c.py &amp;&amp; ! grep -q 'from autopilot.hardware.cameras' autopilot/autopilot/hardware/i2c.py &amp;&amp; grep -q 'class Touch_Detector' autopilot/autopilot/hardware/i2c.py &amp;&amp; grep -q 'class MPR121' autopilot/autopilot/hardware/i2c.py &amp;&amp; python3 -m py_compile autopilot/autopilot/hardware/i2c.py &amp;&amp; python3 -m compileall -q autopilot/autopilot &amp;&amp; python3 tools/check_tree_integrity.py --strict</automated>
  </verify>
  <done>
    `cameras.py` and `usb.py` are gone. `i2c.py` compiles, contains neither the cameras import
    nor `class MLX90640` nor `mlx_cam`, and still contains `class MPR121` and
    `class Touch_Detector`. `setup_mlx90640.sh` has a recorded four-criteria verdict (removed or
    kept, with reasons). Guard `--strict` exits 0. The before/after md5s and byte sizes are
    recorded.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the hardware_libs copy of i2c.py (HYG-05, DB copy)</name>
  <files>
    /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-05-ledger.md
  </files>
  <action>
    `hardware_libs` id 9 (`I2C Driver`, `filename = i2c.py`) has `active_version_id = 26`, whose
    `source_code` the Pi `exec()`s when a toolkit resolves hardware classes. It still contains
    `from autopilot.hardware.cameras import Camera` and `class MLX90640`. A Pi-only edit leaves
    the exec'd copy importing a module that no longer exists — the pilot would start fine and
    then fail the moment a toolkit resolved hardware, silently.

    1. **Diff before deciding.** Dump version 26's `source_code` to `/tmp/i2c.db.py`:
       ```
       docker compose exec -T db psql -U mics_user -d mics_db -tAc \
         "select source_code from hardware_lib_versions where id=26;" > /tmp/i2c.db.py
       ```
       (psql `-tAc` mangles nothing for this column, but confirm the byte count is 35928 before
       trusting it; if it is not, fetch via `GET /api/hardware-libs/9` instead and take the
       JSON `source_code` field.)
       `diff /tmp/i2c.before.py /tmp/i2c.db.py` — this is the **6-byte drift**. Record the exact
       diff hunks in the ledger. This is the first time the drift has been characterised rather
       than just measured.

    2. **Decide and record.** The drift is either (a) trivial whitespace/comment, in which case
       the reconciliation is "adopt the disk copy as canonical", or (b) a substantive difference,
       in which case **stop and report** rather than silently discarding whichever side loses.
       HYG-05 permits either reconciling or *recording why not* — but it does not permit
       reconciling without knowing what you overwrote.

    3. **If (a):** push the edited disk copy as a new version:
       ```
       curl -s -X PUT http://localhost:8000/api/hardware-libs/9 \
         -H "Authorization: Bearer $MICS_API_TOKEN" -H 'Content-Type: application/json' \
         -d "$(python3 -c 'import json,sys; print(json.dumps({"source_code": open("/home/ido/pi-mirror/autopilot/autopilot/hardware/i2c.py").read()}))')"
       ```
       Read `api/routers/hardware_libs.py:409-461` first and match the `SourceUpdateBody` schema
       exactly (it may require `declared_imports`).
       **Assert on the response:** HTTP 200; a new version id > 26; `lib.active_version_id`
       equals it; and `impact.affected_definition_ids` is **empty** — no task definition
       referenced an `MLX90640` method. If `affected_definition_ids` is non-empty, stop and
       report: something references the camera class after all, which contradicts criterion 3.

    4. **Confirm** the new active version: re-query
       `select l.active_version_id, v.version_number, length(v.source_code),
        position('class MLX90640' in v.source_code) from hardware_libs l
        join hardware_lib_versions v on v.id = l.active_version_id where l.id = 9;`
       → position must be 0, length must equal the disk file's byte size.

    5. **Run the backend suite** — this is the only requirement in Phase 30 that touches
       `mics-backend`, so it is the only one that can regress it:
       `docker compose exec -T api python -m pytest -q tests/`. This one **is** a bare-exit-code
       gate: the backend suite is green at 352 pass, unlike the Pi suite.

    6. **Record.** Append to `ledger/30-05-ledger.md`: the drift diff, the reconciliation
       decision and its rationale, the new version id, the `impact` block, the before/after
       lengths, and a proposed `HYG-05 | PROVEN | <evidence>`. **Also record the HYG-05
       three-part correction as a CLOSED finding** — the amendment has already landed in
       REQUIREMENTS.md and 30-CONTEXT.md (see `<HYG_05_is_a_THREE_part_edit>`), so plan 08 carries
       it into §6 as "corrected and confirmed", not as an open action item.

    Do NOT deploy anything to the Pi. Do NOT restart the pilot. The Pi picks up the new version
    on its next `LOAD_HARDWARE_LIBS`, which the user triggers by starting a run.
  </action>
  <verify>
    <automated>docker compose -f /home/ido/mics-backend/docker-compose.yml exec -T db psql -U mics_user -d mics_db -tAc "select position('class MLX90640' in v.source_code) from hardware_libs l join hardware_lib_versions v on v.id=l.active_version_id where l.id=9;" | tr -d ' ' | grep -qx '0' &amp;&amp; docker compose -f /home/ido/mics-backend/docker-compose.yml exec -T db psql -U mics_user -d mics_db -tAc "select position('autopilot.hardware.cameras' in v.source_code) from hardware_libs l join hardware_lib_versions v on v.id=l.active_version_id where l.id=9;" | tr -d ' ' | grep -qx '0' &amp;&amp; docker compose -f /home/ido/mics-backend/docker-compose.yml exec -T api python -m pytest -q tests/ &amp;&amp; grep -q 'HYG-05' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-05-ledger.md</automated>
  </verify>
  <done>
    `hardware_libs` id 9's active version contains neither `class MLX90640` nor the cameras
    import, and its `source_code` length equals the disk file's.
    `impact.affected_definition_ids` was empty. Backend pytest green (352 pass). The 6-byte drift
    is characterised in the ledger with its diff hunks and a stated reconciliation decision, and
    the HYG-05 three-part correction is recorded as closed.
  </done>
</task>

</tasks>

<verification>
- `python3 -m compileall -q /home/ido/pi-mirror/autopilot/autopilot/tasks` exits 0 — the HYG-06
  gate. A failure here would ship `tasks: []` in every handshake.
- `python3 /home/ido/pi-mirror/tools/check_tree_integrity.py --strict` exits 0.
- `docker compose exec -T api python -m pytest -q tests/` green (352 pass) — the backend suite
  *is* a bare-exit-code gate.
- **Pi suite: run the delta command from `30-01-SUMMARY.md`**, not a bare `pytest`. It exits 0
  when no node id fails that was not already failing in `30-PYTEST-BASELINE.json`. Watch
  `tests/test_mpr121_irq_hygiene.py` and `tests/test_check_for_detectors.py` specifically — they
  exercise the `i2c.py` neighbourhood, and `test_check_for_detectors.py` already contributes 17
  of the baseline's 179 failures, so "still 17" is the signal, not "0".
- `grep -rn 'set_cdc_manual(0x3f)' /home/ido/pi-mirror --include=*.py
  --exclude=check_tree_integrity.py --exclude=test_tree_integrity.py
  --exclude-dir=tree_integrity` returns nothing.
- `grep -rn 'OG_TRIGGER' /home/ido/pi-mirror --include=*.py
  --exclude=check_tree_integrity.py --exclude=test_tree_integrity.py
  --exclude-dir=tree_integrity` returns nothing;
  `grep -n 'OG_TRIGGER' /home/ido/pi-mirror/pilot/prefs.json` still returns its live pin
  declaration. Both are correct.
- No git command was run in `/home/ido/pi-mirror`. Nothing was deployed to the Pi.
</verification>

<success_criteria>
1. The seven registry-sweep collateral modules are gone and `compileall` over
   `autopilot/autopilot/tasks/` exits 0.
1b. `utils/registry.py`'s `CHILDREN` enum member is gone with a recorded four-criteria verdict,
   the `'child'`-key grep over `mics-backend` returned zero hits, and the removal of the
   consuming branch at `pilot.py:589-592` is handed to plan 06.
2. `cameras.py` and `usb.py` are gone; `i2c.py` has lost the cameras import, the `MLX90640`
   class and the `mlx_cam` guard, and nothing else. `setup_mlx90640.sh` has a recorded
   four-criteria verdict.
3. `MPR121` and `Touch_Detector` are intact in `i2c.py` — the lick path is untouched.
4. `hardware_libs` id 9's active version matches the edited disk file byte-for-byte, with an
   empty `affected_definition_ids` impact, and the 6-byte drift is characterised in the ledger.
5. `set_cdc_manual(0x3f)` is gone tree-wide, completing HYG-14's third DELETE toggle;
   `OG_TRIGGER` is gone from every `.py` while `pilot/prefs.json`'s live pin declaration is
   untouched.
6. The HYG-05 three-part correction is recorded as a closed finding (the requirement text was
   already amended).
</success_criteria>

<output>
After completion, create
`.planning/phases/30-pi-repo-cleanup/30-05-SUMMARY.md`.
</output>
