---
phase: 30-pi-repo-cleanup
plan: 03
type: execute
wave: 1
depends_on: ["30-01"]
files_modified:
  - /home/ido/pi-mirror/terminal/
  - /home/ido/pi-mirror/run_terminal.sh
  - /home/ido/pi-mirror/.vscode/
  - /home/ido/pi-mirror/autopilot/autopilot/core/gui.py
  - /home/ido/pi-mirror/autopilot/autopilot/core/terminal.py
  - /home/ido/pi-mirror/autopilot/autopilot/core/plots.py
  - /home/ido/pi-mirror/autopilot/autopilot/core/subject.py
  - /home/ido/pi-mirror/autopilot/autopilot/core/styles.py
  - /home/ido/pi-mirror/autopilot/autopilot/core/utils.py
  - /home/ido/pi-mirror/autopilot/autopilot/core/reward.py
  - /home/ido/pi-mirror/autopilot/autopilot/viz/
  - /home/ido/pi-mirror/autopilot/autopilot/data_handlers/
  - /home/ido/pi-mirror/autopilot/autopilot/utils/invoker.py
  - /home/ido/pi-mirror/autopilot/autopilot/utils/Event.py
  - /home/ido/pi-mirror/autopilot/autopilot/setup/request_helpers.py
  - /home/ido/pi-mirror/autopilot/autopilot/setup/install_pyspin.sh
  - /home/ido/pi-mirror/autopilot/autopilot/setup/setup_autopilot.py
  - /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-03-ledger.md
autonomous: true
requirements: [HYG-07]
must_haves:
  truths:
    - "No Terminal-era module remains in the tree"
    - "`import autopilot` is still structurally intact — setup_autopilot.py survives"
    - "No surviving file, string literal, shell script or JSON names a removed Terminal module"
    - "The last shell-script consumer of request_helpers is gone in the same change as request_helpers itself"
  artifacts:
    - path: "/home/ido/pi-mirror/autopilot/autopilot/setup/setup_autopilot.py"
      provides: "Imported unconditionally by autopilot/__init__.py:4 — must survive"
    - path: "/home/ido/pi-mirror/autopilot/autopilot/core/pilot.py"
      provides: "The pilot agent — the whole point of the tree"
    - path: "/home/ido/pi-mirror/autopilot/autopilot/core/loggers.py"
      provides: "init_logger, imported by pilot.py"
    - path: "/home/ido/pi-mirror/autopilot/autopilot/core/View.py"
      provides: "Task view — live"
  key_links:
    - from: "autopilot/autopilot/__init__.py"
      to: "autopilot/autopilot/setup/setup_autopilot.py"
      via: "unconditional import at line 4"
      pattern: "setup_autopilot"
---

<objective>
Remove the Terminal-era tree (HYG-07). The orchestrator replaced the Terminal agent wholesale;
what remains is 18 MB of a GUI nobody launches plus 16.4 MB of its rotated log spew.

Purpose: this is the largest *code* removal in the phase and it is a clean cut — every importer
of every module removed here is itself removed here, in this plan or in plan 02's parallel sweep.
It runs in parallel with plan 02 (no shared files) and must land before plan 04, because 10
`terminal/plugins/*.py` files import `autopilot.hardware.unreal`, which plan 04 deletes.
Output: a tree with a single agent (the pilot), and a ledger fragment justifying each removal
against the four criteria.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-pi-repo-cleanup/30-CONTEXT.md
@.planning/phases/30-pi-repo-cleanup/30-VALIDATION.md
@.planning/phases/30-pi-repo-cleanup/30-01-SUMMARY.md

<pi_rules>
ABSOLUTE (STATE.md:1727-1745). Override anything below:
- Edit ONLY `/home/ido/pi-mirror`. **No git command in that directory — not even `status`.**
  Deletions are plain `rm -rf`, never `git rm`.
- Never `rsync --delete`. This plan does not rsync.
- Never start/stop the pilot. Never run Python on the Pi.
- `autopilot` cannot be imported on this host; `compileall` is the verification ceiling.
</pi_rules>

<verified_reachability>
Confirmed by grep on `/home/ido/pi-mirror` 2026-08-10. Do not re-derive.

**The Terminal cluster is self-contained** — every importer of every module below is itself in
the cluster:
- `core/gui.py` ← imported only by `core/terminal.py:51`
- `core/plots.py` ← `core/terminal.py:48`, `core/gui.py:50`
- `core/subject.py` ← `core/terminal.py:46`, `core/gui.py:43`, `viz/trial_viewer.py:26`,
  `viz/psychometric.py:4`. **`core/subject.py` is NOT the pilot's local-HDF5 path** —
  `pilot.py`'s `open_file()` uses `tables` directly and its only call site is commented out
  (plan 06 removes it). The ~23 plugin references to `Subject` are **docstrings** — see
  `<the_docstring_references_are_not_your_problem>` below.
- `core/styles.py` ← `core/terminal.py:23`, `core/plots.py:36`, `core/gui.py:49`
- `core/reward.py` ← **zero importers anywhere**
- `core/utils.py` ← 0 bytes
- `utils/invoker.py` ← `core/terminal.py:50`, `core/plots.py:37`, `core/gui.py:48`
- `data_handlers/` (`Data_Handler.py`, `ElasticSearchDataHandler.py`) ← only
  `core/terminal.py:47,484,554,782,808`. **This is the Terminal's ES writer, not the pilot's.**
  The pilot's ES path is `Event_Dispatcher` → orchestrator → ES, untouched by this plan.
- `viz/` (`psychometric.py`, `trial_viewer.py`) ← nothing outside `viz/`
- `utils/Event.py` (**singular**) ← referenced only from `terminal/plugins/`. Superseded by
  `utils/Events.py` (**plural**), which `mics_task.py:14`, `task.py` and `RecordingBox.py`
  import and which **must survive**. Read the filename twice before deleting.
- `setup/request_helpers.py` ← Terminal-only helper. **Two surviving consumers, both handled
  here:** `autopilot/tests/test_*` (removed by plan 02, same wave) and
  `setup/install_pyspin.sh:64-65` — see below.

**Survives — do not touch:** `core/pilot.py`, `core/loggers.py`, `core/View.py`,
`core/__init__.py`, `setup/` (everything except `request_helpers.py` and `install_pyspin.sh`),
`utils/Events.py`. `autopilot/__init__.py:4` imports `setup_autopilot` **unconditionally**, so
deleting `setup/` breaks `import autopilot` outright.

**Two string-literal landmines, both real, both verified:**

1. `setup/setup_autopilot.py:198` writes
   `"python3 -m autopilot.core.terminal -f " + str(prefs_fn) + "\\n"` into a launcher file via
   `launch_file_open.write(...)`. It is a string argument to a call, not an import, so no
   import-graph tool sees it — but `check_tree_integrity.py`'s assertion 2(b) does (it is
   deliberately **not** a docstring, so the docstring narrowing does not exempt it), and it will
   fail `--strict` until you fix it.
2. `setup/install_pyspin.sh:64` and `:65`:
   ```sh
   python -c "from autopilot.setup.request_helpers import download_box; download_box('${SPINURL}')"
   python -c "from autopilot.setup.request_helpers import download_box; download_box('${PYSPINURL}')"
   ```
   Guard spec 2(c) scans `.sh`, so this fails `--strict` the moment `request_helpers.py` goes.
   The file is PySpin/Spinnaker camera provisioning for the removed Terminal workflow — its only
   consumer, `hardware/cameras.py:1143`, is removed by plan 05. Task 2 removes it here, with the
   four-criteria verdict recorded, because **the reference is to a module this plan deletes**.
</verified_reachability>

<the_docstring_references_are_not_your_problem>
23 files under `pilot/plugins/` and `autopilot/examples/tasks/blink.py` carry, inside a **class
docstring**, the Sphinx cross-reference:

```
stages. It is used by the :class:`~autopilot.core.subject.Subject` object to make a data table with
```

Those 23 plugin files are not removed until plan 04, in the **next** wave. If the guard's 2(b)
scanned docstrings, deleting `core/subject.py` here would leave 23 surviving files "referencing"
it and the guard would go red at the end of Wave 1 for 23 pieces of inherited upstream prose.

Plan 01's guard therefore skips docstring string constants by node identity, and its unit tests
pin the discrimination: a docstring reference passes, a `.write()` argument (the
`setup_autopilot.py:198` case above) still fails. **You do not need to edit any plugin docstring,
and you must not.** If the guard does flag one of them, the guard is wrong, not the tree — stop
and report rather than editing 23 protected-adjacent files to silence it.

Separately, `autopilot/examples/` is removed by plan 02 in this same wave, and the guard's
`scan_skip` covers `autopilot/tests/`, `autopilot/examples/` and `autopilot/docs/` for exactly
the duration of this wave. `autopilot/tests/test_terminal.py:9` is a **genuine**
`import autopilot.core.terminal` — the skip is what stops it racing this plan.
</the_docstring_references_are_not_your_problem>

<removal_criteria>
A path is removable only when ALL four hold; record the verdict per path in the ledger fragment:
1. Not in the static import closure of `python3 -m autopilot.core.pilot`.
2. Not reachable dynamically (`PLUGINDIR` sweep, `autopilot/tasks/` AST sweep, `importlib`,
   string-keyed dispatch).
3. Not named by the backend — no literal filename, module path or class-name string in
   `mics-backend`, and no `hardware_libs` / `hardware_modules` / `task_toolkits` /
   `available_locked_states` row resolving to it.
4. Not reserved by a pending phase.
</removal_criteria>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the top-level Terminal deployment</name>
  <files>
    /home/ido/pi-mirror/terminal/,
    /home/ido/pi-mirror/run_terminal.sh,
    /home/ido/pi-mirror/.vscode/
  </files>
  <action>
    Plain `rm -rf`. **No git.**

    - `/home/ido/pi-mirror/terminal/` entire — 18 MB, of which 16.4 MB is rotated log spew.
      It contains `terminal/plugins/` (the Terminal's own copy of the legacy task files, 10 of
      which import `autopilot.hardware.unreal`), `terminal/data/`, `terminal/logs/`,
      `terminal/prefs*.json`, `terminal/pilot_db*.json`, `terminal/protocols/`, `terminal/~/`
      (the tilde-literal directory HYG-08 names), and `terminal/terminal.conf`.
    - `/home/ido/pi-mirror/run_terminal.sh` — launcher for the removed agent.
    - `/home/ido/pi-mirror/.vscode/` — editor config, not tree code.

    Before removing, apply criterion 3: grep `/home/ido/mics-backend` for `terminal/`,
    `pilot_db`, `terminal.conf` and for any class name defined under `terminal/plugins/` to
    confirm the backend names none of them. Record the result.

    Keep `/home/ido/pi-mirror/run_pilot.sh`.
  </action>
  <verify>
    <automated>cd /home/ido/pi-mirror &amp;&amp; test ! -e terminal &amp;&amp; test ! -e run_terminal.sh &amp;&amp; test ! -e .vscode &amp;&amp; test -f run_pilot.sh &amp;&amp; python3 tools/check_tree_integrity.py --strict</automated>
  </verify>
  <done>
    `terminal/`, `run_terminal.sh` and `.vscode/` are gone; `run_pilot.sh` remains. The guard
    passes `--strict`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove the Pi-side Terminal modules, install_pyspin.sh, and fix the launcher string</name>
  <files>
    /home/ido/pi-mirror/autopilot/autopilot/core/gui.py,
    /home/ido/pi-mirror/autopilot/autopilot/core/terminal.py,
    /home/ido/pi-mirror/autopilot/autopilot/core/plots.py,
    /home/ido/pi-mirror/autopilot/autopilot/core/subject.py,
    /home/ido/pi-mirror/autopilot/autopilot/core/styles.py,
    /home/ido/pi-mirror/autopilot/autopilot/core/utils.py,
    /home/ido/pi-mirror/autopilot/autopilot/core/reward.py,
    /home/ido/pi-mirror/autopilot/autopilot/viz/,
    /home/ido/pi-mirror/autopilot/autopilot/data_handlers/,
    /home/ido/pi-mirror/autopilot/autopilot/utils/invoker.py,
    /home/ido/pi-mirror/autopilot/autopilot/utils/Event.py,
    /home/ido/pi-mirror/autopilot/autopilot/setup/request_helpers.py,
    /home/ido/pi-mirror/autopilot/autopilot/setup/install_pyspin.sh,
    /home/ido/pi-mirror/autopilot/autopilot/setup/setup_autopilot.py
  </files>
  <action>
    **Order within this task matters** — delete the importers before the imported, so the guard's
    intermediate state is never worse than the start state. Practically: `core/terminal.py` and
    `core/gui.py` first, then `core/plots.py`, then `core/subject.py`, `core/styles.py`,
    `utils/invoker.py`, then `viz/`, `data_handlers/`, then `install_pyspin.sh` (the consumer),
    then `request_helpers.py` (the consumed), then the remaining leaves.

    **A. Delete (plain `rm` / `rm -rf`):**
    - `autopilot/autopilot/core/terminal.py`
    - `autopilot/autopilot/core/gui.py`
    - `autopilot/autopilot/core/plots.py`
    - `autopilot/autopilot/core/subject.py`
    - `autopilot/autopilot/core/styles.py`
    - `autopilot/autopilot/core/utils.py` (0 bytes)
    - `autopilot/autopilot/core/reward.py` (zero importers)
    - `autopilot/autopilot/viz/` entire
    - `autopilot/autopilot/data_handlers/` entire
    - `autopilot/autopilot/utils/invoker.py`
    - `autopilot/autopilot/utils/Event.py` — **singular**. `utils/Events.py` (plural) SURVIVES.
    - `autopilot/autopilot/setup/install_pyspin.sh` — **before** `request_helpers.py`. Apply the
      four criteria and record the verdict:
      1. Not in the pilot's static closure — it is a shell script, never imported.
      2. Not dynamically reachable — no `PLUGINDIR` / `tasks/` sweep hit; nothing shells out to
         it (grep the surviving tree for `install_pyspin`; the only other hit is
         `hardware/cameras.py:1143`, a docstring, and `cameras.py` is removed by plan 05).
      3. Not named by the backend — grep `mics-backend` for `install_pyspin` and `PySpin`.
      4. Not reserved by a pending phase.
      Verdict rationale for the ledger: it is Spinnaker/PySpin **camera provisioning for the
      removed Terminal workflow**, and lines 64–65 invoke
      `from autopilot.setup.request_helpers import download_box`, a module this task deletes.
    - `autopilot/autopilot/setup/request_helpers.py`

    **B. Fix `setup/setup_autopilot.py:198`.** It writes a launcher line invoking
    `autopilot.core.terminal`, which no longer exists. Locate the `if`/branch that writes the
    terminal launcher and remove that branch — keeping the `autopilot.core.pilot` branch — so
    `setup_autopilot` can only ever generate a pilot launcher. Do not delete the file:
    `autopilot/__init__.py:4` imports it unconditionally.
    Then grep the whole surviving tree for any other `autopilot.core.terminal` /
    `autopilot.core.gui` / `autopilot.core.subject` string literal and remove or rewrite each —
    **except docstrings**, which you leave alone (see
    `<the_docstring_references_are_not_your_problem>`).

    **C. Confirm the leaves are truly leaves** before the final `rm`s: after the deletions in A,
    re-run the guard. If it reports a dangling reference you did not expect, stop and report —
    do not delete the referencing file to silence it unless that file is itself on this plan's
    list. In particular, if the guard flags a `pilot/plugins/*.py` docstring, that is a guard
    defect (plan 01 owns it), not a tree defect.

    **D. Do NOT touch** `core/pilot.py`, `core/loggers.py`, `core/View.py`, `core/__init__.py`,
    `utils/Events.py`, `setup/setup_mlx90640.sh` (plan 05 evaluates it with `cameras.py`), or
    anything else under `setup/`.

    **E. Record.** Write `.planning/phases/30-pi-repo-cleanup/ledger/30-03-ledger.md` (create
    `ledger/` if absent) with one row per removed path, four criterion verdicts each, plus the
    `setup_autopilot.py:198` fix and the `install_pyspin.sh:64-65` finding as evidence, plus a
    proposed verdict line `HYG-07 | PROVEN | <evidence>` for plan 08 to transcribe.
    **Do not edit `30-HARDWARE-VALIDATION.md`** — plan 02 runs in parallel; plan 08 merges.
  </action>
  <verify>
    <automated>cd /home/ido/pi-mirror &amp;&amp; test ! -e autopilot/autopilot/core/terminal.py &amp;&amp; test ! -e autopilot/autopilot/utils/Event.py &amp;&amp; test ! -e autopilot/autopilot/setup/request_helpers.py &amp;&amp; test ! -e autopilot/autopilot/setup/install_pyspin.sh &amp;&amp; test -f autopilot/autopilot/utils/Events.py &amp;&amp; test -f autopilot/autopilot/setup/setup_autopilot.py &amp;&amp; test -f autopilot/autopilot/core/pilot.py &amp;&amp; ! grep -rq 'autopilot\.core\.terminal' --include=*.sh --include=*.json . &amp;&amp; ! grep -rq 'request_helpers' --include=*.py --include=*.sh . &amp;&amp; python3 tools/check_tree_integrity.py --strict &amp;&amp; python3 -m compileall -q autopilot/autopilot tools tests</automated>
  </verify>
  <done>
    All 13 listed paths are gone, including `install_pyspin.sh`. `utils/Events.py`,
    `setup/setup_autopilot.py`, `core/pilot.py`, `core/loggers.py` and `core/View.py` are intact.
    No occurrence of `autopilot.core.terminal` in any `.sh` or `.json`, and none in any `.py`
    outside a docstring; no occurrence of `request_helpers` anywhere in `.py` or `.sh`.
    Guard `--strict` exits 0; `compileall` exits 0. `ledger/30-03-ledger.md` written, including
    the four-criteria verdict for `install_pyspin.sh`.
  </done>
</task>

</tasks>

<verification>
- `python3 /home/ido/pi-mirror/tools/check_tree_integrity.py --strict` exits 0 — in particular
  assertion 1 (closure completeness from `core/pilot.py`) still resolves every member, and the
  23 `pilot/plugins/*.py` docstrings naming `autopilot.core.subject` are **not** flagged.
- `python3 -m compileall -q /home/ido/pi-mirror/autopilot/autopilot /home/ido/pi-mirror/tools /home/ido/pi-mirror/tests` exits 0.
- **Pi suite: run the delta command from `30-01-SUMMARY.md`**, not a bare `pytest`. It exits 0
  when no node id fails that was not already failing in `30-PYTEST-BASELINE.json`. The recorded
  baseline is 179 failed / 181 passed over 19 collectable modules, so a bare
  `python3 -m pytest -q` exits 1 on a healthy tree and cannot be chained with `&&`.
- `grep -rn 'from autopilot.core import\|autopilot.core.gui\|autopilot.core.subject' /home/ido/pi-mirror --include=*.py`
  returns only docstring lines (the 23 plugin files, until plan 04 removes them).
- No git command was run in `/home/ido/pi-mirror`.
</verification>

<success_criteria>
1. `terminal/`, `run_terminal.sh`, `.vscode/`, the seven `core/*.py` Terminal modules, `viz/`,
   `data_handlers/`, `utils/invoker.py`, `utils/Event.py`, `setup/request_helpers.py` and
   `setup/install_pyspin.sh` are gone.
2. `utils/Events.py` (plural) survives, as do `core/pilot.py`, `core/loggers.py`, `core/View.py`
   and all of `setup/` except `request_helpers.py` and `install_pyspin.sh`.
3. `setup_autopilot.py` no longer emits an `autopilot.core.terminal` launcher line.
4. No plugin docstring was edited to satisfy the guard.
5. Guard `--strict` and `compileall` both exit 0.
6. `ledger/30-03-ledger.md` justifies every removal against the four criteria — including
   `install_pyspin.sh` — and proposes `HYG-07 | PROVEN`.
</success_criteria>

<output>
After completion, create
`.planning/phases/30-pi-repo-cleanup/30-03-SUMMARY.md`.
</output>
</content>
