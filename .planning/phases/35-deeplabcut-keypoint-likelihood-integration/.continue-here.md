---
context: phase
phase: 35-deeplabcut-keypoint-likelihood-integration
task: 7
total_tasks: 9
status: in_progress
last_updated: 2026-08-31T13:04:14.424Z
---

> # ⛔ PARKED 2026-09-10 — do not resume this phase without the user saying so.
> Work moved to **Phase 38**; see `.planning/HANDOFF.json`. Phase 35 stands at **7/9**: 35-07 ran
> on 2026-09-02 and proved the core chain (runs 587/588), leaving **35-09** (lib 177 v3 + repin
> 434 + quiet liveness re-test — rig-only, needs no Windows box) and **35-08** (occlusion/staleness,
> phase close). Neither blocks 38. The `next_action` at the bottom of this file is STALE.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Agent touches the Pi | The remaining plans are rig work. Running git, Python, or process control on the Pi is forbidden by standing project rule, not by preference. | blocking | Waves 5-6 plans are all `autonomous: false`. Present each rig step as a command for the USER and stop. Never execute it. Executor prompts must carry the `<hard_project_rules>` block used in waves 1-4. |
| Silent misdelivery to a live socket | `dlc-link-live --port` defaulted to 5599, which is `ExtlinkDemo`'s real bound socket on pilot 3, while `dlc_cam1` is on 5601. Keypoints were accepted by the wrong fixture — no error, lib appears empty, model appears broken. | advisory | Fixed in `8191d4b`: `--port` has no default and is required when connecting. When wiring any new `source_id`, read the port from its `pilot_hardware_config` row rather than assuming. |
| Trusting `rtk`-filtered git output | Filtered `git log` hid merge commits entirely, making correctly-merged waves look unmerged. | advisory | Use `rtk proxy git ...` for any git output a decision depends on. |
| Per-individual DLC signals under `identity: false` | The real project is `multianimalproject: true`, 5 individuals, `identity: false`. A `1:nose` signal silently jumps between physical animals. | advisory | Start with `uniquebodyparts` (`LED_on`/`LED_off`). `RUNBOOK.md` reached this independently — see its identity section. |

<current_state>
Phase 35 is 6/9 plans complete. Waves 1-4 all executed, merged, verified and tracked;
every worktree was removed and no `worktree-agent-*` branches remain. Working tree is on
branch `claude` at `8191d4b`.

What remains is exactly the rig work, and all of it needs the user:

- Wave 5 — 35-07 (first live proof) and 35-09 (lib 177 v3 liveness test), both `autonomous: false`
- Wave 6 — 35-08 (occlusion/staleness hardening, phase close), `autonomous: false`

Nothing is half-finished. This is a clean wave boundary, not a mid-plan pause.
</current_state>

<completed_work>

| Plan | What landed | Tip commit |
|---|---|---|
| 35-01 | `dlc_link/` package, `names.py`, `decimate.py` | `0ffb8d5` |
| 35-02 | `35-LIVENESS-FINDINGS.md`, lib 177 v3 candidate + v2 rollback | `bf49ce6` |
| 35-03 | `config_read.py`, `templates.py`, `generate.py`, `generate_cli.py`, AST-contract test | `b385bf9` |
| 35-04 | `signal_map.py`, `processor.py`, `live.py`, `live_probe.py` | `9742f24` |
| 35-05 | `convert.py`, `convert_cli.py`, replay round-trip test | `c7e8414` |
| 35-06 | Live DB fixture, `RUNBOOK.md`, `35-FIXTURE-INVENTORY.md` | `a181ab2` |

Out-of-plan but in-scope: `8191d4b` — the `--port` fix plus a Ports section in `RUNBOOK.md`.
Found while writing the user's bring-up guide. 35-08 is its natural retrospective owner.

Test state at pause: `dlc_link` **178 passed / 3 skipped**, backend **452 passed / 1 skipped**.
The 3 skips are the pandas/tables-gated converter tests, correctly skipping on this host.
</completed_work>

<remaining_work>

- **35-07** (wave 5, user-gated) — model on the vision box over a prerecorded video, keypoints
  reaching the Pi. Blocked on the Windows bring-up (wheels installed, model exported to `.pt`).
- **35-09** (wave 5, user-gated) — publish lib 177 v3, repin task definition 434, re-run the quiet
  liveness test. **Not** blocked on the Windows work; this one could go first.
- **35-08** (wave 6, user-gated) — occlusion semantics, liveness/staleness split, phase close.
  Depends on both wave-5 plans.

Then: code review gate → regression gate → `gsd-verifier` → `phase.complete`.
</remaining_work>

<decisions_made>

- **35-09 runs in wave 5, not wave 2.** The plan index warned its DAG position was wave 2 while the
  plan declares `wave: 5`. Both wave-5 plans are user-gated rig work, so clustering them honours
  author intent; 35-09's only dependency (35-02) landed in wave 1.
- **`--port` has no default.** Not "default to 5601" — the port belongs to a
  `(pilot, source_id)` row in `pilot_hardware_config`, so any default re-arms the trap for the next
  camera. See the anti-pattern table.
- **Wheels, not a git push.** The repo is PUBLIC and `claude` is 128 commits ahead including 46
  `.planning` files. Wheels are 89 KB and meet the need completely.
- **Phase 37 added** for extracting `sdk/` + `dlc_link/` into a client repo — deliberately *after*
  35 verifies, since 35-05..35-08 declare `dlc_link/` paths. Reasoning in `37-SEED.md`.
</decisions_made>

<blockers>
- **Waves 5-6 need the user at the rig.** By design, not an obstacle to fix.
- **Windows bring-up not started.** Blocks 35-07 only. 35-09 can proceed independently.
- **`.pt` export unknown.** `dlc-link-live --model-path` needs the file from
  `deeplabcut.export_model`; whether `exported-models/` is populated was never confirmed.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable twin of this file
2. `dlc_link/RUNBOOK.md` — canonical researcher-facing doc; its Ports and identity sections
3. `.planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-FIXTURE-INVENTORY.md` —
   what exists live in the DB, and pilot 3's bound ports (line ~131)
4. `35-07-PLAN.md` and `35-09-PLAN.md` — the two wave-5 checkpoints
5. `35-02-SUMMARY.md` — carries the exact `PUT`/rollback calls 35-09 needs, already fetched
   against the live API so no ids need guessing

## Infrastructure State

- Backend stack **up** (`docker compose`), backend tests green at 452/1
- `mics_backup` container up 26h
- Live DB objects created by 35-06: hardware lib **243** / version **184** (`dlc_cam1`),
  hardware module **73**, toolkit **157** (`dlc_demo`), task definition **626**,
  pilot 3 `pilot_hardware_config` row **41** (port **5601**, `required=false`)
- Pilot 3 bound extlink ports: **5599** = `ExtlinkDemo`/`demo`, **5601** = `dlc_cam1`
- Wheels staged at `\\isi.storwis.weizmann.ac.il\labs\yizharlab\Mics\wheel\`
  (`mics_link` 42,718 B; `dlc_link` 46,828 B — the 46,828 is the `--port` fix)
- `origin/claude` still at `4af731b`; **128 commits unpushed**. Nothing was published this session.
- Pre-existing uncommitted files, NOT from this session, left untouched deliberately:
  `api/fda_validation.py`, `api/tests/test_task_definitions_validation.py`,
  `.planning/config.json`, phase 31 `deferred-items.md`, and the `session aligner` gitlink.

<context>
Waves 1-4 were run as worktree-isolated `gsd-executor` agents, two at a time where
`files_modified` didn't overlap. Each wave was merged, gated on the full test suite, tracked in
ROADMAP/STATE, and spot-checked against its SUMMARY before moving on — no wave was taken on trust.

The session drifted productively into user-facing work: a published bring-up guide for the
Windows/DeepLabCut box, wheels built and staged on SMB, and the `--port` defect found and fixed
while writing that guide. None of it disturbed the plan set.

Phase 37 was added mid-session for the client-package extraction, sequenced deliberately after 35.
</context>

<next_action>
Run `/gsd-execute-phase 35` and go straight to wave 5. Present 35-07 and 35-09 as checkpoints:
read each plan, hand the user the exact rig commands, and wait. If the user has not yet done the
Windows bring-up, offer **35-09 first** — it is rig-only and does not depend on the vision box.
</next_action>
