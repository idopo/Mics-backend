---
phase: 26-openephys-device-control
plan: 10
type: execute
wave: 3
depends_on: [26-03, 26-07]
files_modified:
  - api/seed_libs/openephys.py
  - api/seed_openephys.py
  - api/tests/test_seed_openephys.py
  - api/main.py
autonomous: true
requirements: [EPHYS-01, EPHYS-03, EPHYS-04]
must_haves:
  truths:
    - "Ephys works after a deploy with no manual lib upload — the OpenEphys lib is seeded idempotently at API startup"
    - "on_run_start drives IDLE -> RECORD, refuses a box already recording, and reports the READ-BACK path"
    - "on_run_stop returns the box to IDLE on every Pi exit path"
    - "A marker send enqueues and returns immediately, and is logged whether or not the send succeeds"
    - "Run-start and run-stop markers bracket the recording; no automatic trial markers exist"
    - "The lib is seeded with kind='hardware' — it is a real ExternalHardware subclass, not a compute lib"
  artifacts:
    - path: "api/seed_libs/openephys.py"
      provides: "the OpenEphys ExternalHardware subclass shipped to the Pi as versioned lib source"
      min_lines: 110
      contains: "class OpenEphys"
    - path: "api/seed_openephys.py"
      provides: "idempotent hardware_libs / hardware_lib_versions / hardware_modules seeding"
      contains: "def seed_openephys_lib"
    - path: "api/tests/test_seed_openephys.py"
      provides: "seeding idempotency + kind='hardware' + lib-source shape assertions"
      contains: "def test_"
    - path: "api/main.py"
      provides: "startup seeding call, after the migration block"
      contains: "seed_openephys_lib"
  key_links:
    - from: "api/seed_libs/openephys.py"
      to: "autopilot.hardware.openephys_client"
      via: "the lib class is thin glue over the autopilot-free client from plan 26-07"
      pattern: "openephys_client"
    - from: "api/seed_libs/openephys.py"
      to: "external_hardware.ExternalHardware"
      via: "subclass — no new transport, no new dispatch shape, no new action vocabulary"
      pattern: "ExternalHardware"
    - from: "api/main.py"
      to: "api/seed_openephys.py"
      via: "called unconditionally after run_run_artifacts_migration, wrapped so a seed failure never stops boot"
      pattern: "seed_openephys_lib"
---

<objective>
Ship the `OpenEphys` hardware lib as a seeded first-party lib, and write the class that actually
drives the device.

Purpose: ephys must work after a deploy with no manual upload, while still living in the normal
versioning/promotion system so a researcher can fork or override it — the exact pattern Phase 23
established for `compute_ops.py`. The class itself is deliberately thin: every REST decision lives
in the `autopilot`-free `openephys_client` (plan 26-07), so what remains here is lifecycle glue.
Output: the lib source, the seeder, its test, and two lines of `main.py`.

**Verification split:** the seeder is fully agent-verifiable in docker. The class imports
`ExternalHardware`, so it cannot be imported on the dev host — it is proven at the consolidated rig
checkpoint (plan 26-13). Everything that COULD be tested without `autopilot` was already pushed into
plan 26-07 on purpose.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-openephys-device-control/26-CONTEXT.md
@.planning/phases/26-openephys-device-control/26-REST-SURFACE.md
@api/seed_libs/compute_ops.py
@api/seed_compute.py
@api/compute_provisioning.py
@/home/ido/pi-mirror/autopilot/autopilot/hardware/openephys_client.py
@/home/ido/pi-mirror/tests/test_openephys_markers.py

<interfaces>
Phase 18 substrate this class consumes. **Read the SHIPPED `external_hardware.py` before writing —
Phase 18's final names may differ from its plans.** Do not reimplement any of it.
```python
class ExternalHardware(Hardware):
    def bind(self, ioloop, view): ...         # TWO args — see note below
    def on_run_start(self, run_ctx): ...      # returns truthy on success; retried in the wait window
    def on_run_stop(self): ...                # all Pi paths + backend safety net
    def release(self): ...                    # MANDATORY override; Task.end() always calls it
    liveness_hook = None                      # CLASS-LEVEL; `role: "none"` MUST override it
    _egress                                   # .enqueue(callable) — FIFO, fire-and-forget, no retry
```

**`bind` takes two arguments, `(ioloop, view)`.** An earlier draft of this plan recorded one.
`18-10-SUMMARY.md` is the authority — read it, not this block, if the two ever disagree.

**`role: "none"` requires a class-level `liveness_hook`, enforced by raising at construction**
(EXTLINK-18 as amended; Phase 18 plans 18-05/18-10). This is not optional and there is no default
that would let OpenEphys through: the base predicate is "a message arrived within `stale_ms`", which
a socketless module can never satisfy, so it would sit permanently `alive=False` and hang the
readiness gate with no diagnosis. Silently defaulting to `alive=True` was explicitly rejected — that
is exactly the "device is off but we think it's fine" failure OE's HTTP check exists to catch.

```python
liveness_hook(self, last_msg_ts_ms, now_ms, stale_ms) -> bool
```
Declare it as a **method on the class**. `validate_role_liveness` is handed
`getattr(type(self), "liveness_hook", None) is not None`, so a hook assigned on the INSTANCE in
`__init__` is invisible to it and the module raises at construction — it fails closed, but the error
is confusing. Phase 18 calls the predicate on a **per-device `LivenessPoller` daemon thread**, never
on the shared IOLoop, precisely so a blocking outbound HTTP call like OE's is safe there.

**`alive` has exactly one writer in the base class (`_recompute_alive`)**, composing the liveness
predicate with the egress failure counter. Never write the `alive` Tracker directly from this lib.
`run_ctx` is a single dict with six keys: `run_id`, `session_id`, `subject_key`, `pilot`,
task-definition id, start timestamp.

The readiness gate keys on **ready** (lib-defined), not merely `alive` — EXTLINK-13 as amended. For
OpenEphys "ready" means *recording has started*, so a box that is reachable but failed to enter
RECORD must fail the gate rather than pass it and silently record nothing.

Report-back channel (device-neutral; consumed by plan 26-11):
```python
self.event_dispatcher.node.send('T', key='ARTIFACT_PATH', value={
    "run_id": ..., "device_name": ..., "resolved_path": ...,
    "started_at": ...,
    "device_fields": {"experiment_number": N, "recording_number": M, "record_node_id": K},
})
```
`Event_Dispatcher` (`autopilot/networking/Event_Dispatcher.py`) holds `.node` and sends via
`node.send('T', key=..., value=...)`; `log_action` already relies on `self.event_dispatcher` being
present on a `Hardware` instance. **Verify both against the shipped code before relying on them**
and record any divergence in the summary.

Seeder precedent — `api/seed_compute.py`: copy the shape but NOT the `kind`. Use `kind='hardware'`
(Pitfall 5: `kind='compute'` would subject this to the stdlib-only import allowlist and the
compute-specific version-resolution rules, neither of which applies to a real hardware lib).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: api/seed_libs/openephys.py — the OpenEphys lifecycle class</name>
  <files>api/seed_libs/openephys.py</files>
  <action>
    Write the versioned lib source. Zero `@signal` declarations — OE's control side is exactly the
    zero-signal control-only module EXTLINK-18 makes legal. **Python 3.7.3 syntax** — this file
    executes on the rig.

    Imports: `ExternalHardware` from `autopilot.hardware.external_hardware`, `openephys_client as oc`
    from `autopilot.hardware`, `log_action` from `autopilot.utils.logging_utils`.

    - `__init__(self, host=None, port=oc.OE_DEFAULT_PORT, artifact_target=None,
      artifact_path_template=None, **kwargs)` — store the OE settings, pass the rest to
      `super().__init__(**kwargs)`. `init_hardware()` splats `pilot_hardware_config.config` in as
      kwargs, and plan 26-11 injects `artifact_target` into that same dict, so both arrive by one
      config path rather than two.

    - `on_run_start(run_ctx)` — returns True/False for the retried readiness contract:
      1. `status = oc.get_status(...)`; `may_start, reason = oc.decide_start(status)`. If not
         `may_start`: log the reason and return False. **Never take over a box already recording** —
         the lease cannot see someone using the OE GUI by hand, so a busy box may be a colleague's
         session. Issue no PUT at all on this path.
      2. `oc.set_recording(...)` with `oc.build_recording_config(...)` derived from
         `self.artifact_target`, plus `start_new_directory=True`. Only the six real fields.
      3. `oc.set_mode(host, port, "RECORD")`.
      4. **Read back**: `recording = oc.get_recording(...)` then
         `oc.resolved_path_from_recording(recording)`. Send `ARTIFACT_PATH` with the resolved
         path(s) and the read-back counters in `device_fields`. **Never predict a path** — a
         computed guess would record a location the data is not at, which is exactly what EPHYS-03
         exists to prevent.
      5. Emit the run-start marker, set the readiness flag, return True.
      Any exception → log and return False. The hook is retried inside the wait window, so a
      transient failure must be recoverable rather than fatal.

    - **`liveness_hook(self, last_msg_ts_ms, now_ms, stale_ms)` — MANDATORY, class-level.** Without
      it the module raises at construction, because its `pilot_hardware_config.config` carries
      `role: "none"` (see `<interfaces>`). The body is one outbound call:
      `oc.get_status(self.host, self.port)` → return True if it returns a status dict, False on any
      connection error or exception. **Never raise** — `LivenessPoller.poll_once` swallows and counts
      a raise as not-alive, but keep the contract explicit here. Ignore all three timestamp
      arguments: they exist for the data-arrival default, and a socketless device has no inbound
      messages to time. Put that reason in a comment so nobody later "fixes" the unused parameters.

      **The HTTP call MUST use a bounded client timeout.** `LivenessPoller.stop()` uses a bounded
      join, so a `get_status` blocked on an unreachable host with no timeout outlives `release()` —
      and the poller is polling at `stale_ms / 2`, so an untimed call also stacks. `get_status`
      takes a `client` parameter (plan 26-01); pass one configured with a timeout comfortably under
      the poll interval rather than relying on a library default.

    - The readiness hook (whatever the shipped base class names it — check `18-10-SUMMARY.md`)
      returns the flag set in `on_run_start` step 5. **Readiness is NOT liveness and the two must
      not be conflated here.** `liveness_hook` answers "is the box reachable"; readiness answers
      "has recording started". A box that is reachable but failed to enter RECORD is `alive=True`
      and NOT ready — it must fail the gate, which is the entire reason EXTLINK-13 was amended from
      *alive* to *ready*. Reachability alone is not readiness.

    - `on_run_stop()` — emit the run-stop marker **first**, then `oc.set_mode(host, port, "IDLE")`.
      Order matters: a stop marker sent after IDLE lands outside the recording and is worthless.
      Idempotent, guarded by an "already stopped" flag: it runs on normal completion, STOP, task
      exception, and possibly again from the backend safety net. Never raise.

    - `release()` — **mandatory override**. Call `on_run_stop()` under the same guard, then the
      base's `release()` if one exists. The `Hardware` base raises when `release` is not overridden,
      and `Task.end()` calls it unconditionally on every hardware object on every run, inside an
      untried loop — an exception here takes down teardown.

    - `send_marker(self, label, run=None, trial=None)` decorated with `@log_action`. The body builds
      `text = oc.marker_text(label, run, trial)`, calls
      `self._egress.enqueue(lambda: oc.send_message(self.host, self.port, text))`, and returns
      `text`. That is all it does. `@log_action` captures the call synchronously on the FDA thread
      the instant it runs — that is the dual-log, it survives total network failure, and it is what
      Phase 28 diffs against. The enqueue is what keeps the FDA thread unblocked.
      **Markers ride Phase 24's existing `hardware` action type — add no new action type.**

    - **No automatic trial markers.** Do not reference `INC_TRIAL_COUNTER` anywhere. Auto trial
      markers were explicitly rejected: a task that does not send the counter would silently produce
      no markers and present as an ephys bug rather than a task bug.

    - A mid-run loss of recording → log loudly and flip `alive` through the base class's mechanism.
      Do NOT kill the behavioural session; the behavioural data stays valid and the researcher
      decides whether to stop.
  </action>
  <verify>
    <automated>python3 - <<'PY'
import ast
src = open('api/seed_libs/openephys.py').read()
tree = ast.parse(src)
fns = {n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)}
required = {'on_run_start', 'on_run_stop', 'release', 'send_marker', 'liveness_hook'}
assert required <= fns, sorted(required - fns)
assert 'INC_TRIAL_COUNTER' not in src, 'auto trial markers were rejected'
assert 'ExternalHardware' in src and 'openephys_client' in src
# liveness_hook must be declared at CLASS level: an instance assignment in __init__ is
# invisible to validate_role_liveness and the module raises at construction.
cls = next(n for n in ast.walk(tree) if isinstance(n, ast.ClassDef))
assert 'liveness_hook' in {n.name for n in cls.body if isinstance(n, ast.FunctionDef)}, \
    'liveness_hook must be a method on the class, not assigned on the instance'
print('LIB_SHAPE_OK')
PY</automated>
  </verify>
  <done>The source parses, declares all four lifecycle methods **plus a class-level
  `liveness_hook`**, references no `INC_TRIAL_COUNTER`, and uses only Python 3.7-compatible syntax.
  Real import is proven at the rig (plan 26-13).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: api/seed_openephys.py + its test — idempotent first-party seeding</name>
  <files>api/seed_openephys.py, api/tests/test_seed_openephys.py</files>
  <behavior>
    - Seeding a fresh DB creates one hardware_libs row with kind='hardware', one stable version, one hardware_modules row
    - Seeding twice creates nothing the second time
    - A seeding failure returns an error dict and never raises
  </behavior>
  <action>
    Copy `api/seed_compute.py` almost verbatim, with these deliberate differences:
    - `SEED_LIB_FILENAME = "openephys.py"`, `SEED_MODULE_NAME = "OPENEPHYS"`,
      `SEED_CLASS_NAME = "OpenEphys"`, display name "Open Ephys".
    - **`kind='hardware'`, not `'compute'`.** This is Pitfall 5 and the single most likely
      copy-paste error in this plan.
    - `declared_imports` reflects what the lib source actually names.
    - Same structure otherwise: existence check on `filename` first, no-op if found; one
      `engine.begin()`; `state='stable'` set directly; both `active_version_id` and
      `stable_version_id` pointed at the new version; `try/except` returning
      `{"created": False, "error": ...}` so a seeding failure never stops the API from booting.

    **Do NOT auto-provision a `pilot_hardware_config` row.** `provision_compute_configs` works for
    compute because a compute module needs no config; `OpenEphys` needs real per-pilot settings
    (`host`, port, artifact template) from day one, and a trivial auto-provisioned row would fail
    preflight's `incomplete_config` check anyway — correctly, since there IS real config to supply.
    Let `missing` / `incomplete_config` do their job the first time a researcher wires an
    OE-equipped toolkit to a pilot.

    **Because the row is hand-entered, the required shape must be documented where a researcher
    will see it** — the seeded lib's module docstring, since that is what the hardware-libs UI
    renders. Minimum viable `pilot_hardware_config.config`:
    ```json
    {"class_name": "OpenEphys", "role": "none", "host": "<OE box IP>",
     "source_id": "openephys", "port": 37497, "artifact_path_template": "..."}
    ```
    `role: "none"` is **mandatory and has no default** — Phase 18 rejects an absent `role` with an
    error rather than inferring one, deliberately, so that deliberate control-only is distinguishable
    from a forgotten field. `host` is still required even with no inbound socket: it is the egress
    target AND the device-lease key (EXTLINK-17 — one physical box is one lease regardless of how
    many modules target it). `source_id` survives purely as the Tracker-name prefix, which is what
    makes `openephys.alive` readable from an FDA exactly like any other device's.

    Write `api/tests/test_seed_openephys.py`:
    - `test_seed_openephys_is_idempotent`: call `seed_openephys_lib(engine)` twice; the second
      returns `created: False`; exactly one `hardware_libs` row with `filename='openephys.py'`.
    - `test_seed_openephys_kind_is_hardware`: that row's `kind` is `'hardware'`, and its version is
      `state='stable'` with both `active_version_id` and `stable_version_id` set.
    - `test_seed_openephys_module_row`: one `hardware_modules` row named `OPENEPHYS` with
      `class_name='OpenEphys'`.
    - `test_seed_source_matches_disk`: the stored `source_code` equals
      `api/seed_libs/openephys.py` on disk and its `sha256_hash` matches. A seeded lib that drifts
      from its source file is undebuggable.
    - `test_seed_openephys_declares_class_level_liveness_hook`: parse the **stored** `source_code`
      with `ast` and assert `liveness_hook` is a `FunctionDef` in the `OpenEphys` class body.
      Phase 18 raises at construction when a `role: "none"` module has no class-level
      `liveness_hook`, so a seeded lib missing it is a module that can never start — and the
      failure would otherwise surface only on the rig, in `init_hardware`'s per-module `except`.
      Asserting on the stored source (not the file) is what makes this catch a drifted seed.
  </action>
  <verify>
    <automated>docker compose up --build -d api && docker compose exec -T api python -m pytest -q api/tests/test_seed_openephys.py && docker compose exec -T api python -m pytest -q api/tests/test_seed_openephys.py -k liveness_hook</automated>
  </verify>
  <done>All five seeding tests pass, and `-k liveness_hook` selects at least one. Idempotency,
  `kind='hardware'`, and the class-level `liveness_hook` are each proven by a test, not by
  inspection.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the seeder into API startup</name>
  <files>api/main.py</files>
  <action>
    Add the import and one call in the existing startup block, immediately after
    `seed_compute_ops_lib(engine)` and after `run_run_artifacts_migration(engine)` (plan 26-05) —
    migrations before seeding, since seeding reads `kind`. Two lines total.

    `api/main.py` is ~2243 lines, far over the 500-line hard limit. Add nothing else to it here. If
    a later task in this phase wants a third line in this file, it belongs in a module instead.
  </action>
  <verify>
    <automated>docker compose up --build -d api && sleep 6 && curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8000/health && docker compose exec -T api python -m pytest -q api/tests/test_seed_openephys.py && docker compose exec -T api python -m pytest -q</automated>
  </verify>
  <done>The api container boots clean, `/health` returns 200, the openephys lib row exists with
  `kind='hardware'` after a cold start, and the full backend suite is green. Paste the health code
  and the pytest summary line.</done>
</task>

</tasks>

<verification>
```bash
docker compose up --build -d api && sleep 6
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8000/health
docker compose exec -T api python -m pytest -q
docker compose exec -T api python -c "from db import engine; from sqlalchemy import text; print(engine.connect().execute(text(\"SELECT kind, filename FROM hardware_libs WHERE filename='openephys.py'\")).fetchall())"
grep -c "INC_TRIAL_COUNTER" api/seed_libs/openephys.py   # must print 0
wc -l api/seed_libs/openephys.py api/seed_openephys.py
```
The class's real import, its lifecycle against a live box, and a marker actually being sent are rig
items and belong to plan 26-13. The agent does not deploy to the Pi, does not run git on the Pi,
and does not run Python on the Pi.
</verification>

<success_criteria>
- `openephys.py` is seeded idempotently at startup with `kind='hardware'` and a stable version.
- The class declares `on_run_start`, `on_run_stop`, `release` and `@log_action send_marker`, reads
  the resolved path back rather than predicting it, and brackets the run with markers.
- No automatic trial markers, no new action type, no reimplementation of Phase 18 machinery.
- Full backend suite green; `/health` 200 after a cold start.
</success_criteria>

<output>
After completion, create `.planning/phases/26-openephys-device-control/26-10-SUMMARY.md` recording
the exact Phase 18 base-class hook names used, the `ARTIFACT_PATH` payload actually emitted, and
anything about `event_dispatcher.node` that differed from the assumed contract.
</output>
