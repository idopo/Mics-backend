# Requirements: MICS Backend

**Defined:** 2026-03-15
**Core Value:** Researchers can define, modify, and deploy behavioral task logic without writing Python or restarting the Pi.

---

## v1 Requirements

### FDA Foundation (Pi)

- [x] **FDA-01**: Pi can start a task with `state_machine` kwarg containing v2 FDA JSON, producing identical behavior to the hardcoded version
- [ ] **FDA-02**: `load_fda_from_json()` in `mics_task` handles both v1 (states list) and v2 (states object with entry_actions) formats
- [ ] **FDA-03**: State entry_actions are executed in order: hardware calls via semantic ref, flag updates, timer calls, special actions (INC_TRIAL_COUNTER), custom toolkit method calls
- [ ] **FDA-04**: SEMANTIC_HARDWARE dict maps friendly names to (group, id) tuples; entry actions use `ref` not `hardware['group']['id']`
- [ ] **FDA-05**: Arg values in entry_actions resolve correctly: literal, `{"param": "name"}`, and `{"flag": "name"}` forms
- [ ] **FDA-06**: `blocking: "stage_block"` causes state to call `wait_for_condition()` (yield loop); `blocking: null` returns immediately
- [ ] **FDA-07**: `return_data` dict is collected after entry_actions and sent as DATA event; supports flag/param/now() value forms
- [x] **FDA-08**: `validate_fda.py` CLI tool exits 0 on valid JSON, exits 1 with specific error for unknown state names / unknown refs / unknown params / unknown callable_method refs
- [x] **FDA-09**: `tools/sync_pi.sh` rsyncs autopilot/ to Pi; `tools/deploy_pi.sh` syncs and restarts pilot
- [ ] **FDA-10**: A state with no `entry_actions` in JSON uses the existing Python toolkit method of that name as a passthrough; `_build_state_method` returns `getattr(self, name)` directly
- [ ] **FDA-11**: `type: "method"` entry_action calls `getattr(self, ref)(*args)` where `ref` must be in toolkit's `CALLABLE_METHODS` list; raises descriptive error at load time if not
- [x] **FDA-12**: `CALLABLE_METHODS` class attr on toolkit declares Python methods usable as entry_action building blocks; included in HANDSHAKE payload and stored in `task_toolkits.callable_methods`
- [x] **FDA-13**: `SEMANTIC_HARDWARE_RENAMES` class attr maps deprecated semantic names to their current replacement; `load_fda_from_json()` resolves old refs transparently via this map; HANDSHAKE includes the map so Phase 2 can detect stale refs in stored task_definitions
- [x] **FDA-14**: `validate_fda.py rename-hw-ref <old> <new> --toolkit <ClassName>` command performs a SQL UPDATE on all `task_definitions.fda_json` rows that reference old name, prints count of updated rows, exits 0 on success
- [ ] **FDA-15**: `load_fda_from_json()` sets `self.<param_name> = resolved_value` for every param after resolution, so toolkit Python methods can use `self.open_duration` directly
- [ ] **FDA-16**: `_build_state_method()` handles `type: "if"` action recursively — evaluates condition at state entry, executes `then` or `else` branch; `then`/`else` can themselves contain nested `if` actions
- [x] **FDA-17**: `validate_fda.py` traverses `if` action blocks recursively; validates `condition.left` and `condition.right` refs against known FLAGS/PARAMS/SEMANTIC_HARDWARE; exits 1 with specific error for unknown ref

### Trigger Assignments (Pi)

- [x] **TRIG-01**: `apply_trigger_assignments()` reads `trigger_assignments` from FDA JSON and appends handlers to `self.triggers[pin]`
- [ ] **TRIG-02**: `touch_detector` handler calls `hw.detect_change()`, updates `view[LICKER{n}]`, optionally emits CONTINUOUS — does NOT replace the existing `handle_trigger` path
- [ ] **TRIG-03**: `digital_input` handler updates `view[view_key]` with current GPIO level — logging still fires unconditionally via `execute_trigger()`
- [ ] **TRIG-04**: If `trigger_assignments` is absent from FDA JSON, `self.triggers` is unchanged (backward compatible)
- [x] **TRIG-05**: All trigger hardware continues to dispatch `Hardware_Event` via `execute_trigger()` regardless of trigger_assignments configuration

### Hot-Reload (Pi + Orchestrator)

- [x] **HOT-01**: Orchestrator `_build_step_task()` includes `state_machine` from task definition's `fda_json` in every START payload (Phase 2); Pi calls `load_fda_from_json()` at task start — changes take effect on next run without Pi restart
- [x] **HOT-02**: HANDSHAKE payload from Pi includes FLAGS, SEMANTIC_HARDWARE, STAGE_NAMES, CALLABLE_METHODS, REQUIRED_PACKAGES in addition to existing fields; stored in `task_toolkits` table

### Variant Tracking (VAR)

| ID | Requirement | Phase |
|---|---|---|
| VAR-01 | Toolkit identity is `(name, hw_hash)` — same name + same SEMANTIC_HARDWARE = same record; same name + different hardware = separate record | 2 |
| VAR-02 | HANDSHAKE with a new hw_hash for an existing toolkit name creates a new `task_toolkits` row (never overwrites) | 2 |
| VAR-03 | `toolkit_pilot_origins` table tracks `(toolkit_id, pilot_id, first_seen_at, last_seen_at)` — upserted on every HANDSHAKE | 2 |
| VAR-04 | `GET /api/toolkits` returns toolkits grouped by name; each variant includes hw_hash, pilot_origins, fda_count | 2 |
| VAR-05 | `GET /api/toolkits/{id}/diff/{other_id}` returns added/removed/changed keys in SEMANTIC_HARDWARE | 2 |
| VAR-06 | FDA creation GUI shows explicit variant picker (with hw diff) when 2+ variants exist for a toolkit name | 3 |
| VAR-07 | `PATCH /api/toolkits/{id}/set-canonical` marks one variant canonical; FDAs bound to others flagged `needs_migration=true` | 4 |

### DB + API (Backend)

- [x] **DB-01**: `task_toolkits` table created; HANDSHAKE handler writes toolkit metadata (states, flags, params_schema, semantic_hardware, callable_methods, required_packages, file_hash)
- [x] **DB-02**: `task_definitions` table extended with `toolkit_name`, `fda_json` (JSONB), `display_name` columns; migration uses IF NOT EXISTS
- [x] **DB-03**: `GET /api/toolkits` returns list of toolkits per pilot with all metadata needed by GUI
- [x] **DB-04**: `GET /api/toolkits/:name` returns full toolkit detail
- [x] **DB-05**: `POST /api/task-definitions` creates task definition from toolkit + FDA JSON + default_params
- [x] **DB-06**: `GET/PUT/DELETE /api/task-definitions/:id` CRUD with full fda_json
- [x] **DB-07**: `POST /api/task-definitions/:id/push?pilot=name` forwards UPDATE_FDA to named pilot via orchestrator; returns HOT_RELOAD_ACK status
- [x] **DB-08**: Orchestrator `_build_step_task()` includes `state_machine` from task definition's fda_json in START payload

### Visual FDA Editor (Web UI)

- [ ] **UI-01**: `/react/task-editor/:id` page loads toolkit metadata and task definition from API
- [ ] **UI-02**: react-flow canvas shows states as nodes, transitions as directed edges; drag-to-connect creates transitions
- [ ] **UI-03**: Clicking a transition edge shows condition builder in right panel: view key dropdown + op dropdown + literal/param/flag value input
- [ ] **UI-04**: Clicking a state node shows state body editor: entry_actions list with add/remove/reorder, blocking toggle, return_data list
- [ ] **UI-05**: "Add action" picker: hardware (semantic ref dropdown + method + args), flag, timer, special (INC_TRIAL_COUNTER), custom method (callable_methods dropdown + optional args)
- [ ] **UI-05a**: Passthrough state nodes (no entry_actions, backed by Python method) shown with lock icon and `{py}` badge; state body panel is read-only for these
- [ ] **UI-06**: Arg inputs are smart: toggle between literal / param-ref / flag-ref modes
- [ ] **UI-07**: Trigger assignment panel (separate tab): shows all trigger hardware, dropdown for handler type (default/touch_detector/digital_input), config fields; note that logging is always automatic
- [ ] **UI-08**: Semantic hardware overrides panel: optional per-task-definition overrides of toolkit's SEMANTIC_HARDWARE
- [ ] **UI-09**: "Push to Pilot" button only enabled when a pilot is running this task definition; calls push endpoint; shows toast on success
- [ ] **UI-10**: Save button serializes canvas + state body + trigger assignments to FDA JSON v2 and calls PUT /api/task-definitions/:id
- [ ] **UI-11**: State body panel "Add action" picker includes "If Condition" option; creates an `if` block with empty `then`/`else` lanes; lanes accept any action type including nested ifs
- [ ] **UI-12**: Condition builder widget: left-side dropdown (tracker/flag/param/hardware), op dropdown (`==` `!=` `>=` `<=` `>` `<`), right-side (literal input OR ref picker matching left types)

### Protocol Integration (Web UI + Backend)

- [x] **PROTO-01**: Protocol step picker in `/react/protocols-create` shows named task definitions instead of raw task_type strings; params come from linked toolkit's `params_schema`
- [x] **PROTO-02**: Protocol step stores `task_definition_id`; session start resolves `fda_json` from that ID and includes it in START payload
- [x] **PROTO-03**: `GET /api/tasks/leaf` deprecated; `GET /api/task-definitions` used instead
- [x] **PROTO-04**: Overrides modal in pilot sessions uses toolkit `params_schema` (via task definition) as param spec, falling back to existing `task.default_params` when `task_definition_id` is absent

### Pi Code Editor — Viewer (Phase A)

- [ ] **EDIT-01**: `GET /api/pi/status` returns `{connected: bool, pilot_state: str|null}` within 2s; JWT required
- [ ] **EDIT-02**: `GET /api/pi/files?path=...` returns `[{name, type, size, mtime}]`; filters __pycache__/.pyc/.git; returns 403 if path outside PI_EDITOR_ROOTS
- [ ] **EDIT-03**: `GET /api/pi/file?path=...` returns `{content, language}`; 404 if not found; 403 if outside roots
- [ ] **EDIT-04**: `web_ui/pi_ssh.py` module with asyncssh cached connection pool; auto-reconnects on next request
- [ ] **EDIT-05**: `/react/pi-editor` page with `PiFileBrowser` (tree, lazy expand), `MonacoEditorPanel` (read-only), `PiStatusBar`
- [ ] **EDIT-06**: `@monaco-editor/react` added to package.json; dynamically imported (React.lazy) to avoid bundle bloat

### Pi Code Editor — Terminal (Phase B)

- [ ] **EDIT-07**: `POST /api/pi/exec` runs command on Pi; returns stdout/stderr/exit_code; 403 unless ALLOW_PI_EXEC=true
- [ ] **EDIT-08**: `WS /ws/pi/exec` streams stdout/stderr lines in real-time; sends `{exit_code: n}` on completion; 403 unless ALLOW_PI_EXEC=true
- [ ] **EDIT-09**: `PiTerminal` component using xterm.js renders streamed output; input bar accepts `!command`; ANSI colors rendered
- [ ] **EDIT-10**: All exec endpoints return 403 when ALLOW_PI_EXEC not set; UI shows "Developer mode not enabled" tooltip

### Pi Code Editor — Edit + Restart (Phase C)

- [ ] **EDIT-11**: `PUT /api/pi/file` writes `{path, content}` to Pi via SFTP; 403 if outside roots or ALLOW_PI_EXEC not set
- [ ] **EDIT-12**: `POST /api/pi/restart` restarts pilot process via SSH; restart command configurable via PI_RESTART_CMD env var
- [ ] **EDIT-13**: Monaco editor switches to editable mode on "Edit" button; dirty indicator; "Save" writes via PUT; "Discard" reverts
- [ ] **EDIT-14**: Unsaved changes trigger `beforeunload` browser guard

### Pi Code Editor — Sync + Packages (Phase D)

- [ ] **EDIT-15**: `GET /api/pi/packages` returns installed vs required packages (cross-references task_toolkits.required_packages)
- [ ] **EDIT-16**: `POST /api/pi/packages` installs package on Pi via pip; streams output
- [ ] **EDIT-17**: Packages tab UI shows required-by-toolkits diff and manual install input

### Hardware Libs (Backend)

| ID | Requirement | Phase |
|---|---|---|
| HW-01 | `hardware_libs` table: `id, name, filename, source_code TEXT, ast_metadata JSONB, version INT, validated BOOL, created_at, updated_at`; `ast_metadata` shape: `{classes: [{name, methods: [{name, args: [{name, annotation, default}]}]}]}` | 9 |
| HW-02 | `POST /api/hardware-libs` validates Python source via `ast.parse()` + `py_compile.compile()` server-side; rejects with 422 + error location if invalid; extracts AST metadata on success | 9 |
| HW-03 | `GET /api/hardware-libs`, `GET /api/hardware-libs/{id}`, `PUT /api/hardware-libs/{id}`, `DELETE /api/hardware-libs/{id}` standard CRUD in `api/routers/hardware_libs.py` | 9 |
| HW-04 | Orchestrator sends `LOAD_HARDWARE_LIBS` ZMQ message before `START` when toolkit has associated libs; payload: `{libs: [{filename, source_code}]}` | 9 |
| HW-05 | Pi `receive_hardware_libs(libs)` in `mics_task.py`: writes each lib to `~/apps/hardware_overrides/`, prepends dir to `sys.path`, calls `importlib.reload()` if module already cached; backward compat: no-op if no message received | 9 |

### Hardware Modules (Backend + UI)

| ID | Requirement | Phase |
|---|---|---|
| HW-06 | `hardware_modules` table: `id, name, display_name, hardware_lib_id FK, class_name TEXT, description, created_at`; create/update validates `class_name` exists in linked lib's AST metadata | 10 |
| HW-07 | Standard CRUD at `/api/hardware-modules`; `GET /api/hardware-modules/{id}/methods` returns method list from linked lib's AST for the specific `class_name` | 10 |
| HW-08 | `pilot_hardware_config` table: `id, pilot_id FK, hardware_module_id FK, config JSONB`; `GET /api/pilots/{id}/hardware-config`, `PUT /api/pilots/{id}/hardware-config/{module_id}` | 10 |
| HW-09 | HANDSHAKE handler reads prefs.json HARDWARE section from payload and POSTs to `/api/pilots/{id}/hardware-config/seed` if pilot has no config yet (one-time migration seeder) | 10 |
| HW-10 | New React page `/react/hardware-modules-ui`: list modules with lib + class chip; create module form (name + pick lib + pick class from AST dropdown); nav entry in `Layout.tsx` | 10 |
| HW-11 | Per-pilot hardware config editor (on pilot detail or new page): table of module | class | config fields; config fields are dynamic from class constructor args in AST metadata | 10 |

### Toolkit Redesign (Backend-Authored)

| ID | Requirement | Phase |
|---|---|---|
| HW-12 | `task_toolkits` extended with: `hardware_module_ids INT[]`, `locked_state_source TEXT`, `is_backend_authored BOOL DEFAULT FALSE`; existing HANDSHAKE-registered toolkits remain valid | 11 |
| HW-13 | `available_locked_states` table: `id, pilot_id FK, task_filename TEXT, state_names TEXT[], updated_at`; populated by HANDSHAKE from `{tasks: [{filename, state_names}]}`; `GET /api/locked-states` returns state libraries grouped by task file | 11 |
| HW-14 | `POST /api/toolkits` with `{name, locked_state_source, selected_states, hardware_module_ids, flags, params_schema}` validates states exist in `available_locked_states` and all hardware_module_ids exist; sets `is_backend_authored=TRUE` | 11 |
| HW-15 | HANDSHAKE updated to accept new format `{tasks: [{filename, state_names}]}`; legacy format still accepted (old Pis continue to work) | 11 |
| HW-16 | Toolkit page redesigned: 5-step authoring flow (name+task file, select locked states, add hardware modules, define flags, define params); existing auto-registered toolkits show with "legacy" badge | 11 |

### Hardware-Aware FDA State Builder

| ID | Requirement | Phase |
|---|---|---|
| HW-17 | State builder `StateBodyPanel`: when adding `type: hardware` entry action, dropdown shows hardware modules from toolkit's `hardware_module_ids`; on module select, methods fetched from `GET /api/hardware-modules/{id}/methods`; arg inputs pre-filled with type annotations and defaults | 12 |
| HW-18 | `PUT /api/hardware-libs/{id}`: re-extracts AST, diffs methods vs previous version, scans all `task_definitions.fda_json` for references to removed/renamed methods, sets `validation_status='broken'` + `validation_message` on affected rows; returns diff summary | 12 |
| HW-19 | `task_definitions` gains `validation_status TEXT DEFAULT 'ok'` and `validation_message TEXT` columns | 12 |
| HW-20 | TaskDefinitions list: warning badge on broken definitions; TaskEditor: banner at top listing specific broken state + method name when `validation_status='broken'` | 12 |

### Pre-Run Cross-Check + End-to-End

| ID | Requirement | Phase |
|---|---|---|
| HW-21 | `POST /api/task-definitions/{id}/validate-for-pilot/{pilot_id}`: checks each hardware module in toolkit has pilot config, class matches, config complete; returns `{ok: bool, issues: [{module_name, issue, detail}]}` | 13 |
| HW-22 | Session start UI calls validate-for-pilot before confirming START; if issues: modal lists problems with links to pilot hardware config editor; if ok: proceed | 13 |
| HW-23 | Orchestrator sends resolved hardware config dict `{module_name: {class_name, pin, polarity, ...}}` to Pi before START; Pi `init_hardware()` uses received config if present, falls back to `self.HARDWARE` class constant if absent | 13 |
| HW-24 | Pi `init_hardware()` accepts `received_hw_config` kwarg: dynamically imports `{lib_module}.{class_name}` from override dir, instantiates with config params, stores in `self.hardware` dict (same structure, no downstream breakage) | 13 |

---

## Bug Fix Requirements

### Toolkit Creation Modal (Phase 14-03)

| ID | Requirement | Phase |
|---|---|---|
| BUG-05 | Toolkit creation wizard skips the locked-states step (step 2) when no source file is selected — the user lands directly on the hw-modules step | 14 |
| BUG-06 | Toolkit creation auto-links all existing hardware libraries with their default version (stable if set, latest active otherwise); no manual selection step required | 14 |
| BUG-07 | `POST /api/toolkits/{id}/hardware-libs` returns 200 through the web_ui proxy (`:8080`); route was missing from `web_ui/app.py` causing 404 | 14 |

---

## v2 Requirements

### FDA Extensions

- **FDAV2-01**: OR conditions between transitions (currently AND-only)
- **FDAV2-02**: Conditional branching within a state body (`if` action type)
- **FDAV2-03**: Toolkit composition (combining methods from two toolkits)
- **FDAV2-04**: Hardware profiles JSON per rig (enables same FDA JSON on different hardware layouts)
- **FDAV2-05**: FDA JSON v1→v2 auto-upgrade script

### Editor Extensions

- **UIV2-01**: Undo/redo in the FDA editor
- **UIV2-02**: FDA diff view (before/after hot-reload)
- **UIV2-03**: Rate-limiting hot-reload pushes per session

### Pi Editor Extensions

- **EDITV2-01**: Multi-Pi support (pilot selector dropdown, PI_HOSTS env var list)
- **EDITV2-02**: Audit log of who saved/exec'd what on the Pi
- **EDITV2-03**: "Save + Validate" integrated button (runs validate_fda.py after save, shows Monaco diagnostics)
- **EDITV2-04**: Read-only mode for non-admin JWT claims

---

## Out of Scope

| Feature | Reason |
|---|---|
| Jupyter/JupyterLab on Pi | Too heavy (1GB+ RAM), wrong paradigm for .py task files |
| code-server (VS Code in browser) on Pi | Too heavy, not viable on Pi hardware |
| exec() / cloudpickle for state bodies | Serializable action JSON is sufficient; no arbitrary code execution in hot-reload path |
| Parallel sub-states / hierarchical FSM | Not needed for current experiments; complexity cost too high |
| Real-time collaborative editing | Single editor per session is fine for lab context |
| FDA editor undo/redo | Deferred to v2 |

---

## Traceability

| Requirement | Phase | Status |
|---|---|---|
| FDA-01 through FDA-17 | Phase 1 (Pi Foundation) | Pending |
| TRIG-01 through TRIG-05 | Phase 1 (Pi Foundation) | Pending |
| HOT-01 through HOT-02 | Phase 1–2 (Pi Foundation + DB) | Pending |
| VAR-01 through VAR-05 | Phase 2 (DB + API) | Pending |
| VAR-06 | Phase 3 (Visual Editor) | Pending |
| VAR-07 | Phase 4 (Protocol Integration) | Complete |
| DB-01 through DB-08 | Phase 2 (DB + API) | Pending |
| UI-01 through UI-12 | Phase 3 (Visual Editor) | Pending |
| PROTO-01 through PROTO-04 | Phase 4 (Protocol Integration) | Pending |
| EDIT-01 through EDIT-06 | Phase 5 (Pi Editor: Viewer) | Pending |
| EDIT-07 through EDIT-10 | Phase 6 (Pi Editor: Terminal) | Pending |
| EDIT-11 through EDIT-14 | Phase 7 (Pi Editor: Edit+Restart) | Pending |
| EDIT-15 through EDIT-17 | Phase 8 (Pi Editor: Sync+Packages) | Pending |
| HW-01 through HW-05 | Phase 9 (HardwareLib Storage + E2E Proof) | Pending |
| HW-06 through HW-11 | Phase 10 (Hardware Modules + Pilot Config) | Pending |
| HW-12 through HW-16 | Phase 11 (Toolkit Redesign: Backend-Authored) | Pending |
| HW-17 through HW-20 | Phase 12 (Hardware-Aware FDA State Builder) | Pending |
| HW-21 through HW-24 | Phase 13 (Pre-Run Cross-Check + End-to-End) | Pending |
| BUG-01 through BUG-02 | Phase 14, Plan 1 (Small Bug Fixes Batch 1) | Complete |
| BUG-03 through BUG-04 | Phase 14, Plan 2 (Hw-lib Warning Badge Fixes) | Complete |
| BUG-05 through BUG-07 | Phase 14, Plan 3 (Toolkit Creation Modal UX Fixes) | Pending |
| TRIGA-07 | Phase 24, Plan 02 (Trigger-Assignment Hard Validation) | Complete |
| TRIGA-08 | Phase 24, Plan 02 (Trigger-Assignment Hard Validation) | Complete |
| TRIGA-10 | Phase 24, Plan 02 (backend) + Plan 04 (Pi-side `fda_vocabulary.py` single-sourcing) | Complete |
| TRIGA-01 | Phase 24, Plan 04 (Handler-Free Trigger Runtime) | Complete |
| TRIGA-06 | Phase 24, Plan 04 (Handler-Free Trigger Runtime) | Complete |
| TRIGA-02 | Phase 24, Plan 01 (Pi Value-Capture Substrate) | Complete |
| TRIGA-03 | Phase 24, Plan 01 (Pi Value-Capture Substrate) | Complete |
| TRIGA-04 | Phase 24, Plan 01 (Pi Value-Capture Substrate) | Complete |
| TRIGA-09 | Phase 24, Plan 05 (Trigger Panel: Shared Action Editor) | Complete |
| TRIGA-14 | Phase 24, Plan 08 (Trigger-Source Dropdown, Method Validation, Detector Write) | Complete |
| TRIGA-15 | Phase 24, Plan 08 (Trigger-Source Dropdown, Method Validation, Detector Write) | Complete |
| TRIGA-16 | Phase 24, Plan 08 (Trigger-Source Dropdown, Method Validation, Detector Write) | Complete |
| TRIGA-17 | Phase 24, Plan 08 (Trigger-Source Dropdown, Method Validation, Detector Write) | Complete |
| CMP-20 | Phase 23, Plan 11 (Operand-Namespace Consistency Pass) | Complete |
| CMP-21 | Phase 23, Plan 11 (Operand-Namespace Consistency Pass) | Complete |
| CMP-22 | Phase 23, Plan 11 (Operand-Namespace Consistency Pass) | Complete |
| CMP-23 | Phase 23, Plan 11 (Operand-Namespace Consistency Pass) | Complete |
| CMP-24 | Phase 23, Plan 12 (narrowed to 24b — `_resolve_arg` `get_state()`, Pi) | Deployed, not rig-exercised |
| CMP-25 | Phase 23, Plan 12 (backend semantic-hardware valid_names) | Deployed, not rig-exercised |
| CANVAS-07 | Phase 29, Plan 03 (FDA Canvas Auto-Layout — `fdaLayout.mts`) | Complete |
| CANVAS-08 | Phase 29, Plan 03 (FDA Canvas Auto-Layout — `fdaLayout.mts`) | Complete |
| CANVAS-14 | Phase 29, Plan 03 (FDA Canvas Auto-Layout — `fdaLayout.mts`) | Complete |
| CANVAS-01 through CANVAS-06, CANVAS-09 through CANVAS-13 | Phase 29 (FDA Builder Canvas UX) | Pending |
| HYG-01 through HYG-14 | Phase 30 (Pi Repo Cleanup) | HYG-03–HYG-14 Complete (12 PROVEN at the plan-08 exit gate, 2026-08-10); HYG-01 + HYG-02 Pending — USER-RUN (publication + rig session, plan 09) |
| PLAT-01 through PLAT-27 | Phase 31 (mics_core Modern Pi Platform) | Planned — 16 plans, 14 waves; PLAT-24/25 gated on USER-RUN hardware capture; PLAT-27 proven by test, not capture |

### Compound Transition Conditions

| ID | Requirement | Phase |
|---|---|---|
| COND-01 | Transition edges support compound boolean conditions expressed as DNF (OR of AND-groups); empty condition_groups = unconditional | 15 |
| COND-02 | UI: Kibana-style filter builder in edge panel — condition rows with left-operand/op/right-operand selectors, +AND within group, +OR adds new group | 15 |
| COND-03 | Legacy transitions using flat `conditions[]` auto-migrate to `condition_groups` on editor open; re-save always writes `condition_groups` | 15 |
| COND-04 | Pi evaluates `condition_groups` as `any(all(c() for c in g) for g in groups)`; falls back to legacy `conditions[]` if `condition_groups` absent | 15 |
| COND-05 | Edge label renders compound expressions as human-readable `a ∧ b ∨ c` notation | 15 |
| COND-06 | `FdaTransition` gains `condition_tree?` field: recursive `ConditionNode` (leaf = `FdaCondition`; branch = `{op:'AND'|'OR', children:ConditionNode[]}`); `condition_groups` and `conditions[]` are legacy fallbacks | 16 |
| COND-07 | Migration chain in `normaliseTransition()`: `conditions[]` → single AND-leaf; `condition_groups` → OR-of-AND-nodes tree; `condition_tree` used as-is; re-save always writes `condition_tree` | 16 |
| COND-08 | Per-row +AND and +OR buttons (Kibana FiltersBuilder model): +AND appends sibling in same AND-context; +OR appends at OR level; depth-based background shading distinguishes nesting levels | 16 |
| COND-09 | Pi `_build_tree_lambda(node)` evaluates `condition_tree` recursively: leaf → `_build_transition_lambda`; AND-node → `all()`; OR-node → `any()`; fallback chain: `condition_tree` → `condition_groups` → `conditions[]` | 16 |
| COND-10 | `condLabel()` renders tree with parentheses where needed: `(A ∨ B) ∧ C` when OR is child of AND; omits parens for flat structures | 16 |

### External Software Integration (MICS-Link)

| ID | Requirement | Phase |
|---|---|---|
| EXTLINK-01 | Each `ExternalHardware` instance binds its OWN ZMQ ROUTER socket on the `listen_port` declared in its `pilot_hardware_config.config` row; runs on the task's Tornado IOLoop alongside the orchestrator DEALER (no new thread). One module = one socket = one port. | 18 |
| EXTLINK-02 | The external SDK on the remote computer connects with ZMQ DEALER using `identity = source_id` (also from `pilot_hardware_config.config`). The Pi rejects any DEALER frame whose identity ≠ the configured `source_id` for that socket. | 18 |
| EXTLINK-03 | Wire protocol uses MessagePack envelope with kind discriminator: `SIG` (signal update), `EVT` (event with payload), `HB` (heartbeat), `ACK` (command result); Pi-outbound `CMD` (command invocation) | 18 |
| EXTLINK-04 | `ExternalHardware` base class supports `@signal(default, stale_after_ms, stale_policy)`, `@event(payload)`, `@command` decorators that declare the wire contract once per class | 18 |
| EXTLINK-05 | Each `@signal` auto-registers a View Tracker named `<source_id>.<signal_name>`; FDA reads via existing `view.get_value(...)` API with zero new call sites | 18 |
| EXTLINK-06 | Per-signal stale policy enforced on every read: `hold_last` returns cached value, `return_default` returns the declared default, `return_none` returns None | 18 |
| EXTLINK-07 | **AMENDED 2026-08-03 — liveness is split from signal staleness.** `<source_id>.alive` means **reachable/operational**, NOT data-fresh. Liveness is determined by a **lib-supplied hook** whose base-class default is "any successfully decoded message within `stale_ms`"; a lib may override it (OpenEphys polls its HTTP status endpoint, the only signal distinguishing *rig off* from *rig quiet*). Signal freshness remains entirely with the per-signal stale policy (EXTLINK-06) — a recording source with no activity for 30s is **alive with stale signals**. Rationale for the split: a `stale_ms` generous enough to cover normal quiet periods would be minutes, making `alive` useless as a failure detector. Every flip emits a CONTINUOUS event for ES. Loss of liveness mid-run is **never automatically fatal** — the FDA author gates on `not view.get_value("<source_id>.alive")` if the experiment requires it. *(Original heartbeat-only wording assumed every source sends MICS `HB` frames; foreign publishers do not.)* | 18 |
| EXTLINK-08 | Ingress validation: messages whose `sig`/`evt`/`cmd` name is not declared in the corresponding `ExternalHardware` subclass are dropped + logged; no exception ever propagates into the Tornado IOLoop | 18 |
| EXTLINK-09 | The hardware-libs AST extractor (`api/routers/hardware_libs.py` — Phase 9 plumbing) recognises `@signal` / `@event` / `@command` decorators and includes them in the lib's `ast_metadata`; downstream toolkit dispatch (Phase 11) treats `ExternalHardware` subclasses no differently — same `HARDWARE` + `PREFS_HARDWARE` dispatch shape, the `config` values differ but the wire is unchanged. The `extlink` block carries per-signal `dtype` (string name) and per-command `{args:[{name,dtype}], returns}` so FDA-editor and state-builder UIs can render typed forms without importing the lib. *(The original '(Phase 19+)' forward reference is STALE and was corrected 2026-08-09: Phase 19 is now the device-health surface and owns none of this. The editor-facing half is **EXTLINK-19**, in this same phase.)* | 18 |
| EXTLINK-10 | A `pilot_hardware_config` row for an external module carries `{class_name, listen_port, source_id, stale_ms}` in its `config` JSON (Phase 17 free-form schema — no new column, no new endpoint); preflight (Phase 13) validates `class_name` like any other module | 18 |
| EXTLINK-11 | Smoke test (`~/pi-mirror/scripts/dev/extlink_smoke.py`) demonstrates end-to-end: standalone DEALER script connects to the configured `listen_port` with the configured `source_id`, pushes `dlc_cam1.left_paw_x = 0.7`, an FDA transition lambda gated on `view.get_value("dlc_cam1.left_paw_x") > 0.5` advances the state machine | 18 |
| EXTLINK-12 | **Type contract.** Every `@signal` declares a value dtype (resolved at class-build time from the method's return annotation, with `type(default)` as fallback); allowed primitives: `int`, `float`, `bool`, `str`. The decorator raises `TypeError` at import time if neither annotation nor default provides a usable type. Every `@command` captures its parameter annotations + return annotation. At wire ingest, `_dispatch_sig` coerces incoming `v` via `spec.dtype(v)`; on `TypeError` / `ValueError` it increments `type_mismatch_count`, drops with a rate-limited log, and NEVER raises into the IOLoop. `bool` is special-cased — only `isinstance(raw, bool)` is accepted (no silent `float(True)` coercion). Same metadata is mirrored in `ast_metadata.extlink` for UI consumption (see EXTLINK-09) | 18 |
| EXTLINK-13 | **Readiness gate.** Per-source `required: bool` (default `true`) and `wait_timeout_s: int` (default `60`, range `[5, 600]`, `null` REJECTED at config save and Pi-side init) live in `pilot_hardware_config.config`. When a task starts on a pilot with one or more required external sources, `mics_task` injects a synthetic `_wait_extlink_ready` FDA pre-state ahead of the user-declared initial state. **AMENDED 2026-08-03 — the gate's success condition generalizes from "all required *alive*" to "all required *ready*", where readiness is lib-defined and defaults to `alive`.** Rationale: for OpenEphys what must be true before trial 1 is *"recording has started"*, not *"a packet arrived"* — a rig that is reachable but failed to enter RECORD would pass a liveness gate and then silently record nothing. For `router_bind` sources with no lifecycle hook, readiness == alive and behaviour is unchanged. The pre-state has THREE exits, in priority order: (a) **all required ready** → user's initial state (normal path); (b) **manual skip** via orchestrator-relayed `EXTLINK_SKIP_WAIT` ZMQ message → user's initial state (with a warning logged + CONTINUOUS event); (c) **timeout** at `max(wait_timeout_s)` across required sources → terminal `_extlink_timeout` state, session aborts cleanly with a `CONTINUOUS ExtlinkTimeout` event listing offline sources. The pre-state is NEVER installed when zero required sources exist (zero overhead for non-MICS-Link tasks). Three escape paths guarantee a task can never hang indefinitely: timeout (always, finite default), manual skip (operator override), and STOP button (existing path, always available). | 18 |

| EXTLINK-14 | **Transport roles + foreign-format decoding.** Each instance selects a `role` in `pilot_hardware_config.config`: `router_bind` (binds its own ROUTER on `listen_port`; a MICS-native SDK DEALER dials in with `identity = source_id`; mismatched identities dropped at the socket layer) or `sub_connect` (the Pi **dials out** with a SUB socket to a foreign publisher at `host:connect_port`). In `sub_connect` there is no DEALER identity to check, so the identity check does not apply; `source_id` is retained purely as the **tracker-name prefix**, keeping view-key naming identical across both roles. A per-lib `@decoder` hook translates a foreign frame into declared signal/event updates. **The decoder lives in the versioned hardware lib, never in platform code** — this makes a third-party wire format a promotable, per-toolkit artifact a researcher can fix without a platform release. `sub_connect` sources declare `@signal`/`@event` normally, so typed FDA reads and the editor's pickers work identically in both roles. | 18 |
| EXTLINK-15 | **Egress path.** Outbound calls (HTTP, ZMQ) never execute on the FDA thread. **One FIFO worker per device** preserves marker order (`cue_on` must reach the recording before `reward`). Sends are **fire-and-forget with NO retry** — a retried marker arrives at the wrong timestamp, and for alignment a late marker is strictly worse than a missing one because it silently corrupts co-registration instead of leaving a visible gap; failures are logged as CONTINUOUS events and the session continues. The queue is **bounded; on overflow the NEWEST is dropped**, a counter increments, and a CONTINUOUS event names what was lost — the loss must be *recorded*, never silently truncated. **N consecutive egress failures flip `alive` false** (threshold configurable), so device health has exactly one signal regardless of which direction failed. | 18 |
| EXTLINK-16 | **Run lifecycle hooks.** `ExternalHardware` gains `on_run_start(run_ctx)` and `on_run_stop()`. `on_run_start` fires **after `.bind()`, asynchronously** — it returns immediately and the `_wait_extlink_ready` pre-state polls until the lib reports ready or the timeout fires (blocking was rejected: the pilot would be unresponsive with no visible state and STOP would not work). **The hook is retried on an interval inside the wait window**, turning a hard failure into a recoverable one. `run_ctx` is a **single dict** (`run_id`, `session_id`, `subject_key`, `pilot`, task-definition id, start timestamp) — explicit named args were rejected because each added field would break every subclass ever written, including researcher-authored libs already promoted to stable. `on_run_stop()` must run on **all Pi paths** (normal completion, STOP, task exception) **plus a backend safety net**: when a run ends without a clean stop (pilot crash, power loss, network loss) the backend releases the lease and issues the stop itself — otherwise an external recorder runs indefinitely and the device stays leased. | 18 |
| EXTLINK-17 | **Device lease.** A device reachable by more than one pilot is arbitrated **backend-side** (the Pi cannot know about other pilots). The lease key is derived from the normalized `host` field — one physical box = one lease regardless of how many ports or modules target it, matching the real constraint that such a device has one record node; `host:port` was rejected because one machine would take separate leases for its control and data modules and admit a second pilot. A held device **hard-blocks the run as a new preflight issue kind** alongside `missing`/`incomplete_config`/`class_mismatch`/`fda_ref_unresolved`, and **the issue names the holder** (pilot, subject, run, since when) so a blocked researcher has something actionable. Released by the same backend reconciliation that stops orphaned recordings (run no longer active ⇒ lease released), **plus a manual force-release** for when reconciliation itself is wedged. Hard-block only — **no queue, notify, or scheduling UX**. | 18 |
| EXTLINK-18 | **Control-only modules are legal.** An `ExternalHardware` subclass with **zero declared signals** (e.g. a purely HTTP-controlled recorder) must instantiate, bind, participate in the readiness gate, and use the egress path. Liveness and the readiness gate must not assume any signal exists. **AMENDED 2026-08-03 — control-only implies NO INBOUND TRANSPORT.** The original wording covered only the zero-`@signal` half; a device whose entire inbound story is an outbound poll (OpenEphys: liveness via HTTP `GET /api/status`) must not be forced to declare a transport `role`, a port, or to bind a socket nothing ever connects to. A **third role, `"none"`**, is therefore required: `socket_plan` returns a plan with **no socket**, `identity_ok` and the `@decoder` path are inapplicable, and config validation must not demand `listen_port` / `connect_port`. `.bind()` still registers the `<source_id>.alive` tracker, starts the liveness poll (which a lib overrides — see EXTLINK-07), starts the egress worker (EXTLINK-15), and fires the lifecycle hooks (EXTLINK-16), so such a module participates fully in the readiness gate. `host` may still be required for **egress**. | 18 |
| EXTLINK-19 | **External signals are authorable in the FDA editor.** Every signal an `ExternalHardware` lib declares — plus its always-present `<source_id>.alive` tracker (EXTLINK-07/18) — is offered as a **view operand** in the editor's condition builder and in the action-argument picker, grouped per external module and labelled with the dtype the AST extractor recorded (EXTLINK-09). Without this a transition gated on an external signal is authorable only by PUTing `fda_json` by hand: `buildViewOptions` builds its Hardware group from `toolkit.semantic_hardware` and its channel groups from detector configs, dotted extlink keys are enumerated nowhere, and the free-text `view key` escape only renders when the toolkit has **no** hardware at all. **The operand is stored as the RESOLVED key** `{"view": "<source_id>.<signal>"}` — the literal Tracker name the Pi registers — NOT as a pilot-agnostic ref. DVK-11's ref rule is deliberately not applied here: a ref shape would need a new resolver branch in `mics_task._build_transition_lambda` **and** in `_resolve_arg`, which EXTLINK-05's "zero new call sites" forbids. The residual cross-pilot risk (`source_id` is per-pilot config) is answered where this project already answers cross-pilot hazards — **preflight**: the target pilot's extlink keys join `preflight_validate`'s `valid_keys`, so a key naming another pilot's `source_id` surfaces as an existing `view_key_unresolved` issue before the animal is in the box, with no new issue kind. The picker additionally renders a cross-pilot `conflict` warning, reusing the detector groups' existing warning line. The DVK-05 keep-current-value escape for unknown keys MUST survive — hand-authored and Python-only tracker keys still need to round-trip. | 18 |
| EXTLINK-20 | **A hand-driven external signal source + a demo task definition.** A cross-platform interactive driver (`mics-backend/tools/extlink_driver/`, macOS now and Windows later, `pyzmq` + `msgpack` only, no POSIX-only calls) lets a researcher sit at a non-Pi machine, type a signal name and a value, and watch the rig's FDA respond. Scope is deliberately minimal: send a named signal an `int`/`float`/`bool` (plus a small dict payload for an `@event`) — an interactive stdin loop, a hands-free `--sweep` auto-ramp, and a `--rate N` soak driver. **`role: router_bind` only** — the Pi binds and the driver dials in, so the laptop's address never enters any `pilot_hardware_config` row and a DHCP/Wi-Fi change cannot break the setup (`sub_connect` is proven separately by the rig checkpoint's Lib C). **No ack handling and no latency measurement in v1** — precise timing is deferred to Phase 28 (TTL vs Network Sync Validation), which owns clock-domain comparison; a laptop send-timestamp against the Pi's `ts_pi_recv` would measure clock skew, not latency. A throwaway `extlink_demo` task definition (`wait → armed → fired → wait`, gated purely on the demo signal, no valve, no reward, no real hardware) makes this runnable on the rig pilot with no animal. The payload/framing half must be unit-testable with **no socket**, since `zmq` is absent from the dev host. The load test is a **soak, not a benchmark**: at ~60 Hz for ~30 s the pilot stays up, the FDA keeps transitioning, the bounded egress/ingress queues report their drops, and ES ingestion keeps up — pass/fail is observable, never a latency number. This driver is separate from and additional to EXTLINK-11's `~/pi-mirror/scripts/dev/extlink_smoke.py`, which is unchanged; it must NOT live under `~/pi-mirror/`, whose contents are rsynced to the Pi. | 18 |

### Per-Pilot Device Health Surface (HEALTH)

Phase 19 — the surfacing half of EXTLINK-07, split out because Phase 18's scope explicitly excludes
*"Per-pilot health dashboard React page + WS forwarding via orchestrator"* and EXTLINK-07's own
commitment stops at the ES event. Device-neutral throughout: keyed on the `.alive` tracker suffix,
never on a device class, so DeepLabCut and photometry light the same indicator for free.

| ID | Requirement | Phase |
|---|---|---|
| HEALTH-01 | `OrchestratorState` carries per-pilot device health in a `device_health` dict keyed by full tracker name (`openephys.alive`), mutated under the same `self._lock` as every other mutator — it is written from the ZMQ handler thread and read by the `/pilots/live` HTTP thread. `snapshot()` exposes it, **defaulting to `{}` and never `None`**, so the overwhelmingly common no-external-device case needs no null check in any consumer | 19 |
| HEALTH-02 | `orchestrator_station.on_data` routes inbound CONTINUOUS payloads whose tracker name ends `.alive` into the state, in addition to the existing ES ingestion. **Matching is on the `.alive` suffix, never on a device class name or `source_id` value** — a second device type must light the indicator with zero orchestrator change. No new ZMQ message key: this rides the existing CONTINUOUS channel, which is what makes it nearly free | 19 |
| HEALTH-03 | The health map reaches the browser over `WS /ws/pilots`. `web_ui/app.py` forwards the orchestrator's `/pilots/live` response verbatim, so this is expected to need **no proxy change** — verify that before assuming it. `PilotLive` in `web_ui/react-src/src/types/index.ts` gains the field | 19 |
| HEALTH-04 | The pilot card (the `PilotCard` function inside `web_ui/react-src/src/pages/index/Index.tsx`, not a separate module) renders a visible warning affordance when a device is not alive during an active run, and renders **nothing** when the pilot has no external devices. Uses existing classes from `style.css` — no invented class names (CLAUDE.md) | 19 |
| HEALTH-05 | A pilot's `device_health` is cleared wherever `active_run` is cleared, so a stale warning can never outlive the run that produced it. A warning that persists after its run is worse than none — it trains researchers to ignore the indicator | 19 |
| HEALTH-06 | Loss of liveness mid-run remains **never automatically fatal** (EXTLINK-07): this phase surfaces, it never aborts a run. The FDA author gates on `not view.get_value("<source_id>.alive")` if the experiment requires it. No alerting outside the browser, no historical charts, no separate dashboard page | 19 |

### Open Ephys Integration (EPHYS)

Phases 26–28 — the first consumer of the Phase 18 `ExternalHardware` substrate. Source:
`docs/open_ephys_integration.pdf`. Two independent OE channels: HTTP REST control on **37497**,
ZMQ data on **5556** (plugin default, configurable — confirm against the rig's plugin settings).
**The Pi owns both channels**; the backend owns only the lease. **The TTL cable stays** — network
markers run alongside it and any cutover is a later decision made on Phase 28's evidence.

| ID | Requirement | Phase |
|---|---|---|
| EPHYS-01 | An `OpenEphys` hardware lib subclasses `ExternalHardware` and drives acquisition over the REST API — IDLE → RECORD at run start, RECORD → IDLE at run end — via the `on_run_start`/`on_run_stop` hooks (EXTLINK-16). The readiness gate keys on **`on_run_start` succeeding** (EXTLINK-13 as amended), so the FDA's first state is not entered until recording has actually started. The researcher never touches the Open Ephys GUI. | 26 |
| EPHYS-02 | The save folder is resolved per run from `run_ctx` (subject, session, run, pilot, timestamp) and set on the OE side before recording starts. The naming must be **unambiguous across rigs** — two pilots' data can never land in one folder. | 26 |
| EPHYS-03 | The resolved recording path is **persisted in MICS** and readable back through the API, so post-hoc tooling locates the recording without a human transcribing the path. | 26 |
| EPHYS-04 | Labelled event markers (`cue_on`, `reward`, …) are written into the recording from a state `entry_action` **and** from a trigger action, dispatched as an ordinary Phase-24 `hardware` action — **no new action type in the editor**. Sends ride the egress queue (EXTLINK-15): never blocking, never retried, failures logged. | 26 |
| EPHYS-05 | Preflight reports (a) OE unreachable and (b) the device already held by another run, naming the holder (EXTLINK-17). Both block the run before the animal is in the box. | 26 |
| EPHYS-06 | The Pi subscribes to the OE ZMQ Interface plugin using `role: "sub_connect"` (EXTLINK-14) and decodes spike/event messages inside the lib's `@decoder`. **No OE-specific parsing exists in platform code.** | 27 |
| EPHYS-07 | Units of interest and the rate window are **declared in `pilot_hardware_config.config`**, not discovered at runtime — sorted unit IDs only exist if sorting is configured upstream. Declaration keeps the resulting view keys static per pilot, so preflight can validate them and the editor can offer them. | 27 |
| EPHYS-08 | A windowed firing rate per declared unit is exposed as an ordinary view key (e.g. `oe.unit_A001_1.rate`), readable via the existing `view.get_value(...)` API. An FDA transition gated on a rate threshold fires on the rig against real spiking. Estimation runs on a periodic callback, never per-spike on the FDA thread. | 27 |
| EPHYS-09 | The derived rate keys are surfaced in the FDA editor's operand pickers through Phase 25's `api/detector_keys.py` mechanism — the established precedent for deriving editor-visible view keys from `pilot_hardware_config.config` rather than from static class declarations. | 27 |
| EPHYS-10 | `(ts_pi_recv, oe_sample_number)` pairs are logged to ES at a steady cadence for the run's duration, sufficient to fit clock drift post-hoc. This is the software co-registration that substitutes for the TTL. | 27 |
| EPHYS-11 | A single recording contains **both** the existing TTL pulses and the network markers for the same behavioural events, so the two are compared within one clock rather than across sessions. | 28 |
| EPHYS-12 | Offset and jitter between the TTL and network paths are quantified over a real session as a **distribution, not a single number**, and reported in a form the lab can act on — stating plainly whether network-only alignment meets the tolerance of the experiments actually being run, and what residual risk remains if the cable is removed. **No cutover is performed**; TTL removal stays a separate later decision. | 28 |

### Compute Primitives + Variables (CMP)

GUI-assembled FDA-JSON-v2 tasks gain value-producing computation (random draws, derived
numbers/booleans) that bridges state-entry → transition, without writing Python or editing
locked toolkit source. Design spec: `~/.claude/plans/i-realized-something-the-ancient-pnueli.md`.

| ID | Requirement | Phase |
|---|---|---|
| CMP-01 | FDA-JSON-v2 gains a top-level `variables` registry alongside `states`/`transitions`. **Delivered early by plan 24 value-capture** (`mics_task.py` `load_fda_from_json` ~1017) — variables support an optional `initial_value`. Phase 23 verifies it holds for `compute`-written variables rather than building it. | 23 (built in 24) |
| CMP-02 | At `load_fda_from_json()` each declared variable is instantiated as a generic `Tracker` and registered in BOTH `self.flags` and `self.view.view` (one object), readable via the existing `{"view": name}` / `{"flag": name}` operand path with zero new read code. **Delivered early by plan 24** — `_validate_output_spec` / `_capture_output` already write any action's return value into a declared variable. | 23 (built in 24) |
| CMP-03 | Entry-action `type: "compute"`: `{ "type": "compute", "op": "<op>", "args": [...], "output": "<var>" }`. Implemented as a **thin alias over the same internal call-a-method-and-capture-`output` helper the `hardware` branch uses** — args resolve via existing `_resolve_arg` forms. Kept as a distinct type (not folded into `hardware`) so validation can require `output`, preflight/`check_determinism()` can treat compute as pure vs hardware as side-effecting, and the state body reads as intent rather than plumbing. | 23 |
| CMP-04 | A compute library is a **class subclassing `Hardware`** (base is pure metadata — `pin = None`, no GPIO), instantiated like any device so its methods carry `@log_action`. **Not a module of pure functions** — `log_action` (`logging_utils.py:24,56`) only dispatches for `Mics_Tracker`/`Hardware` instances, so free functions would log nothing. Seed content (stdlib `random`/`math`/builtins only): random/copy — `random_choice`, `random_int`, `random_float`, `random_bool`, `assign`; numeric — `add`, `subtract`, `multiply`, `divide` (raise on /0), `modulo` (raise on /0), `minimum`, `maximum`, `clamp`. This is **seed content in a user-extensible system, not a closed set**. No comparison/boolean-logic ops — branching stays in transitions (Phase 15 DNF composes booleans). A variable read back as an arg (`add(counter,1)→counter`) is the supported counter pattern. | 23 |
| CMP-05 | Last-write-wins on re-entry: re-entering a state re-runs its compute action and overwrites the same variable slot. No reset between trials. | 23 |
| CMP-06 | Hot-reload path: new `variables` and `compute` actions flow through the existing `UPDATE_FDA` store; `hot_update_fda()` re-runs `load_fda_from_json()` so variables are re-created before transitions referencing them are rebuilt. | 23 |
| CMP-10 | Backend `_validate_task_definition()` (api/routers/toolkits.py ~661) validates the `variables` registry: every `compute` `output` is declared in `variables`; every transition/condition reference to a variable resolves; rejects name collisions with toolkit `FLAGS`, `SEMANTIC_HARDWARE`, and view keys. Hard 422s, distinct from the soft hardware-drift path. Logic in a new `api/fda_validation.py` sibling module (`toolkits.py` is oversized — import + call only). | 23 |
| CMP-11 | `api/fda_utils.py` recursive ref scanner extended to cover `compute` `output` names (so lib/ref impact detection sees them). | 23 |
| CMP-12 | A compute library IS a hardware library: a row in `hardware_libs`/`hardware_lib_versions` distinguished by a new `kind: 'hardware' \| 'compute'` column. Inherits the existing versioning (`version_number`, `source_code`, `sha256_hash`, `state` unvalidated→beta→stable, `ast_metadata`, `validation_error`, promotion trail) and the existing `LOAD_HARDWARE_LIBS` transport (`pilot.py:670`). **No new table, no new transport, no new versioning system.** | 23 |
| CMP-13 | GUI state-builder (`StateBodyPanel`): the action-type list gains **exactly one** entry, "Compute" (not N ops), rendering a single compact row `[output var] = [op ▾] ( [args] )` with ops grouped by lib and the same visual weight as a hardware action. Typing a new `output` name auto-declares it into the task's `variables` registry inline. | 23 |
| CMP-14 | GUI transition-condition builder: operand dropdown is populated from toolkit `FLAGS` + the task's `variables` registry, so a freshly-declared variable is immediately selectable as a condition operand. **Delivered early by TRIGA-14 / plan 24-08.** Phase 23 verifies it still holds for `compute`-written variables rather than building it. | 23 (built in 24) |
| CMP-15 | Discoverability: a `variable_never_written` **preflight issue** — a transition reads a variable no reachable upstream state writes — surfaced through **Phase 25's existing preflight issue renderer** (the `view_key_unresolved` path, incl. the skip-save loop). Promoted from "nice-to-have" to load-bearing: a silently-`None` variable reads as a mysterious dead branch on the rig. Plus a read-only variables inspector in the task editor (variable → writers, readers) — not an authoring surface, not a page. | 23 |
| CMP-16 | Op-level logging is mandatory. Variables are Trackers so `Tracker.set()` already logs the **result** (`{"id":"target","value":true,"func_name":"set"}`); that does not record that it came from `random_bool(0.5)`, and the draw parameters are scientifically material (needed to analyse a sequence and to detect p changing mid-experiment). The `Hardware` subclass + `@log_action` emits a `Hardware_Event` per op. **Both events must reach the event log.** | 23 |
| CMP-17 | Compute libs are **toolkit-scoped**, linked via `toolkit_hardware_libs` exactly like hardware libs — only the active toolkit's libs reach the Pi. Version resolution for **both** lib kinds: (1) task-definition pin `hw_lib_versions`, (2) toolkit link `default_version_id`, (3) **latest stable** (`hardware_libs.stable_version_id`), (4) nothing deployable → preflight issue, not a silent skip. **This corrects existing behaviour** — today the chain is pin → `active_version_id` (`orchestrator_station.py:868`, `hardware_libs.py:541`), `stable_version_id` is never consulted, and an unvalidated active version is dropped with only a log warning. No separate approval gate: compute libs inherit the existing unvalidated→beta→stable promotion flow as-is. | 23 |
| CMP-18 | GUI: compute libs are listed on the existing Hardware Libraries page behind a `kind` filter chip. **No new page, no new nav entry.** | 23 |
| CMP-19 | Forward-compat for deferred per-Pi package management: (a) compute lib versions carry an explicit **declared** imports/requires field from day one — validation accepts stdlib only today and rejects the rest with a clear message, but the dependency is declared rather than inferred; (b) compute libs are wired into the existing `test_import` round-trip (`l_load_hardware_libs` already imports and reports back pre-run); (c) a `compute_lib_import_failed` preflight issue class is reserved alongside `view_key_unresolved`. Three cheap hooks so the deferral is not rework. | 23 |
| CMP-20 | **One read namespace: `view`.** Every operand picker that *reads* a value — transition conditions, `wait_condition`, `if`-action conditions, and action arguments — offers **view / literal / param** only. `flag` and `hardware` become **legacy-only escapes**: present in the type list solely when the stored operand already has that shape, labelled legacy, and gone once the researcher edits it. No migration of stored definitions, no silent rewrite; backend (`fda_validation.py:250-253`) and Pi (`_build_condition_operand`, `_resolve_arg`) keep accepting both forms so hand-authored JSON and existing task definitions run unchanged. Rationale: `view` is the strict superset (toolkit flags + variables + hardware + detector-derived keys), `flag` a subset that mirrors it exactly (`Tracker.get_state()` *is* `return self.value`, and the same object is registered in both dicts), and `{"hardware": X}` is outright broken — `_build_condition_operand` (`mics_task.py:633-634`) reads `.value`, which no `Hardware` class, subclass, or seed lib defines. Writes are unaffected and stay flag-shaped (CMP-22). | 23 |
| CMP-21 | Declared variables are selectable inside **`if`/`else` conditions in the state builder**. `IfActionEditor` already receives `variableNames` and forwards it into its then/else `ActionEditor`s, but drops it (and `hwModuleNames`) at its own `ConditionBuilder` (`IfActionEditor.tsx:82-87`), so the flag list collapses to `toolkit.flags` and the view picker loses both the variables and the Hardware group. Everything below the GUI already supports this: `fda_utils.py::_scan_action_conditions` walks `if`-action conditions, `fda_validation.py::validate_compute_variables` accepts the resulting operands, and `_build_condition_operand` is the *same* builder transitions use. | 23 |
| CMP-22 | Declared variables are selectable as a `type:"flag"` action `ref` — the **write** side, which stays flag-shaped because `self.flags` is the correct registry for a tracker write. Variables resolve to the `'Tracker'` method set (`increment`, `set`), **not** the `?? 'Counter_Tracker'` default. Today `regularFlagKeys` (`ActionEditor.tsx:153`) is built from `toolkit.flags` alone, so the only way to write a variable is a compute action's `output` — leaving "if X then set my_var = 1" unexpressible. Also removes the phantom `decrement`/`reset` entries from `Counter_Tracker`'s method list (`ActionEditor.tsx:43-47`): neither exists in `Tracker.py`, so picking one saves clean and raises `AttributeError` on the rig. | 23 |
| CMP-23 | `ArgInput` gains a `view` mode, replacing `! Flag` under CMP-20's rule — an argument value is a read. The Pi's `_resolve_arg` has supported `{"view": key}` since Phase 24 (`mics_task.py:511-518`), but no picker can emit it, so a detector-derived view key can never be passed as an action argument even though it can be compared against in a condition. | 23 |
| CMP-24 | Pi-side prerequisite for CMP-23: `_resolve_arg`'s view branch reads `get_state()` rather than `.value` (`mics_task.py:518`), matching `View.get_value()`. Without it `{"view": X}` evaluates fine in a condition but `AttributeError`s as an *argument* whenever X is hardware — and CMP-23's new view arg-mode offers semantic hardware, so the swap would ship a live crash path. **Scope narrowed 2026-08-05 (user decision):** originally three Pi edits; the other two were built, then reverted before deployment because neither is required by the read-namespace change. Both remain open in `23/deferred-items.md` with their fixes and tests fully described — (i) mirroring the auto-created `trial_counter` into `self.view.view` (a latent bug reachable only via hand-authored JSON: the auto-create branch fires exactly when the toolkit omits `trial_counter`, in which case the GUI cannot offer it in the view picker either), and (ii) accepting `type:"view"` in `_build_state_method`'s pre-validation loop (an unrelated pre-existing bug about view *actions*, not view *operands*). Rationale: CMP-20–23 is a UI-layer change; the live-rig diff stays at the one edit it actually depends on. | 23 |
| CMP-25 | Backend: the condition-operand `valid_names` set (`fda_validation.py:245`, via `_valid_flag_names`) includes `toolkit.semantic_hardware` keys. The view picker already offers semantic hardware (`ConditionBuilder.tsx:100` feeds it into `buildViewOptions`), so on a semantic (non-backend-authored) toolkit, selecting hardware in a condition 422s **today** with "references unknown variable/flag". CMP-20 promotes that path to the only read path, making the gap load-bearing. | 23 |

> **Operand-namespace note (CMP-20–25, added 2026-08-05):** these six close out Phase 23 as a
> consistency pass over the operand pickers rather than new compute capability. The governing rule
> is **write by name (flag/tracker actions); read anything through `view`**. Verified against the
> runtime before adoption: flags and variables register the *same* Tracker object in both
> `self.flags` and `self.view.view` (`mics_task.py:339-340`, `:1060-1061`), and `View.get_value()`
> → `Tracker.get_state()` → `return self.value`, so the two operand forms are exactly equivalent
> for everything the flag picker offers. The change is therefore UI-layer only, plus the three
> Pi-side and one backend fix (CMP-24/25) that make the equivalence total instead of
> nearly-total.

> **Deferred (not tracked):** the `expr` escape-hatch (former CMP-07–09 — a sandboxed restricted-AST expression evaluator with a `type:"expr"` action), and equally **inline Python typed into a state body**. Both forfeit versioning, sha, reuse, AST-driven GUI help, and — critically — have no object for `@log_action` to attach to, so they cannot satisfy CMP-16. Python authoring happens in the hardware-lib editor; the state body only selects and wires. Documented in the design spec (`~/.claude/plans/i-realized-something-the-ancient-pnueli.md`); can be re-added as its own phase if a concrete need arises.
>
> **Also deferred:** **compute actions in trigger assignments** (user decision 2026-08-03). CMP-13's compute affordance is state-body only. Because `ActionEditor` is shared with `TriggerAssignmentPanel` — which already threads `variableNames` — the option is actively gated off in the trigger context via the existing `allowTriggerContext` flag, not merely left unwired. UI-only: the Pi builds both action lists through the same `_build_action_callable`.
>
> **Also deferred:** third-party PyPI packages + per-Pi package management (numpy/scipy). User-authored Python is already solved — source ships from the DB and is test-imported. Third-party wheels are a genuine package-management problem; CMP-19 reserves the hooks. Sandboxing user compute code is likewise deferred — a compute lib runs arbitrary Python in the task thread, mitigated for now by the stable-version promotion flow rather than a sandbox.

### Trigger Assignment Action Lists (TRIGA)

A hardware trigger runs the same action vocabulary a state's `entry_actions` uses, assembled in
the task-editor UI instead of hard-coded as a Python method. Reference case: `learning_cage.detectedLick`.
Context: `.planning/phases/24-trigger-assignment-action-lists/24-CONTEXT.md`.

| ID | Requirement | Phase |
|---|---|---|
| TRIGA-01 | A `trigger_assignments` entry carries an ordered `actions` list using the **same action schema as state `entry_actions`**; each action is built through the existing `_build_action_callable` (`mics_task.py:524`) so `hardware` / `flag` / `timer` / `special` / `method` / `if` all work identically in a trigger and in a state body. Load-time `ValueError` on unknown type/ref is preserved. | 24 |
| TRIGA-02 | The composed trigger callable declares `level` / `tick` parameters so `execute_trigger`'s `inspect.signature` check (`task.py:286`) passes them, while the underlying action callables stay zero-arg. `_resolve_arg` (`mics_task.py:401`) gains a `{"trigger": "level"\|"tick"}` form so an action can consume the trigger invocation context. | 24 |
| TRIGA-03 | A `view` action type can write `self.view.view[key]` — the trackers created by `check_for_detectors` via `view.add_Tracker` live only in `self.view.view`, never in `self.flags`, so no existing action type can reach them. Supports passing `pi_timestamp`. | 24 |
| TRIGA-04 | A hardware/method action's **return value** can be captured into a named slot instead of being discarded (`_hw_call` currently drops it). The capture mechanism is shape-compatible with Phase 23's `variables` / `compute` `output` design — one mechanism, not two. | 24 |
| TRIGA-05 | The `detectedLick` pattern — choosing the target tracker from a value **returned** by the hardware call (`device_str = f"{device_name}{pin_number}"`) — is expressible from the UI. Approach selected and justified during planning (see 24-CONTEXT.md Open Decision 1). | 24 |
| TRIGA-06 | **The `handler` enum is removed; `actions` is the only vocabulary.** `_build_touch_detector_callback` and `_build_digital_input_callback` are deleted, not corrected — there is no touch-specific code left in the trigger runtime. Justified by a live DB check (2026-07-26): the only row with non-empty `trigger_assignments` is task definition 185 — three malformed `touch_detector` entries, no `config`, referenced by no protocol step, and guaranteed to raise `KeyError` on the Pi if started. Those entries are cleaned up. Absent/empty `trigger_assignments` still leaves `self.triggers` untouched, and legacy Python tasks assigning `self.triggers[...]` directly are unaffected (`apply_trigger_assignments` only ever *adds*). Handler-specific tests in `~/pi-mirror/tests/test_trigger_assignments.py` are replaced by `actions` equivalents; all non-handler tests still pass. | 24 |
| TRIGA-07 | Backend validates `trigger_assignments` on save and returns **422** — `trigger_name` non-empty, action type in the allowed set, `hardware_ref` resolves against the toolkit's `SEMANTIC_HARDWARE`, `method` refs in `CALLABLE_METHODS`, `output` names declared in `variables`, referenced flag keys exist. Today there is **zero** trigger validation in `api/`; a bad ref surfaces only as a Pi `ValueError` at session start — task definition 185 is a live example. Legacy `config`-key validation is moot now that the `handler` enum is gone (TRIGA-06). View-**key** resolution stays out of scope at save time: task definitions are pilot-agnostic, and valid keys like `LICKER0…LICKER3` are derived at runtime from a pilot's `prefs.json`; that check belongs to Phase 13's `preflight_validate`. | 24 |
| TRIGA-08 | `api/fda_utils.py`'s recursive reference scanner covers hardware refs inside `trigger_assignments`, so a hardware-lib version change flags the task definitions whose triggers use it. | 24 |
| TRIGA-09 | The trigger panel hosts the **same action editor** the state body panel uses (`ActionEditor.tsx` / `ArgInput`), not a parallel implementation. | 24 |
| TRIGA-10 | The allowed **action** vocabulary is single-sourced. With the `handler` enum gone (TRIGA-06) the `HANDLERS` array in `TriggerAssignmentPanel.tsx` and the `if/elif` chain in `apply_trigger_assignments` both disappear, removing that divergence by construction. What remains is the action-type list, shared on the Pi between `mics_task` and `tools/validate_fda.py` via the new `fda_vocabulary.py`, with backend 422 validation as the practical enforcement point for anything the React editor could send. | 24 |
| TRIGA-11 | ~~**Rig proof:** lick detection runs on the real pilot with **no Python callback registered for `TOUCH_INT`**.~~ **DROPPED 2026-07-27 (sourceless-only scope change).** Vacuous on a backend-authored toolkit: it dispatches to `mics_task` and never had a Python callback to unregister, so the grep-based proof tests a property that cannot exist. Its rig-proof half is superseded by run 475 (47 `TOUCH_INT` firings, alternating levels, UI-assembled action list, no `learning_cage`). Replaced by **TRIGA-11a**. | ~~24~~ |
| TRIGA-11a | **Rig proof (replaces TRIGA-11):** on a **sourceless** toolkit, touching each of the four MPR121 electrodes updates the matching `LICKER{n}` view tracker with a `pi_timestamp`, **and touching electrode 2 never moves `LICKER0/1/3`** — the cross-talk negative is the property the constrained editor affordance (24-CONTEXT R3) exists to guarantee and must be asserted, not assumed. The UI round-trip checkpoint runs **first**, not last: five of the seven post-execution defects in waves 1–2 were in the UI↔API seam and surfaced only by using the editor. Negative case: an invalid assignment is rejected at save time (422), not at session start. Verifiability note: `@log_action` records kwargs only, so `hardware.set(x)` never logs `x` — only `Tracker.set` logs a `value`, which is what makes this checkpoint observable in ES at all. | 24 |
| TRIGA-12 | `check_for_detectors` (`mics_task.py:241-266`) discovers touch detectors by **capability** (`num_detectors` + `device_name` + `read()`), not by `isinstance` against the `i2c.Touch_Detector` imported at `mics_task.py:17`. A detector declared through the backend hardware-module registry lands in group `"Modules"` with its class `exec`'d from `source_code` by `_resolve_hardware_classes` — a different class object, so identity matching yields zero `add_Tracker` calls, no `LICKER0…LICKER3` in the view, and a `view` action that writes into a non-existent key: no exception, no lick data. `hardware/i2c.py` is off-limits, so the fix lives in `check_for_detectors`. Per-group duplicate-`device_name` `ValueError`, the `f"{device_name}{i}"` key format and the `add_Tracker` call are unchanged. Detector trackers stay **view-only** (`add_Tracker` writes `self.view.view` only, unlike `init_flags`) — they are `{"view": "LICKER0"}`, never `{"flag": "LICKER0"}`. | 24 |
| TRIGA-13 | **MOVED TO PHASE 25 (DVK-01/02/03) 2026-07-27.** The stated mechanism is structurally impossible: a registry-declared detector **never appears in `prefs.HARDWARE`** — its config arrives per-run via `PREFS_HARDWARE` in the START payload, after HANDSHAKE. Only the backend can derive these keys, from `pilot_hardware_config`, which is exactly what DVK-01/02/03 specify. Phase 24 no longer needs them: the constrained detector affordance (24-CONTEXT R3) removes the need on the trigger path, and the runtime-resolved `{device_name}` token (R5) removes the need for `device_name`. **Transitions on `LICKER2` are Phase 25**, scheduled to run immediately after 24. DVK-01's "one derivation, one key format" is the reason not to build a minimal second derivation here. Original text: | ~~24~~ → 25 |
| ~~TRIGA-13~~ | ~~The Pi derives them at HANDSHAKE from `prefs.HARDWARE` (`device_name` × `num_detectors`, same capability rule and same `f"{device_name}{i}"` format as `check_for_detectors`) and ships them alongside `flags`/`semantic_hardware`; they are stored on the toolkit row, returned by the toolkit API, and offered in the editor's view-operand dropdown and the `view` action's key-template picker. Today `grep -rn "num_detectors\|device_name" api/ web_ui/react-src/src` returns **zero hits** and `ConditionBuilder` renders a closed `<select>` (lines 64-67, 79-87), so `LICKER0` is currently neither pickable nor typable. Nothing outside the Pi computes a key, and nothing hardcodes `LICKER` or `4`. Scope: prefs-declared hardware only, last-handshake-wins per toolkit — registry-declared detectors and per-pilot disagreement are Phase 25 (DVK-01/02/06).~~ | ~~24~~ |
| TRIGA-14 | **Condition operand pickers offer the task's declared `variables`, not just toolkit flags** — `ArgInput` flag options and `ConditionBuilder` operand options both become `toolkit.flags ∪ variables`. Without it the canonical payload cannot be assembled by clicking: its guard is `{"flag": "pin_number"}` and its view value is `{"flag": "level"}`, both *variables* captured via `output`, while `flagKeys` is `Object.keys(toolkit?.flags ?? {})` (`ArgInput.tsx:60`) in a closed dropdown. This is CMP-14 pulled forward from Phase 23, which then verifies rather than builds it. On the Pi both live in `self.flags`, so this is one namespace, not two. | 24 |
| TRIGA-15 | **`trigger_name` is chosen, not typed.** Only hardware whose class sets `is_trigger` can carry a trigger: `init_hardware` calls `hw.assign_cb(partial(self.handle_trigger, hardware=hw))` for exactly those, and `handle_trigger` maps the firing BCM pin → board → `self.pin_id` back to that hardware's `HARDWARE[group][id]` key, which is what `self.triggers` is keyed by. Today only `gpio.Digital_In` and `gpio.Digital_Out` set the flag (`Hardware.is_trigger = False` by default), so no i2c device — MPR121 included — can ever be a trigger source. The Pi reports the qualifying entries in HANDSHAKE (`trigger_sources`, derived with that same `is_trigger` predicate so the UI list and the runtime wiring cannot drift), they are stored on the toolkit and served by the API, the trigger panel renders a dropdown over them, and the backend 422s a `trigger_name` outside the set when the set is known. Without this a typo is a **silent no-op** — the assignment saves, `self.triggers["TOCH_INT"]` is created, and nothing ever fires it. **Amended 2026-07-27:** `Digital_Out` sets `is_trigger` too (`gpio.py:343`), so every LED qualifies. The list is reported **truthfully and grouped inputs-vs-outputs** rather than filtered — `init_hardware` really does call `assign_cb` on them, and narrowing would make the UI understate what the runtime wires. The `Digital_Out` flag is recorded as a probable wart for a later look, not silently hidden. | 24 |
| TRIGA-16 | **A hardware action's `method` is validated on save.** `fda_validation` checks `CALLABLE_METHODS` only for `type: "method"`, so `{"type":"hardware","ref":"MPR121","method":""}` returns **200** and becomes a silent no-op on the Pi — the exact failure class TRIGA-07 exists to prevent, and the UI is currently the only guard. Three of the seven post-execution defects in waves 1–2 lived in that seam. AST metadata for hardware libs already exists (Phase 09), so the method set is available: an unknown or empty `method` on a `hardware` action returns 422 with the offending ref and method named. Applies to actions inside `trigger_assignments` and inside state `entry_actions` alike. | 24 |
| TRIGA-17 | **The detector write is a constrained one-pick affordance in the editor, emitting general JSON.** The researcher picks the detector device and nothing else; the target view key and the written value are both derived from that call's own return, so it is structurally impossible to read electrode 2 and write `LICKER0`. The widget is a **UI macro** — it emits the ordinary `hardware`+`output` / `view`+`key_template` chain, so nothing new reaches the Pi as a concept and Phase 23's `compute` works inside a trigger with zero rework. A stored action list that no longer matches the recognised shape degrades to the raw action editor, which stays reachable. The Pi runtime gains **no** detector-specific code — the exception to the separation principle is editor-side only. Rejected: a `detector` action type expanded on the Pi (re-introduces what TRIGA-06 deleted); backend expansion of a compact stored form (stored JSON ≠ dispatched JSON). | 24 |
| TRIGA-18 | **`key_template` supports a runtime-resolved `{device_name}` token, keeping task definitions pilot-agnostic.** `LICKER` is `device_name` from `pilot_hardware_config` — per-pilot data. Baking the literal `"LICKER{pin_number}"` in at save time makes the definition pilot-specific: on a pilot whose MPR121 is named `TONGUE` it writes into a view key that does not exist — silent no-op, no exception, no data. Instead the `view` action carries a `source_ref` and `resolve_key_template` (`fda_vocabulary.py`) resolves `{device_name}` from that hardware object's own attribute at run time. The token is **generic, not detector-specific** — any hardware has a name. Note `resolve_key_template` substitutes solely from the captured-values dict today, so the resolver must be *handed* `device_name`, not merely taught the token. Consequence: Phase 25 never has to solve `device_name` for the trigger path, only for the editor's operand pickers. | 24 |
| TRIGA-19 | **The licker level comes from `detect_change`'s return, never from the trigger's `level`.** `Touch_Detector.detect_change()` (`i2c.py:831-843`) returns `(pin_number, level)` where `level` is that electrode's new state; `execute_trigger`'s `level` is the GPIO edge on the `TOUCH_INT` IRQ line. The MPR121 holds IRQ asserted until its touch-status register is read, so `EITHER_EDGE` yields two edges per real event — assert, then deassert when `detect_change` performs the read. Run 475's alternating 0/1 was that handshake, not touch/release, consistent with 140 `Mid_LED` calls for 47 firings. With four electrodes, `{"trigger":"level"}` would write IRQ polarity into whichever `LICKER` changed: **wrong data that looks correct in ES.** Both `pin_number` and `level` are captured from action[0]'s `output`; only `pi_timestamp` comes from the trigger context. `{"trigger":"level"}` remains correct for GPIO inputs (beam-break, IR) where the edge level *is* the payload. | 24 |

---

### Detector-Derived View Keys (DVK)

Phase 25. Follow-up to Phase 24: TRIGA-12 makes the **Pi** create `LICKER0…LICKER3` reliably; DVK makes the **backend and the editor** know those keys exist.

| ID | Requirement | Phase |
|---|---|---|
| DVK-01 | **One derivation, one key format.** A single backend helper turns a stored hardware config carrying `device_name` + `num_detectors` into its view keys, producing exactly `f"{device_name}{i}"` for `i in range(num_detectors)` — byte-identical to `check_for_detectors` (`mics_task.py:241-266`). Every consumer (editor options, preflight, validation) calls that one helper; the format is never re-implemented. Configs lacking either field yield no keys and are not an error. | 25 |
| DVK-02 | **A pilot-agnostic design-time source, chosen deliberately.** Task definitions are pilot-agnostic, but `device_name`/`num_detectors` live in `pilot_hardware_config.config` (per-pilot; `hardware_modules` stores no defaults — only `name`/`class_name`/`hardware_lib_id`/`description`). The editor needs keys before a pilot is chosen. Exactly one source is implemented — union across pilots configured for that module, a declared default on the module, or a per-toolkit declaration — and where pilots disagree (different `num_detectors`) the disagreement is **surfaced**, not silently merged. | 25 |
| DVK-03 | **Detector channels appear in the view-operand picker.** `ConditionBuilder.tsx` builds `viewOpts` from `semantic_hardware` + hardware-module names + flags (lines 64-67) and renders a `<select>` whenever that list is non-empty (79-87), so a detector channel is currently *unreachable by typing*. Detector channels join that list — grouped and labelled by **`device_name`** (`"LICKER channels"`, `"TONGUE channels"`) so `MPR121` (the device) reads differently from its channels — everywhere the component is used: transition conditions, `ConditionGroupsEditor`, and state-body operands. **Amended 2026-07-29 (see DVK-11):** picking a channel stores a detector *reference*, not the resolved key; the group label must come from a backend-supplied `device_name` and never from stripping digits off a key. | 25 |
| DVK-04 | **The `key_template` field offers the same keys.** The Phase 24 trigger `view` action's `key_template` is free text. The editor offers the detector prefix plus token completion (`LICKER{pin_number}`) so the canonical lick payload is assembled by picking from known values, not by typing a string that only fails on the rig. | 25 |
| DVK-05 | **Unknown keys degrade, never disappear.** A stored FDA JSON referencing a key outside the derived set stays editable and is flagged as unknown — preserving `ConditionBuilder.tsx:85`'s existing keep-current-value behaviour (line 81 is `</select>`; the escape is the `!viewOpts.includes(key)` option at 85) — and a free-text path remains reachable for keys the backend cannot model. Opening and re-saving an old definition must not silently rewrite or drop its view keys. **Scope narrowed 2026-07-29:** this covers keys the backend cannot model, **not** legacy detector keys — all 149 `task_definitions` rows were walked and none stores a literal detector key (the six with view operands use `TIMER`, `counter`, `cue_led2`, `Mixer_AUDIO1`, `Timers_TIMER_TO_NEW_TRIAL`, `boolean_flag`), because the editor could never emit one. No detector-key migration exists to write. | 25 |
| DVK-06 | **Per-pilot resolution at preflight.** `preflight_validate` (`api/routers/toolkit_dispatch.py`) — named by 24-02 as this check's home — resolves every transition/action view key and every `key_template` against the derived key set of *that* pilot and returns any mismatch in the existing issue shape so `HardwareCheckModal` renders it. A typo fails before START instead of writing into a view key that does not exist (a silent no-op today: no exception, no data). | 25 |
| DVK-07 | **Detector keys are view-only, never flags.** `add_Tracker` writes `self.view.view` alone, unlike `init_flags` which writes both `self.flags` and the view (TRIGA-12). Derived keys must not enter flag pickers, flag-name validation sets, or the `variables` namespace: `{"view": "LICKER0"}` is valid, `{"flag": "LICKER0"}` is not and must stay invalid. | 25 |
| DVK-09 | **Detector channels are declared, not assumed to start at zero.** Found on hardware 2026-07-27 (runs 480/481, `24-HARDWARE-VALIDATION.md` §2b Finding A): the rig's four spouts are wired to MPR121 channels **1–4**, but `check_for_detectors` builds `f"{device_name}{i}" for i in range(num_detectors)`, so `num_detectors: 4` creates `LICKER0…LICKER3` — `LICKER0` can never fire and **channel 4's writes are silently discarded** (9 real events lost — see DVK-10 for why no exception surfaced). A tracker's name **must** equal its hardware channel index, because the key is `{device_name}{pin_number}` where `pin_number` is the raw index `detect_change` returns — so renumbering is not an option and the range itself must be declarable. The config gains `first_channel` (default `0`) alongside the existing `num_detectors`, so derivation becomes `range(first_channel, first_channel + num_detectors)` — `first_channel: 1` on the rig's MPR121 yields `LICKER1…LICKER4`. **Decision locked 2026-07-29 (user):** scalar offset, *not* an explicit channel list — the rig only evidences contiguous 1–4 and a list does not justify the extra editor UI. Non-contiguous wiring (e.g. skipping a dead electrode) is therefore **not** expressible; revisit only if real wiring demands it. Absent `first_channel`, behaviour is today's `range(0, n)`, so every existing config is unchanged. The single DVK-01 derivation helper and `check_for_detectors` consume the same declaration, so the Pi's trackers and the editor's offered keys cannot disagree. | 25 |
| DVK-10 | **A failing trigger action must not be swallowed as "no trigger".** Verified 2026-07-29 tracing DVK-09's silence: the unknown-key guard *does* raise — `mics_task.py:717-721` raises `KeyError("view action: key 'LICKER4' … not found. Available: …")` — and `_run_trigger_actions` (`mics_task.py:1323-1328`) is `try`/`finally` with no `except`, so it propagates into `execute_trigger`'s `except KeyError` (`task.py:285-298`). That handler exists for a missing `self.triggers[pin]` lookup but wraps the **callback invocation** too, so a genuine action-list failure is reported as `logger.debug("No valid trigger for {pin}")` — naming neither the key nor the cause. Narrow the guard to the `self.triggers[pin]` lookup alone; a callback that raises must surface as an error (log + event) without killing the worker thread. This is why DVK-09 lost 9 events with no visible sign, and it hides every future action-list key error that DVK-06's preflight does not catch first. Note `process_queue` (`task.py:262-266`) still has no handler of its own — a non-`KeyError` callback exception permanently disables all trigger processing (separate, already on the Outstanding list). | 25 |
| DVK-11 | **A task definition never stores a per-pilot detector name.** Raised by the user 2026-07-29 on review of the first plan set, which stored the resolved key (`LICKER2`) in transition operands — putting a per-pilot label inside a pilot-agnostic artifact, so renaming `device_name` orphaned stored definitions with no strong signal (the red `!` is driven by `validation_status`, computed against the *toolkit*; nothing revalidates on a `pilot_hardware_config` change). Instead a condition operand stores a **detector reference plus channel index** — e.g. `{"view_detector": {"ref": "MPR121", "channel": 2}}` — and the Pi resolves `f"{device_name}{channel}"` at build time from the referenced hardware object, the same way the Phase 24 `view` **action** already resolves `{device_name}` from its `source_ref` (`mics_task.py:689-716`). One resolution site: `_build_transition_lambda` delegates both operands to `_build_condition_operand` (`mics_task.py:1206-1207`, `view` branch at `539-542`) for the unified `{left, op, right}` format the GUI emits; legacy hand-authored `{view, op, rhs}` (`:1214-1223`) is out of scope. `device_name`/`num_detectors`/`first_channel`/`address` all stay in `pilot_hardware_config` — the pilot owns what is true about the cage, the task definition owns what the experiment means. The validator must reject a reference to a module that is not a detector, and preflight (DVK-06) must reject an out-of-range channel for the chosen pilot. | 25 |
| DVK-08 | **Proof:** attach MPR121 to a toolkit → the FDA editor's view-operand dropdown lists `LICKER0…LICKER3` → a transition declared on `LICKER2` saves, passes preflight against the configured pilot, and fires on the rig. Negative case: a definition whose `key_template` names a device that pilot does not have is caught at preflight with a readable message, not at session start. **Amended 2026-07-29 (see DVK-11):** the framing above predates the design change and is kept only for the negative case, which is unchanged. The dropdown lists channels labelled by `device_name` (`"LICKER channels"` → *"MPR121 — channel 1…4 (→ LICKER1…4)"`), and under the rig's `first_channel: 1` the offered set is `1–4`, not `0–3`. A transition is declared as *"MPR121 — channel 2"* and **saves as `{"view_detector": {"ref": "MPR121", "channel": 2}}`** — no per-pilot name enters the definition. Plan 25-06 task 3 already tests the amended form; this note exists so the requirement row and the plan agree. | 25 |

### FDA Builder Canvas UX (CANVAS)

Phase 29. Pure UI/UX on the task editor canvas (`web_ui/react-src/src/pages/task-editor/TaskEditor.tsx`). No change to FDA semantics, validation, or anything sent to the Pi. Every requirement below traces to a defect the user hit while reading real graphs.

| ID | Requirement | Phase |
|---|---|---|
| CANVAS-01 | **Parallel transitions bow apart instead of crossing.** `fdaToEdges` (`TaskEditor.tsx:153`) emits plain default edges and `StateNode` exposes exactly one target handle (`Position.Left`) and one source handle (`Position.Right`), so `A→B` and `B→A` trace the same corridor in opposite directions and cross into an "hourglass". A custom `TransitionEdge` component groups edges by unordered node pair and gives each member a perpendicular offset proportional to its index within the group, so both directions are separately traceable. Applies to same-direction duplicates too (two `A→B` transitions with different conditions), not just opposing pairs. | 29 |
| CANVAS-02 | **Every edge's condition label is readable.** Both edges of a bidirectional pair currently render `condLabel(t)` at the same path midpoint, so one label is drawn on top of the other and only one survives. Each edge's label rides at a staggered position along **its own** offset curve. The label text itself is unchanged — `condLabel`/`renderTreeLabel` (`TaskEditor.tsx:82-103`) keep producing the same simplified string, including Phase 16 parenthesisation. | 29 |
| CANVAS-03 | **Directionality is visible on the canvas.** No `markerEnd` is set on any edge today, so a transition's direction is discoverable only by clicking it and reading `from → to` in the right-hand panel. Every edge renders an arrowhead at its target end, sized to stay legible at the default zoom and correctly oriented on the offset curve (the arrow must follow the curve's tangent at the target, not the straight-line angle). | 29 |
| CANVAS-04 | **Self-transitions render as a readable loop.** An `A→A` transition currently degenerates. It draws as a visible loop anchored on the node with its own label and arrowhead, and participates in CANVAS-01's offset grouping so multiple self-loops on one state remain distinguishable. | 29 |
| CANVAS-05 | **Node positions survive a refresh.** `fdaToNodes` (`TaskEditor.tsx:144`) recomputes `position` from array index (`x:(i%4)*270, y:floor(i/4)*170`) on every mount, so dragging is discarded on reload. Positions persist in a new `task_definitions.ui_layout` JSONB column of shape `{"nodes": {"<state>": {"x": n, "y": n}}}`, added via the existing `ADD COLUMN IF NOT EXISTS` pattern in `api/db.py` (`run_toolkit_migrations`), returned by `GET /api/task-definitions/{id}`, and accepted by `PUT`. | 29 |
| CANVAS-06 | **Layout is stored outside `fda_json`, deliberately.** `file_hash = sha256(json.dumps(fda_json, sort_keys=True))` (`api/routers/toolkits.py:590`, reassigned on update at `:838`) and is `unique` on the table (`api/models.py:522`). Putting layout in `fda_json` would make every node drag rewrite the task definition's content hash and ship canvas coordinates to the Pi inside the FDA payload. `ui_layout` is therefore a separate column, never merged into `fda_json`, and never sent to the Pi. Follow the raw-SQL update pattern used for the other migrated columns — they are not declared on the ORM class. | 29 |
| CANVAS-07 | **The default layout is layered and spaced, not an index grid.** For states with no stored position, a hand-rolled layered algorithm (no new npm dependency) ranks states by BFS depth from `initial_state` along transitions to assign columns, and spreads siblings vertically within each column; states unreachable from `initial_state` are placed in a trailing column rather than dropped. Spacing is sufficient that no two nodes or their labels visually collide at default zoom. | 29 |
| CANVAS-08 | **Newly added states are placed, not grid-stamped, and never disturb existing nodes.** `addState` (`TaskEditor.tsx:423`) and the toolkit-sync effect that appends missing toolkit states (`TaskEditor.tsx:268-300`) both use the index grid today, which can stamp a new node directly on top of a positioned one. Both route through the same placement logic, which assigns a free position without moving any already-positioned node. | 29 |
| CANVAS-09 | **"Restore default layout" is reachable from the canvas.** Right-clicking empty canvas (`onPaneContextMenu`) opens a context menu — reusing the styling of the existing node context menu (`TaskEditor.tsx:679-710`) — with an action that discards stored positions, recomputes the CANVAS-07 layered layout, and persists the result. | 29 |
| CANVAS-10 | **Dragging a node is not an FDA edit.** The existing autosave (`TaskEditor.tsx:318-351`) debounces on `fdaJson` and is deliberately gated behind two save-holds: an incomplete `trigger_assignment` and a half-built `entry_action` both block the PUT. Layout must not ride that path — a node drag would otherwise be blocked by an unrelated unfinished trigger, and would emit "Unsaved…" churn for a non-semantic change. Layout persistence uses its own debounced PUT with its own status handling, and dragging never marks the FDA dirty. | 29 |
| CANVAS-11 | **`TaskEditor.tsx` gets smaller, not bigger.** The file is already 873 lines against the project's 300-line target and 500-line hard limit (`CLAUDE.md` coding standards; the file is explicitly flagged there). The layered layout algorithm, the `TransitionEdge` component, and the layout-persistence hook each land in their own new module under `web_ui/react-src/src/`, and the net change to `TaskEditor.tsx` must not increase its line count. | 29 |
| CANVAS-13 | **A backward edge routes clear of the states it spans.** Found 2026-08-05 by walking real data: in task definition 186 (`source_less_toolkit FDA_tes_interuuppt`) the BFS layout puts `init → trial_onset → play_led → rand` in four single-node columns, and the transition `rand → trial_onset` runs backward across two columns — straight through `play_led`. CANVAS-01's pair offset cannot help: the edge is unpaired (there is no `trial_onset → rand`), so it is never grouped. A backward edge (target column strictly left of source column) is detected and bowed clear of the intervening band on one **consistent** side, with magnitude scaled to the number of columns spanned, so it reads as a return path rather than a line through a node. A backward edge spanning a single column keeps CANVAS-01's pair treatment — it is already adjacent and has nothing to clear. | 29 |
| CANVAS-14 | **Unreachable states pack into a block, not one tall column.** CANVAS-07 places states unreachable from `initial_state` in a trailing column. Real definitions make that degenerate: `bbb FDA` (172) has 14 states with only 3 appearing in any transition, and `ccc FDA` (161) and `bbb_testing backend auth` (155) are 11-and-3 — a single trailing column would be an 11-node vertical stack beside a 3-node graph. These are toolkit states auto-added by the sync effect (`TaskEditor.tsx:268-300`) that were never wired into the FDA. Unreachable states wrap into a grid block whose column count keeps the block's aspect ratio reasonable against the connected graph's own extent, instead of growing without bound in one axis. | 29 |
| CANVAS-12 | **Proof:** open a task definition whose graph has a bidirectional pair (e.g. a `CUE ⇄ TIMEOUT`) → both arcs are separately visible, each with its own readable condition label and an arrowhead pointing the right way → drag three nodes apart → hard-refresh the browser → the three nodes are exactly where they were left → right-click empty canvas → "Restore default layout" → nodes snap to a spaced layered arrangement and that arrangement itself survives the next refresh. Negative case: with an unfinished trigger assignment present (autosave held), dragging a node still persists its position. **Amended 2026-08-05 (CANVAS-13/14):** run the walkthrough on **task definition 186** (`source_less_toolkit FDA_tes_interuuppt`), whose 5 transitions exercise every routing case at once — two adjacent forward edges, the `play_led ⇄ rand` pair (CANVAS-01), and the `rand → trial_onset` two-column back-edge (CANVAS-13). All five must be separately traceable with no edge crossing a node. Second graph: open definition **172** (`bbb FDA`, 14 states with 3 in transitions) and confirm the unreachable states form a readable block rather than a stack running off-canvas (CANVAS-14). | 29 |

### Pi Repo Hygiene (HYG)

Phase 30. Removal-only, plus one credential remediation and two restorations. Every requirement is judged by **runtime reachability under the current architecture**, never by file age or git history — that distinction is the phase's whole premise, because the tree's residue was left by phases 9–25 that each shipped a capability without reclaiming the ground it superseded. Audit of record completed 2026-08-10 (five independent agents, cross-checked against the live DB and against the `.cpython-37` bytecode the Pi itself wrote).

| ID | Requirement | Phase |
|---|---|---|
| HYG-01 | **The leaked credential is revoked and absent from history.** `pilot/plugins/AssociationLearning.py:1323-1324` hardcodes a Gmail address and a 16-character Google app password used by `send_email()` over `smtplib` — the only such hit in a full-tree scan. Deleting the file is **not** sufficient: the credential is in the 188 MB `.git` history, where a later `git rm` does not remove it. The new repository is a **fresh `git init` over the pruned tree**, never a clone and never a history rewrite (a rewrite leaves reflogs and packed objects, and the mirror's history has no other value here). Proof: `git log -p` over the new repo's full history, grepped for the address and the password, returns nothing. The credential must also be revoked at Google — that is a human action outside the repo and is recorded as done before publication. | 30 |
| HYG-02 | **The pilot still starts and still runs a session.** The acceptance test is not an import check. From the cleaned tree the pilot reaches HANDSHAKE on the rig and a real session runs to completion with CONTINUOUS events landing in ES indistinguishably from before. This is deliberately the strongest criterion in the phase, because the Pi has **no `TASK_ERROR` emitter at all** — `pilot.py:609` catches a failed START, sets state IDLE, and reports nothing, so the backend marks the run `running` indefinitely. Every removal failure mode in this phase is therefore silent by default, and only a live session surfaces it. | 30 |
| HYG-03 | **`pilot/plugins/` and its orphans are removed as one change, never base-class-first.** The 27 task files go, and with them the three modules whose only importers are those files: `tasks/learning_cage.py`, `tasks/mics_cage_task.py`, `hardware/unreal.py` (nothing in `autopilot/` imports `unreal.py`; 15 plugins do). Ordering is load-bearing: `api/main.py:1059-1070` resolves a task's `base_class` only within the same handshake payload and raises 400 if absent, and because the commit is at `:1117` inside `orchestrator_station.py:85-183`'s single `try`, that 400 discards the tasks upsert, the toolkit upsert, the hardware-config seed **and** the locked-states upsert together. Subclasses-then-base-classes is safe; the reverse silently breaks every handshake. The directory itself is retained with a `.gitkeep` — `plugins.py:46-48` logs an exception and returns `{}` on a missing `PLUGINDIR`, which is survivable but noisy. | 30 |
| HYG-04 | **An empty HANDSHAKE is proven to be a no-op, not assumed.** With no plugins, `discover_tasks_metadata()` reports `tasks: []`. Verified against the live API before the sweep is trusted: the orchestrator skips its upserts (`orchestrator_station.py:93,102`), the API returns `tasks_received: 0` (`api/main.py:1119-1123`), no row is deleted, and no 500 is raised. Note the backend **never prunes** — there is no delete path anywhere on the handshake — so every existing toolkit row persists and keeps appearing in the UI with no signal the Pi stopped reporting it. That staleness is accepted, not fixed here. | 30 |
| HYG-05 | **`cameras.py` is removed together with the import *and the subclass* that hold it alive, in both copies.** `hardware/i2c.py:8` does `from autopilot.hardware.cameras import Camera`, and `i2c.py` is imported by `mics_task.py:4`, so deleting the 69 KB driver alone stops the pilot at import time. **Corrected 2026-08-10 during planning:** an earlier reading of this row claimed `Camera` was unused in `i2c.py` and that removing the import sufficed. That is **false and dangerous** — `Camera` is the **base class of `MLX90640`** (`i2c.py:580`, class body spanning `:580`–`:798`, immediately before `MPR121` at `:799`). Removing only the import leaves an undefined name evaluated at module-import time → `NameError` → `i2c.py` fails → `mics_task.py:4` fails → the pilot dies, silently, because nothing on the Pi reports a failed start. The edit is therefore **three-part**: drop the import, drop the `MLX90640` class, drop `cameras.py`. `MLX90640` independently satisfies all four removal criteria — zero hits across `mics-backend/api` and `orchestrator`, no `hardware_libs` or `hardware_modules` row, no `MLX` key in any pilot's `prefs.HARDWARE`, and `i2c.py:33`'s `import MLX90640 as mlx_cam` guard already sets `MLX90640_LIB = False` on this rig. **All three edits must also reach `hardware_libs` version 26**, whose `source_code` is `exec()`'d on the Pi when a toolkit resolves hardware classes; a Pi-only edit leaves the exec'd copy importing a deleted module. Confirmed live: version 26 contains the cameras import at byte offset 189 and `class MLX90640` at 21,345, and is 35,928 bytes against 35,934 on disk — the 6-byte drift, now measured. This phase reconciles the two copies or records why not. `hardware/usb.py` goes with `tasks/children.py`, its only importer. | 30 |
| HYG-06 | **Registry sweep collateral is removed, and nothing broken is left in `autopilot/tasks/`.** `tasks/{children,nafc,gonogo,free_water,test,RecordingBox,protocol_scripts}.py` are imported at every boot solely because `registry.get_task()` AST-sweeps that directory, dragging in `cameras.py`, `usb.py`, and the whole `transform/` and `stim/` trees — roughly 200 KB of import work per start, plus ~700 KB from the plugin sweep. Deleting them is safe; leaving a **syntactically broken** file there is not, because `common.py:47-67`'s `list_classes` has no per-file guard and one bad file makes `discover_tasks_metadata`'s blanket handler ship `tasks: []` in every handshake. (`pilot/plugins/` *is* guarded per file — the asymmetry is real.) `py_compile` over the surviving `tasks/` directory is a gate. | 30 |
| HYG-07 | **The Terminal-era tree is removed.** `terminal/` entire (17.9 MB, of which 16.4 MB is log spew), `run_terminal.sh`, `.vscode/`, and the Pi-side Terminal modules `core/{gui,terminal,plots,subject,styles,utils,reward}.py`, `viz/`, `data_handlers/`, `utils/invoker.py`, `utils/Event.py` (singular — superseded by `Events.py`, referenced only from `terminal/plugins/`), `setup/request_helpers.py`. The orchestrator replaced the Terminal agent wholesale. Note `core/subject.py` is **not** used for local HDF5 on the pilot — `open_file()` uses `tables` directly and its only call site is commented out; the 20 plugin references to `Subject` are docstrings. **`setup/` otherwise stays**: `autopilot/__init__.py:4` imports `setup_autopilot` unconditionally, so deleting it breaks `import autopilot` outright. | 30 |
| HYG-08 | **Vendored and generated bulk is removed.** `autopilot/code_2023.deb` (87 MB, a VS Code installer, zero references), `autopilot/docs/` (9 MB of pre-built Sphinx *output*), `examples/`, `autopilot/tests/` (upstream suite, needs PySide2, unmaintained here), `src/` (two uninitialised submodules — drop `.gitmodules` with them), `home/`, `~/` and `terminal/~/` (tilde never expanded), `auto_pi_lot.egg-info/`, `output.txt`, `environment.yml.save` (a nano emergency-save differing by one leading tab, i.e. the invalid copy), all `__pycache__/` and `.pyc`, `.pytest_cache/`, the seven zero-byte logger-name files at `autopilot/` root, upstream CI dotfiles, `Cow.wav` and the SoundBible wav (both unreferenced; the latter is third-party audio). **`LICENSE` (MPL-2.0) is mandatory and stays.** Root `blip.wav`/`alt_blip8.wav` are byte-identical duplicates of the `pilot/sounds/` copies and only the latter are reachable, since `mixer.py:18` resolves by bare filename through `SOUNDDIR`. | 30 |
| HYG-09 | **Removing `autopilot/pytest.ini` comes with a replacement in the same change.** It forces `--cov=autopilot` and `qt_api=pyside2`, which actively breaks the root suite — but it is the *only* pytest config in the tree, and the 23 root test modules currently work via per-file `sys.path` hacks. Removing it alone leaves the new repo with no config at all and CI failing on first run. | 30 |
| HYG-10 | **The repo carries no rig-specific configuration.** Decided 2026-08-10 (user): strip everything rig-specific. `pilot/prefs.json` ships as a template with no lab IPs, no `SUBJECT`, no `PORT_CALIBRATION`, no rig-specific pin values, and no stale `UNREAL` block (dead once HYG-03 lands). `pilot/{data,logs}/` keep their paths — `prefs.py:478-484` `mkdir`s every `Scopes.DIRECTORY` pref at boot — but ship empty behind `.gitkeep` + `.gitignore`. This costs nothing operationally: `tools/sync_pi.sh` syncs only `autopilot/` and never touches `pilot/`, so the device keeps its own real prefs and calibration regardless. The eventual cutover must assert that rather than rely on it. **`pilot/sounds/` is untouched** — an unreferenced-looking wav can still be a live cue resolved by bare filename. | 30 |
| HYG-11 | **244 lines of dead commented-out code are removed, and only those.** Concentrated in the upstream layers, not the lab's: `networking/station.py` (52 — including `:623-651`, which carries the original author's live note *"all of the below should be removed but i am paranoid… so i am just commenting for now"*), `networking/message.py` (50, entirely Python-2-era residue), `hardware/gpio.py` (28), `tasks/task.py` (28, three successive superseded trigger-dispatch implementations), and 11 smaller files. `mics_task.py` yields only 11 — it is the cleanest large file in the tree, contrary to expectation. Also removed regardless of any other decision: `pilot.py:49`, which hardcodes another person's home directory. **Excluded from this sweep:** ~122 lines classified as disabled *behaviour* rather than superseded implementation (see HYG-14), and ~72 lines referencing symbols that are still live. | 30 |
| HYG-12 | **`Event_Dispatcher.py` is never blanket-stripped.** Phase 25 left debug prints deployed on the live Pi in `task.py`, `mics_task.py` and `Event_Dispatcher.py`, but the `Event_Dispatcher.py` change is **not only prints** — it also carries a guarded tick read and the `_dropped_no_clock` / `_dropped_on_send` counters, which are real fixes (`25-HARDWARE-VALIDATION.md:365-375`). Those survive. Relatedly, every pi-mirror edit from phase 25 is an **uncommitted working-tree change**; the sweep must not mistake uncommitted-but-live for stray. | 30 |
| HYG-13 | **The five `external_hardware*` files, all 23 root test modules, and `fda_vocabulary.py` survive intact**, proven by a post-sweep manifest diff. Two of the five — `external_hardware_ingress.py` and `external_hardware_binding.py` — emerged during Phase 18 execution and appear in **no plan's `files_modified`**, so any deletion heuristic keyed on "named in a PLAN" destroys them; `external_hardware.py` imports both, and `_binding.py` is the single writer of the `alive` tracker Phase 19 depends on entirely. `fda_vocabulary.py` has zero top-level classes, so the AST sweep would never find it — it survives on one import at `mics_task.py:18` — and it is the hand-maintained twin of `api/detector_keys.py`, where drift is silent (preflight reports `ok` while the Pi writes view keys nobody reads). The Pi test suite has **never been run anywhere**; deleting any of it destroys unexecuted evidence. Reserve `hardware/openephys_client.py` and its two test modules as Phase 26 names, not strays. | 30 |
| HYG-14 | **Zero restorations, six holds, and the defects are not silently folded in.** Both restorations were deferred by the user on 2026-08-10, so the requirement is now wholly about *not* disturbing things. *Hold — user-deferred:* the silenced handshake-watchdog logging in `station.py` — the retry itself is already live and uncommented, so nothing is broken; Pi log files are 0 bytes by design, making `logger.*` the wrong channel; the block is a sweep carve-out — `station.py` still loses its other 52 dead commented lines. *Hold — user-deferred:* `pilot.py:1137-1148`, where the NTP-enable and clock-freeze call sites are commented out while the methods stay live at `:498`/`:514`. Confirmed 2026-08-10 as a **regression, not a decision**, and material on a rig that timestamps behavioural events to the millisecond — but the user has taken it on and will handle it separately, so the phase leaves the block byte-identical. The gate is **inverted** accordingly: the lines must still be present *and* still commented, which guards them against HYG-11's dead-comment sweep. *Remove as a set:* `open_file()` (55 lines), the `self.h5f` cleanup at `:640-641`, and the ~23 commented lines at `:1174-1261` — ES is the sole data path, confirmed 2026-08-10, so the half-disabled subsystem goes whole rather than leaving a live method nobody calls. *Holds, now resolved:* the touch-sensor constant `0x3f` (vs the live `0x08` — an ~8× lick-sensitivity difference), the IR1–IR7 trigger registrations and the `OG_TRIGGER` optogenetics pulse are **deleted** (each encoded hardware knowledge recorded nowhere else, so each removal carries a ledger verdict; `@auto_log` already reports `Digital_In` level changes as `Hardware_Event`, which is what makes `detectedIR` redundant rather than merely unused). The silenced handshake-watchdog warnings are **restored**. `IR1` and `OG_TRIGGER` survive as live `HARDWARE.GPIO` pin declarations in `prefs.json` by design — the gates assert the commented *call forms*, never bare tokens. *Out of scope, tracked separately:* `mics_task.py:1589` imports `autopilot.autopilot.core.pilot`, a path that does not resolve — so **`LOAD_HARDWARE_LIBS` silently fails whenever a task is running**, the exception dying unhandled in the `Net_Node` listen thread; `i2c.py:819`'s `except(e):` on an undefined name (and `i2c.py` is off-limits under TRIGA-12); `pilot.py:887`'s call to the undefined `get_hardware_class`. | 30 |

### Platform: Modern Pi (Bookworm 64-bit, Python 3.11, lgpio, unattended boot) — Phase 31

All 27 land in `~/mics_core` on branch `phase-31-modern-pi-platform`, never in `mics-backend`.
`PLAT-` follows the `HYG-` precedent: a phase-scoped prefix introduced because the existing set
(`BUG CANVAS CMP COND DB DVK EDIT EPHYS EXTLINK FDA HEALTH HOT HW HYG PROTO TRIG TRIGA UI VAR`)
contains no platform prefix.

| ID | Requirement | Phase |
|---|---|---|
| PLAT-01 | **The shed lands as one change.** HDF5/`TrialData`, port calibration (`dur_from_vol`, `calibrate_port`, `compute_calibration`), the setup-wizard GUI deps and the dead audio config are removed, and `tools/tree_integrity/final_checks.py`'s `PORT_CALIBRATION` assertion is updated in the same commit. Measured consequence: deleting `autopilot/setup/` removes the `npyscreen` import at `autopilot/__init__.py:4` that currently poisons **179 of 381** test results. | 31 |
| PLAT-02 | **The numpy-alias breakages are fixed together with the numpy bump, not before it:** `gpio.py:367,506,1039,1042,1077`, `pilot.py:794`, and the four non-pilot modules. `np.int`/`np.bool` were removed in numpy 1.24, so they break the moment the pin moves off `1.20.0`. Pin `numpy==1.26.4` — numpy ≥2.5 requires Python ≥3.12 and would not install at all. | 31 |
| PLAT-03 | **`requirements.txt` is rewritten to the AUDITED direct runtime set with real pins and no betas**, every pin proven to resolve to a prebuilt aarch64 cp311 wheel. The "7 direct dependencies" figure in CONTEXT §3 is a target, not a measurement: a direct import scan also finds `pytz` (`mics_task.py:1`, on the pilot path) and `tzlocal` (`utils/common.py:18`, undeclared today), plus `packaging`, `requests`, `validators`, `pygame`, `psychopy` in modules whose reachability must be decided by the closure tool. The delivered number is whatever the audit says. | 31 |
| PLAT-04 | **A single idempotent installer provisions a stock Raspberry Pi OS Lite 64-bit Bookworm card:** apt packages, I²C enable, venv, dependency install, group membership, unit installation, `pigpiod` purge. Re-running it is a no-op. Locked scope (CONTEXT §11.2): **it owns the box** — dedicated rig Pi assumed, no coexistence mode, no interactive prompts, no `--dry-run` requirement. Reversible via `uninstall.sh` and `<file>.mics.bak` backups. | 31 |
| PLAT-05 | **Firmware writes target `/boot/firmware/`, never `/boot/`,** and `mics.conf` lives at `/boot/firmware/mics.conf`. Current Bookworm replaced the `/boot/config.txt` symlink with a placeholder file saying "DO NOT EDIT THIS FILE"; writing it reports success and does nothing. This corrects the CONTEXT §7 design. | 31 |
| PLAT-06 | **The pilot runs from a venv with no `--system-site-packages` and with `PYTHONNOUSERSITE=1` in the unit.** Verified empirically: `--system-site-packages` also injects `~/.local/lib/pythonX.Y/site-packages` onto `sys.path`, letting a stray `pip install --user` shadow a pinned rig dependency. | 31 |
| PLAT-07 | **`mics-firstboot-once.service` runs exactly once** (hostname `mics-<serial8>` from the LOW bytes of the CPU serial, the `/etc/hosts` `127.0.1.1` rewrite, SSH host keys, machine-id, filesystem expand, `/boot/firmware/mics-device.txt`), guarded by an explicit stamp file, **not** by `ConditionFirstBoot` — which keys off `/etc/machine-id`, which this unit itself regenerates. | 31 |
| PLAT-08 | **`mics-prefs.service` runs on EVERY boot** and re-renders `pilot/prefs.json` from `prefs.template.json` + `/boot/firmware/mics.conf`, ordered `Before=mics-pilot.service`. `prefs.json` becomes gitignored because `prefs.set()` rewrites the whole file at runtime (`prefs.py:505-525`, called from `pilot.py:595` on every run start), so a tracked copy conflicts on every `git pull`. The split guards against a later `mics.conf` edit silently doing nothing. | 31 |
| PLAT-09 | **`mics-pilot.service` starts the pilot unattended with `Restart=always`, `RestartSec`, and `StartLimitIntervalSec=0`.** systemd's default 5-starts-in-10 s limit latches a crash-looping unit into `failed` permanently — fatal for an unattended 24/7 rig, and the exact failure this phase exists to eliminate. | 31 |
| PLAT-10 | **The pilot unit sets `WorkingDirectory=` and `Environment=LG_WD=` to a writable directory backed by `RuntimeDirectory=`.** Verified in lgpio source: `import lgpio` opens a FIFO at `<workdir>/.lgd-nfy<N>` via a RELATIVE path at module-import time; systemd's default CWD is `/`. Without this, **all** edge callbacks fail with no exception and no log line. | 31 |
| PLAT-11 | **The pilot user is a member of `gpio` and `i2c`; `DynamicUser=` is not used.** From `raspberrypi-sys-mods` `99-com.rules`: `SUBSYSTEM=="gpio", GROUP="gpio", MODE="0660"` and the same for `i2c-dev`. | 31 |
| PLAT-12 | **The gpiochip is resolved by label with a line-count assertion, with a `prefs.GPIOCHIP_LABEL` override, never by index.** On Pi 5 under Bookworm the header chip has been `gpiochip4`, then `gpiochip0` (kernel 6.6.45), then `gpiochip15` (kernel 6.12.96); the `60-gpiochip4.rules` udev workaround is already defunct. Allowlist `pinctrl-rp1`, `pinctrl-bcm2711` (Pi 4 — **not** `bcm2835`, which is Pi 2/3/Zero, correcting CONTEXT §5), `pinctrl-bcm2835`, plus a `pinctrl-*` fallback. | 31 |
| PLAT-13 | **The I²C surface moves to `lgpio.i2c_*`** (2× `i2c_open`, 14× `i2c_write_byte_data`, 7× `i2c_read_i2c_block_data`, 6× `i2c_read_byte_data`, all in `hardware/i2c.py`). Signatures map 1:1; only the block-read return shape changes (`(count, bytearray)`), so all 7 sites are verified individually. | 31 |
| PLAT-14 | **Edge detection moves to `gpio_claim_alert` + `callback`; `set_glitch_filter` → `gpio_set_debounce_micros`; `set_pad_strength`/`get_pad_strength` are deleted** (no lgpio equivalent — zero occurrences of "pad" in `lgpio.h`). `callback()` does not claim the line, so the claim must come first or the callback silently never fires. The debounce timestamp shift (events are stamped `debounce` µs late, deliberately) is documented and handled with one convention tree-wide. | 31 |
| PLAT-15 | **`Digital_Out.store_series()`/`series()` and the whole pigpio-script machinery** (`store_script`/`run_script`/`script_status`/`stop_script`/`delete_script`, the `PI_SCRIPT_INITING` polling loop) **are replaced by `tx_wave` for one-shots and `tx_pulse` for repeating trains. No Python `time.sleep` anywhere in the pulse path** — a sleep there makes valve-open duration jitter under load and reward volume variable, a data-quality defect no functional test would catch. **Locked (CONTEXT §11.1): the LED train must emit 8000 µs on / 8000 µs off bit-exactly** (the class says 20 Hz, the code says 60 Hz, the hardware has always emitted 62.5 Hz via `round(8.333)`), with a mandatory regression test, a class rename, and the frequency exposed in prefs. | 31 |
| PLAT-16 | **`pilot.py`'s pigpio lifecycle is deleted** (`:16 import pigpio`, `:205-206 self.init_pigpio()`, `:949 external.start_pigpiod()`, `:1069-1080 init_pigpio()`), `self.pi` becomes a gpiochip handle, and no module in the pilot import path imports pigpio. Outputs are driven to a documented safe level on SIGINT before the chip is closed. | 31 |
| PLAT-17 | **The pilot process runs at `SCHED_FIFO` at a modest priority so the lgpio tx and alert threads inherit it, and the choice is justified by MEASUREMENT.** lgpio has zero RT-scheduling calls; pigpio set `SCHED_FIFO` at max priority process-wide (`pigpio.c:8340-8343`); `lgPthTxStart()`/`lgPthAlertStart()` are called inside `lgGpiochipOpen()` (`lgGpio.c:772-774`) and POSIX threads inherit policy by default. Confirmed with `chrt -p` on every thread, not asserted. | 31 |
| PLAT-18 | **`Event_Dispatcher.dispatch_event()` accepts the kernel edge timestamp from the alert callback** and falls back to reading a clock only when no edge timestamp exists. Today it calls `self.pi.get_current_tick()` in the calling thread (`Event_Dispatcher.py:77`), i.e. it stamps when *Python* runs and discards the edge time the callback already carries. Fixing this is what makes the kernel-timestamp claim visible in ES. | 31 |
| PLAT-19 | **Every dispatched event carries both a raw `CLOCK_MONOTONIC` nanosecond field and a derived UTC field**, with the monotonic field designated as the interval-analysis source. Kernel gpio v2 events default to `CLOCK_MONOTONIC` and lgpio never sets `EVENT_CLOCK_REALTIME` (the flag is commented out at `lgGpio.c:243-246`), so the epoch offset is computed **per event** as two vDSO reads — the only form that stays correct across a wall-clock step. | 31 |
| PLAT-20 | **`chrony` replaces `systemd-timesyncd`, keeping Debian's stock `makestep 1 3`,** with lab NTP sources added via a drop-in in `/etc/chrony/conf.d/`. `systemd-timesyncd` steps whenever `\|offset\| ≥ 0.4 s` (`NTP_MAX_ADJUST`, `timesyncd-manager.c:52`) with no config option to prevent it. journald is set to `Storage=volatile` with a size cap (locked, CONTEXT §11.3) so 24/7 operation writes no logs to the SD card. | 31 |
| PLAT-21 | **`mics-pilot.service` is ordered `After=chrony-wait.service`** (not `Requires=`) so no session starts before the clock has converged, while a network-free rig still boots — just late. `chrony-wait.service` ships in Debian bookworm's chrony package, runs `chronyc waitsync 0 0.1 0.0 1`, `TimeoutStartSec=180`. | 31 |
| PLAT-22 | **The clock-freeze block at `pilot.py:1137-1148` is deleted and the `--final` F3 assertion that requires it to exist in COMMENTED form is retired in the same change.** The gate is *inverted* (HYG-14) and will fail loudly if the block is simply deleted. The deferral is **resolved, not dropped**: the block existed because a wall-clock jump corrupted the estimated tick→timestamp mapping, and PLAT-18/19 replaced that estimate with kernel timestamps. | 31 |
| PLAT-23 | **A GPIO loopback timing harness exists in-repo** and produces a machine-readable capture of commanded-vs-measured pulse widths and periods, with a pigpio backend and an lgpio backend writing one format. The capture side must run on **Python 3.7** so the pigpio baseline is obtainable on the pre-migration image before that card is reflashed. | 31 |
| PLAT-24 | **Acceptance is a PAIRED before/after capture on the same spare hardware,** under idle and loaded conditions, with lgpio required to be no worse than the pigpio baseline. G1 (primary, comparative): `err_p99` and `err_sd` ≤ 1.25× baseline for every profile × load, `n_missing == 0` everywhere. Absolute thresholds (secondary, so a jointly bad baseline cannot pass by comparison alone): `valve` `err_p99` ≤ 1.0 ms, `valve_short` ≤ 0.5 ms, `ttl` ≤ 200 µs, `train` `period_sd` ≤ 200 µs, `train` \|`rate_ppm`\| ≤ 100. Pass/fail is a process exit code. | 31 |
| PLAT-25 | **The 24/7 claim is validated by a FORCED CLOCK STEP (forwards and backwards) mid-capture, not by waiting 24 hours:** monotonic timestamps stay continuous and monotonic, intervals unaffected, derived UTC stepping by exactly the applied amount, and the pilot survives. Supported by a 75-minute pigpio capture demonstrating the 71.58-minute 32-bit tick wrap, an 8–12 h overnight soak under the real systemd unit, and a restart/reboot resilience test including 10 kills in 30 s. | 31 |
| PLAT-27 | **The single-clock invariant survives the port — one clock aligns every event in the system.** User-stated requirement (CONTEXT §13), verified 2026-08-16 against the live rig's own `site-packages/pigpio.py`. Three parts. (a) **`assign_cb` keeps its signature and its three-argument callback contract** — it is the seam `task.py:199` uses to wire every trigger, so an adapter *inside* `assign_cb` absorbs lgpio's 4-arg `(chip, gpio, level, timestamp)` callback down to `(gpio, level, ts)`. The conversion site has to move there: patched pigpio did it inside its notification thread (`pigpio.py:1239-1240`), and lgpio has no such thread. (b) **Both event paths read ONE epoch offset from one module** — `execute_trigger`'s `pi_timestamp` (`task.py:275-276`) and `Event_Dispatcher`'s `t_mono_ns`. No second offset, and no cached scalar copy: the cached copy *is* today's defect, where `_sync_offset` is passed by value into the callback thread (`pigpio.py:1157`) and the wrap re-sync is never propagated, so the two paths drift apart after the first 71.6-minute wrap — ~20 times a day under 24/7 — and revert to raw ticks on `OverflowError`, at which point `localize_tz`'s `strptime` receives an int and raises. (c) **`localize_tz` (`utils/common.py:329-334`) takes kernel monotonic nanoseconds** and rejects the old ISO-string form loudly rather than producing a plausible wrong answer. **Mandatory regression test:** one injected edge yields a `pi_timestamp` and a dispatcher timestamp that refer to the same instant. | 31 |
| PLAT-26 | **The dev host can run the tree's own test suite, and a baseline-relative delta gate exists.** The 23 test modules in `tests/` have never been executed anywhere; measured 2026-08-16 at **179 failed / 203 passed / 381 collected**, every failure a third-party import chain. Until this is fixed every downstream gate in the phase is meaningless. Delivered as `requirements-dev.txt`, a dev venv, `31-PYTEST-BASELINE.json`, a committed `tools/pytest_delta.py` (promoting Phase 30's inline heredoc), and a recording fake `lgpio` with a signature-contract test so port tests run with no hardware. | 31 |


---

**Coverage:**
- v1 requirements: 86 total (HW-01–24 added for Hardware Libs Centralization + Hardware Modules + Toolkit Redesign)
- Pi repo hygiene requirements: 14 (HYG-01–14, Phase 30) — credential remediation + fresh-history publication (01), live-session acceptance (02), the plugin cluster removed as one ordered change (03) with an empty HANDSHAKE proven benign (04), `cameras.py` removed in both its Pi and DB copies (05), registry sweep collateral (06), the Terminal-era tree (07), vendored/generated bulk (08), pytest config replacement (09), rig-specific config templated (10), 244 lines of dead commented code (11) with `Event_Dispatcher.py` excluded (12), a survival manifest for the files a naive sweep would destroy (13), and the restore/hold/defer split (14). **Departs from the plan of record deliberately** — no phase doc in 1–29 authorizes deleting a Pi file; justification and evidence in the ROADMAP entry and `30-CONTEXT.md`.
- Platform requirements: 27 (PLAT-01–27, Phase 31) — shed (01), Python 3.11 + audited deps (02–03), installer + boot-partition provisioning (04–05), venv hygiene (06), the firstboot/every-boot unit split (07–08), unattended 24/7 unit directives (09–11), the lgpio port (12–16), measured RT scheduling (17), dual-timebase kernel timestamps (18–19), chrony slew-not-step + volatile journald (20–21), the Phase 30 clock-freeze deferral resolved and its inverted guard retired (22), the acceptance instrument and gates (23–25), the dev-host test harness (26), and the single-clock invariant (27). **PLAT-26 added during planning**: the dev host could not run the tree's tests at all (179 failed / 203 passed, all import-chain), so Wave 0 needed a requirement of its own rather than being smuggled into PLAT-01. **PLAT-27 added during plan revision (2026-08-17)**: the `assign_cb` callback path and its `pi_timestamp` were owned by no plan, and porting them silently is how the phase would have shipped two clocks while claiming one — the plan-checker's B9. Owned by plan 12 (adapter, the one offset, `localize_tz`), closed by plan 14 (the cross-path same-instant assertion, which needs the ported dispatcher).
- Bug requirements: 7 (BUG-01–07)
- Compound condition requirements: 10 (COND-01–10)
- External integration requirements: 13 (EXTLINK-01–13)
- Compute primitives + variables requirements: 12 (CMP-01–06, CMP-10–15); expr escape hatch (CMP-07–09) deferred — see note above
- Trigger assignment requirements: 19 active (TRIGA-01–10, 11a, 12, 14–19). **Re-plan 2026-07-27:** TRIGA-11 dropped (vacuous on a sourceless toolkit) and replaced by TRIGA-11a; TRIGA-13 moved to Phase 25 (DVK-01/02/03 — the Pi structurally cannot derive those keys); TRIGA-16–19 added (method validation, constrained detector affordance, `{device_name}` token, level-source lock). Wave-3/4 mapping: TRIGA-12 + 17 + 18 + 19 → 24-06′; TRIGA-11a → 24-07′; TRIGA-14 + 15 + 16 → 24-08′.
- FDA builder canvas UX requirements: 14 (CANVAS-01–14, Phase 29) — pure UI/UX; edge readability (01–04), layout persistence in a dedicated `ui_layout` column kept out of the `file_hash`-bearing `fda_json` (05–08), restore-default action (09), autosave isolation (10), file-size discipline (11), proof (12). **CANVAS-13/14 added 2026-08-05** after walking real definitions: 186's unpaired `rand → trial_onset` back-edge crosses `play_led` and the pair-offset rule structurally cannot catch it (13); 172/161/155 carry 11+ states absent from every transition, so CANVAS-07's single trailing column degenerates into a tall stack (14)
- Detector-derived view key requirements: 11 (DVK-01–11, Phase 25; DVK-11 added 2026-07-29 — a task definition stores a detector reference + channel index, never the resolved per-pilot key; DVK-09 added 2026-07-27 from rig evidence — detector channels must be declared, not assumed 0-based, resolved 2026-07-29 to `first_channel` + count; DVK-10 added 2026-07-29 — `execute_trigger`'s over-broad `except KeyError` swallowed DVK-09's error as "no valid trigger")
- Mapped to phases: 168
- Unmapped: 0 ✓

---
*Requirements defined: 2026-03-15*
*Last updated: 2026-05-31 — External integration (MICS-Link) requirements added (EXTLINK-01–13) for Phase 18; EXTLINK-12 covers the signal/command type contract (build-time declaration + wire-time coercion + AST emission); EXTLINK-13 covers the per-source readiness gate with three guaranteed escape paths (timeout, manual skip, STOP)*
*Last updated: 2026-06-01 — Compute primitives + variables requirements added, then consolidated into single Phase 23 (3 plans): Pi runtime variables+compute (CMP-01–06), backend validation + compute-library storage (CMP-10–12), GUI state-builder + variable/transition wiring (CMP-13–15). The `expr` escape-hatch (CMP-07–09) was decoupled/deferred. Phases 19–22 left free for the MICS-Link SDK arc.*

*Last updated: 2026-08-17 — PLAT-01–27 added for Phase 31 (mics_core Modern Pi Platform). PLAT-01–25 adopted from `31-RESEARCH.md`; **PLAT-26 added by the planner** to cover the dev-host test harness, which nothing else covered and on which every other gate in the phase depends; **PLAT-27 added during plan revision** to cover the single-clock invariant (CONTEXT §13) — the `assign_cb` callback path and its `pi_timestamp` were owned by no plan.*
