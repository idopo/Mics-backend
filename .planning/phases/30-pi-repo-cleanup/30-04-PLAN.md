---
phase: 30-pi-repo-cleanup
plan: 04
type: execute
wave: 2
depends_on: ["30-01", "30-03"]
files_modified:
  - /home/ido/pi-mirror/pilot/plugins/
  - /home/ido/pi-mirror/autopilot/autopilot/tasks/learning_cage.py
  - /home/ido/pi-mirror/autopilot/autopilot/tasks/mics_cage_task.py
  - /home/ido/pi-mirror/autopilot/autopilot/hardware/unreal.py
  - /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-04-ledger.md
autonomous: true
requirements: [HYG-03, HYG-04]
must_haves:
  truths:
    - "A HANDSHAKE carrying `tasks: []` is proven against the live API to change no row and raise no 500"
    - "The 27 legacy plugins and the three modules whose only importers are those plugins are gone in one change"
    - "No base class was removed before its subclasses"
    - "`pilot/plugins/` still exists as a directory, so plugins.py does not log an exception at boot"
    - "Two of the three DELETE-resolved toggles are fully retired tree-wide; the third has exactly one surviving instance, in a file plan 05 removes"
  artifacts:
    - path: "/home/ido/pi-mirror/pilot/plugins/.gitkeep"
      provides: "Keeps PLUGINDIR present — plugins.py:46-48 logs an exception and returns {} on a missing dir"
    - path: "/home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-04-ledger.md"
      provides: "HYG-04 API evidence (request, response, before/after row counts) + HYG-03 removal ledger + the toggle hand-off to plan 05"
  key_links:
    - from: "autopilot/autopilot/utils/plugins.py"
      to: "PLUGINDIR (pilot/plugins/)"
      via: "per-file guarded sweep at plugins.py:64-66"
      pattern: "PLUGINDIR"
---

<objective>
Prove an empty HANDSHAKE is a backend no-op (HYG-04), then delete `pilot/plugins/` and its three
orphans in a single change (HYG-03).

Purpose: this is the phase's highest-risk removal and the one with a hard ordering constraint.
`api/main.py:1059-1070` resolves a task's `base_class` only within the same handshake payload
and raises 400 if it cannot; because the commit sits at `api/main.py:1117` inside
`orchestrator_station.py:85-183`'s single `try`, that 400 discards the tasks upsert, the toolkit
upsert, the hardware-config seed **and** the locked-states upsert together. Subclasses-then-base-
classes is safe; the reverse silently breaks every handshake. So the plugins and their base
classes go as ONE change.
Output: an empty (but present) `PLUGINDIR`, three removed orphan modules, and recorded API
evidence that the resulting empty handshake is benign.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-pi-repo-cleanup/30-CONTEXT.md
@.planning/phases/30-pi-repo-cleanup/30-VALIDATION.md
@.planning/phases/30-pi-repo-cleanup/30-01-SUMMARY.md
@.planning/phases/30-pi-repo-cleanup/30-03-SUMMARY.md
@api/main.py
@orchestrator/orchestrator/orchestrator_station.py

<pi_rules>
ABSOLUTE (STATE.md:1727-1745). Override anything below:
- Edit ONLY `/home/ido/pi-mirror`. **No git command in that directory — not even `status`.**
  Deletions are plain `rm -rf`, never `git rm`.
- Never `rsync --delete`. This plan does not rsync. **This plan does not deploy anything.**
- Never start/stop the pilot. Never run Python on the Pi.
- `autopilot` cannot be imported on this host; `compileall` is the verification ceiling.
</pi_rules>

<why_this_is_safe_now>
From 30-CONTEXT.md and the ROADMAP decision block — this is the phase's deliberate departure
from the plan of record, and the justification is load-bearing:

The corpus (phases 1–29) explicitly *retains* `pilot/plugins/*.py` because they feed
`available_locked_states`, and retains `learning_cage.detectedLick` as "the reference
implementation and a one-line rollback". **That posture was correct while source-authored
toolkits were still dispatched. It no longer holds:** all live work is backend-authored and
sourceless — user-confirmed 2026-08-10 and corroborated by the DB, where protocols 56/57/58 all
run `source_less_toolkit`, whose NULL `locked_state_source` dispatches to `mics_task`, not to any
plugin file. The 43 protocol steps naming `elastic_test` and the 15 naming `AppetitveTaskReal`
are legacy rows that are not run.

Consequence to accept, not fix: the backend **never prunes**. There is no delete path anywhere on
the handshake, so every existing toolkit row persists and keeps appearing in the UI with no
signal the Pi stopped reporting it. That staleness is accepted by HYG-04 and is out of scope.
</why_this_is_safe_now>

<verified_inventory>
Confirmed on `/home/ido/pi-mirror` 2026-08-10:
- `pilot/plugins/` holds **28 `.py` files**, one extensionless file named `test` (5.6 KB), and a
  `__pycache__/`. HYG-03's "27 task files" is the audit's count of *task* plugins; the directory
  contents are as listed. Remove all of it, keep the directory.
- The credential is at `pilot/plugins/AssociationLearning.py:1323-1324`. Plan 01 Task 3 already
  captured the literals to `/home/ido/.hyg01-probe.txt`. **Verify that file exists, is mode 600,
  has ≥2 non-empty lines and contains no blank line before deleting `AssociationLearning.py`** —
  after this task the strings are unrecoverable from the working tree.
- 15 `pilot/plugins/*.py` files import `autopilot.hardware.unreal`.
  **Correction, 2026-08-10:** an earlier draft said `tasks/mics_cage_task.py` also imports it. It
  does **not**. Verified with
  `grep -rln 'hardware.unreal\|hardware import unreal' /home/ido/pi-mirror --include=*.py`:
  **every** importer lives under `pilot/plugins/` or `terminal/plugins/` — nothing under
  `autopilot/autopilot/` imports `unreal` at all. This does not change what is deleted (the
  importers still go before the imported), only the stated reason. Re-run that grep before step 5
  and confirm it returns no path under `autopilot/autopilot/`.
  `hardware/mixer.py` mentions "unreal" only in two comments (lines 26, 42) — comments, not an
  import; leave `mixer.py` alone.
- `terminal/plugins/` also imported `unreal` — plan 03 removed that whole tree, which is why
  this plan depends on 30-03.
- 23 `pilot/plugins/*.py` files name `autopilot.core.subject` **in a class docstring** (Sphinx
  prose inherited from upstream). Plan 03 already deleted `core/subject.py`; the guard's 2(b)
  skips docstrings, so those 23 files were never a violation and need no edit before removal.
- The `HARDWARE.UNREAL` group still exists in `pilot/prefs.json`. It becomes a dead declaration
  the moment `unreal.py` goes. **Plan 07 (HYG-10) removes it** — do not touch `prefs.json` here,
  and expect the guard's `--final` F2 check to stay red until plan 07 lands.
</verified_inventory>

<CORRECTED_toggle_inventory>
## 30-CONTEXT.md's `OG_TRIGGER` claim is wrong twice. Use this table instead.

30-CONTEXT.md states: *"`OG_TRIGGER` remains declared in the live `HARDWARE` dict at
`mics_cage_task.py:97` and `learning_cage.py:82` — both files are deleted by HYG-03, so no orphan
declaration survives."* Verified against the tree, that misses two declarations. The coordinator
owns the 30-CONTEXT.md amendment; this plan is already written against the corrected reading.

**Complete inventory of the three DELETE-resolved toggles, with the plan that retires each site:**

| Token | Site | Kind | Removed by |
|---|---|---|---|
| `set_cdc_manual(0x3f)` | `pilot/plugins/elastic_test.py:138` (commented) | toggle | **04** (this plan) |
| | `pilot/plugins/AssociationCueReward.py:134` (commented) | toggle | **04** |
| | `pilot/plugins/Task1.py:123`, `Task2.py:123`, `ReleaseWater.py:108` (live) | live call | **04** |
| | `pilot/plugins/FindTouchThreshold.py:99` (`high_threshold = 0x3f`) | constant | **04** |
| | `tasks/learning_cage.py:193` (commented) | toggle | **04** |
| | `tasks/RecordingBox.py:104` (commented) | toggle | **05** ← **not this plan** |
| `self.triggers['IR1']` | `tasks/learning_cage.py:141` (commented) | toggle | **04** |
| | `tasks/mics_cage_task.py:204` (live) | live registration | **04** |
| | `pilot/plugins/{simple,WaterCalibration,FullTrainingProtocol,TrainingProtocol,blink_withUnreal}.py` | live/commented | **04** |
| `detectedIR` | `learning_cage.py`, `mics_cage_task.py`, 4 × `pilot/plugins/*.py` | callback | **04** |
| `pulse_and_notify(`…`OG_TRIGGER``)` | `tasks/mics_cage_task.py:438` (commented) | toggle | **04** |
| `OG_TRIGGER` **declaration** | `tasks/mics_cage_task.py:97`, `tasks/learning_cage.py:82` | HARDWARE dict | **04** |
| | `tasks/RecordingBox.py:53` | HARDWARE dict | **05** ← **missed by 30-CONTEXT.md** |
| | `pilot/prefs.json:241,243` | **live pin declaration** | **never — survives by design** |
| `IR1` **declaration** | `pilot/prefs.json:278,282` | **live pin declaration** | **never — survives by design** |
| | `pilot/prefs_wsl*.json` | dev-host prefs | **07** |

**Two consequences this plan must respect:**

1. **Do not assert tree-wide absence of `0x3f` or `OG_TRIGGER` here.** `tasks/RecordingBox.py`
   survives this wave and carries both (`:104` and `:53`). An earlier draft of this plan's verify
   did exactly that and would have gone red on a file it never touches. Assert instead that
   `set_cdc_manual(0x3f)` has **exactly one** surviving occurrence and that it is in
   `RecordingBox.py` — which is a stronger statement than "absent", because it also proves plan
   05 still has work to do.
2. **Never assert bare `IR1` or `OG_TRIGGER` anywhere in this phase.** `pilot/prefs.json` carries
   both as live `HARDWARE.GPIO` pin declarations that HYG-10 deliberately preserves. What HYG-14
   retires is the commented *toggle call form*, never the pin. Plan 01's F3 is written this way
   for the same reason; see its `<F3_must_assert_call_forms_not_tokens>` block.

`detectedIR` **is** safe to assert tree-wide as a bare token after this plan — every occurrence
lives in `learning_cage.py`, `mics_cage_task.py`, `pilot/plugins/` or `terminal/plugins/`, all
removed by plans 03 and 04.
</CORRECTED_toggle_inventory>

<four_held_toggles_resolved>
From 30-CONTEXT.md — all four were settled with the user on 2026-08-10. Three of them live in
files this plan (and plan 05) delete, so they need no separate edit; the fourth is plan 06's.

- Touch constant `0x3f` — **DELETE**, goes with the files. The live default is `0x08` at
  `i2c.py:817` (~8× lick-sensitivity difference). Preserving `0x3f` as a documented alternative
  was considered and rejected. Note the last site is in `RecordingBox.py`, i.e. **plan 05**.
- IR1–IR7 trigger registrations (`learning_cage.py:141-147`) — **DELETE**, goes with the file.
  User's rationale: a `Digital_In` level change is already reported automatically by
  `@auto_log` / `@log_action` (`utils/logging_utils.py`) as a `Hardware_Event`; the dedicated
  `detectedIR` callback is redundant, not merely unused.
- `OG_TRIGGER` opto pulse (`mics_cage_task.py:437-438`) — **DELETE**, goes with the file.
  Optogenetic stimulation runs through the BlueBerry/BLE path now, not this GPIO pulse. The
  **pin declaration in `pilot/prefs.json` is not part of this** and stays.
- Handshake watchdog (`station.py:1333-1346`) — **RESTORE, not delete.** `station.py` survives.
  That is **plan 06's** work. Do not touch `station.py` here.
</four_held_toggles_resolved>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove an empty HANDSHAKE is a backend no-op (HYG-04)</name>
  <files>
    /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-04-ledger.md
  </files>
  <action>
    **This is a gate. If it fails, do not run Task 2.** HYG-04 says the empty handshake is
    "verified against the live API, not assumed", and it must be verified *before* the sweep is
    trusted.

    Read `api/main.py` around lines 1050-1130 and
    `orchestrator/orchestrator/orchestrator_station.py:85-183` first, to confirm the exact
    endpoint path, payload shape and the `tasks_received` response field before constructing the
    request. Do not guess the route from this plan.

    1. **Snapshot before.** Against the live DB (`docker compose exec -T db psql -U mics_user
       -d mics_db -tAc "..."`), record row counts for `task_definitions`, `task_toolkits`,
       `available_locked_states`, `hardware_libs`, `hardware_modules`, and the per-pilot
       hardware-config rows. Also snapshot the toolkit rows for the pilot you will target.
    2. **Pick a target pilot** — use a non-production pilot id if one exists; otherwise use the
       pilot the orchestrator already knows. Read the id from
       `docker compose exec -T db psql -U mics_user -d mics_db -tAc "select id, name from pilots order by id;"`.
    3. **POST an empty tasks payload** to the handshake tasks endpoint with the
       `MICS_API_TOKEN` bearer token (`curl -s -o /tmp/hyg04.json -w '%{http_code}'`). The
       payload's `tasks` array is empty — mirror the exact schema the orchestrator sends.
    4. **Assert:** HTTP 200 (not 400, not 500); response body contains `tasks_received: 0`.
    5. **Snapshot after** with the identical queries. **Assert every count is unchanged**, and
       that the target pilot's toolkit rows are still present (this is the "backend never prunes"
       behaviour — confirming it, not fixing it).
    6. Record request, response body, and the before/after count table in
       `.planning/phases/30-pi-repo-cleanup/ledger/30-04-ledger.md` (create `ledger/` if absent),
       with a proposed verdict line `HYG-04 | PROVEN | <evidence>`.

    If the API returns 400 or 500, or any count changes: **stop, do not delete anything**, and
    report. A non-benign empty handshake invalidates HYG-03's premise.
  </action>
  <verify>
    <automated>test -f /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-04-ledger.md &amp;&amp; grep -q 'tasks_received' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-04-ledger.md &amp;&amp; grep -q 'HYG-04 | PROVEN' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-04-ledger.md &amp;&amp; docker compose -f /home/ido/mics-backend/docker-compose.yml exec -T api python -m pytest -q tests/</automated>
  </verify>
  <done>
    The ledger fragment records a real HTTP request and response showing status 200 and
    `tasks_received: 0`, and a before/after row-count table in which every count is identical.
    Backend pytest suite still green (352 pass).
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove pilot/plugins/ and its three orphans as one change (HYG-03)</name>
  <files>
    /home/ido/pi-mirror/pilot/plugins/,
    /home/ido/pi-mirror/autopilot/autopilot/tasks/learning_cage.py,
    /home/ido/pi-mirror/autopilot/autopilot/tasks/mics_cage_task.py,
    /home/ido/pi-mirror/autopilot/autopilot/hardware/unreal.py,
    /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-04-ledger.md
  </files>
  <action>
    **Single change, subclasses first.** Do the four deletions in this exact order, in one task,
    with no verification pause between them — the ordering constraint exists because a partial
    state (base classes gone, subclasses present) is the state that silently breaks every
    handshake.

    **Pre-flight, before any `rm`:**
    - Confirm `/home/ido/.hyg01-probe.txt` exists, is mode 600, has &ge;2 non-empty lines and
      contains **no blank line** (`! grep -qc '^$'`). If not, stop — plan 01 Task 3 did not run
      correctly and the HYG-01 proof becomes impossible or unsound.
    - Confirm Task 1's ledger fragment records `HYG-04 | PROVEN`.

    **Deletions, in order:**
    1. `rm -rf /home/ido/pi-mirror/pilot/plugins/` — all 28 `.py` files (the subclasses), the
       extensionless `test` file, and `__pycache__/`.
    2. Immediately recreate the directory and add `.gitkeep`:
       `mkdir -p /home/ido/pi-mirror/pilot/plugins &amp;&amp; touch /home/ido/pi-mirror/pilot/plugins/.gitkeep`.
       This is required: `utils/plugins.py:46-48` logs an exception and returns `{}` on a missing
       `PLUGINDIR` — survivable but noisy, and noise on a rig whose dominant failure mode is
       silence is exactly what you do not want.
    3. `rm /home/ido/pi-mirror/autopilot/autopilot/tasks/learning_cage.py`
    4. `rm /home/ido/pi-mirror/autopilot/autopilot/tasks/mics_cage_task.py`
    5. `rm /home/ido/pi-mirror/autopilot/autopilot/hardware/unreal.py`

    Plain `rm`. **No git.**

    **Do NOT touch in this task:** `pilot/prefs.json` (the dead `HARDWARE.UNREAL` block is plan
    07's; its `HARDWARE.GPIO.OG_TRIGGER` and `HARDWARE.GPIO.IR1` pin declarations are **live and
    survive the phase**), `networking/station.py` (the watchdog restore is plan 06's),
    `hardware/mixer.py` (its two "unreal" mentions are comments), `tasks/RecordingBox.py`
    (plan 05 — it still carries the last `set_cdc_manual(0x3f)` and a third `OG_TRIGGER`
    declaration), `tasks/mics_task.py`, `tasks/task.py`, `tasks/graduation.py`,
    `tasks/fda_vocabulary.py`, `pilot/protocols/`, `pilot/sounds/`.

    **Post-check, in this same task:**
    - `python3 tools/check_tree_integrity.py --strict` must exit 0. If it reports a surviving
      importer of `unreal`, `learning_cage` or `mics_cage_task`, you have found a reference the
      audit missed — **restore nothing, delete nothing more; stop and report.**
    - `python3 -m compileall -q autopilot/autopilot` must exit 0.
    - **Toggle greps — read `&lt;CORRECTED_toggle_inventory&gt;` before running these.** Assert, over
      surviving `.py` files:
      - `detectedIR` — **zero** occurrences tree-wide.
      - `self.triggers['IR1']` / `self.triggers["IR1"]` — **zero** occurrences tree-wide.
      - a `pulse_and_notify(` call naming `OG_TRIGGER` — **zero** occurrences tree-wide.
      - `set_cdc_manual(0x3f)` — **exactly one** occurrence, and it must be
        `autopilot/autopilot/tasks/RecordingBox.py`. This is the hand-off to plan 05; asserting
        "exactly one, there" is stronger than asserting absence, because it also proves plan 05
        still has the work.
      Exclude `tools/check_tree_integrity.py`, `tools/tree_integrity/` and
      `tests/test_tree_integrity.py` from every one of these scans — the guard contains all four
      literals *because* it searches for them, and a scanner that reads its own assertions as
      tree content can never go green.

      **Do NOT grep for bare `IR1`, bare `OG_TRIGGER` or bare `0x3f` tree-wide.** `pilot/prefs.json`
      carries `HARDWARE.GPIO.IR1` and `HARDWARE.GPIO.OG_TRIGGER` as live pin declarations that
      survive this phase by design, and `RecordingBox.py` still holds a declaration and a toggle
      until plan 05. A bare-token assertion here fails on files this plan never touches, and the
      only way to "fix" it would be to strip live GPIO config — see plan 01's
      `&lt;F3_must_assert_call_forms_not_tokens&gt;`.

    **Record.** Append to `ledger/30-04-ledger.md`: one row per removed path with four criterion
    verdicts, the `.gitkeep` recreation, the toggle-grep results (including the single surviving
    `RecordingBox.py` occurrence and its hand-off to plan 05), and a proposed verdict line
    `HYG-03 | PROVEN | &lt;evidence&gt;`. Note explicitly that HYG-14's three DELETE toggles are
    **partially** satisfied here — two fully, `0x3f` pending plan 05 — and that HYG-14's RESTORE
    half remains open for plan 06. Also record the corrected `OG_TRIGGER` inventory (the
    `RecordingBox.py:53` declaration and the two live `prefs.json` pin entries) as a finding, so
    plan 08 carries it into §6 and 30-CONTEXT.md can be amended.
  </action>
  <verify>
    <automated>cd /home/ido/pi-mirror &amp;&amp; test -d pilot/plugins &amp;&amp; test -f pilot/plugins/.gitkeep &amp;&amp; test -z "$(ls pilot/plugins/*.py 2&gt;/dev/null)" &amp;&amp; test ! -e autopilot/autopilot/tasks/learning_cage.py &amp;&amp; test ! -e autopilot/autopilot/tasks/mics_cage_task.py &amp;&amp; test ! -e autopilot/autopilot/hardware/unreal.py &amp;&amp; test -f autopilot/autopilot/tasks/mics_task.py &amp;&amp; python3 tools/check_tree_integrity.py --strict &amp;&amp; python3 -m compileall -q autopilot/autopilot tools tests &amp;&amp; python3 - &lt;&lt;'PY'
import pathlib, sys

# The guard necessarily contains these literals in order to search for them.
SELF = ('tools/check_tree_integrity.py', 'tests/test_tree_integrity.py', 'tools/tree_integrity')

def scan(pred):
    hits = []
    for f in pathlib.Path('.').rglob('*.py'):
        s = str(f)
        if '.git' in f.parts or '__pycache__' in f.parts:
            continue
        if any(k in s for k in SELF):
            continue
        for i, l in enumerate(f.read_text(errors='replace').splitlines(), 1):
            if pred(l):
                hits.append(s + ':' + str(i))
    return hits

bad = []
for name, pred in [
    ('detectedIR', lambda l: 'detectedIR' in l),
    ("self.triggers['IR1']", lambda l: "self.triggers['IR1']" in l or 'self.triggers["IR1"]' in l),
    ('pulse_and_notify(...OG_TRIGGER...)', lambda l: 'pulse_and_notify(' in l and 'OG_TRIGGER' in l),
]:
    h = scan(pred)
    if h:
        bad.append(name + ' still present: ' + str(h))

cdc = scan(lambda l: 'set_cdc_manual(0x3f)' in l)
if [x.rsplit(':', 1)[0] for x in cdc] != ['autopilot/autopilot/tasks/RecordingBox.py']:
    bad.append('set_cdc_manual(0x3f) expected exactly once in RecordingBox.py, got ' + str(cdc))

if bad:
    print('\n'.join(bad))
    sys.exit(1)
print('toggle state OK: 2 of 3 fully retired; 0x3f hand-off to plan 05 confirmed')
PY</automated>
  </verify>
  <done>
    `pilot/plugins/` exists, contains only `.gitkeep`, and holds no `.py` file.
    `tasks/learning_cage.py`, `tasks/mics_cage_task.py` and `hardware/unreal.py` are gone.
    `tasks/mics_task.py`, `tasks/task.py`, `tasks/graduation.py` and `tasks/fda_vocabulary.py`
    survive. `detectedIR`, `self.triggers['IR1']` and the `OG_TRIGGER` pulse call form are
    absent tree-wide; `set_cdc_manual(0x3f)` survives in exactly one place,
    `tasks/RecordingBox.py`, handed off to plan 05. Guard `--strict` and `compileall` both exit 0.
    `ledger/30-04-ledger.md` carries the HYG-03 and HYG-04 evidence plus the corrected
    `OG_TRIGGER` inventory.
  </done>
</task>

</tasks>

<verification>
- `python3 /home/ido/pi-mirror/tools/check_tree_integrity.py --strict` exits 0 — assertion 2
  proves no surviving file imports `unreal`, `learning_cage` or `mics_cage_task`.
- `python3 -m compileall -q /home/ido/pi-mirror/autopilot/autopilot` exits 0.
- `docker compose exec -T api python -m pytest -q tests/` still green (352 pass).
- **Pi suite: run the delta command from `30-01-SUMMARY.md`**, not a bare `pytest`. It exits 0
  when no node id fails that was not already failing in `30-PYTEST-BASELINE.json`. The recorded
  baseline is 179 failed / 181 passed over 19 collectable modules, so a bare
  `python3 -m pytest -q` exits 1 on a healthy tree and cannot be chained with `&amp;&amp;`.
- `grep -rn 'set_cdc_manual(0x3f)' /home/ido/pi-mirror --include=*.py
  --exclude=check_tree_integrity.py --exclude=test_tree_integrity.py` returns exactly one line,
  in `tasks/RecordingBox.py`. Anything else — in either direction — is a finding.
- No git command was run in `/home/ido/pi-mirror`. Nothing was deployed to the Pi.
</verification>

<success_criteria>
1. HYG-04 proven against the live API with a recorded request/response and an unchanged
   before/after row-count table.
2. All 28 plugin `.py` files, the extensionless `test` file, `tasks/learning_cage.py`,
   `tasks/mics_cage_task.py` and `hardware/unreal.py` removed in one change, subclasses first.
3. `pilot/plugins/.gitkeep` present so `PLUGINDIR` still resolves.
4. Guard `--strict` and `compileall` exit 0; backend suite green.
5. Toggle state, asserted by **call form** and never by bare token:
   `detectedIR`, `self.triggers['IR1']` and `pulse_and_notify(`…`OG_TRIGGER`…`)` are absent
   tree-wide; `set_cdc_manual(0x3f)` survives in exactly one file, `tasks/RecordingBox.py`,
   which plan 05 removes. `pilot/prefs.json`'s `HARDWARE.GPIO.IR1` and `HARDWARE.GPIO.OG_TRIGGER`
   pin declarations are untouched and stay that way.
6. The corrected `OG_TRIGGER` inventory is recorded as a finding for plan 08 §6.
</success_criteria>

<output>
After completion, create
`.planning/phases/30-pi-repo-cleanup/30-04-SUMMARY.md`.
</output>
