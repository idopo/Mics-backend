---
phase: 31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot
plan: 03
type: execute
wave: 3
depends_on: ["31-02"]
files_modified:
  - /home/ido/mics_core/autopilot/autopilot/hardware/gpio.py
  - /home/ido/mics_core/autopilot/autopilot/core/pilot.py
  - /home/ido/mics_core/autopilot/autopilot/networking/station.py
  - /home/ido/mics_core/autopilot/autopilot/networking/node.py
  - /home/ido/mics_core/autopilot/autopilot/stim/managers.py
  - /home/ido/mics_core/autopilot/autopilot/transform/geometry.py
  - /home/ido/mics_core/autopilot/autopilot/transform/transforms.py
  - /home/ido/mics_core/autopilot/autopilot/stim/sound/base.py
  - /home/ido/mics_core/requirements.txt
  - /home/ido/mics_core/tests/test_python311_compat.py
  - /home/ido/mics_core/tests/test_requirements_wheels.py
autonomous: true
requirements: [PLAT-02, PLAT-03]
must_haves:
  truths:
    - "No numpy alias removed in 1.24 survives anywhere in the tree, so bumping numpy cannot silently break the rig"
    - "requirements.txt names the ACTUAL runtime import closure, not an aspirational subset"
    - "Every pin in requirements.txt resolves to a prebuilt aarch64 cp311 wheel, so a fresh Pi install needs no compiler"
  artifacts:
    - path: "/home/ido/mics_core/requirements.txt"
      provides: "The rig runtime dependency set, pinned, beta-free"
      min_lines: 10
    - path: "/home/ido/mics_core/tests/test_python311_compat.py"
      provides: "Static gate on removed numpy aliases and removed threading APIs"
      min_lines: 25
    - path: "/home/ido/mics_core/tests/test_requirements_wheels.py"
      provides: "PyPI wheel-tag check per pin; skips cleanly when offline"
      min_lines: 30
  key_links:
    - from: "requirements.txt"
      to: "the tree's third-party import set"
      via: "an audited import scan, not the CONTEXT.md estimate"
      pattern: "numpy"
---

<objective>
Stage 1, part 2. Make the source Python-3.11-safe and rebuild the dependency file from measured
evidence.

Purpose: the numpy alias breakages and the numpy bump are **coupled** — `np.int`/`np.bool` were
removed in numpy 1.24, so they break the moment numpy moves off the current `1.20.0` pin. Fixing
them separately from the pin is how you ship a rig that imports fine and then dies on first pulse.

**This plan must also correct an optimistic number.** CONTEXT §3 and `31-RESEARCH.md` both state a
"7 direct dependencies" floor. A direct import scan of the post-shed tree on 2026-08-16 found the
tree also imports `pytz` (including `tasks/mics_task.py:1`, squarely on the pilot path), `tzlocal`
(`utils/common.py:18`), `packaging` (`utils/requires.py:15`), `requests` (`utils/wiki.py`,
`utils/plugins.py`), `validators` (`utils/types.py:5`), `pygame` (`hardware/mixer.py:2`) and
`psychopy` (`stim/visual/`). The 7-dep figure is a target, not a measurement. Reaching it requires
deciding each of those on evidence — which this plan does — or the figure goes in the summary as
"7 + N, here is N and why".

Output: an importable-on-3.11 tree, a real `requirements.txt`, and two permanent static gates.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/CONTEXT.md
@.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/31-RESEARCH.md
@.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/31-02-SUMMARY.md

<repo_boundary>
ALL code changes land in `/home/ido/mics_core` on branch **`phase-31-modern-pi-platform`**.
</repo_boundary>

<pi_rules>
ABSOLUTE:
- **NEVER run git on the Pi. NEVER start/stop the pilot. NEVER run any Python file on the Pi.**
- `Message` and `hardware_state` classes are **off limits** to this sweep, including the
  `setDaemon` pass. If a `setDaemon` call lives inside either, leave it and note it.
- RTK-proxied grep can render a matching line blank. Re-verify with a direct `grep -rn`.
</pi_rules>

<integrity_gate>
`cd /home/ido/mics_core && python3 tools/check_tree_integrity.py --strict` must exit 0 after each
task. **NEVER `--rebaseline`.** `requirements.txt` is not in the protected manifest.
</integrity_gate>

<verified_facts>
Line references verified 2026-08-16; re-confirm with `grep -n` before editing (plan 02's deletions
shift them).

**numpy aliases removed in 1.24 — the complete site list, re-verified 2026-08-17 by reading every
`.py` under `autopilot/` and `pilot/` and matching `np.(float|int|bool|object|str)\b`. Fourteen
matches, thirteen real:**

| File | Lines | Alias |
|---|---|---|
| `hardware/gpio.py` | 367, 506, 1039, 1042, 1077 | `.astype(np.int)` -> `.astype(int)` |
| `core/pilot.py` | 794 | `dtype=np.bool` -> `dtype=bool` |
| `stim/sound/base.py` | 74, 77, 341, 344 | `.astype(np.int)` -> `.astype(int)` |
| `stim/managers.py` | 433 | `.astype(np.float)` -> `.astype(float)` |
| `transform/geometry.py` | 740, 741 | `.astype(np.float)` -> `.astype(float)` |
| `transform/transforms.py` | 153 | **a COMMENT** — `# if self.format_out['type'] in (int, np.int, )`. Delete the line or leave it; either is fine, but the scanner must not fail the build over a comment forever. Decide once and say which. |

Note `gpio.py:1039`, `:1042` and `:1077` are inside the `PWM` class, which plan 13's
`<pwm_and_led_rgb_disposition>` deletes. Fix them here anyway — this plan is wave 3 and plan 13 is
wave 11, and a deleted line is trivially fixed.

**Do NOT "fix" these — they are valid numpy and always have been:**
- `utils/common.py:285-286` — `np.int_`, `np.intc`, `np.intp`, `np.int8/16/32/64`,
  `np.uint8/16/32/64` inside a JSON encoder's `isinstance` tuple. These are **sized dtype scalar
  types**, not the removed bare aliases, and they are exactly what a numpy-aware JSON encoder is
  supposed to check. `:289-290`'s `np.float_`, `np.float16/32/64` are the same story. Touching them
  breaks ES serialisation of every numpy value the tree emits. This is the reason the scanner's
  word-boundary rule and its `np.int64` positive control exist.

*(An earlier review claimed `stim/managers.py` and `transform/geometry.py` had zero alias sites and
should be dropped from this list. That claim is wrong — verified by direct file read, they have one
and two respectively. The list above stands.)*

**Cosmetic but do it now** (deprecated 3.10, removed 3.13): `setDaemon()` in
`networking/station.py` and `networking/node.py` -> `.daemon = True`.

**Confirmed CLEAN across the whole tree** — do not spend time re-checking: no `getargspec`, no
`imp`, no `distutils`, no `asyncio.coroutine`, no `isAlive`, no `time.clock`, no `binhex`. Every
`from collections import` is `OrderedDict`/`deque`/`namedtuple`, so there is **zero** ABC breakage,
which is the usual killer in code this old.

**Current `requirements.txt` problems, all verified:**
- `pyzmq==23.0.0b2` is a **beta** and must not be in a rig requirements file.
- `msgpack==1.0.5` was pinned only because ">=1.1 requires Python >=3.9"; that constraint
  evaporates on 3.11.
- `numpy==1.20.0`, `tornado==6.1` predate the target.
- Two `adafruit-circuitpython-*` lines are **deliberately unpinned** with a comment saying to
  resolve them during the first fresh install. Honour that: leave them unpinned here and pin them
  in plan 09 from the actual install, or pin them now and re-verify in plan 09. State which you did.
- `tzlocal` is imported by `utils/common.py:18` and is **not in requirements.txt at all** — a
  genuine undeclared runtime dependency, not an oversight of this plan.

**Version guidance from research (verified against the PyPI JSON API):**
| Package | Pin | Why |
|---|---|---|
| `numpy` | `==1.26.4` | Last 1.x line, cp311; keeps the migration surface to exactly the 1.24 alias removals. numpy 2.5 **requires Python >=3.12** and cannot install; 2.3.x is the last cp311-capable 2.x. |
| `pyzmq` | `>=26,<28` | cp311 `manylinux_2_28_aarch64` wheel exists. |
| `tornado` | `>=6.4` | ships `cp39-abi3-manylinux2014_aarch64` — one wheel covers 3.9+. |
| `msgpack` | `>=1.0.8` | cp311 aarch64 wheel exists. |
| `lgpio` | `==0.2.2.0` | PyPI ships a `cp311 manylinux_2_34_aarch64` wheel; Bookworm's glibc is 2.36, so no SWIG and no compiler. apt `python3-lgpio 0.2.2-1~rpt1` is the documented fallback. |
| `Adafruit-PureIO` | (transitive) | **sdist only, no wheels.** Pure Python so it installs without a compiler, but it is the one dep that builds from source — verify on the first real install. |
| `Adafruit-Blinka` | `9.2.0` | No longer hard-depends on `RPi.GPIO`, so it will not drag in a Pi-5-incompatible package. Depends on `sysv_ipc>=1.1.0`, which does have a cp311 aarch64 wheel. |

**Research validity note:** `31-RESEARCH.md` marks itself valid until 2026-09-15 and calls out
PyPI wheel availability as one of two things that move faster than that. Re-check the pins against
PyPI at execution time rather than trusting the table.
</verified_facts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove every Python-3.11/numpy-1.24 breakage, guarded by a static test</name>
  <files>
    /home/ido/mics_core/tests/test_python311_compat.py
    /home/ido/mics_core/autopilot/autopilot/hardware/gpio.py
    /home/ido/mics_core/autopilot/autopilot/core/pilot.py
    /home/ido/mics_core/autopilot/autopilot/networking/station.py
    /home/ido/mics_core/autopilot/autopilot/networking/node.py
    /home/ido/mics_core/autopilot/autopilot/stim/managers.py
    /home/ido/mics_core/autopilot/autopilot/transform/geometry.py
    /home/ido/mics_core/autopilot/autopilot/transform/transforms.py
    /home/ido/mics_core/autopilot/autopilot/stim/sound/base.py
  </files>
  <behavior>
    - `tests/test_python311_compat.py` scans every `.py` under `autopilot/` and `pilot/` (excluding
      the tree-integrity guard's own `SELF_PATHS`) and FAILS on any of:
      `np.int\b`, `np.bool\b`, `np.float\b`, `np.object\b`, `np.str\b`, `.setDaemon(`,
      `getargspec`, `\bimport imp\b`, `distutils`, `asyncio.coroutine`, `isAlive(`, `time.clock(`.
    - The scan reports FILE:LINE for every hit, so a failure is actionable without re-grepping.
    - Word boundaries matter: `np.int8`, `np.int32`, `np.intp`, `np.int_`, `np.float64`,
      `np.float_` are legitimate and must NOT be flagged. Include a positive test asserting
      `np.int64` passes the scanner, **and one asserting the real `utils/common.py:285-290` block
      passes** — it uses nine of them in one `isinstance` tuple and is the most likely false
      positive in the tree.
  </behavior>
  <action>
    1. Write the test first and watch it fail with the current tree, listing the known sites.
    2. Fix each site. `.astype(np.int)` -> `.astype(int)`; `dtype=np.bool` -> `dtype=bool`.
       Do not "improve" surrounding code.
    3. `setDaemon(True)` -> `.daemon = True` in `networking/station.py` and `networking/node.py`.
       If a `setDaemon` call is inside `Message` or `hardware_state`, LEAVE IT, exclude that class
       in the scanner with a named exemption, and say so in the summary. The off-limits rule wins.
    4. For `stim/managers.py:433`, `transform/geometry.py:740-741`, `stim/sound/base.py:74,77,341,344`:
       fix them in place here. Deciding whether they survive at all is Task 2's call, and a deletion
       in Task 2 simply makes this fix moot — that ordering is deliberate, so this task never blocks
       on a scoping decision.
    5. `transform/transforms.py:153` is a **comment**, not code. Either delete the line or add it as
       a named comment-only exemption in the scanner; do not leave the scanner permanently red over
       a commented-out expression. Say which you chose.
    6. **Leave `utils/common.py:285-290` alone.** `np.int_`/`np.intc`/`np.intp`/`np.int8..64` and
       `np.float_`/`np.float16..64` are valid sized dtype scalar types inside a JSON encoder's
       `isinstance` tuple, not removed aliases. The word-boundary rule already spares them; if the
       scanner flags them, the scanner is wrong, not the file.
  </action>
  <verify>
    <automated>cd /home/ido/mics_core && /home/ido/.venvs/mics_core_dev/bin/python -m pytest -q tests/test_python311_compat.py --tb=short && /home/ido/.venvs/mics_core_dev/bin/python tools/pytest_delta.py && python3 tools/check_tree_integrity.py --strict</automated>
  </verify>
  <done>`test_python311_compat.py` passes with zero hits; `np.int64`-style names are demonstrably not flagged; delta gate green; `--strict` exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audit the real import closure and rewrite requirements.txt from it</name>
  <files>
    /home/ido/mics_core/requirements.txt
    /home/ido/mics_core/tests/test_requirements_wheels.py
  </files>
  <behavior>
    - `tests/test_requirements_wheels.py` parses `requirements.txt`, and for each pinned
      distribution queries the PyPI JSON API for the resolved version's files, asserting at least
      one wheel whose tag matches `cp311` (or `py3`/`abi3`) AND an `aarch64`/`manylinux` platform.
    - When the network is unavailable the test **skips** with a clear reason; it never fails for
      being offline and never passes silently.
    - A distribution that is sdist-only (`Adafruit-PureIO`) is asserted against an explicit
      allow-list with a comment naming why it is tolerated, not skipped by accident.
    - A second test asserts `requirements.txt` contains no pre-release specifier
      (`re.search(r'==\d+\.\d+\.\d+(a|b|rc)\d', ...)`), which is what catches `pyzmq==23.0.0b2`.
  </behavior>
  <action>
    1. **Audit before writing.** Produce the actual third-party import set of the post-shed tree:
       ```
       cd /home/ido/mics_core && grep -rhoE "^[[:space:]]*(import|from) [a-zA-Z_][a-zA-Z0-9_]*" \
         --include=*.py autopilot/autopilot pilot | awk '{print $2}' | sort -u
       ```
       then subtract the standard library. Cross-check against the tree-integrity closure:
       `python3 tools/check_tree_integrity.py --strict` prints the closure member count, and
       `tools/tree_integrity/resolver.py` is the module that computes it — use it, do not eyeball.

    2. For each third-party name NOT in the 7-dep target set (`numpy`, `pyzmq`, `tornado`,
       `msgpack`, `adafruit-circuitpython-mpr121`, `adafruit-circuitpython-motorkit`, `lgpio`),
       decide on evidence and record the decision in the summary table:
       - **On the pilot import path** -> it is a real dependency; add it with a pin. Known members:
         `pytz` (`tasks/mics_task.py:1`), `tzlocal` (`utils/common.py:18`).
       - **Reachable only from a module nothing imports** -> delete that module, in this plan,
         and note it. Candidates measured today: `hardware/mixer.py` (`pygame`),
         `stim/visual/xclient.py` + `stim/visual/visuals.py` (`psychopy`), `utils/wiki.py` and
         `utils/plugins.py` (`requests`), `utils/types.py` (`validators`),
         `utils/requires.py` (`packaging`).
         **Prove unreachability with the closure tool before deleting anything.** If a module IS in
         the closure, it stays and its dependency is declared. Do not delete to hit a number.
       - `inputs`, `python-osc`, `scikit-video` were flagged "audit for need" in CONTEXT §3 —
         resolve them the same way.

    3. Rewrite `requirements.txt`:
       - Header comment: this is the RIG RUNTIME set for Python 3.11 on Raspberry Pi OS Lite 64-bit
         (Bookworm). Dev-host packages live in `requirements-dev.txt` and are not installed by
         `deploy/install.sh`.
       - Pins per the table in `<verified_facts>`, re-checked against PyPI at execution time.
       - **No betas.** Delete the `pyzmq==23.0.0b2` line.
       - Keep the existing comment explaining the deliberately-unpinned adafruit lines, updated to
         say the resolution now happens on the Bookworm install in plan 09.
       - Delete the Python-3.7-era rationale comments that no longer hold (the msgpack note).

    4. If the final direct count is not 7, say the real number in the summary with the reason for
       each extra. Do not adjust the tree to make a document's estimate true.
  </action>
  <verify>
    <automated>cd /home/ido/mics_core && /home/ido/.venvs/mics_core_dev/bin/python -m pytest -q tests/test_requirements_wheels.py --tb=short && /home/ido/.venvs/mics_core_dev/bin/python tools/pytest_delta.py && python3 tools/check_tree_integrity.py --strict && ! grep -qE "==[0-9]+\.[0-9]+\.[0-9]+(a|b|rc)[0-9]" requirements.txt && echo "no prereleases"</automated>
  </verify>
  <done>`requirements.txt` contains only pins that resolve to aarch64 cp311/abi3 wheels (or the documented sdist exception); no pre-release specifiers remain; `tzlocal` and `pytz` are declared if the audit shows them on the pilot path; the delta gate and `--strict` are green.</done>
</task>

</tasks>

<verification>
1. `python3 tools/check_tree_integrity.py --strict` -> exit 0
2. `/home/ido/.venvs/mics_core_dev/bin/python -m pytest -q tests/test_python311_compat.py tests/test_requirements_wheels.py` -> pass
3. `grep -rnE "np\.(int|bool|float|object|str)\b" --include=*.py /home/ido/mics_core/autopilot /home/ido/mics_core/pilot` -> no hits
4. `grep -rn "setDaemon" --include=*.py /home/ido/mics_core/autopilot` -> no hits (or only a named, documented exemption inside an off-limits class)
5. `grep -c . /home/ido/mics_core/requirements.txt` -> materially smaller than the 37-line original
</verification>

<success_criteria>
- The tree imports and runs under Python 3.11 semantics with a numpy 1.26 pin.
- `requirements.txt` is derived from a measured import closure, not from an estimate.
- Two permanent static gates prevent regression of both problems.
- The summary states the TRUE direct-dependency count, whatever it is.
</success_criteria>

<output>
After completion, create `31-03-SUMMARY.md`. It must contain: the final direct-dependency table
with a one-line justification per entry; the list of modules deleted during the audit with the
closure evidence for each; and an explicit correction paragraph if the "7 direct dependencies"
figure in CONTEXT §3 / RESEARCH turned out to be wrong.
</output>
