---
phase: 35-deeplabcut-keypoint-likelihood-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-LIVENESS-FINDINGS.md
  - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/extlink_demo_v3.py
  - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/liveness_hook_snippet.py
autonomous: false
requirements: [DLC-06]

must_haves:
  truths:
    - "The reason hardware lib 177 v2 had to hardcode liveness_hook is written down with file:line evidence from both Pi trees, not left as folklore."
    - "A hardware lib exists whose liveness_hook is NOT a hardcoded True and which still reports alive while a sender is streaming."
    - "With the sender stopped, <source_id>.alive goes false within a bounded number of stale windows, observed in Elasticsearch by the user."
    - "With the sender running but a signal not updating, <source_id>.alive stays true while that signal goes stale — the two are demonstrably independent."
    - "Every rig action in this plan was performed by the USER; the agent ran no git, no Python and no process control on the Pi."
  artifacts:
    - path: ".planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-LIVENESS-FINDINGS.md"
      provides: "The clock-domain analysis, the hypotheses ranked, the rig evidence, and the verdict on DLC-06"
      min_lines: 80
    - path: ".planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/liveness_hook_snippet.py"
      provides: "The canonical liveness_hook source the generator will emit in plan 35-03"
    - path: ".planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/extlink_demo_v3.py"
      provides: "Lib 177 v3 source — v2 with the DIAGNOSTIC OVERRIDE removed and the canonical hook in its place"
  key_links:
    - from: "fixtures/extlink_demo_v3.py"
      to: "hardware_lib_versions.source_code for lib 177"
      via: "PUT /api/hardware-libs/177 creating version 3"
      pattern: "hardware-libs/177"
    - from: "task definition 434"
      to: "lib 177 version 3"
      via: "PUT /api/task-definitions/434/hw-lib-versions/177"
      pattern: "hw-lib-versions"
---

<objective>
Clear the Phase 34 debt that blocks DLC-06. Hardware lib 177 version 2 hardcodes
`def liveness_hook(...): return True` as a "DIAGNOSTIC OVERRIDE", so `demo.alive` would read true
with the sender switched off. SDK-05 is therefore unproven on hardware and DLC-06 ("liveness and
tracking-loss are distinct and both gateable") cannot be satisfied on top of it.

Purpose: DLC-06. Also: the override's own docstring says the default path *"reported not-alive while
`on_recv` was demonstrably stamping `_last_msg_ts_ms`, stranding the readiness gate in 2 of 4 rig
runs"*. **This is an investigation, not a checkbox (D-24).** Removing the override may re-expose that
bug. This plan budgets for that and names the leading hypothesis up front so the executor debugs
from evidence rather than from scratch.

Output: `35-LIVENESS-FINDINGS.md` with a verdict, a lib 177 version 3 whose liveness is real, and
the canonical `liveness_hook` source that plan 35-03's generator will emit into every DLC lib.

**HARD CONSTRAINT — read before doing anything.** The agent does NOT run git on the Pi, does NOT
start or stop the pilot process, and does NOT run Python on the Pi. Every rig action below is
USER-RUN: the agent supplies the exact command, the user runs it and reports back. Nothing under
`/home/ido/mics_core/` or `/home/ido/pi-mirror/` is edited — the fix must live entirely in the
hardware lib's own source, which is exactly where a lib-level `liveness_hook` override belongs.
</objective>

<leading_hypothesis>
## H1 — the two sides of the liveness subtraction are on DIFFERENT clocks (mics_core only)

Verified by reading both trees, 2026-08-31. The executor MUST re-confirm these five readings before
acting on them; they are the whole basis of the fix.

1. `mics_core/autopilot/autopilot/hardware/external_hardware_binding.py` `_now_ms()` returns
   `int(time.time() * 1000)`, and its comment says it is *"deliberately NOT converted through the
   one clock"* (Phase 31 Plan 10's F7 exempts the site by name).
2. `_liveness_tick(owner, pred)` in the same file calls `pred(owner._last_msg_ts_ms, now_ms, owner.stale_ms)`
   with that `time.time()`-derived `now_ms`.
3. `external_hardware_wire.default_liveness(last_msg_ts_ms, now_ms, stale_ms)` returns
   `(now_ms - last_msg_ts_ms) < stale_ms` — a bare subtraction of the two.
4. `mics_core/.../external_hardware_ingress.py` `on_recv` sets `owner._last_msg_ts_ms = ts_ms`
   where `ts_ms` comes from `_now_ms_and_mono_ns()`: `clock.now_from_tick()` (the pigpio-tick
   fitted mapping) converted by `MicsClock.to_utc_ns`. That is the ONE CLOCK, not `time.time()`.
5. `pi-mirror/.../external_hardware_ingress.py` stamps `int(time.time() * 1000)` instead.

So on the `mics_core` stack, `now_ms` (system wall clock) is subtracted from `last_msg_ts_ms`
(pigpio-tick-derived), and the difference includes whatever offset the fitted tick mapping carries
at that moment. If that offset exceeds `stale_ms` (3000 ms for the demo fixture) the device reads
dead forever while frames are arriving; if it is negative and large the device reads alive forever.
Intermittency across runs ("2 of 4") is exactly what a mapping re-fit at pilot start produces. On
`pi-mirror` both sides are `time.time()` and the bug cannot occur — which matches the fact that the
symptom appeared as the rig moved to `mics_core`.

**The permitted fix is lib-level, never substrate-level.** A `liveness_hook` override that reads
the CURRENT time from the same source the ingress stamp came from restores a like-for-like
comparison without touching a single Pi file. `make_liveness` (external_hardware_wire.py:170-173)
REPLACES the default with the override, so the hook owns the decision completely.

## Competing hypotheses to rule out before concluding H1

- **H2 — the egress probe, not liveness.** `recompute_alive` is `liveness_alive and not owner._egress_failed`.
  `ExtlinkDemo`'s egress probe targets `132.77.73.125:5597`; with that TCP echo listener down,
  three failed probes at 1/s trip `egress_fail_threshold: 3` and `alive` flips false about three
  seconds into every run regardless of liveness. This would look identical from Elasticsearch.
  **Confirm 5597 is open before drawing any conclusion from any run in this plan.**
- **H3 — poller cadence.** `bind_liveness` sets `interval_s = (stale_ms / 2) / 1000`, so with
  `stale_ms: 3000` the predicate is evaluated every 1.5 s and a false reading can persist up to
  that long after the fact. This affects the timing of an observation, not its truth value.
- **H4 — a genuine `ClockNotReady`/`ClockFault` fallback.** `_now_ms_and_mono_ns` falls back to
  `int(time.time() * 1000)` and counts the fallback. If the pilot is in permanent fallback, both
  sides are on `time.time()` again and H1 predicts NO failure — which would itself be a finding.
</leading_hypothesis>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-CONTEXT.md
@.planning/phases/34-mics-link-sdk-client-package/34-HARDWARE-VALIDATION.md
@.planning/phases/34-mics-link-sdk-client-package/34-09-PLAN.md
@sdk/examples/rig_checkpoint_sender.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the clock-domain analysis and author the canonical liveness hook</name>
  <files>.planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-LIVENESS-FINDINGS.md, .planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/liveness_hook_snippet.py, .planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/extlink_demo_v3.py</files>
  <read_first>
    - /home/ido/mics_core/autopilot/autopilot/hardware/external_hardware_binding.py (all of `_now_ms`, `bind_liveness`, `_liveness_tick`, `sweep_stale`, `_on_egress_alive_change`, `recompute_alive`)
    - /home/ido/mics_core/autopilot/autopilot/hardware/external_hardware_wire.py lines 141-175 (`resolve_stale_value`, `default_liveness`, `make_liveness`)
    - /home/ido/mics_core/autopilot/autopilot/hardware/external_hardware_ingress.py (`_now_ms_and_mono_ns`, `now_ms`, `on_recv`)
    - /home/ido/pi-mirror/autopilot/autopilot/hardware/external_hardware_ingress.py (the `time.time()` stamp — the contrast that localises the defect to the mics_core stack)
    - /home/ido/mics_core/autopilot/autopilot/utils/clock.py (`MicsClock.now_from_tick`, `to_utc_ns`, `_epoch_offset_ns`, `ClockNotReady`, `ClockFault`, `get_clock`)
    - /home/ido/mics_core/autopilot/autopilot/hardware/external_hardware.py lines 60-80 (the class-level-declaration requirement for `liveness_hook`)
    - .planning/phases/34-mics-link-sdk-client-package/34-HARDWARE-VALIDATION.md section 3d (observation B, verbatim, including the override's docstring)
    - The CURRENT source of lib 177 version 2, fetched with: curl -s -H "Authorization: Bearer $MICS_API_TOKEN" http://localhost:8000/api/hardware-libs/177
  </read_first>
  <action>
Re-confirm every one of H1's five readings against the files above and record each as a
`path:line` citation. If any reading does not hold, say so and re-rank the hypotheses; do not
proceed to a fix built on a claim you could not reproduce.

Write `.planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/liveness_hook_snippet.py`
containing ONLY the canonical hook, as source text intended to be pasted into a generated or
hand-written `ExternalHardware` subclass. Its shape and its constraints:
- Declared at CLASS level, never assigned in `__init__` — `validate_role_liveness` reads
  `getattr(type(self), "liveness_hook", None) is not None`, so an instance-level assignment is
  invisible to it and construction fails closed but confusingly.
- Signature exactly `def liveness_hook(self, last_msg_ts_ms, now_ms, stale_ms):`.
- Body: return False when `last_msg_ts_ms` is None (never arrived is dead). Otherwise obtain the
  current time from the SAME source the ingress stamp came from, by importing
  `now_ms as ingress_now_ms` from `autopilot.hardware.external_hardware_ingress` INSIDE the
  function body (a module-level import would run at lib-exec time on a backend that has no
  `autopilot`, and the AST extractor parses but the upload path also needs the module to import
  cleanly on the Pi only). Wrap that import and call in a `try`/`except Exception` that falls back
  to the passed-in `now_ms`, so the hook degrades to the substrate default rather than raising into
  the LivenessPoller. Return `(current - last_msg_ts_ms) < stale_ms`.
- Docstring stating, in two sentences: why the passed-in `now_ms` is not used (it is
  `time.time()`-derived while `last_msg_ts_ms` is one-clock-derived on the mics_core stack, so the
  subtraction crosses clock domains), and that on the pi-mirror stack this hook is numerically
  identical to `default_liveness` because both sides are `time.time()` there.
- A comment naming this as a WORKAROUND for a substrate defect that this phase is forbidden to fix
  (DLC-01), and pointing at the finding recorded in `35-LIVENESS-FINDINGS.md` for the eventual
  Phase 18/31 amendment.
- One extra diagnostic line, gated so it fires at most once per process: on the first call, log via
  `self.logger.warning` the pair `(last_msg_ts_ms, now_ms, ingress_now_ms())` so the rig run
  produces the direct measurement of the offset in the pilot journal. Use `self.logger` and never
  `print`.

Write `fixtures/extlink_demo_v3.py`: lib 177 version 2's source with the `DIAGNOSTIC OVERRIDE (v2)`
`liveness_hook` deleted and the canonical hook put in its place, and with a version comment at the
top recording what changed and why. Change NOTHING else — same class name `ExtlinkDemo`, same
signals, same decorators, same egress behaviour. A second difference in this file makes the rig
observation uninterpretable.

Create `35-LIVENESS-FINDINGS.md` with sections: (1) the Phase 34 observation B verdict verbatim and
why it blocks DLC-06; (2) H1 with its five citations; (3) H2/H3/H4 with what would distinguish
each; (4) the fix and why it is lib-level rather than substrate-level; (5) an EMPTY results table
with one row per rig observation (`alive true while streaming`, `alive false after sender stop`,
`alive true while a signal goes stale`, `readiness gate not stranded`), each `pending`; (6) a
section titled "Substrate defect recorded for a later phase" stating the cross-clock subtraction as
a finding with its citations, explicitly NOT fixed here, and naming Phase 18/31 as the owner.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('.planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/extlink_demo_v3.py').read()); ast.parse(open('.planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/liveness_hook_snippet.py').read()); print('both parse')" && python3 -c "
import sys; sys.path.insert(0,'api')
from extlink_ast import extract_extlink_metadata
m = extract_extlink_metadata(open('.planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/extlink_demo_v3.py').read())
assert 'ExtlinkDemo' in m, m.keys()
print('ast ok', sorted(m['ExtlinkDemo']['signals']))
" && grep -v '^ *#' .planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/extlink_demo_v3.py | grep -c "return True" | grep -qx 0 && echo "no hardcoded liveness"</automated>
  </verify>
  <acceptance_criteria>
    - Both fixture files parse with `ast.parse`.
    - `api.extlink_ast.extract_extlink_metadata` on `extlink_demo_v3.py` returns a dict containing the key `ExtlinkDemo` whose `signals` set is identical to the set returned for lib 177 version 2's source (compare explicitly; a changed signal set invalidates the whole observation).
    - `grep -v '^ *#' fixtures/extlink_demo_v3.py | grep -c "return True"` is 0 — the unconditional override is gone.
    - `fixtures/extlink_demo_v3.py` contains the string `def liveness_hook(self, last_msg_ts_ms, now_ms, stale_ms):` at class indentation level (four spaces).
    - `35-LIVENESS-FINDINGS.md` contains all five H1 citations in `path:line` form, the strings `H2`, `H3`, `H4`, a results table with the literal word `pending` in at least four rows, and a section heading containing `Substrate defect recorded for a later phase`.
    - `git status --porcelain` shows no modification under `/home/ido/mics_core/` or `/home/ido/pi-mirror/`.
  </acceptance_criteria>
  <done>The clock-domain analysis is confirmed and cited, a lib-level hook that restores a like-for-like comparison exists as reviewable source, and a v3 of lib 177 differing from v2 in exactly one method is ready to upload.</done>
</task>

<task type="auto">
  <name>Task 2: Publish lib 177 version 3 and repin task definition 434</name>
  <files>.planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-LIVENESS-FINDINGS.md</files>
  <read_first>
    - api/routers/hardware_libs.py lines 409-462 (`PUT /hardware-libs/{lib_id}` — creates the next version, sets `active_version_id`, 422s on `unvalidated`, and returns an `impact` block listing removed methods and affected task definitions)
    - api/routers/hardware_libs.py lines 827-870 (`PUT /task-definitions/{id}/hw-lib-versions/{lib_id}` and its `VersionSelectBody`)
    - .planning/phases/34-mics-link-sdk-client-package/34-HARDWARE-VALIDATION.md section 3c ("Change that produced this" — the exact rollback value of task def 434's `hw_lib_versions`, which must be re-read live rather than copied from that document)
    - docker-compose.yml (the api service, port 8000, and `MICS_API_TOKEN` from `orchestrator/orchestrator/prefs.json`)
  </read_first>
  <action>
Confirm the local stack is the backend the rig actually uses before changing anything: the
`ExtlinkDemo` pilot config row's `host` is `132.77.73.125` (the dev host) and the orchestrator's
`prefs.json` names `MICS_API_URL` as `http://127.0.0.1:8000`. State this confirmation in the
findings document. If the reading contradicts it, STOP and ask the user which backend serves
pilot 3 rather than writing to the wrong database.

Capture the rollback state FIRST and record it verbatim in `35-LIVENESS-FINDINGS.md`: the full
current `hw_lib_versions` JSON of task definition 434 (via
`GET /api/task-definitions/434/hw-lib-versions`), lib 177's current `active_version_id`, and the
version 2 `source_code` saved to `fixtures/extlink_demo_v2_rollback.py`.

Then `PUT /api/hardware-libs/177` with `source_code` set to `fixtures/extlink_demo_v3.py`'s content
and `declared_imports` unchanged from version 2's. Record the returned `impact` block verbatim —
if `removed_methods` is non-empty for `ExtlinkDemo` the v3 source diverged from v2 by more than the
hook and must be corrected before going further.

Then pin task definition 434 to the new version with
`PUT /api/task-definitions/434/hw-lib-versions/177` and body `{"version_id": <new id>}`. Do NOT
touch `hardware_libs.stable_version_id` and do NOT change any other lib's pin — in particular leave
lib 8's pin at version id 174, which run 583 established as the working GPIO version for this task
definition.

Record in the findings document: the new version id, the new version number, its `state`, the
`impact` block, and the exact rollback commands (the reverse `PUT` restoring version 2's source and
the pin restoration) so the user can revert without reading this plan.
  </action>
  <verify>
    <automated>cd /home/ido/mics-backend && TOKEN=$(python3 -c "import json;print(json.load(open('orchestrator/orchestrator/prefs.json'))['MICS_API_TOKEN'])") && curl -sf -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/hardware-libs/177 | python3 -c "
import json,sys
lib=json.load(sys.stdin)
src=lib.get('source_code') or lib.get('active_source_code') or ''
body=[l for l in src.splitlines() if not l.strip().startswith('#')]
assert 'def liveness_hook' in src, 'no liveness_hook in active version'
assert not any(l.strip()=='return True' for l in body), 'hardcoded liveness override still active'
print('lib 177 active version has a real liveness_hook')
" && curl -sf -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/task-definitions/434/hw-lib-versions | python3 -c "
import json,sys
rows=json.load(sys.stdin)
r=[x for x in rows if x['hardware_lib_id']==177][0]
assert r['selected_version_id']==r['active_version_id'], r
print('434 pinned to lib 177 active version', r['selected_version_id'])
"</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/hardware-libs/177` returns an active version whose source contains `def liveness_hook` and contains no non-comment line equal to `return True`.
    - `GET /api/task-definitions/434/hw-lib-versions` shows the row for `hardware_lib_id` 177 with `selected_version_id` equal to the newly created version id.
    - The same response shows lib 8 still pinned to version id 174.
    - `35-LIVENESS-FINDINGS.md` contains the pre-change `hw_lib_versions` JSON of task def 434 verbatim and a numbered rollback procedure.
    - `fixtures/extlink_demo_v2_rollback.py` exists and its content is byte-identical to what `GET /api/hardware-libs/177/versions` reports for version 2.
  </acceptance_criteria>
  <done>Lib 177 version 3 is live with a real liveness hook, task def 434 points at it, nothing else about the standing fixture changed, and a one-command rollback is written down.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT (USER-RUN) — prove liveness is real, and that it is independent of staleness</name>
  <read_first>
    - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-LIVENESS-FINDINGS.md (the hypotheses and the empty results table this checkpoint fills in)
    - sdk/examples/rig_checkpoint_sender.py (the script the user runs; `--mode quiet` is the mode this checkpoint needs)
    - .planning/phases/34-mics-link-sdk-client-package/34-HARDWARE-VALIDATION.md section 3b caveat 2 (pilot 3's `pilot_hardware_config` row 33 is `required: false`, so the correct order on THIS pilot is RUN FIRST, then sender — the opposite of pilot 1)
  </read_first>
  <what-built>
    Lib 177 version 3, with the hardcoded `liveness_hook -> True` removed and replaced by a hook that
    compares the ingress stamp against a time read from the same clock the ingress stamp came from.
    Task definition 434 is pinned to it. Nothing else about the standing fixture changed. The
    analysis behind the change, its citations and its rollback are in `35-LIVENESS-FINDINGS.md`.
  </what-built>
  <how-to-verify>
**Precondition, and it will silently invalidate everything if skipped.** The TCP echo listener on
`132.77.73.125:5597` must be UP. `ExtlinkDemo`'s egress probe targets it; three failures at 1/s trip
`egress_fail_threshold: 3` and `demo.alive` flips FALSE about three seconds into every run
regardless of liveness — which is hypothesis H2 and would look identical to a liveness failure.

    timeout 3 bash -c 'echo probe > /dev/tcp/132.77.73.125/5597' && echo "5597 OPEN"

If it is closed, start a plain TCP echo listener on port 5597 on the dev host. **Do NOT restart,
kill or reconfigure the orchestrator** — the orchestrator is a different process on port 5560 and
touching it is a damaging misstep, not a remedy.

Then, on pilot 3 / RecordingBox (`132.77.73.213`), in this order:

**Step A — alive while streaming.** Start a run on pilot 3 with task definition 434 FIRST (row 33
is `required: false`, so there is no readiness gate holding the run for the sender). Then, from the
Windows vision box or the dev host:

    python3 sdk/examples/rig_checkpoint_sender.py --mode transition --host 132.77.73.213 --port 5599 --source-id demo

Let it run about 20 seconds, then stop the sender with Ctrl+C but leave the run going.

**Step B — alive goes false after the sender stops.** With the run still going and the sender now
stopped, wait 60 seconds, then stop the run from the UI at
`/react/pilots/RecordingBox/sessions-ui`.

**Step C — alive stays true while a signal goes stale.** Start a NEW run, then:

    python3 sdk/examples/rig_checkpoint_sender.py --mode quiet --seconds 40 --host 132.77.73.213 --port 5599 --source-id demo

`--mode quiet` sends exactly one signal and then nothing at all for 40 seconds while the SDK's
heartbeat keeps running. `stale_after_ms` for `left_paw_x` is 200 ms and the module's `stale_ms` is
3000 ms, so the signal must go stale within a second while `demo.alive` must stay true for the whole
40 seconds. Let the sender exit on its own, then stop the run.

**What to report back, per step:**
1. Was 5597 OPEN before you started? (If you had to start the listener, say so.)
2. The run id and session id for each of the three runs.
3. From Elasticsearch (`132.77.73.217`, index `event_log_v2`, isolate with `subject: bp_s<session>_r<run>`):
   every `demo.alive` document with its timestamp and value, for each run. `alive` is logged on
   CHANGE only, so "two documents at t=0.06 and nothing after" means "stayed at that value" — report
   the documents you actually see, not an interpretation.
4. For step B: the wall-clock gap between the last `left_paw_x` document and the first
   `demo.alive = false` document.
5. For step C: whether any `demo.alive = false` document appears at all during the 40 quiet seconds,
   and whether `left_paw_x` documents show the value reverting to `0.0` (read `value_raw`, NOT
   `event_data.value` — the latter is an int-cast and shows 0 for 0.7).
6. From the pilot journal on `.213`: the single `liveness_hook` diagnostic warning line emitted on
   the first call, verbatim. This is the direct measurement of the clock offset and is the single
   most valuable line in the whole checkpoint.
7. Did the run start normally, or did the readiness gate stall or time out? (This is the "2 of 4 rig
   runs" symptom the override was masking.)
8. Anything else that looked wrong, including tracebacks in the journal.

If the readiness gate stalls or `alive` never goes true, that is a RESULT, not a failure to hide:
report it and stop. The rollback commands are in `35-LIVENESS-FINDINGS.md`.
  </how-to-verify>
  <files>.planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-LIVENESS-FINDINGS.md</files>
  <action>
Present the checkpoint below to the user, wait for their report, then transcribe what they reported
into `35-LIVENESS-FINDINGS.md`: fill section 5's results table, replacing every `pending` with exactly
one of `PASS`, `FAIL` or `NOT EXERCISED`, and record each row's run id and session id alongside the
Elasticsearch documents the user actually saw. Paste the `liveness_hook` diagnostic journal line
verbatim under section 6 and state what offset it measured. Do not interpret silence as a value, do
not fill a row from a different run, and do not smooth over a FAIL - if a row fails, add a short
paragraph naming which hypothesis (H1, H2, H3 or H4) the evidence supports and what the next
investigation step is. Run no command on the Pi yourself.
  </action>
  <verify>
    <automated>python3 - <<'EOF'
import re
t = open(".planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-LIVENESS-FINDINGS.md").read()
assert "pending" not in t.lower(), "results table still has pending rows"
n = len(re.findall(r"PASS|FAIL|NOT EXERCISED", t))
assert n >= 4, ("expected a verdict for each of the four observations, found", n)
assert not re.search(r"\blatenc", t.lower()), "no latency may be claimed"
print("liveness findings complete:", n, "verdicts")
EOF</automated>
  </verify>
  <done>Every liveness observation has a PASS, FAIL or NOT EXERCISED verdict backed by a run id and the Elasticsearch documents the user reported, the journal diagnostic is recorded verbatim, and any failure names the hypothesis it supports.</done>
  <acceptance_criteria>
    - The user reports whether `132.77.73.125:5597` was open BEFORE any run; no observation is accepted without it.
    - Step A: the user reports at least one `demo.alive = true` document in Elasticsearch for the streaming run.
    - Step B: the user reports a `demo.alive = false` document appearing after the sender stopped, with the measured gap; or reports that none appeared, which is recorded as a FAILURE of DLC-06 with the override's bug re-exposed.
    - Step C: the user reports whether any `demo.alive = false` appeared during the 40 quiet seconds and whether `left_paw_x` `value_raw` reverted to `0.0` — proving liveness and staleness are independent, or proving they are not.
    - The `liveness_hook` first-call diagnostic line from the pilot journal is reported verbatim.
    - Every reported observation is written into `35-LIVENESS-FINDINGS.md`'s results table with its run id, and each row's status becomes exactly one of `PASS`, `FAIL` or `NOT EXERCISED` — never left as `pending` and never inferred from a different run.
    - If any row is `FAIL`, the findings document states which hypothesis the evidence supports and what the next investigation step is; the plan is NOT reported complete on a green-only subset.
  </acceptance_criteria>
  <resume-signal>Report the eight numbered items above. Type "approved" only after all three steps have been run and reported, or state which step could not be run and why.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev host -> live backend database | This plan writes a new `hardware_lib_versions` row and repins a live task definition used by real runs. |
| `hardware_lib_versions.source_code` -> Pi `exec` | The v3 source is executed on the Pi. It is hand-authored here, not generated, but it is still code crossing into the pilot. |
| vision box / dev host -> Pi ROUTER on 5599 | Unauthenticated stream into a device that actuates hardware. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05 | Tampering | lib 177 v3 source diverging from v2 by more than the hook | mitigate | Task 1 asserts the extracted signal set is identical to v2's; Task 2 asserts the API's `impact.removed_methods` is empty for `ExtlinkDemo`; v2's source is saved to `fixtures/extlink_demo_v2_rollback.py` before the write. |
| T-35-06 | Denial of Service | the pilot's readiness gate and STOP channel | mitigate | The hook's clock read is wrapped in `try`/`except` falling back to the passed-in `now_ms`, so it can never raise into `LivenessPoller`; the poller already runs on its own daemon thread, never the shared IOLoop. |
| T-35-07 | Repudiation | an unreproducible rig result | mitigate | Every observation is recorded with its run id and session id; `alive` is change-logged, so the plan requires reporting the documents seen rather than an interpretation of silence. |
| T-35-08 | Spoofing | `source_id` on the ROUTER is an identity, not an authentication | accept | Lab-internal network; the substrate's `identity_ok` check drops mismatched identities silently and this phase adds no new ingress. Recorded rather than mitigated; authenticating the extlink channel is a Phase 18 amendment, not this plan's work. |
| T-35-SC | Tampering | package-manager installs | mitigate | This plan performs no install of any kind. The blocking legitimacy checkpoint lives in plan 35-07. |
</threat_model>

<verification>
- `35-LIVENESS-FINDINGS.md` has no row left as `pending`.
- Lib 177's active version contains a real `liveness_hook` and no unconditional `return True`.
- Task def 434 is pinned to that version and lib 8 remains pinned to version id 174.
- No file under `/home/ido/mics_core/` or `/home/ido/pi-mirror/` was modified.
- No git, Python or process-control command was run on any Pi by the agent.
</verification>

<success_criteria>
- DLC-06's liveness half is either PROVEN on hardware with evidence, or recorded as FAILED with the hypothesis the evidence supports and the next step named.
- A canonical `liveness_hook` source exists for plan 35-03's generator to emit.
- The cross-clock substrate defect is written down as a finding owned by a later phase, not silently fixed here.
</success_criteria>

<output>
Create `.planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-02-SUMMARY.md` when done.
</output>
