---
phase: 35-deeplabcut-keypoint-likelihood-integration
plan: 04
type: execute
wave: 3
depends_on: ["35-01", "35-03"]
files_modified:
  - dlc_link/src/dlc_link/signal_map.py
  - dlc_link/src/dlc_link/processor.py
  - dlc_link/src/dlc_link/live.py
  - dlc_link/tests/test_signal_map.py
  - dlc_link/tests/test_processor.py
  - dlc_link/tests/test_live.py
autonomous: true
requirements: [DLC-03, DLC-04, DLC-07, DLC-12, DLC-13]

must_haves:
  truths:
    - "A pose array from a DeepLabCut model becomes declared signals on the wire without the researcher restructuring their pipeline."
    - "The adapter imports the generated signal map and never re-derives a name from a bodypart string."
    - "A three-dimensional pose array is refused loudly with a message naming single_animal=True, never silently mis-indexed."
    - "A pose whose row count does not match the emitted POSE_ORDER is refused, so an unverified row order cannot silently send the wrong keypoint's numbers under the right name."
    - "Coordinates leave the adapter normalised 0..1, so an authored condition is a property of the arena and not of the current resize setting."
    - "Every value crosses the wire via as_scalar, never float()."
    - "A video file is played at its native frame rate, so stale_after_ms is exercised on the same timebase a live camera will produce."
    - "One command prints the model runner's actual pose shape and bodypart ordering without connecting to the Pi, so D-42's open question is settled by measurement before any signal map is trusted."
    - "The client is created outside the callback and outlives it; the Processor never opens or closes one."
    - "No frame, image or video ever leaves the adapter toward MICS."
  artifacts:
    - path: "dlc_link/src/dlc_link/signal_map.py"
      provides: "Loading a generated <source_id>_signals.py by path and asserting it pairs with the deployed lib"
      exports: ["load_signal_map", "assert_pairs_with", "SignalMapError"]
    - path: "dlc_link/src/dlc_link/processor.py"
      provides: "DLCProcessor - a dlclive-compatible Processor that sends declared signals"
      exports: ["DLCProcessor", "DLCLIVE_AVAILABLE", "PoseShapeError"]
      min_lines: 90
    - path: "dlc_link/src/dlc_link/live.py"
      provides: "The paced video-file loop and the dlc-link-live CLI"
      exports: ["run_video_loop", "main", "compare_pose_order"]
      min_lines: 80
  key_links:
    - from: "dlc_link/src/dlc_link/processor.py"
      to: "mics_link.values.as_scalar"
      via: "import from the SDK public module, applied to every pose value"
      pattern: "as_scalar"
    - from: "dlc_link/src/dlc_link/live.py"
      to: "mics_link.timing.Pacer"
      via: "pacer.wait_until(index / fps) in the frame loop"
      pattern: "wait_until"
    - from: "dlc_link/src/dlc_link/processor.py"
      to: "the generated signal map module"
      via: "load_signal_map, never a local re-derivation of bodypart names"
      pattern: "SIGNALS"
---

<objective>
Build the sender-side adapter (DLC-07): a `dlclive`-compatible `Processor` that pushes declared
signals into a `mics_link` client, and a standalone paced video-file loop that IS the live pipeline
with a file substituted for a camera.

Purpose: this is D-29's workflow A, the path proven FIRST at the rig. It is deliberately structured
so that swapping the prerecorded video for a live camera is dropping one line and nothing else.

Output: `dlc_link.signal_map`, `dlc_link.processor` and `dlc_link.live`, all unit-tested on a
machine with no dlclive, no cv2, no torch and no GPU.
</objective>

<three_rules_copied_verbatim>
From `sdk/examples/callback_sender.py` lines 1-20 - the load-bearing rules this plan applies:

1. The client is created OUTSIDE the callback and OUTLIVES it. The `with connect(...)` block owns
   the lifetime; the callback only borrows `link`. Here that means constructor injection: the caller
   builds `DLCProcessor(link, ...)` inside their own `with connect(...)` block and hands it to
   `DLCLive(...)`.
2. The callback returns whatever its host expects. `dlclive` assigns `self.pose` from
   `Processor.process`'s return value, so `process()` MUST return the pose.
3. Per-callback state (deadbands, last values) lives on the callback object, not in the SDK.

And: never open a client INSIDE a callback, and never rely on a library's teardown hook to close it.
`Processor.save()` is called by the DLC-Live GUI, not by `DLCLive`, so a client created in a
Processor's constructor may never be closed (D-32).
</three_rules_copied_verbatim>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-CONTEXT.md
@.planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-PATTERNS.md
@sdk/examples/callback_sender.py
@sdk/examples/rig_checkpoint_sender.py
@sdk/src/mics_link/replay.py
@sdk/src/mics_link/values.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load the generated signal map and prove it pairs with the deployed lib</name>
  <files>dlc_link/src/dlc_link/signal_map.py, dlc_link/tests/test_signal_map.py</files>
  <read_first>
    - dlc_link/src/dlc_link/templates.py (render_signal_map - the exact constant names this loader consumes: SOURCE_ID, CLASS_NAME, BODYPARTS, SIGNALS, SIGNAL_NAMES, STALE_AFTER_MS, ENGINE, SOURCE_FORMAT, CONFIG_PATH, CONFIG_SHA256, GENERATED_AT, GENERATOR_VERSION, LIB_SHA256)
    - dlc_link/src/dlc_link/names.py (the transform this module must NOT duplicate)
    - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-CONTEXT.md decision D-12
  </read_first>
  <action>
Write `dlc_link/src/dlc_link/signal_map.py`.

`SignalMapError(Exception)` for every refusal, always naming the file path.

`load_signal_map(path)`: import the generated map module by filesystem path using
`importlib.util.spec_from_file_location`, `module_from_spec` and `exec_module`, and return a plain
record exposing the constants. Validate after loading and raise `SignalMapError` when: any required
constant is missing; `SIGNALS` is not a dict of the expected shape; `SIGNAL_NAMES` does not equal
the flattened names derivable from `SIGNALS`; any `index` is not an int; or two bodyparts share an
`index`. The error message must state that the file is generated by `dlc-link-generate` and must not
be hand-edited.

`assert_pairs_with(smap, lib_source_text)`: compare `smap.LIB_SHA256` against the sha256 of the
given lib source and raise `SignalMapError` on mismatch, with a message stating that the map and the
deployed lib came from different generator invocations and that the consequence is silent total
failure - every frame whose name is not declared on the Pi is dropped as `unknown_name` and is
invisible from the sender. Plan 35-06's runbook uses this; the CLI in Task 3 exposes it behind a
`--verify-lib PATH` flag.

Do NOT re-derive any name here. This module reads what the generator emitted; the transform lives in
`dlc_link.names` and is called only by the generator.

Write `dlc_link/tests/test_signal_map.py`: generate a map with `dlc_link.generate.generate`, write it
under `tmp_path`, load it, and assert every constant round-trips; assert a hand-corrupted
`SIGNAL_NAMES` raises; assert a duplicated `index` raises; assert `assert_pairs_with` passes for the
paired lib source and raises for a one-character-different one.
  </action>
  <verify>
    <automated>cd /home/ido/mics-backend/dlc_link && python3 -m pytest -q tests/test_signal_map.py</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest -q tests/test_signal_map.py` exits 0 with at least 6 tests collected.
    - `load_signal_map` on a freshly generated map returns a record whose `SIGNAL_NAMES` equals the generator's expected set.
    - Corrupting `SIGNAL_NAMES` in the generated file, or duplicating an `index`, each raises `SignalMapError`.
    - `assert_pairs_with(smap, lib_source)` passes for the paired source and raises `SignalMapError` when one character of the source is changed.
    - `grep -c "to_identifier\|build_name_map" src/dlc_link/signal_map.py` returns 0 - the loader never re-derives a name.
  </acceptance_criteria>
  <done>The adapter has exactly one way to obtain signal names - reading what the generator emitted - plus a mechanical check that the map it read and the lib actually deployed came from the same invocation.</done>
</task>

<task type="auto">
  <name>Task 2: DLCProcessor, the dlclive-compatible callback</name>
  <files>dlc_link/src/dlc_link/processor.py, dlc_link/tests/test_processor.py</files>
  <read_first>
    - sdk/examples/callback_sender.py (the whole 38-line file: the three rules and the as_scalar usage)
    - sdk/src/mics_link/values.py lines 1-14 and 82-87 (as_scalar and the exact-type-check rationale: numpy.float64 passes an isinstance(value, float) check but cannot be packed by msgpack, turning a call-site error into an invisible IO-thread failure)
    - sdk/src/mics_link/client.py lines 118-160 (send_signal returns True on accept and False on drop; stats; commands_dropped)
    - dlc_link/src/dlc_link/decimate.py and dlc_link/src/dlc_link/signal_map.py
    - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-CONTEXT.md decisions D-07, D-08, D-17, D-18, D-20, D-32, D-33, D-34, plus D-37 (identity:false makes per-individual signals technically unavailable live) and D-42 (the pose array's row order and membership are an OPEN empirical question settled by plan 35-07's probe)
    - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-DLC-LIVE-NOTES.md section "The sender (vision box - Phase 34 SDK)" - copy the SHAPE of that worked MicsProcessor (deadband dict, per-part loop, likelihood-then-coordinate send order) and NOT its literal model_type="base", which D-01/D-02 supersede with "pytorch"
  </read_first>
  <action>
Write `dlc_link/src/dlc_link/processor.py`.

Base-class resolution at module import: attempt `from dlclive import Processor as _Base`; on
`ImportError` set `_Base = object` and `DLCLIVE_AVAILABLE = False`. This is what lets the whole
adapter be unit-tested on a machine with no dlclive and no GPU, and the module docstring must state
it as a deliberate testability seam rather than leaving it looking like defensive accident.
`DLCProcessor.__init__` calls `super().__init__()` unconditionally, which is correct against both
bases.

`PoseShapeError(Exception)`.

`DLCProcessor(link, signal_map, frame_width, frame_height, decimator=None, on_drop=None)`:
- `link` is injected, never constructed here and never closed here (rule 1, D-32). The docstring must
  say why in one sentence: `Processor` has no guaranteed teardown, because `save()` is called by the
  DLC-Live GUI and not by `DLCLive`.
- `signal_map` is a loaded `dlc_link.signal_map` record. Names come from it and are never re-derived.
- `frame_width` and `frame_height` are the dimensions of the frames actually handed to the model,
  that is, after any `DLCLive(resize=...)`, because that is the space the returned coordinates live in.
  The target project's own videos are a mix of 1280x960 and 1920x1080, which is precisely why D-18
  normalises: the same authored condition `led_on_x > 0.6` stays correct across both, where a raw
  pixel threshold would not.
- `decimator` defaults to a `Decimator` built from `dlc_link.decimate.RECOMMENDED_DEFAULTS` keyed by
  the map's real signal names.
- Counters on the instance: `frames`, `signals_considered`, `signals_sent`, `signals_dropped`,
  `signals_suppressed`, `out_of_frame`, `values_rejected`. Counts only: no duration, no rate, no
  latency, and no field whose name could be read as one.

`process(self, pose, **kwargs)`:
- Guard first, and there are now TWO guards, both raising `PoseShapeError`:
  (a) **rank.** If `pose` exposes `ndim` and it is not 2, raise with a message stating that THIS
  ADAPTER REQUIRES `single_animal=True`, naming that flag literally, giving the observed shape, and
  explicitly not blaming the engine - multi-animal PyTorch models are fully supported by DLC-Live and
  `(num_individuals, num_keypoints, 3)` is a legitimate shape this adapter refuses by choice.
  Per-individual expansion is a non-goal for a stronger reason than preference: the target project
  sets `identity: false` with `default_track_method: ellipse`, so identity is assigned post-hoc by
  `convert_detections2tracklets` and `stitch_tracklets`, a batch step DLC-Live never runs - a
  `mouse3_nose_x` signal would silently change which animal it refers to between frames (D-37). Put
  that sentence in the error message, not just in the docstring.
  (b) **row count.** If `len(signal_map.POSE_ORDER) != pose.shape[0]`, raise naming both numbers and
  stating that every index in the map was taken from `POSE_ORDER`, so a length mismatch means the
  indices point at the wrong keypoints. This is D-42's guard: DLC-Live's documentation states only
  `(num_bodyparts, 3)` and does not say whether `single_animal=True` yields the 10 multianimal parts
  or all 32 including the unique ones, nor in what order. If the map was generated with
  `POSE_ORDER_SOURCE == "config-declared-UNVERIFIED"`, include that in the message and name plan
  35-07's probe as the way to settle it. Check this once per instance and cache the result - it is a
  per-frame hot path.
- For each bodypart in the map, in map order: read the row at its recorded `index`, take x, y and
  likelihood as the three columns, and convert EVERY one with `mics_link.values.as_scalar` - never
  `float(...)` (D-34).
- Send the likelihood FIRST, then the coordinates when that bodypart has them. Order matters at the
  Pi: the likelihood is the guard the coordinate condition is ANDed with, so it must not lag its own
  coordinates within a frame.
- Coordinates are divided by `frame_width` and `frame_height` before sending, producing normalised
  0..1 values (D-18). Do NOT clamp. A value outside 0..1 means the model reported a keypoint outside
  the frame, and silently clamping would manufacture a plausible-looking in-arena position. Increment
  `out_of_frame` and send the value unchanged.
- Every send goes through the decimator's `should_send(name, value)` first; a suppressed value
  increments `signals_suppressed` and is not sent. A `send_signal` returning False increments
  `signals_dropped` and, when `on_drop` is set, calls it with the name. The docstring must state why
  this counter matters: a frame lost at ZMQ's high-water mark is counted NOWHERE on the Pi, so the
  sender's own accounting is the only end-to-end drop visibility that exists (D-21(ii)).
- `return pose` unconditionally, as the last statement (rule 2, D-33). A test asserts object identity.
- Never raise out of `process` for a per-signal problem: an `InvalidValueError` from the SDK
  increments `values_rejected` and is skipped, because raising here propagates into DLC's inference
  thread and stops inference. `PoseShapeError` is the single deliberate exception, and it is a
  programming error the researcher must fix before the run means anything.

`snapshot()` returns all counters plus the decimator's, as one dict, for the end-of-run summary.

`save(self, filename=None)`: an explicit no-op returning True, with a comment stating it exists
because DLC-Live's GUI calls it and that it deliberately does not close `link` (rule 1).

Nothing in this module may accept, store, forward or reference an image, a frame buffer or a video
path - the adapter receives a pose array and sends scalars, and DLC-12 makes that boundary a scope
decision rather than an implementation detail.

Write `dlc_link/tests/test_processor.py` with a `FakeLink` recording `(name, value)` calls and a
configurable accept/drop pattern. Cover: a 2-bodypart map with coordinates on one yields exactly the
expected names in likelihood-first order; a pixel value of 640 on a 1280-wide frame sends 0.5 and the
same keypoint at 960 on a 1920-wide frame also sends 0.5, proving the normalisation makes a condition
resolution-independent across the project's two video sizes; every sent value satisfies
`type(value) is float` and is not a numpy scalar, tested both with a numpy pose array under
`pytest.importorskip("numpy")` and with a pure-Python fake array that always runs; a 3-D pose raises
`PoseShapeError` whose message contains `single_animal=True` and `stitch_tracklets`; a pose whose row
count differs from `len(POSE_ORDER)` raises `PoseShapeError` naming both numbers, and the message
additionally names plan 35-07's probe when `POSE_ORDER_SOURCE` is `config-declared-UNVERIFIED`; `process` returns the
identical object it was given; a dropped send increments `signals_dropped` and calls `on_drop`; an
out-of-range coordinate increments `out_of_frame` and is sent unclamped; a decimator that suppresses
everything produces zero sends and a non-zero `signals_suppressed`; an `InvalidValueError` from the
link increments `values_rejected` and does not propagate; `save()` returns without touching `link`;
and no key in `snapshot()` matches the words latency, jitter, drift, elapsed or duration.
  </action>
  <verify>
    <automated>cd /home/ido/mics-backend/dlc_link && python3 -m pytest -q tests/test_processor.py && python3 -c "
import sys; sys.path.insert(0,'src'); sys.path.insert(0,'../sdk/src')
from dlc_link.processor import DLCLIVE_AVAILABLE
assert DLCLIVE_AVAILABLE is False
print('processor imports with no dlclive')
" && test $(grep -v '^ *#' src/dlc_link/processor.py | grep -c 'float(') -eq 0 && echo "no float() coercion"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest -q tests/test_processor.py` exits 0 with at least 11 tests collected.
    - `import dlc_link.processor` succeeds on the dev host with `dlclive` absent and `DLCLIVE_AVAILABLE` is False.
    - `grep -v '^ *#' src/dlc_link/processor.py | grep -c 'float('` returns 0.
    - A test asserts `type(sent_value) is float` for a value taken from a real numpy array, proving `as_scalar` ran.
    - A 3-D pose array raises `PoseShapeError` and the message contains the literals `single_animal=True` and `stitch_tracklets`.
    - A 2-D pose whose row count differs from `len(signal_map.POSE_ORDER)` raises `PoseShapeError` naming both numbers; when `POSE_ORDER_SOURCE` is `config-declared-UNVERIFIED` the message also names the probe that settles it.
    - The row-count check is performed once per instance, not once per frame (asserted by a test that drives many frames and counts the checks).
    - Normalisation is asserted at two frame sizes (1280x960 and 1920x1080) yielding the same normalised value for proportionally-equal pixel coordinates.
    - `process(pose)` returns the same object identity it received (`assert result is pose`).
    - An out-of-range normalised coordinate is sent unchanged and increments `out_of_frame`; no clamping appears anywhere in the module.
    - `grep -ciE "cv2|imread|VideoCapture|frame_buffer" src/dlc_link/processor.py` returns 0 - the Processor never touches an image.
    - `processor.py` is under 300 lines.
  </acceptance_criteria>
  <done>A dlclive-compatible Processor sends declared, normalised, correctly-typed signals in the guard-first order, refuses a multi-animal pose shape by name, never opens or closes a client, never touches an image, and is fully tested with no dlclive installed.</done>
</task>

<task type="auto">
  <name>Task 3: The paced video-file loop and the dlc-link-live CLI</name>
  <files>dlc_link/src/dlc_link/live.py, dlc_link/tests/test_live.py</files>
  <read_first>
    - sdk/src/mics_link/replay.py (the whole file: replay() takes a link and does NOT create one; main() builds it via the public connect(...) inside a with block; _build_parser's flag shape; the counts-only end-of-run summary)
    - sdk/src/mics_link/timing.py (Pacer, its origin-relative contract and behind_count())
    - sdk/examples/rig_checkpoint_sender.py lines 44-55 and 153-166 (_print_stats and _build_parser: the --host/--port/--source-id flags with sane overridable defaults, and the counts-only print posture)
    - dlc_link/src/dlc_link/processor.py and dlc_link/src/dlc_link/signal_map.py
    - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-CONTEXT.md decisions D-01, D-02, D-09, D-27, D-29, D-30, and D-42 (the open pose-shape/row-order question this task's --probe-pose mode exists to answer)
  </read_first>
  <action>
Write `dlc_link/src/dlc_link/live.py`, split exactly the way `mics_link/replay.py` is split: a pure
driver function the tests exercise with fakes, and a `main()` that owns argparse and the real
`with connect(...)` block.

`run_video_loop(link, frames, infer, processor, pacer, fps, max_frames=None)`:
- `frames` is any iterable yielding frame objects. `infer` is any callable taking a frame; in
  production it is `DLCLive.get_pose`, which internally calls `Processor.process`, so the processor
  and the loop share one instance.
- For frame index `i` starting at 1: call `pacer.wait_until(i / fps)` BEFORE `infer(frame)`, then
  call `infer(frame)`. Pacing is mandatory for a file (D-30): `cv2.VideoCapture` on a file yields
  frames as fast as the model consumes them, so unpaced, `stale_after_ms` is exercised on a timebase
  unrelated to the live case and the run proves the wrong thing.
- The docstring must carry, verbatim as a comment on the `wait_until` line, that dropping this ONE
  line is the entire change required to swap in a live camera (D-29). That comment is the reason this
  function is shaped this way and it must survive into the shipped module.
- Return a counts-only record: `frames_read`, `frames_inferred`, `behind_count` from the pacer, and
  the processor's `snapshot()`. No wall-clock number is compared against anything.
- Never create or close `link`.

`main(argv=None)`: argparse with `--video` (required, path to the prerecorded video),
`--model-path` (required; state in the help text that for the PyTorch engine this is the `.pt` FILE
produced by `deeplabcut.export_model(...)`, not a directory and not the project directory - D-09),
`--signal-map` (required, path to the generated `<source_id>_signals.py`), `--host`, `--port`
(default 5599), `--source-id` (default read from the loaded signal map, overridable),
`--resize` (float, optional, passed to DLCLive), `--max-frames`, `--fps` (override the video's
reported fps), `--verify-lib PATH` (calls `signal_map.assert_pairs_with` against that lib source and
exits non-zero on mismatch before connecting), `--dry-run` (run the loop with a link that counts and
discards, so the researcher can confirm inference and decimation before touching the rig), and
`--probe-pose`.

**`--probe-pose` is D-42's probe and it must exist here rather than as a throwaway script**, because
this is the only module that already constructs `DLCLive` correctly. With it: read the first frame,
construct `DLCLive` exactly as a real run would, call `init_inference(frame)`, obtain ONE pose, then
print and exit WITHOUT connecting to the Pi and without sending anything. Print, each on its own
labelled line: `pose.shape`; the number of rows; whatever bodypart ordering the constructed runner
exposes (search the live object for the attribute that carries it - candidates seen in DLC-Live are
`cfg`, `dlc_config` and a pose-configuration mapping with an `all_joints_names`-like key; print the
attribute name you found it under, and print an explicit NOT FOUND line naming the attributes you
tried if none carries it, rather than printing nothing); the `--signal-map`'s `POSE_ORDER` and
`POSE_ORDER_SOURCE` when a map was supplied; and a verdict line stating whether the two orders match,
whether the row count matches, and whether the unique bodyparts are present. This output is the
input to regenerating the lib with a correct `--pose-order`.

Import `cv2` and `dlclive` INSIDE `main()`, never at module scope, so `dlc_link.live` imports and its
pure function is testable on a machine with neither. Construct `DLCLive(model_path, model_type="pytorch",
single_animal=True, processor=..., resize=...)` - `model_type="pytorch"` per D-01/D-02, because
`base`, `tensorrt` and `lite` are the TensorFlow-engine values and do not apply to this target; and
`single_animal=True` per D-07, which is also what makes `DLCProcessor`'s 2-D guard satisfiable.
Read frame width and height from the capture and, when `--resize` is given, multiply them by it
before handing them to `DLCProcessor` - the coordinates DLC-Live returns are in the RESIZED frame.
Call `live.init_inference(first_frame)` before the loop and `live.close()` after it.

Wrap everything in `with connect(host, port, source_id) as link:` (rule 1). Print, at exit,
the loop's counts and `link.stats.snapshot()`. Print the wall-clock duration of the run as a
duration, and never compare it to anything on the Pi: no latency, no jitter, no per-frame timing.
Print a reminder line that the sender should be started BEFORE the run when the pilot's config row
has `required: true`, and AFTER the run when it has `required: false` - and that on pilot 3 the row
is `required: false`, so the correct order there is run first, then sender (D-27, plus Phase 34's
run-582 burst caveat).

Write `dlc_link/tests/test_live.py` driving `run_video_loop` with a fake iterable of sentinel frames,
a fake `infer` recording calls, a `Pacer` built with an injected fake clock and fake sleep so nothing
actually waits, and a `FakeLink`. Cover: the pacer is called once per frame with `i / fps` and the
sequence of requested offsets is exactly `1/fps, 2/fps, ...`; `infer` is called after `wait_until`
for each frame; `max_frames` stops the loop early; an empty frame iterable returns zeroed counts and
does not raise; the returned record contains the processor snapshot; `run_video_loop` never calls
`close()` on the link; `import dlc_link.live` succeeds with cv2 and dlclive both absent; and the
pure comparison helper behind `--probe-pose`'s verdict line (given an observed row count and an
observed ordering, decide match / mismatch / ordering-not-discoverable) is exercised for all three
outcomes with plain lists and no dlclive.
  </action>
  <verify>
    <automated>cd /home/ido/mics-backend/dlc_link && python3 -m pytest -q tests/test_live.py && python3 -c "
import sys; sys.path.insert(0,'src'); sys.path.insert(0,'../sdk/src')
import dlc_link.live as L
assert hasattr(L,'run_video_loop') and hasattr(L,'main')
src=open('src/dlc_link/live.py').read()
assert 'model_type=\"pytorch\"' in src or \"model_type='pytorch'\" in src, 'model_type must be pytorch (D-02)'
assert 'base' not in src.split('model_type')[1][:40]
print('live module ok with no cv2/dlclive')
" && python3 -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest -q tests/test_live.py` exits 0 with at least 7 tests collected.
    - `import dlc_link.live` succeeds on the dev host with `cv2` and `dlclive` both absent (proving both imports are inside `main()`).
    - The module source contains `model_type="pytorch"` and `single_animal=True`, and contains neither `model_type="base"` nor `tensorrt` nor `lite`.
    - A test asserts the exact sequence of `Pacer.wait_until` arguments is `[1/fps, 2/fps, 3/fps]` for a three-frame input.
    - A test asserts `run_video_loop` never calls `close()` or `__exit__` on the link.
    - `live.py` contains the literal comment stating that dropping the `wait_until` line is the one-line swap to a live camera.
    - `--probe-pose` appears in `--help`, and the help text states that it never connects to the Pi and sends nothing.
    - The probe's verdict helper is a pure function tested with plain lists for all three outcomes (match, mismatch, ordering-not-discoverable) with dlclive absent.
    - A test asserts `--probe-pose` is documented as requiring no `--host`, so it can be run before any rig fixture exists.
    - `grep -ciE "latency|jitter|drift" src/dlc_link/live.py` counts only lines inside comments that state the prohibition; `grep -v '^ *#' src/dlc_link/live.py | grep -ciE "latency|jitter"` returns 0.
    - `cd dlc_link && python3 -m pytest -q` (whole package suite) exits 0.
    - `live.py` is under 300 lines.
  </acceptance_criteria>
  <done>A paced file loop that is the live pipeline with a file substituted for a camera, with the camera swap documented as one line, argparse mirroring the SDK's replay CLI, all heavy imports deferred into main(), and the whole driver testable with fakes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DLC inference thread -> mics_link IO thread | `send_signal` is called synchronously from DLC's inference thread once per frame, inside `get_pose()` and `_post_process_pose()`. SDK-15 exists for exactly this. |
| vision box -> Pi ROUTER on 5599 | Untrusted scalars cross the network into a device that actuates hardware. |
| generated map module -> `exec_module` on the sender | `load_signal_map` executes a Python file the researcher generated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-14 | Denial of Service | the DLC inference thread | mitigate | `process()` never raises for a per-signal problem; `InvalidValueError` is counted into `values_rejected` and skipped. A raise here stops inference on the researcher's machine mid-experiment. |
| T-35-15 | Denial of Service | Pi shared Tornado IOLoop and the STOP channel | mitigate | Every send passes the `Decimator` first, defaulting to the D-22 budget. `run_video_loop` paces at the video's native fps so the offline workflow cannot outrun the live case. |
| T-35-16 | Tampering | silently wrong values on the wire | mitigate | `as_scalar` on every value, asserted by a test that a real numpy scalar arrives as `type(v) is float`; a `numpy.float64` passes `isinstance(v, float)` but cannot be packed by msgpack, which turns a call-site error into an invisible IO-thread failure. |
| T-35-16b | Tampering | pose rows read at indices derived from an unverified order (D-42) | mitigate | `DLCProcessor` asserts `len(POSE_ORDER) == pose.shape[0]` once per instance and refuses with both numbers named; the map carries `POSE_ORDER_SOURCE` so an unverified order is stated in the error rather than assumed away. |
| T-35-17 | Tampering | coordinates silently changing meaning | mitigate | Coordinates are normalised by the post-resize frame dimensions and are never clamped; an out-of-frame value is counted rather than manufactured into a plausible in-arena position. |
| T-35-18 | Elevation of Privilege | `load_signal_map` executing a generated module | accept | The file is generated on the researcher's own machine by their own `dlc-link-generate` run and executed in their own process at their own privilege; it crosses no trust boundary. The map is validated after load, and `assert_pairs_with` detects substitution of a map that does not match the deployed lib. |
| T-35-19 | Information Disclosure | video or image content leaking toward MICS | mitigate | DLC-12: the Processor accepts a pose array only and the module is asserted to contain no image-handling identifier; `cv2` appears solely inside `live.main()`, which reads frames locally and never sends one. |
| T-35-SC | Tampering | package-manager installs | mitigate | This plan performs no install; `dlclive`, `cv2` and `numpy` are all optional at test time. The blocking legitimacy checkpoint for `deeplabcut-live` lives in plan 35-07. |
</threat_model>

<verification>
- `cd dlc_link && python3 -m pytest -q` green on the dev host with dlclive, cv2 and torch absent.
- `git status --porcelain sdk/ api/` is empty.
- No file under `/home/ido/mics_core/` or `/home/ido/pi-mirror/` modified.
</verification>

<success_criteria>
- The adapter ships in the two shapes DLC-07 requires: a `dlclive` Processor and a standalone loop.
- Names come only from the generated map; nothing re-derives them.
- Values cross the wire through `as_scalar`; coordinates are normalised and never clamped.
- The camera swap is one deleted line, documented in the shipped source.
- No image, frame or video path is ever sent toward MICS.
</success_criteria>

<output>
Create `.planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-04-SUMMARY.md` when done.
</output>
