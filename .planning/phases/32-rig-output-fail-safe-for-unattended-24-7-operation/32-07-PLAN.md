---
phase: 32-rig-output-fail-safe-for-unattended-24-7-operation
plan: 07
type: execute
wave: 2
depends_on: ["32-06"]
files_modified:
  - web_ui/react-src/src/types/index.ts
  - web_ui/react-src/src/components/deviceHealth.mts
  - web_ui/react-src/tests/deviceHealth.test.mts
  - web_ui/react-src/src/pages/index/Index.tsx
  - web_ui/static/style.css
  - CLAUDE.md
autonomous: true
requirements: [FAULT-08]
must_haves:
  truths:
    - "A pilot carrying a standing fault shows a warning affordance on its card in the pilot grid."
    - "The affordance is visible whether the pilot is IDLE or RUNNING, because a pilot-scoped fault is about the device, not the run."
    - "Hovering or opening the affordance names the fault, when it was first raised, and how many times it has recurred."
    - "A pilot with no faults renders exactly as it does today — no layout shift, no empty badge."
    - "The type and the summarising logic are generic over fault_key, so Phase 19's liveness case can render through the same component later."
  artifacts:
    - path: "web_ui/react-src/src/components/deviceHealth.mts"
      provides: "Pure, tested summarising logic: fault list -> badge severity, label and tooltip text"
    - path: "web_ui/react-src/tests/deviceHealth.test.mts"
      provides: "node --test coverage of the summariser including the empty and malformed cases"
    - path: "web_ui/react-src/src/types/index.ts"
      provides: "DeviceHealth and DeviceFault types on PilotLive"
      contains: "device_health"
  key_links:
    - from: "web_ui/react-src/src/pages/index/Index.tsx"
      to: "deviceHealth.mts summariser"
      via: "import and call inside PilotCard"
      pattern: "summariseDeviceHealth"
---

<objective>
Surface pilot-scoped faults on the pilot grid: a warning affordance on `PilotCard`, driven by the
`device_health` field plan 06 added to `/pilots/live`.

Purpose: FAULT-08. Phase 19 would have built `OrchestratorState.device_health -> /pilots/live ->
WS /ws/pilots -> PilotCard` for the narrower `.alive` liveness case. This builds the last leg of
that path carrying pilot-scoped faults, and **must not narrow it to `.alive`** — the summarising
logic keys off a free-form `fault_key`, never off a hardcoded list, so re-scoping Phase 19's case
onto it later is a data change and not a component rewrite. HEALTH-06 is carried forward: this
surfaces, it never aborts a run and it never blocks a control.

Output: types, a pure tested summariser, the badge, and its CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/32-rig-output-fail-safe-for-unattended-24-7-operation/32-06-SUMMARY.md
</context>

<interfaces>
<!-- Verified shapes. Do not rediscover; do not invent class names. -->

**Where the CSS actually lives:** `web_ui/static/style.css`, linked by
`web_ui/templates/react/index.html` as `/static/style.css`. `CLAUDE.md` says
`web_ui/react-src/src/style.css` — that file does not exist. Edit `web_ui/static/style.css` and
fix the `CLAUDE.md` line as part of this plan.

**Existing pilot-card classes** (`web_ui/static/style.css`): `.pilot-card` (675, 3314),
`.pilot-offline-badge` (696), `.pilot-card-header` (3285), `.pilot-status-dot` (3293),
`.dot-offline` / `.dot-idle` / `.dot-running` (3301-3313), `.pilot-run-info` (3328).
Severity colours already exist as CSS variables: `--warn: #fbbf24`, `--error: #f87171`.
Use them; do not introduce new colour literals.

**`PilotCard`** is a local function component inside
`web_ui/react-src/src/pages/index/Index.tsx` (not a separate file). Its header is a
`div.pilot-card-header` containing an `h2` and a flex `div` holding the gear `Link` and the
`span.pilot-status-dot`. The status line below it is
`div.status` with conditional `pilot-offline-badge` / `connected` / `disconnected` modifiers.

**`PilotLive`** (`web_ui/react-src/src/types/index.ts:1-6`) currently has `connected`, `state`,
`active_run`, `updated_at`. Plan 06 added `device_health` to every pilot object in
`/pilots/live`, always present, never `null`, with two buckets: `faults` (an object keyed by
`fault_key`) and `liveness` (reserved, empty). Each fault record carries `fault_key`, `severity`
(`"warn"` or `"error"`), `message`, `detail`, `raised_at`, `count`.

**Testing convention** (`CLAUDE.md`): pure, testable logic goes in
`web_ui/react-src/src/components/<name>.mts` with its test at
`web_ui/react-src/tests/<name>.test.mts`, run by `npm run test:unit` (`node --test`). React
components have no test harness — extract the logic and test that. `detectorOptions.mts`,
`operandTypes.mts` and `argModes.mts` are the existing examples.

**Escaping constraint carried from plan 06 (T-32-22):** `message` and `detail` are authored on the
Pi. Render them as React text children only. `dangerouslySetInnerHTML` must not appear.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types and a pure device-health summariser</name>
  <files>web_ui/react-src/src/types/index.ts, web_ui/react-src/src/components/deviceHealth.mts, web_ui/react-src/tests/deviceHealth.test.mts</files>
  <read_first>
    - web_ui/react-src/src/types/index.ts (the whole file — the existing type style and where PilotLive sits)
    - web_ui/react-src/src/components/detectorOptions.mts (the `.mts` module shape to copy: pure functions, explicit exported types, no React import)
    - web_ui/react-src/tests/detectorOptions.test.mts (the `node --test` idiom: `describe`/`it` or `test`, `assert` from `node:assert`)
    - .planning/phases/32-.../32-06-SUMMARY.md (the exact device_health payload plan 06 emits)
  </read_first>
  <behavior>
    - Test 1: an empty `faults` object summarises to `null` — the card renders unchanged, no badge, no empty element.
    - Test 2: one `severity: "error"` fault summarises to severity `error`, a count of 1, and a label naming the `fault_key`.
    - Test 3: two faults, one `warn` and one `error`, summarise to severity `error` — the badge shows the worst.
    - Test 4: two `warn` faults summarise to severity `warn` and a count of 2.
    - Test 5: the tooltip text lines are one per fault, each containing the `fault_key`, the `message`, the `raised_at` value and the recurrence `count`, in a stable order sorted by `raised_at` ascending so the oldest standing fault reads first.
    - Test 6: a fault whose `fault_key` is an invented string never seen before still summarises correctly — the summariser has no allowlist. This is the FAULT-08 no-narrowing property and the test is the thing that enforces it.
    - Test 7: a missing `device_health`, a `null` one, a missing `faults` bucket, and a `faults` value that is an array rather than an object all summarise to `null` without throwing.
    - Test 8: a fault record missing `count` or `raised_at` still renders a line, with the missing field shown as an em dash rather than `undefined`.
  </behavior>
  <action>
Add to `web_ui/react-src/src/types/index.ts`:
`DeviceFault` — `fault_key: string`, `severity: 'warn' | 'error'`, `message: string`,
`detail?: Record<string, unknown> | null`, `raised_at?: string | null`, `count?: number | null`.
`DeviceHealth` — `faults: Record<string, DeviceFault>`, `liveness: Record<string, unknown>`.
Then add `device_health?: DeviceHealth | null` to `PilotLive`. Make it optional and nullable even
though plan 06 always sends it, because the WebSocket sends `{}` when the orchestrator is
unreachable (`web_ui/app.py`'s `safe_send({})` on `ConnectError`) and a non-optional field would
be a lie about what arrives.

Create `web_ui/react-src/src/components/deviceHealth.mts` exporting
`summariseDeviceHealth(health: DeviceHealth | null | undefined): DeviceHealthSummary | null`
where `DeviceHealthSummary` is `{ severity: 'warn' | 'error'; count: number; label: string;
lines: string[] }`.

Rules: return `null` when there are no faults or the input is unusable; severity is `error` if any
fault is `error`, else `warn`; `count` is the number of distinct fault keys; `label` is the single
fault's `fault_key` when there is one, and `<n> faults` when there are more; `lines` is one string
per fault sorted by `raised_at` ascending with an absent `raised_at` sorting last, each line naming
the key, the message, the raised-at value and the recurrence count, using an em dash for any
missing field.

**No allowlist, no enum, no switch on `fault_key`.** Put a comment saying so and naming FAULT-08:
Phase 19's liveness case must be able to render through this without a code change here.

Write `web_ui/react-src/tests/deviceHealth.test.mts` first and see Tests 1-8 fail, then implement.
  </action>
  <verify>
    <automated>cd web_ui/react-src && npm run test:unit && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `cd web_ui/react-src && npm run test:unit` passes, and the total test count is 8 higher than the 84 recorded in `CLAUDE.md`.
    - `cd web_ui/react-src && npx tsc -b` exits 0.
    - `grep -c 'device_health' src/types/index.ts` returns at least 1, and `DeviceFault` and `DeviceHealth` are both exported.
    - No allowlist crept in: `grep -cE "fault_key ===|includes\(.*fault_key|KNOWN_FAULTS|switch *\(.*fault_key" src/components/deviceHealth.mts` returns 0.
    - Test 6 uses a deliberately invented `fault_key` string and asserts the summary is non-null.
    - Test 7 covers all four unusable-input shapes in separate assertions.
    - The summary quotes the RED run for Tests 1, 6 and 7.
  </acceptance_criteria>
  <done>The badge's logic is a pure function with 8 passing tests and no knowledge of which faults exist.</done>
</task>

<task type="auto">
  <name>Task 2: Render the badge on PilotCard and style it</name>
  <files>web_ui/react-src/src/pages/index/Index.tsx, web_ui/static/style.css, CLAUDE.md</files>
  <read_first>
    - web_ui/react-src/src/pages/index/Index.tsx (the whole file — PilotCard's header, status line and run-info block, and the existing conditional-class idiom)
    - web_ui/react-src/src/components/deviceHealth.mts (the summariser this consumes)
    - web_ui/static/style.css (lines 3285-3335 — the pilot-card block the new rules join, and lines 14-18 for the `--warn` / `--error` variables)
    - CLAUDE.md (the CSS line that names a nonexistent file, and the Tests table count to update)
  </read_first>
  <action>
In `PilotCard` (`web_ui/react-src/src/pages/index/Index.tsx`), call
`summariseDeviceHealth(info.device_health)` and, when it is non-null, render a
`span.pilot-health-badge` in the header's flex container, immediately **before** the
`span.pilot-status-dot`, so the persistent device warning sits left of the transient run state.

The badge content is an exclamation mark `!`. Give it `title={summary.lines.join('\n')}` for the
hover text and `aria-label` naming the severity and the label, so it is not a mouse-only
affordance. Apply the severity as a modifier class, `pilot-health-warn` or `pilot-health-error`.

Render it **regardless of `info.state`** — a pilot-scoped fault is about the device and stays true
while a run is happening. Render it even when `connected` is false, because a standing fault on a
pilot that has since gone offline is exactly the thing an operator needs to see. Add a one-line
comment saying both, so a later reader does not "tidy" the badge into the `connected && run` block
where every other detail lives.

Do not make the badge clickable, do not add a modal, and do not gate any existing control on it.
HEALTH-06: this surfaces, it never blocks. The tooltip is the whole interaction in this plan.

Never use `dangerouslySetInnerHTML` — `message` and `detail` come from the Pi.

In `web_ui/static/style.css`, add `.pilot-health-badge` next to the existing `.pilot-status-dot`
rule (around line 3293) so the pilot-card block stays contiguous: a small inline-flex circle sized
to match the status dot, `cursor: help`, and `.pilot-health-warn { color: var(--warn); }` /
`.pilot-health-error { color: var(--error); }` using the existing variables. Add no new colour
literals and no new font sizes — reuse what the block already sets.

Fix `CLAUDE.md`: the "CSS: Use classes from `web_ui/react-src/src/style.css`" line names a file that
does not exist. Point it at `web_ui/static/style.css`, keep the "do not invent new class names"
rule, and update the frontend unit-test count in the Tests table to the new total.
  </action>
  <verify>
    <automated>cd web_ui/react-src && npx tsc -b && npm run build && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `cd web_ui/react-src && npx tsc -b` exits 0 and `npm run build` exits 0.
    - `grep -c 'summariseDeviceHealth' src/pages/index/Index.tsx` returns at least 1.
    - `grep -c 'pilot-health-badge' /home/ido/mics-backend/web_ui/static/style.css` returns at least 1, and `pilot-health-warn` and `pilot-health-error` are both defined.
    - `grep -c 'dangerouslySetInnerHTML' src/pages/index/Index.tsx` returns 0.
    - No new colour literal was added: `git diff /home/ido/mics-backend/web_ui/static/style.css | grep -cE '^\+.*#[0-9a-fA-F]{3,6}'` returns 0.
    - The badge is outside any `state === 'RUNNING'` or `connected &&` guard — asserted by reading the JSX and stated in the summary with the surrounding condition quoted.
    - `CLAUDE.md` no longer contains the string `react-src/src/style.css`.
    - Rendered check after `docker compose up --build web_ui`: with a fault injected into Redis by hand — `docker compose exec -T redis redis-cli hset pilot:<key> device_health '{"faults":{"pigpiod_unavailable":{"fault_key":"pigpiod_unavailable","severity":"error","message":"no GPIO daemon","raised_at":"2026-09-06T10:00:00","count":3}},"liveness":{}}'` — the `!` appears on that pilot's card at `/react/` and its tooltip names the fault, the timestamp and the count.
  </acceptance_criteria>
  <done>An operator looking at the pilot grid can see that a rig has a standing device fault, on a card that otherwise looks completely normal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pi-authored strings -> browser DOM | `message` and `detail` originate on a device and are rendered to an operator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-23 | Tampering | Pi-authored `message` rendered as markup | mitigate | React text children and the `title` attribute only; a counted assertion that `dangerouslySetInnerHTML` appears zero times in `Index.tsx` |
| T-32-24 | Denial of service | A very long or high-cardinality fault set breaking the card layout | mitigate | `label` collapses to `<n> faults` above one fault; the per-fault detail lives in the tooltip, not the card body, so the card's height is fixed regardless of fault count |
| T-32-25 | Repudiation | A badge that clears itself, training operators to ignore it | mitigate | The badge has no local dismissal and no timer; it renders exactly what `device_health` says, and plan 06 clears a fault only on an explicit `cleared` report |

No package-manager installs occur in this plan; no new npm dependency is added.
</threat_model>

<verification>
- `npm run test:unit` passes with 8 new tests.
- `npx tsc -b` and `npm run build` both exit 0.
- The manual Redis injection makes the badge appear at `/react/` with a correct tooltip.
- `CLAUDE.md`'s CSS path and test count are both correct.
</verification>

<success_criteria>
A pilot-scoped fault is visible on the pilot grid with its key, first-raised time and recurrence
count; the logic that produces it is a pure function with no knowledge of which faults exist; and a
pilot with no faults renders byte-identically to today.
</success_criteria>

<output>
Create `.planning/phases/32-rig-output-fail-safe-for-unattended-24-7-operation/32-07-SUMMARY.md` when done
</output>
</content>
