---
phase: 23-compute-primitives-variables
plan: 03
type: execute
wave: 3
depends_on: ["23-02"]
files_modified:
  - web_ui/react-src/src/types/index.ts
  - web_ui/react-src/src/api/computeLibrary.ts
  - web_ui/react-src/src/components/ActionEditor.tsx
  - web_ui/react-src/src/components/ComputeActionEditor.tsx
  - web_ui/react-src/src/components/StateBodyPanel.tsx
  - web_ui/react-src/src/components/ConditionBuilder.tsx
  - web_ui/react-src/src/components/ArgInput.tsx
  - web_ui/react-src/src/pages/task-editor/TaskEditor.tsx
autonomous: false
requirements: [CMP-13, CMP-14, CMP-15]
must_haves:
  truths:
    - "The state-builder offers a `compute` action type in the action editor"
    - "A `compute` editor picks a primitive (from the compute-library AST), supplies per-arg ArgInputs, and names an output"
    - "Typing a new `output` name auto-declares it into the task's `variables` registry inline (no separate step)"
    - "The transition-condition operand dropdown is populated from toolkit FLAGS + the task's `variables`, so a freshly-declared variable is immediately selectable"
    - "Save serializes `variables` + compute actions to FDA-JSON-v2 and round-trips on reload"
    - "(nice-to-have) the editor warns when a transition reads a variable no reachable upstream state writes"
  artifacts:
    - path: "web_ui/react-src/src/types/index.ts"
      provides: "FdaJson.variables + compute member of FdaAction"
      contains: "variables"
    - path: "web_ui/react-src/src/api/computeLibrary.ts"
      provides: "Fetch compute-library primitive signatures (name + args) from the hardware-libs AST"
      contains: "fetch"
    - path: "web_ui/react-src/src/components/ActionEditor.tsx"
      provides: "compute action editor with inline output auto-declare"
      contains: "compute"
    - path: "web_ui/react-src/src/components/ConditionBuilder.tsx"
      provides: "operand dropdown sourced from FLAGS + task variables"
      contains: "variables"
  key_links:
    - from: "ActionEditor output field"
      to: "TaskEditor fdaJson.variables"
      via: "onChange callback registering a new output name into the variables registry"
      pattern: "variables"
    - from: "ConditionBuilder operand dropdown"
      to: "task variables + toolkit FLAGS"
      via: "merged option list passed as prop"
      pattern: "variables"
    - from: "TaskEditor save (fda_json)"
      to: "FDA-JSON-v2 variables + compute"
      via: "serialize/deserialize round-trip"
      pattern: "variables"
---

<objective>
Wire the GUI end of the compute/variables loop: types for `variables` + `compute`
actions; a fetch for the compute-library primitive signatures; a `compute` action
editor in the state-builder with inline output auto-declare; a transition-condition operand
dropdown sourced from FLAGS + task `variables`; and TaskEditor serialize/deserialize of the
`variables` registry. Optional reachability warning.

Purpose: Let a researcher assemble the whole "compute at entry, branch in transitions" loop
in the browser without touching Python or the locked toolkit source.
Output: types, an API helper, ActionEditor/StateBodyPanel/ConditionBuilder edits, and
TaskEditor serialization.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md

GUI plan — agent-driven. React SPA only (no Jinja2 for new UI). Use existing CSS classes
from web_ui/react-src/src/style.css — do NOT invent class names. No `any` (use `unknown`/
specific types). Build with `cd web_ui/react-src && npm run build` (faster than full compose
rebuild); typecheck with `npx tsc --noEmit`. This plan is NOT autonomous — it ends with a
human-verify checkpoint (visual + round-trip).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-compute-primitives-variables/23-CONTEXT.md
@.planning/phases/23-compute-primitives-variables/23-02-PLAN.md

<interfaces>
<!-- Existing GUI contracts to extend. Verified during planning (types/index.ts:216-289). -->

types/index.ts:
```typescript
export interface FdaAction {
  type: 'hardware' | 'flag' | 'timer' | 'special' | 'method' | 'if'   // ADD 'compute'
  ref?: string; method?: string; args?: unknown[]; action?: string; duration?: unknown
  condition?: FdaCondition; then?: FdaAction[]; else?: FdaAction[]
  // ADD for compute: op?: string; output?: string   (args reused)
}
export interface FdaJson {
  version: 2
  initial_state: string
  states: Record<string, FdaState>
  transitions: FdaTransition[]
  trigger_assignments: FdaTriggerAssignment[]
  hw_overrides?: Record<string, unknown>
  // ADD: variables?: Record<string, Record<string, never>>   // untyped scratch: name → {}
}
```

ActionEditor.tsx (components/ActionEditor.tsx) — Props { action: FdaAction; onChange:
  (updated: FdaAction) => void; toolkit; hwModules; ... }. Dispatches per action.type via a
  uiType select (~159-222 sets type/ref/method on change). Renders per-type bodies
  (hardware/timer/flag/if/method/special) using ArgInput (./ArgInput) for args. ADD a compute
  body here. Uses AstMethod type already imported from ../types.

ArgInput.tsx (components/ArgInput.tsx) — VERIFIED: today it supports ONLY three arg modes —
  `literal` / `param` / `flag` (ArgMode union ~3; MODE_* maps; switchMode ~63; flag dropdown
  reads `toolkit.flags` keys ~60). There is NO `variable` mode, and task `variables` are NOT in
  `toolkit.flags`, so a compute output variable cannot currently be selected as an arg. This
  BLOCKS the counter/derived-value pattern (`add(counter,1)→counter`). Task 2 MUST add a 4th
  `variable` mode that emits `{"view": name}` (matches the `{"view":...}` branch added to the
  Pi's `_resolve_arg` in 23-01), populated from a NEW `variables?: string[]` prop threaded from
  TaskEditor → StateBodyPanel → ActionEditor → ArgInput. Keep MODE_COLORS/LABELS/TOOLTIPS
  parity; reuse the existing dropdown-or-freetext fallback pattern (~156-174).

StateBodyPanel.tsx — TYPE_COLORS map (~5) + summarizeAction (~37) + the action-type chip
  (~111) + ActionEditor render (~132). ADD compute to TYPE_COLORS + summarizeAction so
  it renders with a chip + readable summary. The "add action" control (~68) seeds new
  actions — add a compute seed ({type:'compute',op:'',args:[],output:''}).

ConditionBuilder.tsx — OperandEditor (~48) builds option lists: hwOpts (semantic_hardware +
  hwModules), flagOpts (toolkit.flags keys), paramOpts, viewOpts = unique(hwOpts+flagOpts).
  ADD task `variables` keys to flagOpts/viewOpts so a freshly-declared variable is selectable
  as a {"view": name} operand. ConditionBuilder must receive the task `variables` (thread a
  prop from TaskEditor down through the condition editing path).

TaskEditor.tsx (pages/task-editor/TaskEditor.tsx) — normaliseFda (~104) normalizes states +
  transitions; createDefaultFda (~353) seeds {initial_state, states, transitions}; save
  payload builds `fda_json: fdaJson` (~279); updateStateBody (~349) writes
  fdaJson.states[name]. ADD: preserve/serialize `variables` through normaliseFda + save;
  provide an onAddVariable handler that merges a new name into fdaJson.variables; thread
  fdaJson.variables to ActionEditor (for output auto-declare) and ConditionBuilder (operand
  dropdown).

api/ (src/api/) — existing typed fetch helpers per domain. ADD a small helper to GET the
  compute-library AST (the hardware_libs row storing compute primitives from 23-02) and map
  ast_metadata.functions → [{name, args}] for the primitive picker. The compute lib is
  identified by the name/filename convention documented in 23-02's SUMMARY — read it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + compute-library fetch helper</name>
  <files>web_ui/react-src/src/types/index.ts, web_ui/react-src/src/api/computeLibrary.ts</files>
  <action>
    (a) types/index.ts (CMP-13/14 foundation): extend `FdaAction.type` union to include
    `'compute'`; add optional `op?: string`, `output?: string`
    fields (args is already present and reused by compute). Add
    `variables?: Record<string, Record<string, never>>` to `FdaJson` (untyped scratch slots:
    name → {}). Add a `ComputePrimitive` type `{ name: string; args: { name: string;
    annotation?: string; default?: string }[] }` mirroring the AST shape from 23-02.
    (b) Create web_ui/react-src/src/api/computeLibrary.ts: a typed fetch
    `getComputePrimitives(): Promise<ComputePrimitive[]>` that GETs the compute-library
    hardware_libs row's ast_metadata and maps `functions` → ComputePrimitive[]. Use the
    existing apiFetch client (src/api/client.ts) and the name/filename convention from 23-02's
    SUMMARY to locate the row. Explicit return types; no `any`.
  </action>
  <verify>
    <automated>cd /home/ido/mics-backend/web_ui/react-src && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>FdaAction supports compute + op/output; FdaJson has variables; ComputePrimitive type exists; getComputePrimitives compiles with explicit types; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: compute action editor + variable-arg support + inline output auto-declare</name>
  <files>web_ui/react-src/src/components/ArgInput.tsx, web_ui/react-src/src/components/ActionEditor.tsx, web_ui/react-src/src/components/StateBodyPanel.tsx</files>
  <action>
    (0) ArgInput.tsx (PREREQUISITE — the arithmetic primitives are useless without it): add a
      4th arg mode `variable` to the `ArgMode` union + MODE_COLORS/MODE_LABELS/MODE_TOOLTIPS,
      detected via `'view' in value`, emitting `{"view": name}`. Add a `variables?: string[]`
      prop; in `variable` mode render the same dropdown-or-freetext fallback used by `flag`
      (~156-174) over `variables`. This lets a compute arg reference a task variable (counter
      pattern `add(counter,1)→counter`) and resolves on the Pi via the `{"view":...}` branch
      added in 23-01. No `any`; keep ArgInput under the size limit.
    (a) ActionEditor.tsx (CMP-13): add `compute` to the action-type select. Render:
      - compute body: a primitive picker `<select>` populated from getComputePrimitives()
        (fetch on mount, cache via the existing query pattern); on pick, set action.op and
        reset args to the primitive's arg count. Render one ArgInput per primitive arg
        (reuse ./ArgInput — now literal/param/flag/variable), passing the task `variables` so
        a variable is selectable as an arg. An `output` text input (free text) sets action.output.
      - The `output` field must, on change with a NEW name, call an onAddVariable callback
        (new prop) so the name is registered into the task's variables registry inline
        (CMP-13 "auto-declare"). Pass existing variables in so the output field can also be a
        dropdown of existing variables + "type new".
      Use existing CSS classes (param-field, param-name, etc.). No new class names. No `any`.
      SIZE GUARD: ActionEditor.tsx is ALREADY ~533 lines (past the 500 hard limit). Do NOT
      inline a large compute body — extract it into a sibling `ComputeActionEditor.tsx` and
      render it from ActionEditor's dispatch, so neither file grows past the limit.
    (b) StateBodyPanel.tsx: add compute to TYPE_COLORS and summarizeAction (e.g.
      compute → `op → output`); add the action-type chip rendering;
      add compute to the "add action" seed control. Thread the variables +
      onAddVariable props from TaskEditor down to ActionEditor, and `variables` onward to
      each ArgInput (so compute args can pick a variable).
  </action>
  <verify>
    <automated>cd /home/ido/mics-backend/web_ui/react-src && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ArgInput gains a `variable` mode (emits {"view":name}) sourced from task variables; ActionEditor renders a compute (primitive picker + ArgInputs + output) editor; typing a new output triggers onAddVariable; StateBodyPanel chips/summaries/add-control cover compute; tsc clean; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: ConditionBuilder operand dropdown from variables + TaskEditor serialization</name>
  <files>web_ui/react-src/src/components/ConditionBuilder.tsx, web_ui/react-src/src/pages/task-editor/TaskEditor.tsx</files>
  <action>
    (a) ConditionBuilder.tsx (CMP-14): add the task's `variables` keys to the operand option
      lists (merge into flagOpts and viewOpts so a variable is selectable as {"view": name}
      or {"flag": name}). Add a `variables?: string[]` prop and thread it from TaskEditor
      through the condition-editing path. A freshly-declared variable (added via Task 2's
      auto-declare) must appear immediately in the dropdown (driven by the same fdaJson state).
    (b) TaskEditor.tsx: (i) preserve `variables` through `normaliseFda` (carry the field
      unchanged) and include it in the save `fda_json` payload (~279) so it round-trips on
      reload (CMP-13/14 round-trip). (ii) Add `onAddVariable(name)` that merges
      `{ [name]: {} }` into `fdaJson.variables` (immutably, via setFdaJson) and pass it +
      `Object.keys(fdaJson.variables ?? {})` down to StateBodyPanel/ActionEditor and
      ConditionBuilder. (iii) createDefaultFda seeds `variables: {}`.
      (iv) CMP-15 (nice-to-have): compute a reachability warning — for each transition that
      reads a variable, check whether some upstream-reachable state writes it via a
      compute output; if none, surface a non-blocking warning in the editor (reuse the
      existing stateWarnings surface pattern ~248-274). Keep it best-effort; do not block save.
    Watch file sizes (TaskEditor): if edits push it past ~300 lines, extract the variable/
    reachability helper into a sibling util. No `any`.
  </action>
  <verify>
    <automated>cd /home/ido/mics-backend/web_ui/react-src && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ConditionBuilder operand dropdown includes task variables; TaskEditor serializes/deserializes variables (round-trip), auto-declare flows into fdaJson.variables, createDefaultFda seeds variables:{}; reachability warning surfaces non-blockingly; tsc clean; build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: GUI compute loop verification</name>
  <action>Human verification gate — the user rebuilds the web_ui and exercises the compute editor + operand dropdown + save round-trip per how-to-verify, and confirms the result.</action>
  <what-built>
    GUI: compute action editor + inline variable auto-declare + ConditionBuilder operand
    wiring + TaskEditor variables serialization. The full no-Python loop is now buildable in
    the browser and validated end-to-end with the Pi (23-01) + backend (23-02).
  </what-built>
  <how-to-verify>
    Rebuild and open the task editor:
      1. `cd /home/ido/mics-backend && docker compose up --build -d web_ui` (or
         `cd web_ui/react-src && npm run build` then refresh).
      2. Open `http://localhost:8080/react/` → navigate to the task editor for a toolkit.

    Confirm:
      3. In a state's body, add a `compute` action: pick `random_bool`, fill arg `0.5`, type
         a NEW output name `target`. Confirm the primitive picker lists ALL compute-library
         primitives (from 23-02) — the 5 random/copy + 8 numeric/util (add…clamp).
      3b. Add a second `compute` action exercising the counter pattern: pick `add`, set arg 0
         to mode `variable` = an existing variable `counter` (declare it), arg 1 literal `1`,
         output `counter`. Confirm `counter` is selectable as a variable arg and round-trips.
      4. Add a transition out of that state; open its condition builder. Confirm `target`
         appears in the operand dropdown immediately (auto-declared). Build
         `target == true` → go_state and `target == false` → nogo_state.
      5. Save. Reload the editor. Confirm `variables` { target } and the compute
         action round-trip (still present and correct).
      6. (CMP-15) Add a transition reading a variable no state writes → confirm a non-blocking
         warning appears.
      7. Try saving an invalid case (output not declared / name colliding with a FLAG) →
         confirm the backend 422 from 23-02 surfaces as a clear error in the UI.
  </how-to-verify>
  <resume-signal>Type "approved" when the loop builds, auto-declare + operand dropdown work, and save round-trips, or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- tsc --noEmit clean; npm run build succeeds.
- compute editor renders with primitive picker + output; inline auto-declare registers into variables.
- ConditionBuilder operand dropdown includes task variables; freshly-declared variable selectable immediately.
- TaskEditor save serializes variables + compute and round-trips on reload.
- Human-verify: full gonogo-translation loop built in browser; backend 422 surfaces in UI.
</verification>

<success_criteria>
The StateBodyPanel offers a `compute` action; typing a new `output` auto-declares it
into `variables` and makes it selectable in the ConditionBuilder operand dropdown; save
serializes `variables` + the compute action to FDA-JSON-v2 and round-trips on reload.
(Nice-to-have) the editor warns on a transition reading a variable no reachable upstream
state writes.
</success_criteria>

<output>
After completion, create `.planning/phases/23-compute-primitives-variables/23-03-SUMMARY.md`
</output>
