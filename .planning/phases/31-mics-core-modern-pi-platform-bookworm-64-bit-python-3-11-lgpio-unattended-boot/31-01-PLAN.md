---
phase: 31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/ido/mics_core/requirements-dev.txt
  - /home/ido/mics_core/tools/pytest_delta.py
  - /home/ido/mics_core/conftest.py
  - /home/ido/mics_core/tests/fakes/__init__.py
  - /home/ido/mics_core/tests/fakes/fake_lgpio.py
  - /home/ido/mics_core/tests/test_fake_lgpio_contract.py
  - /home/ido/mics_core/tests/test_pytest_delta.py
  - /home/ido/mics_core/README.md
  - /home/ido/mics-backend/.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/31-PYTEST-BASELINE.json
autonomous: true
requirements: [PLAT-26]
must_haves:
  truths:
    - "A dedicated feature branch exists in ~/mics_core and every later plan commits onto it"
    - "python -m pytest on the dev host runs the tree's own tests instead of dying on a missing third-party import"
    - "A machine-checkable pytest DELTA gate exists, so a later task can prove it broke nothing"
    - "Port tests can exercise lgpio call shapes on the dev host, with no Pi and no lgpio installed"
  artifacts:
    - path: "/home/ido/mics_core/requirements-dev.txt"
      provides: "Dev-host import closure so pytest can collect and run the tree"
      min_lines: 10
    - path: "/home/ido/mics_core/tools/pytest_delta.py"
      provides: "Baseline-relative pass/fail gate; exits non-zero only on NEW failures"
      min_lines: 30
    - path: "/home/ido/mics_core/tests/fakes/fake_lgpio.py"
      provides: "Recording fake of the lgpio 0.2.2 surface the port uses"
      min_lines: 60
    - path: "/home/ido/mics-backend/.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/31-PYTEST-BASELINE.json"
      provides: "Frozen failing-node-id set + counts, the reference every later delta compares to"
      contains: "failing_node_ids"
  key_links:
    - from: "tools/pytest_delta.py"
      to: "31-PYTEST-BASELINE.json"
      via: "json.load of the baseline path, set-difference against FAILED node ids"
      pattern: "31-PYTEST-BASELINE"
    - from: "tests/fakes/fake_lgpio.py"
      to: "sys.modules['lgpio']"
      via: "conftest fixture that installs the fake before the module under test imports lgpio"
      pattern: "sys.modules"
---

<objective>
Wave 0. Make the phase measurable before it changes anything.

Purpose: the test modules in `~/mics_core/tests/` have **never been executed anywhere**. Measured
on the dev host 2026-08-16: **179 failed / 203 passed / 381 collected**, and every one of the 179
is a third-party `ModuleNotFoundError` chain (`npyscreen` first, then `tzlocal`), not a code
defect. Until that is fixed, `pytest && next-thing` fails for reasons unrelated to whatever it is
gating, and every downstream verification in this phase is worthless. This plan builds the
instrument, freezes the baseline, and creates the fake `lgpio` module that lets Stage 3's port
tests run on a machine with no Pi.

Output: a feature branch, a dev venv recipe, a committed delta gate, a recording lgpio fake, and a
frozen baseline JSON in the planning repo.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/CONTEXT.md
@.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/31-RESEARCH.md
@.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/31-VALIDATION.md
@.planning/phases/30-pi-repo-cleanup/30-PYTEST-BASELINE.json

<repo_boundary>
ALL code changes land in `/home/ido/mics_core` on branch **`phase-31-modern-pi-platform`**.
NEVER modify `/home/ido/mics-backend` source — only its `.planning/` documents.
</repo_boundary>

<pi_rules>
ABSOLUTE. These override anything else in this plan:
- **NEVER run git on the Pi.** No commit/merge/push/pull/checkout/reset. No `rsync --delete`.
- **NEVER start or stop the pilot process. NEVER run any Python file on the Pi.** Hand the user
  the literal command and wait.
- Pi log files are 0 bytes **by design**. Never "fix" them.
- `Message` and `hardware_state` classes are **off limits** to every sweep, refactor and lint pass.
- SSH for reading/grepping is fine: `ssh -i ~/.ssh/pi_mics pi@132.77.72.28`.
- RTK-proxied grep can render a matching line blank. Re-verify every "unused" claim with a direct
  `grep -n`.
</pi_rules>

<integrity_gate>
`cd /home/ido/mics_core && python3 tools/check_tree_integrity.py --strict` must run after ANY
change and must exit 0. **NEVER `--rebaseline`.**
Baseline today (verified 2026-08-16):
`OK: 40 closure members, 30 protected files, 1 known-dangling exemptions held, 0 violations`
</integrity_gate>

<measured_facts>
Verified on the dev host 2026-08-16 — do not re-derive:

- Dev host `python3 --version` is **3.12.3**. The rig target is 3.11; 3.12 is close enough for the
  static and fake-driven tests, and the real 3.11 check happens on the provisioned card in plan 09.
- `cd ~/mics_core && python3 -m pytest` gives **179 failed, 203 passed** over 381 collected, in ~5s.
- All 179 trace to `autopilot/autopilot/__init__.py:4` -> `from autopilot.setup import
  setup_autopilot` -> `setup_autopilot.py:6` -> `import npyscreen`. Stubbing `npyscreen` alone does
  **not** fix them: the next link is `autopilot/autopilot/utils/common.py:18`
  `from tzlocal import get_localzone`.
- Third-party modules the tree imports that are MISSING on the dev host: `tzlocal, tornado, pandas,
  tables, scipy, blosc, npyscreen, inputs, validators, skvideo, pyqtgraph, PySide2, python-osc,
  adafruit_blinka (board/busio)`. Already present: `numpy, pytz, zmq, msgpack, requests, PIL`.
- Root `conftest.py` already `collect_ignore`s `tests/test_compute_ops.py` and
  `tests/test_log_action_values.py` for exactly the npyscreen reason.
- 30 files carry a `baseline_sha256` in `tools/tree_protect_list.json`, including
  `autopilot/autopilot/networking/Event_Dispatcher.py` and 21 `tests/test_*.py` modules.
  **Any edit to one of those breaks `--strict` check 3.** None of this plan's files are protected.
- Phase 30 solved the same problem with `30-PYTEST-BASELINE.json` plus an inline heredoc
  `delta_command`. This plan turns that heredoc into a committed script so it is reviewable and
  cannot drift.
</measured_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cut the feature branch and make the dev host able to import the tree</name>
  <files>
    /home/ido/mics_core/requirements-dev.txt
    /home/ido/mics_core/README.md
  </files>
  <action>
    1. In `/home/ido/mics_core` (NOT the Pi, NOT pi-mirror), create and check out the branch:
       `git checkout -b phase-31-modern-pi-platform`
       Confirm with `git branch --show-current`. Every later Phase 31 plan commits here.
       Do NOT push. Do NOT touch `main`. Do NOT re-import old history — Phase 30 published this
       repo via a fresh `git init` because a live Gmail app password existed in the old history.

    2. Determine the ACTUAL dev-host import closure empirically; do not guess from the list above.
       Iterate: run
       `python3 -m pytest tests/ --tb=line 2>&1 | grep -oE "No module named '[^']+'" | sort -u`,
       add the corresponding distribution to `requirements-dev.txt`, reinstall, repeat until the
       set is empty or the remaining names are genuinely Pi-only.

    3. Create the dev venv:
       `python3 -m venv /home/ido/.venvs/mics_core_dev`
       `/home/ido/.venvs/mics_core_dev/bin/pip install -r requirements-dev.txt`
       This venv is the dev-host test runner for the whole phase.

    4. `requirements-dev.txt` is a DEV-HOST file and must say so in a header comment: it exists so
       `pytest` can import the tree on a machine with no GPIO hardware. It is NOT the rig runtime
       set (that is `requirements.txt`, rewritten in plan 03) and it is NOT installed by
       `deploy/install.sh`. Record resolved versions from `pip freeze`; do not pin anything you did
       not actually install.

    5. Anything that CANNOT be satisfied on the dev host stays in `conftest.py`'s `collect_ignore`
       with a one-line reason naming the module and the import site. No blanket ignores.

    6. Add a `## Running the tests` section (max 15 lines) to `README.md` giving the venv path, the
       delta command from Task 2, and the `--strict` integrity command.

    Do NOT delete `npyscreen` or the setup wizard here — that is plan 02, and it is a code change
    with its own integrity gate. Here you only make the host able to run what already exists.
  </action>
  <verify>
    <automated>cd /home/ido/mics_core && git branch --show-current | grep -qx phase-31-modern-pi-platform && /home/ido/.venvs/mics_core_dev/bin/python -m pytest tests/ --tb=no | tail -3</automated>
  </verify>
  <done>Branch `phase-31-modern-pi-platform` is checked out; `/home/ido/.venvs/mics_core_dev` exists; pytest in it prints a pass/fail COUNT line rather than a collection error, and the failure count is strictly below the 179 measured with the system interpreter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze the Phase 31 pytest baseline and commit the delta gate</name>
  <files>
    /home/ido/mics_core/tools/pytest_delta.py
    /home/ido/mics_core/tests/test_pytest_delta.py
    /home/ido/mics-backend/.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/31-PYTEST-BASELINE.json
  </files>
  <behavior>
    - On an unchanged tree, `pytest_delta.py` exits 0 and prints `new failures: 0`.
    - Given a baseline whose `failing_node_ids` omits a currently-failing node, it exits 1 and
      names that node id.
    - On a pytest COLLECTION error (returncode 2, or "error" plus "during collection" in stdout) it
      exits 1 with `COLLECTION ERROR - not a delta, a break`. A collection break must never be
      reported as "no new failures".
    - A node failing in the baseline that now passes is reported as `fixed: <id>` and does NOT fail
      the gate.
  </behavior>
  <action>
    1. Write `/home/ido/mics_core/tools/pytest_delta.py`. Port the logic from
       `30-PYTEST-BASELINE.json`'s `delta_command` heredoc — read it, it is the reference
       implementation — into a real script, and add the `fixed:` reporting the heredoc lacks.
       - `--baseline PATH` (default: the Phase 31 JSON written below), `--root PATH`
         (default `/home/ido/mics_core`).
       - Run pytest as `[sys.executable, '-m', 'pytest', '-q', 'tests/']` from `--root`.
       - `pytest.ini` carries `addopts = -q`, so `-q tests/` runs at `-qq`: the counts line is
         suppressed but `FAILED <nodeid>` lines are not. Parse the FAILED lines, never the counts
         line.
       - Standard library only. No new dependency.

    2. Capture the baseline with the dev venv and write `31-PYTEST-BASELINE.json` into the PHASE
       DIRECTORY (planning repo, not mics_core), with the same key set Phase 30 used: `captured`,
       `host`, `command`, `collected`, `passed`, `failed`, `errors`, `collect_ignored`,
       `collectable_modules`, `collectable_module_paths`, `failing_node_ids`, `note`. The `note`
       must state that this baseline is taken AFTER the dev-host dependency fix and BEFORE any code
       change, and must quote the pre-fix 179/203 numbers so the improvement is auditable.

    3. Write `tests/test_pytest_delta.py` driving `pytest_delta` with a synthetic baseline and a
       stubbed `subprocess.run`. Do NOT shell out to a real nested pytest run.
  </action>
  <verify>
    <automated>cd /home/ido/mics_core && /home/ido/.venvs/mics_core_dev/bin/python -m pytest -q tests/test_pytest_delta.py --tb=short && /home/ido/.venvs/mics_core_dev/bin/python tools/pytest_delta.py</automated>
  </verify>
  <done>`tests/test_pytest_delta.py` passes; `tools/pytest_delta.py` exits 0 on the unchanged tree printing `new failures: 0`; `31-PYTEST-BASELINE.json` exists with a non-empty `failing_node_ids` and a `note` quoting the 179/203 pre-fix numbers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Recording fake lgpio module and its contract test</name>
  <files>
    /home/ido/mics_core/tests/fakes/__init__.py
    /home/ido/mics_core/tests/fakes/fake_lgpio.py
    /home/ido/mics_core/tests/test_fake_lgpio_contract.py
    /home/ido/mics_core/conftest.py
  </files>
  <behavior>
    - `fake_lgpio` exposes every name the port will use, with the lgpio 0.2.2 signatures verified in
      `31-RESEARCH.md`: `gpiochip_open(n)`, `gpiochip_close(h)`,
      `gpio_get_chip_info(h) -> [status, lines, name, label]`, `gpio_claim_input`,
      `gpio_claim_output`, `gpio_claim_alert(h, gpio, eFlags, lFlags=0, notify_handle=None)`,
      `gpio_read`, `gpio_write`, `gpio_set_debounce_micros(h, gpio, us)`,
      `tx_pulse(h, gpio, on_us, off_us, offset_us, cycles)`, `tx_wave(h, gpio, pulses)`, `tx_busy`,
      `tx_room`, `tx_servo`, `callback(h, gpio, edge, func)` returning an object with `.cancel()`,
      the `pulse(group_bits, group_mask, pulse_delay)` record type, `i2c_open`, `i2c_close`,
      `i2c_write_byte_data`, `i2c_read_byte_data`, `i2c_read_i2c_block_data`, the constants
      `RISING_EDGE FALLING_EDGE BOTH_EDGES SET_PULL_UP SET_PULL_DOWN SET_PULL_NONE TX_PWM TX_WAVE`,
      and `class error(Exception)`.
    - Every call is appended to a `.calls` list as `(name, args, kwargs)`, so a test can assert the
      exact `tx_wave` pulse list a production call produced.
    - `gpio_get_chip_info` is driven by an injectable chip table, so one test can present the Pi 4
      layout (`pinctrl-bcm2711`, 58 lines), another the Pi 5 kernel-6.6.45 layout (`pinctrl-rp1` at
      index 0, 54 lines), another the kernel-6.12.96 layout (`pinctrl-rp1` at index 15).
    - Any function can be armed to raise `fake_lgpio.error`, so Pitfall 8 (an exception escaping a
      hardware callback) is testable.
    - A `fake_lgpio` pytest fixture installs the fake at `sys.modules['lgpio']` BEFORE the module
      under test is imported, and removes it on teardown.
  </behavior>
  <action>
    Build `tests/fakes/fake_lgpio.py` and the `fake_lgpio` fixture in the root `conftest.py`.

    Source of truth for the signatures is `31-RESEARCH.md` -> `## Code Examples` and
    `## Architecture Patterns`; those were read from the shipped lgpio 0.2.2 sources. Do NOT invent
    signatures and do NOT `pip install lgpio` on the dev host to introspect it — the fake exists
    precisely so the tests need neither the library nor the hardware.

    `tests/test_fake_lgpio_contract.py` asserts, per name, that `inspect.signature()` matches the
    documented parameter names and defaults. This is the guard that stops the fake drifting into a
    fiction the real library does not honour: a fake that disagrees with lgpio makes every Stage 3
    test a lie.

    `conftest.py` edit: add the fixture and leave the existing `collect_ignore` block and its
    explanatory comment intact (plan 02 revisits it). `conftest.py` is NOT in the protected
    manifest, so editing it is safe.

    Do not wire the fake into any production module here — nothing imports lgpio yet.
  </action>
  <verify>
    <automated>cd /home/ido/mics_core && /home/ido/.venvs/mics_core_dev/bin/python -m pytest -q tests/test_fake_lgpio_contract.py --tb=short && /home/ido/.venvs/mics_core_dev/bin/python tools/pytest_delta.py && python3 tools/check_tree_integrity.py --strict</automated>
  </verify>
  <done>`tests/test_fake_lgpio_contract.py` passes; the delta gate reports `new failures: 0`; `--strict` still prints `OK: ... 0 violations`.</done>
</task>

</tasks>

<verification>
1. `cd /home/ido/mics_core && git branch --show-current` -> `phase-31-modern-pi-platform`
2. `/home/ido/.venvs/mics_core_dev/bin/python -m pytest tests/ --tb=no | tail -1` -> a counts line,
   with failures strictly below 179
3. `/home/ido/.venvs/mics_core_dev/bin/python tools/pytest_delta.py` -> exit 0, `new failures: 0`
4. `python3 tools/check_tree_integrity.py --strict` -> exit 0
5. `31-PYTEST-BASELINE.json` parses as JSON and has a non-empty `failing_node_ids`
</verification>

<success_criteria>
- The phase has a branch, a runnable test suite, a frozen baseline and a committed delta gate.
- A later plan can prove "I broke nothing" in one command that does not depend on the dev host's
  ambient package set.
- Stage 3 port tests have a fake lgpio whose signatures are guarded against drift.
- Zero production source files changed. `--strict` unchanged at 0 violations.
</success_criteria>

<output>
After completion, create
`.planning/phases/31-mics-core-modern-pi-platform-bookworm-64-bit-python-3-11-lgpio-unattended-boot/31-01-SUMMARY.md`.
Record the exact post-fix pass/fail counts, the resolved `requirements-dev.txt` contents, and any
module that had to stay in `collect_ignore` with its reason.
</output>
