---
phase: 25-detector-derived-view-keys-visible-in-the-fda-editor
plan: 06
type: execute
wave: 4
depends_on: [02, 04, 05]
files_modified: []
autonomous: false
requirements: [DVK-03, DVK-04, DVK-05, DVK-06, DVK-08, DVK-09, DVK-10]
# DVK-03/04/05/06 appear here as their MANUAL verification home (task 3 steps 2-3), not as new
# implementation — they are implemented in plans 03/04/05. See 25-VALIDATION.md Manual-Only table.

must_haves:
  truths:
    - "Attaching MPR121 to a toolkit lets a researcher pick LICKER2 from the view-operand dropdown"
    - "That definition saves, preflights clean against pilot_raspberry_lior, and its transition fires on the rig"
    - "Touching the spout on MPR121 channel 4 writes LICKER4 — the write that was discarded in runs 480/481 now lands"
    - "Touching one spout moves only that spout's tracker"
    - "A definition naming a key the pilot cannot produce is REPORTED at preflight with the key named and the pilot's real channels listed — preflight is advisory by Phase 13 decision, so reporting, not prevention, is the bar"
    - "A stored key outside the derived set is still selected, marked (unknown), and survives a save/reopen round trip"
    - "A trigger action that raises names the key in an error log and a TRIGGER_ACTION_ERROR event, instead of 'No valid trigger for {pin}' at DEBUG"
  artifacts:
    - path: ".planning/phases/25-detector-derived-view-keys-visible-in-the-fda-editor/25-HARDWARE-VALIDATION.md"
      provides: "the rig evidence for DVK-08/09/10, in the shape of 24-HARDWARE-VALIDATION.md"
  key_links:
    - from: "pilot_hardware_config MPR121 config"
      to: "the Pi's LICKER1..LICKER4 trackers"
      via: "first_channel: 1 set through the plan-05 affordance, shipped in prefs_hardware, consumed by check_for_detectors"
      pattern: "first_channel"
    - from: "the FDA editor's view-operand dropdown"
      to: "a firing transition on the rig"
      via: "the DVK-08 success gate, end to end"
      pattern: "LICKER"
---

<objective>
Deploy the phase and prove it on the real rig. This is the only plan that touches the Pi
filesystem or the running system.

Purpose: every other plan in this phase is verified by unit tests and a type-check. The three
claims that matter — the editor offers the keys, the keys are the ones the Pi builds, and a wrong
key now fails loudly — can only be settled on hardware. Phase 24's experience is the argument:
five of its seven post-execution defects lived in the UI↔API seam and surfaced only by using the
editor, which is why the UI round-trip runs first here, not last.

Output: a deployed, restarted system and `25-HARDWARE-VALIDATION.md`.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
@.claude/skills/pi-deploy/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/24-trigger-assignment-action-lists/24-HARDWARE-VALIDATION.md
@.planning/phases/25-detector-derived-view-keys-visible-in-the-fda-editor/25-02-SUMMARY.md
@.planning/phases/25-detector-derived-view-keys-visible-in-the-fda-editor/25-03-SUMMARY.md
@.planning/phases/25-detector-derived-view-keys-visible-in-the-fda-editor/25-04-SUMMARY.md
@.planning/phases/25-detector-derived-view-keys-visible-in-the-fda-editor/25-05-SUMMARY.md

<pi_rules>
**Absolute. A step that violates one of these is wrong even if it would work.**

1. **NEVER run git in `/home/ido/pi-mirror`** — not even `status`. Byte-identity is proved with
   `diff <(ssh -i ~/.ssh/pi_mics pi@132.77.72.28 'cat ~/Apps/mice_interactive_home_cage/<path>') /home/ido/pi-mirror/<path>`.
2. **Never start or stop the pilot process. Never run any Python file on the Pi.** Hand the user
   the exact command and wait. The Pi pytest run is USER-RUN.
3. Deploy **only** the files plan 02 edited, via scoped `rsync --relative`. Never the whole
   mirror, never `--delete`.
4. `autopilot/autopilot/hardware/i2c.py` stays byte-identical.
5. Do **not** pull Pi → mirror before deploying, and do not pull at any point in this plan.
   Plan 02's edits are in the mirror and a pull would clobber them. Mirror-Pi identity was
   established **before** plan 02's first edit, by `diff` rather than by `rsync` (plan 02 pi_rules
   #5 / task 1 step 0) — read that result out of `25-02-SUMMARY.md` and state it when presenting
   task 2. If plan 02 did not record it, stop and ask the user before deploying; do not
   reconstruct it with a pull.
</pi_rules>

<rig_state>
Verified 2026-07-29 — confirm again at execution time, do not assume.

| Thing | Value |
|---|---|
| Pilot | `pilot_raspberry_lior` (pilot_id 1) |
| Toolkit | 100, `hardware_module_ids = [1,2,3,5,6,7,8]` |
| Module 7 | `MPR121` → `Touch_Detector`, config `{"name":"MPR121","num_detectors":4,"device_name":"LICKER","address":true}` |
| Module 8 | `TOUCH_INT` → `Digital_In`, pin 8 |
| Task def 186 | the canonical detector-write payload from 24-07 |
| Wiring | four spouts on MPR121 channels **1–4**; channel 0 is unwired |
| DB access | `docker exec mics_db psql -U mics_user -d mics_db` |

**Do not touch task definitions 181, 185 or 187.** All three hold state-body hardware actions with
an empty `method` and cannot be re-saved until fixed (TRIGA-16 blast radius); **185 is Gili's**.
Create a NEW task definition for this proof, or work on 186.

Runs of task def 186 last ~20.1 s (5 trials × ~4 s) and then stop — runs 476/477/479 logged zero
touch events purely because touching began after the task ended. Raise the protocol step's trial
limit before testing, or start touching immediately.
</rig_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deploy and rebuild</name>
  <files>(no repo files modified — deployment and rebuild only)</files>
  <action>
**a. Pi deploy.** Take the exact file list from `25-02-SUMMARY.md`. It should be:
```
autopilot/autopilot/tasks/fda_vocabulary.py
autopilot/autopilot/tasks/mics_task.py
autopilot/autopilot/tasks/task.py
tests/test_detector_view_keys.py
tests/test_check_for_detectors.py
tests/test_execute_trigger_guard.py
```
Deploy with one scoped `--relative` push (no `--delete`, no whole-mirror source):
```bash
rsync -avz --relative -e "ssh -i ~/.ssh/pi_mics" \
  /home/ido/pi-mirror/./autopilot/autopilot/tasks/fda_vocabulary.py \
  /home/ido/pi-mirror/./autopilot/autopilot/tasks/mics_task.py \
  /home/ido/pi-mirror/./autopilot/autopilot/tasks/task.py \
  /home/ido/pi-mirror/./tests/test_detector_view_keys.py \
  /home/ido/pi-mirror/./tests/test_check_for_detectors.py \
  /home/ido/pi-mirror/./tests/test_execute_trigger_guard.py \
  pi@132.77.72.28:~/Apps/mice_interactive_home_cage/
```

**b. Prove what landed, without git.** For each deployed file:
```bash
diff <(ssh -i ~/.ssh/pi_mics pi@132.77.72.28 'cat ~/Apps/mice_interactive_home_cage/<path>') /home/ido/pi-mirror/<path>
```
must print nothing. Run the same check on
`autopilot/autopilot/hardware/i2c.py` — it must also print nothing, proving the off-limits file is
untouched on both sides.

**c. Rebuild the backend and the SPA.**
```bash
docker compose up --build -d api web_ui
docker exec mics_api python3 -m pytest /app/tests/ -q
cd web_ui/react-src && npm run test:unit && npx tsc --noEmit
```
Confirm the served bundle is plan 04's build by comparing the `TaskEditor-<hash>.js` /
`HardwareCheckModal-<hash>.js` filenames recorded in `25-04-SUMMARY.md` / `25-05-SUMMARY.md`
against `ls web_ui/static/react/`. **Never** try to prove a React rebuild by grepping `main.js` —
the output is code-split.

**d.** Confirm live API shape before handing over — **both** read routes, because
`TaskEditor.tsx` uses the by-name one:
```bash
docker exec mics_web_ui python3 -c "
import json,os,urllib.request
def get(p):
    r=urllib.request.Request('http://api:8000'+p,headers={'Authorization':'Bearer '+os.environ['MICS_API_TOKEN']})
    return json.load(urllib.request.urlopen(r))
for label,d in (('by-id',get('/api/toolkits/100')),
                ('by-name',get('/api/toolkits/by-name/source_less_toolkit')[0])):
    print(label, json.dumps(d.get('detector_channels')))
"
```
Both must show the MPR121 entry. Run it from `mics_web_ui`: `MICS_API_TOKEN` is set only there
(`mics_api` and the host do not have it), and `api` is reachable as `http://api:8000` on the
compose network.
  </action>
  <verify>
    <automated>docker exec mics_api python3 -m pytest /app/tests/ -q && cd web_ui/react-src && npm run test:unit && npx tsc --noEmit</automated>
  </verify>
  <done>All six Pi files byte-identical on both sides; `i2c.py` byte-identical; plan 02's pre-edit identity result read out of `25-02-SUMMARY.md`; backend suite green; React unit tests green; the served bundle hashes match plan 04/05's build; `detector_channels` visible on **both** `/api/toolkits/100` and `/api/toolkits/by-name/source_less_toolkit`.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: user runs the Pi test suite and restarts the pilot</name>
  <files>none (checkpoint — no file is edited by this task)</files>
  <action>
Agent side, before pausing: re-run the byte-identity `diff` for all six deployed files and for
`i2c.py`, and confirm the pilot is currently connected. Report those results as part of
presenting the checkpoint — a green Pi suite against a Pi that did not receive the files proves
nothing. Then hand over the two commands in `<how-to-verify>` and wait.

Do **not** run pytest on the Pi. Do **not** start or stop the pilot. Hand over and wait.
  </action>
  <what-built>
Three Pi files changed and deployed: `fda_vocabulary.py` (the shared derivation),
`mics_task.py` (`check_for_detectors` honours `first_channel`), `task.py` (`execute_trigger`'s
`except KeyError` narrowed to the lookup, per-callback errors contained and reported).

Three test modules deployed alongside them. `autopilot` cannot be imported on the dev host
(`npyscreen`/`board`/`busio`/`PySide2`/`pigpio` missing), so everything except
`test_detector_view_keys.py` has only ever run there.

**Note:** the Phase 24 suite (~60 tests, including `test_check_for_detectors.py` and
`test_log_action_values.py`) has still never been run anywhere — STATE.md Outstanding item 1.
This run clears both phases at once.
  </what-built>
  <how-to-verify>
Run these two on the Pi yourself. Claude will not run Python on the Pi and will not start or stop
the pilot.

1. Full Pi test suite:
```bash
cd ~/Apps/mice_interactive_home_cage && python3 -m pytest tests/ -q
```
Expected: green. Paste the tail of the output. If `test_check_for_detectors.py` or
`test_execute_trigger_guard.py` fail, stop here — those are this phase's core changes and the rig
proof below would be meaningless.

2. Restart the pilot process so the new `mics_task.py` / `task.py` are loaded.
  </how-to-verify>
  <resume-signal>Paste the pytest tail and type "pilot restarted", or describe the failures.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: rig proof — DVK-08, DVK-09, DVK-10</name>
  <files>none (checkpoint — no repo file is edited; `25-HARDWARE-VALIDATION.md` is written after sign-off)</files>
  <action>
Agent side, before pausing:
1. Confirm the pilot is connected and running the deployed code (`diff` against the mirror for
   `mics_task.py` and `task.py`; ask the user to restart if task 2's restart has not happened).
2. Confirm **both** `GET /api/toolkits/100` and `GET /api/toolkits/by-name/source_less_toolkit`
   return `detector_channels` (the by-name route is the one the editor loads from — a green
   by-id check alone proves nothing about the pickers), and that `api`/`web_ui` are serving the
   rebuilt images with plan 04/05's bundle hashes.
3. Re-read the live rig state — pilot id, toolkit modules, the MPR121 config row, and which task
   definitions exist — and correct the `<rig_state>` table above if anything moved.
4. Confirm task definitions 181/185/187 are untouched.
Report all of that when presenting the checkpoint. If any check fails, fix it before handing
over — 24's checkpoints were repeatedly run against stale code and that cost more time than the
checks do.

Then hand over the numbered steps in `<how-to-verify>` and wait. Claude does not touch the rig,
does not start or stop the pilot, and does not run Python on the Pi.
  </action>
  <what-built>
The full phase: backend derivation of detector view keys, those keys as pickable options in the
FDA editor, per-pilot resolution at preflight, the declarable channel range, and the narrowed
trigger guard.
  </what-built>
  <how-to-verify>
Do these in order. Checkpoint 1 runs **first** deliberately: five of Phase 24's seven
post-execution defects were in the UI↔API seam and only the editor surfaced them.

**1 — declare the real wiring (DVK-09 data change).**
Open `/react/pilots/pilot_raspberry_lior/...` hardware config → MPR121. Set `first_channel` to
`1` using the new field. Save.
- The live key preview under the field should read `LICKER1, LICKER2, LICKER3, LICKER4`.
- Confirm what was written:
  `docker exec mics_db psql -U mics_user -d mics_db -c "SELECT config FROM pilot_hardware_config WHERE pilot_id=1 AND name='MPR121';"`
  → must contain `"first_channel": 1` **and still contain** `num_detectors`, `device_name`,
  `address`, `name`.

**2 — the editor offers the keys (DVK-03/04, the DVK-08 success gate).**
Open the FDA editor on a task definition backed by toolkit 100 (use 186, or create a new one —
**do not touch 181/185/187**).
- Open a transition's condition. In the view-operand dropdown, confirm a group labelled something
  like *"MPR121 channels"* listing `LICKER1…LICKER4`, **separate** from the hardware group that
  lists `MPR121` itself.
- Confirm `LICKER0` is **not** offered (the config now starts at channel 1).
- Confirm the flag dropdown contains **no** `LICKER*` entry (DVK-07).
- Declare a transition whose condition is `LICKER2 == 1`. Save. It must save cleanly (no 422).
- Open the trigger's `view` action and confirm the `key_template` field offers pickable
  suggestions — `{device_name}`, the declared variables, and the literal channel keys — and is
  still typeable.
- **DVK-05 round trip — the only behavioural proof of it, and it is nearly free here.** Step 1 set
  `first_channel: 1`, so any definition still storing `LICKER0` now names a key outside the derived
  set. Open such a definition (task def 186 stores the canonical payload; if none stores a literal
  `LICKER0`, set one operand to `LICKER0` in a scratch copy first, save while the config still
  reads `first_channel: 1`, and reopen).
  1. The stored value must still be **selected** in the view-operand dropdown — not blanked, not
     silently rewritten to `""` (`ConditionBuilder.tsx:85`'s keep-current-value escape).
  2. Its option label must be marked **"(unknown)"**.
  3. **Save without touching that field, reopen, and confirm `LICKER0` is still there** — the
     round trip is the assertion; a value that renders but is dropped on save fails DVK-05.
  4. Confirm via the API that the stored JSON still contains `LICKER0` verbatim (from
     `mics_web_ui`, the only place the token is set):
     ```bash
     docker exec mics_web_ui python3 -c "
     import json,os,urllib.request
     r=urllib.request.Request('http://api:8000/api/task-definitions/<id>',headers={'Authorization':'Bearer '+os.environ['MICS_API_TOKEN']})
     print(json.dumps(json.load(urllib.request.urlopen(r))).count('LICKER0'))
     "
     ```
     must print a non-zero count.
  Record the result explicitly in `25-HARDWARE-VALIDATION.md` under DVK-05.

**3 — preflight resolves against this pilot (DVK-06 positive + negative).**
- Start a session for the definition from step 2 on `pilot_raspberry_lior`. Preflight must pass
  with **no** view-key issue.
- Now edit a copy to name `LICKER0` (or `LICKER9`) and try to start it. The hardware-check modal
  must show a `view_key_unresolved` entry naming the key and listing `LICKER1…LICKER4`. Cancel.
- **Then re-check the config was not damaged:** re-run the psql query from step 1. The MPR121
  config must be unchanged. (The modal's save loop PUTs a config per issue; plan 05 excluded this
  issue kind, and this is the check that it really did.)

**4 — the rig (DVK-08 + DVK-09).**
Raise the protocol step's trial limit first — runs of 186 end after ~20 s and 24's runs 476/477/479
logged nothing purely because touching started too late.
Run on `pilot_raspberry_lior` and touch **each of the four spouts, one at a time**.
- Every touch must move the tracker matching the electrode: spout on channel 4 → `LICKER4`.
  **This is the event class that was silently discarded in runs 480/481.**
- Touching one spout must move **only** that tracker — assert the cross-talk negative, do not
  assume it.
- The transition declared on `LICKER2` must fire when that spout is touched.
- Confirm in ES / the pilot log, and record the counts the way `24-HARDWARE-VALIDATION.md` §2b
  does (firings, writes, per-channel distribution).

**5 — a bad key is now loud (DVK-10).**

**Construction matters — the obvious version is blocked by the preflight this phase just built.**
A literal `key_template: "LICKER9"` contains no `{` tokens, so plan 03's resolver treats it as an
operand, reports `view_key_unresolved`, and preflight returns `ok: false` — you would be testing
step 3's modal again, not the runtime guard. Use the construction that reaches the Pi:

> Keep the canonical `key_template: "{device_name}{pin_number}"` with `source_ref: MPR121`
> (statically unresolvable **by design** — plan 03 deliberately reports nothing for it), and
> temporarily set the MPR121 config's `first_channel` to `2` on the hardware-config page. The Pi
> then builds `LICKER2…LICKER5`, and touching the spout wired to channel 1 resolves to a
> nonexistent `LICKER1` at run time. Restore `first_channel: 1` afterwards.

(The literal-`LICKER9` route also works if you prefer it, but only by clicking **Save & Start**
through the preflight modal — preflight is advisory, and after plan 05 task 1c that click writes
no hardware config. Say which route you took.)

Start a run and touch the spout.
- The pilot log must contain an **error** naming the callback and the `KeyError`'s own message
  (which names the missing key — `LICKER1` in the `first_channel: 2` construction, `LICKER9` in the
  literal one — and lists the available keys), plus a `TRIGGER_ACTION_ERROR` event.
- It must **not** say `No valid trigger for TOUCH_INT`.
- The run must keep working afterwards — other trigger writes still land, the worker thread
  survives.
- Restore the good `key_template` **and** `first_channel: 1` afterwards, and re-read the
  `pilot_hardware_config` row to confirm the restore landed. The rig must not be left declaring a
  channel range it is not wired for.

Record everything in
`.planning/phases/25-detector-derived-view-keys-visible-in-the-fda-editor/25-HARDWARE-VALIDATION.md`,
following `24-HARDWARE-VALIDATION.md`'s structure (what is proven, defects found, exact deployed
and DB state).
  </how-to-verify>
  <resume-signal>Type "approved" with the observed counts, or describe what failed at which step.</resume-signal>
</task>

</tasks>

<verification>
1. All six deployed Pi files byte-identical between Pi and mirror; `i2c.py` byte-identical.
2. Pi suite green (user-run). Backend suite green. React unit tests green. `tsc --noEmit` clean.
3. `pilot_hardware_config` MPR121 row carries `first_channel: 1` alongside its original keys —
   re-checked at the END of task 3 too, after step 5's temporary `first_channel: 2` is restored.
4. DVK-05 round trip recorded: a stored key outside the derived set was selected, labelled
   "(unknown)", and was still present verbatim in the definition JSON after a save/reopen.
5. Rig evidence recorded in `25-HARDWARE-VALIDATION.md` with per-channel counts.
6. STATE.md Outstanding item 4 (`process_queue` has no exception handler) is still listed as
   **open** — this phase did not fix it and must not be recorded as having done so.
</verification>

<success_criteria>
DVK-08's gate, verbatim: attach MPR121 to a toolkit → the FDA editor's view-operand dropdown lists
the detector channels → a transition declared on `LICKER2` saves, passes preflight against the
configured pilot, and fires on the rig. Negative case: a definition whose key the pilot cannot
produce is caught at preflight with a readable message, not at session start.

**Preflight is ADVISORY, and that is correct — do not record it as a defect.** `handleStart`
(`HardwareCheckModal.tsx:309-333`) is the only forward path and always calls `onStart()`; both
callers treat preflight as non-blocking (`PilotSessions.tsx:101`, `SubjectSessions.tsx:108`), which
is Phase 13's deliberate decision so a broken preflight endpoint can never stop the rig. DVK-06's
"fails before START" means **reported before START, with the key named**, not **prevented**. If the
tester clicks through the modal and the session starts anyway, that is the designed behaviour; the
only failure at step 3 is a missing, unreadable, or config-destroying issue row.

Plus, from this phase's own rig evidence: channel 4's writes land in `LICKER4`, a stored unknown
key survives a save round trip, and a failing trigger action is reported as an error naming the
key.
</success_criteria>

<output>
After completion, create
`.planning/phases/25-detector-derived-view-keys-visible-in-the-fda-editor/25-06-SUMMARY.md`
and `25-HARDWARE-VALIDATION.md`.
</output>
