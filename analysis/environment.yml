name: mics-analysis
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - requests
  - numpy
  - matplotlib
  - pandas       # level1_characterization.py (master trial table + summaries)
  - seaborn      # optional; level1 figures fall back to matplotlib if absent
