---
phase: 30-pi-repo-cleanup
plan: 06
type: execute
wave: 4
depends_on: ["30-01", "30-04", "30-05"]
files_modified:
  - /home/ido/pi-mirror/autopilot/autopilot/networking/station.py
  - /home/ido/pi-mirror/autopilot/autopilot/networking/message.py
  - /home/ido/pi-mirror/autopilot/autopilot/hardware/gpio.py
  - /home/ido/pi-mirror/autopilot/autopilot/tasks/task.py
  - /home/ido/pi-mirror/autopilot/autopilot/core/pilot.py
  - /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-06-ledger.md
autonomous: true
requirements: [HYG-11, HYG-14, HYG-12]
must_haves:
  truths:
    - "The NTP enable and clock-freeze call sites in pilot.py are still commented out, byte-identical to their pre-phase text, and were NOT swept away as dead comments"
    - "l_start no longer dispatches into the CHILDREN registry entry plan 05 deleted — the START path carries no orphaned string-keyed lookup"
    - "A silent handshake failure now produces a logged warning instead of a bare print"
    - "The half-disabled local-HDF5 subsystem is gone whole — implementation AND the docstring that still advertises it"
    - "Event_Dispatcher.py's _dropped_no_clock and _dropped_on_send counters are untouched"
    - "No file on the HYG-13 protect-list was edited by the comment sweep"
  artifacts:
    - path: "/home/ido/pi-mirror/autopilot/autopilot/core/pilot.py"
      provides: "Pilot agent with no HDF5 residue; clock block left commented, untouched, and exempt from the sweep"
      contains: "# self.enable_ntp_and_wait()"
    - path: "/home/ido/pi-mirror/autopilot/autopilot/networking/station.py"
      provides: "Station with a watchdog that reports"
      contains: "_handshake_watchdog"
    - path: "/home/ido/pi-mirror/autopilot/autopilot/networking/Event_Dispatcher.py"
      provides: "Phase 25 guarded tick read + drop counters — must be byte-identical to baseline"
      contains: "_dropped_no_clock"
  key_links:
    - from: "autopilot/autopilot/core/pilot.py run_task"
      to: "# self.enable_ntp_and_wait() / # self.disable_ntp()"
      via: "commented call sites preserved verbatim at ~:1137-1148, methods still live at :498/:514"
      pattern: "#\\s*self\\.(enable_ntp_and_wait|disable_ntp)\\(\\)"
---

<objective>
Remove the 244 lines of dead commented-out code (HYG-11) and apply the two restorations plus the
set-removal (HYG-14), while leaving `Event_Dispatcher.py` alone (HYG-12).

Purpose: this is the only plan that edits *inside* surviving files, and it is the one where a
careless sweep does real damage. Two of the three edits here are **additions of live behaviour**,
not deletions: a confirmed clock-discipline regression is restored, and a silenced handshake
watchdog starts reporting again. On a rig whose dominant failure mode is silence, both matter.
Output: a tree with no dead commented-out code, restored clock discipline, a watchdog that logs,
and no half-disabled HDF5 subsystem.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-pi-repo-cleanup/30-CONTEXT.md
@.planning/phases/30-pi-repo-cleanup/30-VALIDATION.md
@.planning/phases/30-pi-repo-cleanup/30-01-SUMMARY.md
@.planning/phases/30-pi-repo-cleanup/30-04-SUMMARY.md

<pi_rules>
ABSOLUTE (STATE.md:1727-1745). Override anything below:
- Edit ONLY `/home/ido/pi-mirror`. **No git command in that directory — not even `status`.**
- Never `rsync --delete`. This plan does not rsync. **This plan does not deploy anything.**
- Never start/stop the pilot. Never run Python on the Pi.
- `autopilot` cannot be imported on this host; `py_compile` / `compileall` is the ceiling.
</pi_rules>

<this_plan_carries_the_restorations_do_not_let_a_bad_gate_undo_them>
## Read this before you fix any red gate in this plan.

Two of the three HYG-14 items here are **restorations of live behaviour**: the NTP /
clock-freeze call sites in `pilot.py`, and the handshake-watchdog logging in `station.py`. They
are the only *additive* work in the whole phase.

That makes this plan the worst possible place for a mis-specified assertion, because the obvious
way to make a red gate go green in a removal phase is to remove something — and here the nearest
removable things are the two restorations. **If a gate in this plan goes red, the answer is never
"revert the restoration".** Two specific traps have already been found and pre-empted below:

1. `! grep -q 'open_file'` would fail on `pilot.py:1125`, a **docstring line** in `run_task` that
   the original HDF5 set-removal did not cover. Task 1 now removes that line as part of the set —
   see `<HYG_14_verified_sites>`.
2. `python3 -m pytest -q` chained with `&&` would fail unconditionally: the recorded baseline is
   179 failed / 181 passed over 19 collectable modules, and 2 modules cannot be collected at all.
   Task 2 uses the delta command from `30-01-SUMMARY.md` instead.

If you hit a third such trap, report it. Do not satisfy it by deleting live code.
</this_plan_carries_the_restorations_do_not_let_a_bad_gate_undo_them>

<HYG_12_hard_boundary>
## `autopilot/autopilot/networking/Event_Dispatcher.py` is NOT edited by this plan.

Phase 25 left debug prints deployed on the live Pi in `task.py`, `mics_task.py` **and**
`Event_Dispatcher.py`. It is tempting to sweep all three. **Do not.** The `Event_Dispatcher.py`
change is not only prints — it also carries a guarded tick read and the `_dropped_no_clock` /
`_dropped_on_send` counters, which are real fixes
(`25-HARDWARE-VALIDATION.md:365-375`). Verified locations in the current file:

```
Event_Dispatcher.py:25   self._dropped_no_clock = 0
Event_Dispatcher.py:26   self._dropped_on_send  = 0
Event_Dispatcher.py:55-58   increments + throttled report on send-drop
Event_Dispatcher.py:79-84   increments + throttled report on no-clock drop
```

The file is on the HYG-13 protect-list with a recorded sha256 baseline. **If you modify it, the
guard fails `--strict` as "protected file modified" and the plan is wrong, not the guard.**

Related: every pi-mirror edit from Phase 25 is an **uncommitted working-tree change**. Since no
git command may be run here, you cannot tell "uncommitted" from "stray" by inspection — so
treat every Phase-25-era construct as live unless a requirement names it for removal.
</HYG_12_hard_boundary>

<HYG_14_verified_sites>
Verified in `/home/ido/pi-mirror` 2026-08-10. Line numbers are pre-edit and will shift.

**HOLD 0 (was RESTORE 1) — `core/pilot.py`, the clock block (currently ~:1136-1148).
DEFERRED BY THE USER, 2026-08-10. Do NOT uncomment. Do NOT delete. Leave byte-identical.**

The technical finding is unchanged and still stands: this is a **regression, not a decision** —
the methods stay live at `:498` (`enable_ntp_and_wait`) and `:514` (`disable_ntp`), the
surviving comment reads "Freeze wall clock so it never jumps during the task", and NTP stepping
the wall clock mid-task is material on a rig that timestamps behavioural events to the
millisecond. **The user has taken this on explicitly and will deal with it separately.** That is
their call; this plan does not re-litigate it and does not restore the calls.

**The active risk this creates is the opposite of the original one.** These two lines are
commented-out code sitting in a file whose *other* job in this task is a ~244-line sweep of dead
commented-out code. Left unguarded, the sweep is exactly what deletes them — and unlike leaving
them commented, deletion is not reversible from the file itself. **Both lines are on the sweep
exemption list** (see the sweep rules below) and the verify gate asserts they are still present
in commented form. Record them in the ledger under `Deferred by the user`, not under `Removed`.

Current text — this is also the *required post-condition*, verbatim:
```python
        # ---- CLOCK SETUP ----
        # self.enable_ntp_and_wait()
        self.pi = pigpio.pi(sync_ticks=True)

        if not self.pi.connected:
            raise RuntimeError("Failed to connect to pigpiod")

        # Align pigpio tick → system time
        self.pi.synchronize()

        # Freeze wall clock so it never jumps during the task
        # self.disable_ntp()
```
**Leave every line of that block exactly as it stands** — both `#`-prefixed call sites, the
`# ---- CLOCK SETUP ----` banner and the "Freeze wall clock…" comment that explains what the
disabled call was for. Deleting the explanatory comment while keeping the disabled call would
leave the next reader with no way to know why it is there. **Do not uncomment, do not delete,
do not reflow.**

**RESTORE 2 — `networking/station.py`, `_handshake_watchdog` (currently :1330-1350).** The
">21s no PING from orchestrator" and "handshake retry failed" warnings are commented out and
replaced by a live bare `print("")`. Current text:
```python
    def _handshake_watchdog(self):
        now = time.time()

        # logger = getattr(self, "logger", None)

        if (
            self._handshake_callback
            and now - self._last_ping_from_orch > 21
        ):
            try:
                # if logger:
                #     logger.warning("No PING from orchestrator for >21s, retrying HANDSHAKE")
                self._handshake_callback()
            except Exception:
                print("")
                # if logger:
                #     logger.exception("Handshake retry failed")
```
Uncomment the `logger = getattr(self, "logger", None)` line and both guarded logger calls; drop
the bare `print("")`. Keep the `if logger:` guard — `station.py` is instantiated in contexts
where `logger` may be absent, which is presumably why the guard was written that way.
Rationale (30-CONTEXT.md): a silent handshake failure on a running rig is invisible — the same
class of defect as the missing `TASK_ERROR` emitter.

**REMOVE AS ONE SET — `core/pilot.py` local HDF5.** ES is the sole data path (user-confirmed
2026-08-10), so the half-disabled subsystem goes whole rather than leaving a live method nobody
calls. **Four sites, not three** — the fourth was missed by the original scope and is the reason
the `open_file` gate would otherwise be unsatisfiable:
- `open_file()` — 55 lines, currently spanning roughly `:999`–`:1054` (it contains
  `tables.open_file(local_file, mode='a')` at `:1016`, a second `tables.open_file(..., mode='w')`
  at `:1021`, and `return h5f, None, None` at `:1054`). Locate by `def open_file` and the
  following `def`.
- The `self.h5f` cleanup at `:640-641`:
  ```python
        # TODO: Cohere here before closing file
        if hasattr(self, 'h5f'):
            self.h5f.close()
  ```
- The ~23 commented lines at `:1174-1261` that reference the same subsystem, including
  `:1175  # h5f, table, row = self.open_file()`.
- **`:1125`, inside `run_task`'s docstring** (verified — the docstring opens at `:1122` under
  `def run_task` at `:1121`):
  ```
        Opens a file with :meth:`~.autopilot.open_file` , then
        continually calls `task.stages.next` to process stages.
  ```
  This line **documents behaviour that will no longer exist**, and it sits outside both the
  `:999`–`:1054` method and the `:1174`–`:1261` block, so neither of the other removals reaches
  it. Delete the `Opens a file with :meth:`~.autopilot.open_file` , then` line and fix up the
  sentence that follows so the docstring still reads correctly (it becomes
  `Continually calls `task.stages.next` to process stages.`). Removing stale documentation of a
  removed subsystem is the same edit, not scope creep — and leaving it turns
  `! grep -q 'open_file' pilot.py` into a red gate on the plan that carries the two
  **restorations**, which is exactly the pressure that gets restorations reverted.
- Any now-unused `import tables` left behind. Check whether `tables` is used elsewhere in
  `pilot.py` before removing the import.

**Also removed regardless of any other decision:** `core/pilot.py:49`,
`#os.chdir('/home/liors/Apps/mice_interactive_home_cage/autopilot')` — hardcodes another
person's home directory. The `print(os.getcwd())` on the line above is live code; HYG-11 does not
cover it and it is not commented out, so leave it (note it in the ledger as a candidate for a
later cleanup, not this one).
</HYG_14_verified_sites>

<HYG_11_scope>
244 lines, concentrated in the **upstream** layers, not the lab's. Named counts from the audit:
- `networking/station.py` — 52, **including `:623-651`**, which carries the original author's
  live note: *"all of the below should be removed but i am paranoid… so i am just commenting for
  now"*. That note is itself the justification; remove the block and the note.
- `networking/message.py` — 50, entirely Python-2-era residue.
- `hardware/gpio.py` — 28.
- `tasks/task.py` — 28, three successive superseded trigger-dispatch implementations.
- `core/pilot.py:49` — the hardcoded home directory (see above).
- 11 smaller files — find them; do not guess. `mics_task.py` yields only **11** lines and is the
  cleanest large file in the tree, contrary to expectation.

**Explicitly EXCLUDED from this sweep** (these are the ~122 + ~72 lines the audit set aside):
- ~122 lines classified as disabled *behaviour* rather than superseded implementation. Three of
  the four were resolved DELETE and went with their host files in plans 04 and 05
  (`0x3f`, IR1–IR7, `OG_TRIGGER` — note the last `0x3f` site went with `RecordingBox.py` in plan
  05, not plan 04); the fourth is the watchdog, RESTORED above.
- ~72 lines referencing symbols that are **still live**. A commented line naming a symbol that
  still exists is not obviously dead — leave it and record it.
- `Event_Dispatcher.py` — see the hard boundary above.
- **Every file on the HYG-13 protect-list.** See `<the_protect_list_is_off_limits>`.

**Discrimination rule.** A commented block is removable only if it is a *superseded
implementation*: the thing it did is now done by live code elsewhere, or by nothing because the
capability was withdrawn deliberately. A commented block that disables a *behaviour* nothing else
provides is a held toggle and stays. When you cannot tell, keep it and record it in the ledger as
"undecided" — this phase has already burned four toggles' worth of user time on exactly this
distinction, and a wrong deletion here is unrecoverable without git.
</HYG_11_scope>

<the_protect_list_is_off_limits>
## Task 2 may touch up to 11 files this plan's `files_modified` does not name. Bound it.

Task 2's action says "the 11 smaller files — identify them from your scan", which by construction
cannot be pre-listed in `files_modified`. That is acceptable for a comment sweep, but it means the
blast radius is defined by a *rule* rather than by a list, so the rule has to be tight:

**No file on `tools/tree_protect_list.json`'s `protected` array may be edited by this plan.**
All 30 of them carry a sha256 baseline recorded in Wave 0, and any edit — even removing a genuinely
dead comment — fails `check_tree_integrity.py --strict` as "protected file modified" and breaks
HYG-13's zero-drift manifest diff in plan 08. That set includes, and is not limited to:
`Event_Dispatcher.py`, `log_value.py`, `fda_vocabulary.py`, the five `external_hardware*.py`
files, `autopilot/LICENSE`, and **all 21 modules under `tests/`**.

Before editing any file the scan surfaces, check it against the protect-list. Cheapest form:
```
python3 -c "import json,sys; p=json.load(open('/home/ido/pi-mirror/tools/tree_protect_list.json'))['protected']; sys.exit(0 if sys.argv[1] in p else 1)" <repo-relative-path> && echo PROTECTED-SKIP
```
Every file Task 2 actually edits beyond the four named in `files_modified` must be listed by path
in the ledger fragment, with its removed-line count. That list is the after-the-fact
`files_modified` and plan 08 reads it when it diffs the manifest.
</the_protect_list_is_off_limits>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the HYG-14 restorations and the HDF5 set-removal</name>
  <files>
    /home/ido/pi-mirror/autopilot/autopilot/core/pilot.py,
    /home/ido/pi-mirror/autopilot/autopilot/networking/station.py
  </files>
  <action>
    Restorations first, deletions second — if something goes wrong mid-task, a tree with restored
    behaviour and un-swept comments is strictly better than the reverse.

    1. **Do NOT touch the clock block** in `core/pilot.py` — see `<HYG_14_verified_sites>`
       HOLD 0. The user deferred this restoration on 2026-08-10 and will handle it separately.
       Your only job here is to make sure the sweep in step 4 does not eat it: add
       `# self.enable_ntp_and_wait()`, `# self.disable_ntp()`, `# ---- CLOCK SETUP ----` and the
       `# Freeze wall clock so it never jumps during the task` line to the sweep exemption list
       **before** running the sweep, and confirm afterwards that all four lines are still there
       byte-identical. Do not uncomment them. If you find them already uncommented or missing,
       stop and report — that means something outside this plan changed the file.

    2. **Restore the watchdog** in `networking/station.py` exactly as described in RESTORE 2.
       Uncomment the `logger` binding and the two guarded logger calls; delete the bare
       `print("")`.

    3. **Remove the HDF5 set** from `core/pilot.py` — **all four sites**:
       (a) `open_file()` (~`:999`–`:1054`);
       (b) the `self.h5f` cleanup at `:640-641`;
       (c) the ~23 commented lines at `:1174-1261`;
       (d) **`:1125`, the `run_task` docstring line** `Opens a file with
       :meth:`~.autopilot.open_file` , then` — plus the sentence fix-up so the docstring still
       reads correctly. Site (d) is the one the original scope missed; without it the
       `! grep -q 'open_file'` gate cannot pass and the pressure lands on the restorations.
       Also remove any now-unused `import tables`.

       Confirm `open_file` has no surviving *caller* anywhere in the tree first
       (`grep -rn 'open_file' /home/ido/pi-mirror --include=*.py`) — its only call site was
       already commented out, but verify rather than assume. `tables.open_file` inside the method
       being deleted does not count as a caller, and the `:1125` docstring mention is
       documentation, not a call.

    4. **Remove `core/pilot.py:49`**, the `#os.chdir('/home/liors/...')` line. Leave the
       `print(os.getcwd())` above it.

    5. **Remove the orphaned `'child'` dispatch branch at `core/pilot.py:589-592`** — handed off
       by plan 05, which deleted the `CHILDREN` member of `REGISTRIES` that this branch dispatches
       into. Read plan 05's `<the_string_keyed_reference_the_guard_cannot_see>` and its ledger
       entry first; the four-criteria verdict lives there, not here.

       Inside `l_start`, replace:
       ```python
       if 'child' in value.keys():
           task_class = autopilot.get('children', value['task_type'])
       else:
           task_class = autopilot.get_task(value['task_type'])
       ```
       with the unconditional:
       ```python
       task_class = autopilot.get_task(value['task_type'])
       ```
       Locate it by the `if 'child' in value.keys():` statement, not by line number — steps 1-4
       have already shifted the file. Change nothing else in `l_start`; in particular leave the
       surrounding `try:` / `except Exception` and the `self.state = 'RUNNING'` / `self.running.set()`
       lines exactly as they are.

       **Do NOT touch the unrelated parent/child lineage mechanism at `pilot.py:189-193`**
       (`if prefs.get('LINEAGE') == "CHILD": self.child = True` / `else: self.child = False`).
       That is a different feature with a different spelling — `self.child` and the `"CHILD"`
       LINEAGE pref — and it is not part of this removal. Verified: the quoted lowercase literal
       `'child'` occurs at exactly **one** site in `pilot.py` (the branch above), which is why the
       verify block can assert its total absence safely.

       **Re-verify the guard condition before deleting, and stop if it does not hold:**
       `grep -rn "'child'" /home/ido/mics-backend/orchestrator /home/ido/mics-backend/api` must
       return **zero** hits — no MICS START message carries a `child` key, which is what makes
       this branch unreachable. Plan 05 ran the same grep; running it again here is cheap and this
       is the START path, where a wrong call presents as "the run silently didn't start"
       (`pilot.py:607-609` sets `IDLE` and reports nothing).

       This is the only behavioural change to the START path in the phase, and it is the one
       change that has a proof scoped to it: **plan 09's live rig session exercises START
       end-to-end.** Record it in the ledger as such.

    6. `python3 -m py_compile` both edited files immediately.

    Do NOT touch `Event_Dispatcher.py`. Do NOT touch `mics_task.py` in this task.
  </action>
  <verify>
    <automated>cd /home/ido/pi-mirror &amp;&amp; python3 -c "
import re,sys
src=open('autopilot/autopilot/core/pilot.py').read().split(chr(10))
# HOLD 0 — user-deferred 2026-08-10. Assert the clock block survived the sweep, still commented.
for pat,label in ((r'^\s*#\s*self\.enable_ntp_and_wait\(\)\s*\$','# self.enable_ntp_and_wait()'),
                  (r'^\s*#\s*self\.disable_ntp\(\)\s*\$','# self.disable_ntp()'),
                  (r'^\s*#\s*-+ CLOCK SETUP -+','# ---- CLOCK SETUP ----'),
                  (r'Freeze wall clock so it never jumps','freeze-wall-clock comment')):
    assert any(re.search(pat,l) for l in src), 'HOLD 0: the sweep deleted '+label+' — it is user-deferred, not dead code'
for pat,label in ((r'^\s*self\.enable_ntp_and_wait\(\)','enable_ntp_and_wait'),
                  (r'^\s*self\.disable_ntp\(\)','disable_ntp')):
    assert not any(re.search(pat,l) for l in src), 'HOLD 0: '+label+' was uncommented — the user deferred this restoration'
print('HOLD 0 OK: clock block present, still commented, not swept')
" &amp;&amp; ! grep -q 'open_file' autopilot/autopilot/core/pilot.py &amp;&amp; ! grep -q 'h5f' autopilot/autopilot/core/pilot.py &amp;&amp; ! grep -q 'liors' autopilot/autopilot/core/pilot.py &amp;&amp; grep -Eq '^\s*logger\.(warning|exception)\(' autopilot/autopilot/networking/station.py &amp;&amp; ! grep -Eq '^\s*print\(""\)\s*$' autopilot/autopilot/networking/station.py &amp;&amp; python3 -m py_compile autopilot/autopilot/core/pilot.py autopilot/autopilot/networking/station.py &amp;&amp; python3 -c "
import ast,sys
src=open('autopilot/autopilot/core/pilot.py').read()
t=ast.parse(src)
fn=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='run_task']
assert fn, 'run_task disappeared'
d=ast.get_docstring(fn[0]) or ''
assert 'open_file' not in d, 'run_task docstring still advertises open_file'
assert not [n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='open_file'], 'open_file method still defined'
q=chr(39)
assert q+'child'+q not in src, 'orphaned child dispatch branch still present (plan 05 deleted the CHILDREN registry entry it dispatches into)'
assert 'get('+q+'children'+q not in src, 'autopilot.get(children, ...) still present'
ls=[n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='l_start']
assert ls, 'l_start disappeared'
assert 'get_task' in ast.unparse(ls[0]), 'l_start no longer dispatches via get_task'
assert 'LINEAGE' in src, 'the unrelated LINEAGE/self.child parent-child mechanism was removed by mistake'
print('pilot.py HDF5 set fully removed, docstring included; child branch collapsed')" &amp;&amp; python3 tools/check_tree_integrity.py --strict</automated>
  </verify>
  <done>
    `# self.enable_ntp_and_wait()` and `# self.disable_ntp()` are still present in `pilot.py`
    **in commented form**, along with the `# ---- CLOCK SETUP ----` banner and the
    "Freeze wall clock…" comment — user-deferred 2026-08-10, exempted from the sweep, and
    neither uncommented nor deleted. `open_file` appears nowhere in `pilot.py` — not as a
    method, not in the `run_task` docstring — and `h5f` and the `liors` path are gone.
    `_handshake_watchdog` in `station.py` has at least one uncommented `logger.warning(` /
    `logger.exception(` and no bare `print("")`. `l_start` dispatches unconditionally through
    `autopilot.get_task(...)`; neither `'child'` nor `autopilot.get('children'` appears anywhere
    in `pilot.py`, and the zero-hit `'child'` grep over `mics-backend/orchestrator` and
    `mics-backend/api` is recorded in the ledger. Both files compile. Guard `--strict` exits 0 —
    in particular `Event_Dispatcher.py`'s sha256 still matches its baseline.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep the 244 dead commented-out lines (HYG-11)</name>
  <files>
    /home/ido/pi-mirror/autopilot/autopilot/networking/station.py,
    /home/ido/pi-mirror/autopilot/autopilot/networking/message.py,
    /home/ido/pi-mirror/autopilot/autopilot/hardware/gpio.py,
    /home/ido/pi-mirror/autopilot/autopilot/tasks/task.py,
    /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-06-ledger.md
  </files>
  <action>
    Apply the discrimination rule in `<HYG_11_scope>`. Work file by file, counting as you go —
    the audit's 244 is the target, and a large deviation in either direction means you have
    misclassified something.

    1. **Enumerate first, delete second.** Write a scan that lists candidate blocks:
       ```
       grep -rn '^\s*#\s*[a-zA-Z_@].*' /home/ido/pi-mirror/autopilot/autopilot --include=*.py
       ```
       then filter to lines that parse as Python (commented-out code, not prose). Produce a
       per-file candidate count before touching anything, and put that table in the ledger. A
       comment that is prose, a `# TODO`, a `# NOTE`, a section banner (`# ---- CLOCK SETUP ----`)
       or a docstring-style explanation is **not** commented-out code.

    2. **Named targets** (delete the superseded-implementation blocks):
       - `networking/station.py` (~52), including the `:623-651` block and the author's
         "i am paranoid" note that introduces it.
       - `networking/message.py` (~50) — Python-2-era residue.
       - `hardware/gpio.py` (~28).
       - `tasks/task.py` (~28) — three successive superseded trigger-dispatch implementations.
         Confirm the live dispatch path exists before removing the superseded ones.

    3. **The 11 smaller files** — identify them from your scan, not from memory. For each, apply
       the discrimination rule and record the verdict. `mics_task.py` should yield ~11 lines;
       if your scan says far more there, you are catching prose.

    4. **Do not touch:**
       - **Any file on the HYG-13 protect-list.** Read `<the_protect_list_is_off_limits>` — this
         is the hard bound on the blast radius, since the 11 smaller files cannot be pre-listed
         in `files_modified`. The list includes `Event_Dispatcher.py`, `log_value.py`,
         `fda_vocabulary.py`, the five `external_hardware*.py` files, `autopilot/LICENSE` and all
         21 modules under `tests/`. Check each file against the list *before* editing it.
       - `Event_Dispatcher.py` specifically (HYG-12 hard boundary), on top of the above.
       - Any commented line naming a symbol that still exists in the tree — that is the ~72-line
         excluded set. Grep the symbol before deleting the line.
       - The four held toggles (three are already gone with their files in plans 04/05; the
         fourth was restored in Task 1).
       - `tools/check_tree_integrity.py`, `tools/tree_protect_list.json` and
         `tests/test_tree_integrity.py` — the instrument. Editing the guard to make the sweep
         pass inverts the whole point of Wave 0.

    5. **Record.** Write `.planning/phases/30-pi-repo-cleanup/ledger/30-06-ledger.md` with:
       the per-file candidate table, the per-file removed count, the running total against the
       244 target, **an explicit list of every file edited beyond the four named in this plan's
       `files_modified`** (path + removed-line count — this is the after-the-fact `files_modified`
       plan 08 reads when it diffs the manifest), every "undecided — kept" line with its
       file:line and the symbol that kept it, and proposed verdict lines
       `HYG-11 | PROVEN | <evidence>`, `HYG-14 | PROVEN | <evidence>` and
       `HYG-12 | PROVEN | Event_Dispatcher.py sha256 unchanged from the §1 baseline;
       _dropped_no_clock and _dropped_on_send present`.
       **Do not edit `30-HARDWARE-VALIDATION.md`** — plan 07 runs in parallel; plan 08 merges.
  </action>
  <verify>
    <automated>cd /home/ido/pi-mirror &amp;&amp; python3 -m compileall -q autopilot/autopilot tools tests &amp;&amp; python3 tools/check_tree_integrity.py --strict &amp;&amp; grep -q '_dropped_no_clock' autopilot/autopilot/networking/Event_Dispatcher.py &amp;&amp; grep -q '_dropped_on_send' autopilot/autopilot/networking/Event_Dispatcher.py &amp;&amp; grep -q 'HYG-11' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-06-ledger.md &amp;&amp; grep -q 'HYG-12 | PROVEN' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/ledger/30-06-ledger.md &amp;&amp; python3 - &lt;&lt;'PY'
import json, subprocess, sys

BASE = '/home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-PYTEST-BASELINE.json'
b = json.load(open(BASE))
r = subprocess.run([sys.executable, '-m', 'pytest', '-q', 'tests/'], capture_output=True, text=True)
if 'during collection' in r.stdout:
    print('COLLECTION ERROR — a break, not a delta')
    print(r.stdout[-2000:])
    sys.exit(1)
now = {l.split()[1] for l in r.stdout.splitlines() if l.startswith('FAILED')}
new = sorted(now - set(b['failing_node_ids']))
print('baseline failures %d, now %d, new %d' % (b['failed'], len(now), len(new)))
for n in new:
    print('  NEW:', n)
sys.exit(1 if new else 0)
PY</automated>
  </verify>
  <done>
    Every named file has had its superseded-implementation comment blocks removed, with a total
    close to 244 lines and a per-file breakdown recorded. No protect-list file was edited —
    proven by `--strict` reporting zero "protected file modified" findings, and
    `Event_Dispatcher.py` byte-identical to its Wave 0 baseline. `compileall` exits 0. The Pi
    suite shows **no new failing node ids** against `30-PYTEST-BASELINE.json` (the baseline's 179
    failures are pre-existing debt and are expected to still be there).
    `ledger/30-06-ledger.md` records every kept-because-undecided line with its file:line and the
    live symbol that kept it, plus the full list of files edited beyond this plan's declared four.
  </done>
</task>

</tasks>

<verification>
- `python3 -m compileall -q /home/ido/pi-mirror/autopilot/autopilot /home/ido/pi-mirror/tools /home/ido/pi-mirror/tests` exits 0.
- `python3 /home/ido/pi-mirror/tools/check_tree_integrity.py --strict` exits 0 — this is also the
  HYG-12 check and the protect-list bound: all 30 protected files carry sha256 baselines, so any
  edit to any of them fails as "protected file modified".
- **Pi suite: run the delta command from `30-01-SUMMARY.md`**, never a bare `pytest`. It exits 0
  when no node id fails that was not already failing in `30-PYTEST-BASELINE.json`. Watch
  `tests/test_trigger_assignments.py` (51 baseline failures) and
  `tests/test_execute_trigger_guard.py` (11) specifically after the `tasks/task.py`
  trigger-dispatch comment removal — the signal is "still 51 and 11", not "0".
- `grep -rn 'liors' /home/ido/pi-mirror --include=*.py` returns nothing.
- `grep -rn 'open_file' /home/ido/pi-mirror/autopilot/autopilot/core/pilot.py` returns nothing,
  including inside docstrings.
- No git command was run in `/home/ido/pi-mirror`. Nothing was deployed to the Pi.
</verification>

<success_criteria>
1. `pilot.py`'s clock block is **unchanged**: `# self.enable_ntp_and_wait()` and
   `# self.disable_ntp()` are still commented, still present, and the sweep did not remove them
   or their explanatory comments. The restoration is **deferred to the user** (2026-08-10) and
   recorded in the ledger under `Deferred by the user`, not under `Removed`.
2. `station.py`'s `_handshake_watchdog` logs instead of printing an empty string.
3. The HDF5 subsystem is gone as one set across **all four** sites — `open_file()`, the `h5f`
   cleanup, the commented block, and the `run_task` docstring line that advertised it.
4. ~244 lines of superseded-implementation comments removed, with a per-file breakdown, an
   explicit list of every file edited beyond the declared four, and an explicit
   kept-because-undecided list.
5. No file on the HYG-13 protect-list was edited; `Event_Dispatcher.py` sha256 unchanged from the
   Wave 0 baseline and both drop counters present.
6. The Pi suite has no new failing node ids relative to `30-PYTEST-BASELINE.json`. Neither
   restoration was reverted to satisfy a gate.
</success_criteria>

<output>
After completion, create
`.planning/phases/30-pi-repo-cleanup/30-06-SUMMARY.md`.
</output>
