---
phase: 18-extlink-pi-transport
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/ido/pi-mirror/requirements.txt
  - /home/ido/pi-mirror/autopilot/requirements.txt
autonomous: false
requirements: [EXTLINK-03]
must_haves:
  truths:
    - "`msgpack` is installed in the rig's `~/.venv/autopilot` and its resolved version is known"
    - "Both pi-mirror requirements files pin that exact version, so a rebuilt venv reproduces it"
    - "No `import msgpack` lands in Pi source before this pin exists"
  artifacts:
    - path: "/home/ido/pi-mirror/requirements.txt"
      provides: "top-level msgpack pin matching the rig's Python 3.7.3"
      contains: "msgpack"
    - path: "/home/ido/pi-mirror/autopilot/requirements.txt"
      provides: "autopilot-package msgpack pin"
      contains: "msgpack"
  key_links:
    - from: "/home/ido/pi-mirror/requirements.txt"
      to: "the rig's ~/.venv/autopilot"
      via: "a version resolved by a real pip install on the rig, never a guessed pin"
      pattern: "msgpack=="
---

<objective>
Wave 0, part D. Resolve and record the `msgpack` version pin for the rig's Python 3.7.3.

Purpose: `18-CONTEXT.md` claimed msgpack was "already transitive through the pyzmq/tornado
stack". Research disproved that by SSH into the exact venv `run_pilot.sh` activates:
`ModuleNotFoundError: No module named 'msgpack'`, and every existing wire format in this
codebase is JSON. It is a genuine new dependency. Current msgpack (1.1.x) requires Python >= 3.9;
the rig runs 3.7.3, so an older release is needed and the exact one CANNOT be guessed — it must
come from a real resolver run. Per the project's Pi rules the agent cannot install packages on
the rig, so this plan is a one-command SSH ask, not a rig session.
Output: both pi-mirror requirements files carry an exact, verified `msgpack==X.Y.Z` pin.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-extlink-pi-transport/18-RESEARCH.md
@/home/ido/pi-mirror/requirements.txt
@/home/ido/pi-mirror/autopilot/requirements.txt

<interfaces>
Verified rig facts (18-RESEARCH.md, live SSH read-only this research session):
- interpreter: Python **3.7.3**; venv: `~/.venv/autopilot` (the one `run_pilot.sh` activates)
- `pyzmq 23.0.0b1`, `tornado 6.1` installed; **`msgpack` absent**
- `~/Apps/mice_interactive_home_cage/environment.yml` pins `pyzmq==23.0.0b2`, `tornado==6.1`,
  `python=3.7.16`
- SSH: `ssh -i ~/.ssh/pi_mics pi@132.77.72.28`

Project rules that bound this plan (absolute):
- NEVER run git on the Pi. NEVER start/stop the pilot process. NEVER run a Python file on the Pi.
- Installing a package is a USER action. The agent only reads and only edits `~/pi-mirror/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage the dependency entry with a TBD marker</name>
  <files>/home/ido/pi-mirror/requirements.txt, /home/ido/pi-mirror/autopilot/requirements.txt</files>
  <action>
    Read both files first and match their existing formatting exactly (comment style, pin
    operator, ordering).

    Add to each:
    ```
    # MICS-Link wire codec (Phase 18, EXTLINK-03). Pin resolved on the rig's Python 3.7.3 —
    # msgpack >= 1.1 requires Python >= 3.9, so this MUST stay at whatever pip actually
    # resolves there. Do not bump without re-running the install on the rig.
    msgpack==TBD
    ```
    Leave `TBD` literally in place — Task 3 replaces it with the value Task 2 returns. Do not
    guess a version; a wrong pin fails at `pip install -r` time on the rig, which is a rig visit
    to discover.

    No git command anywhere under `/home/ido/pi-mirror` — that repo is user-owned.
  </action>
  <verify>
    <automated>grep -c "msgpack==TBD" /home/ido/pi-mirror/requirements.txt /home/ido/pi-mirror/autopilot/requirements.txt</automated>
  </verify>
  <done>Both files report exactly 1 match, and `git -C /home/ido/mics-backend status --short`
  shows no change under mics-backend (these files live in the other repo).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: USER resolves the msgpack pin on the rig</name>
  <files>none — this task changes no file; it returns a version string</files>
  <action>Present the <how-to-verify> block below to the user verbatim and STOP. Do not
  attempt the install yourself: package installs on the rig are a USER action per the
  project's Pi rules, and guessing a pin is worse than waiting for one.</action>
  <verify>
    <automated>USER-RUN — see &lt;how-to-verify&gt;. The agent runs nothing on the rig.</automated>
  </verify>
  <done>The user has reported a concrete `Version:` line from `pip show msgpack`, or a
  concrete failure message.</done>
  <what-built>Both pi-mirror requirements files now carry a `msgpack==TBD` placeholder. The
  agent cannot install packages on the rig (project rule), and the correct version for Python
  3.7.3 cannot be guessed — pip's resolver has to pick it.</what-built>
  <how-to-verify>
    Run these two commands and paste the output back:

    ```bash
    ssh -i ~/.ssh/pi_mics pi@132.77.72.28 \
      'source ~/.venv/autopilot/bin/activate && python -V && pip install msgpack && python -c "import msgpack; print(\"RESOLVED\", msgpack.version)"'
    ```

    ```bash
    ssh -i ~/.ssh/pi_mics pi@132.77.72.28 \
      'source ~/.venv/autopilot/bin/activate && pip show msgpack | head -3'
    ```

    Expected: `python -V` prints `Python 3.7.x`, the install succeeds, and the last line prints
    `RESOLVED (1, 0, N)` for some N. Paste the `Version:` line from `pip show`.

    This does NOT touch the pilot process, does not run any project Python file, and does not
    run git. It is a package install only.

    If `pip install msgpack` FAILS with "requires a different Python", re-run it as
    `pip install "msgpack<1.1"` and report what that resolves to instead.
  </how-to-verify>
  <resume-signal>Paste the `Version:` line from `pip show msgpack` (e.g. `Version: 1.0.5`), or
  the error if the install failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the resolved pin</name>
  <files>/home/ido/pi-mirror/requirements.txt, /home/ido/pi-mirror/autopilot/requirements.txt</files>
  <action>
    Replace `msgpack==TBD` with `msgpack==<the version the user reported>` in both files.
    Extend the comment with the resolution date and the Python version it was resolved against,
    e.g. `# resolved 2026-08-XX against Python 3.7.3 on the rig`.

    If the user reported a failure instead of a version, do NOT invent a fallback — record the
    failure verbatim in the SUMMARY, leave `TBD` in place, and flag plan 18-12's rig checkpoint
    as blocked on it. A broken pin in requirements.txt is worse than an honest TBD.
  </action>
  <verify>
    <automated>grep -h "msgpack" /home/ido/pi-mirror/requirements.txt /home/ido/pi-mirror/autopilot/requirements.txt | grep -v "^#"</automated>
  </verify>
  <done>Both lines print `msgpack==` followed by a concrete version, and no `TBD` remains.</done>
</task>

</tasks>

<verification>
```bash
grep -n "msgpack" /home/ido/pi-mirror/requirements.txt /home/ido/pi-mirror/autopilot/requirements.txt
```
The pinned version must be one the user's own `pip show` reported — never one this plan chose.
</verification>

<success_criteria>
- The rig's `~/.venv/autopilot` has an importable `msgpack`.
- Both pi-mirror requirements files pin its exact version with a comment explaining the 3.7.3
  constraint.
- Nothing else on the rig was touched; the pilot process was not restarted.
</success_criteria>

<output>
After completion, create `.planning/phases/18-extlink-pi-transport/18-04-SUMMARY.md`.
Record the resolved version verbatim — plan 18-12's deploy step depends on it.
</output>
