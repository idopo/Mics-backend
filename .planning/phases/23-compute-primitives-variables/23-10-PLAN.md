---
phase: 23-compute-primitives-variables
plan: 10
type: execute
wave: 6
depends_on: ["23-04", "23-05", "23-06", "23-07", "23-08", "23-09"]
files_modified:
  - .planning/phases/23-compute-primitives-variables/23-HARDWARE-VALIDATION.md
autonomous: false
requirements: [CMP-01, CMP-02, CMP-03, CMP-04, CMP-05, CMP-06, CMP-13, CMP-14, CMP-16, CMP-18]
must_haves:
  truths:
    - "The gonogo random-target logic runs as FDA-JSON-v2 with a compute action, on the real rig"
    - "Both guarded branches fire across trials and `target` is recomputed once per state entry"
    - "Both events reach the event log per compute call: a Hardware_Event with the op and its args, and the Tracker set with the result"
    - "A researcher-authored compute lib reaches the Pi and its ops appear in the GUI picker with no platform code change"
    - "The GUI gained exactly one action-type entry and one filter chip — no new page, no new nav"
  artifacts:
    - path: ".planning/phases/23-compute-primitives-variables/23-HARDWARE-VALIDATION.md"
      provides: "What is proven on hardware, what is not, and the exact deployed/registry/DB state"
      min_lines: 60
  key_links:
    - from: "orchestrator LOAD_HARDWARE_LIBS"
      to: "Pi HARDWARE_OVERRIDE_DIR/compute_ops.py"
      via: "the resolved stable version's source_code, test_import=True"
      pattern: "compute_ops.py"
    - from: "compute op call"
      to: "Elasticsearch event_log_v2"
      via: "@log_action Hardware_Event AND Tracker.set — both, per call (CMP-16)"
      pattern: "Hardware_Event"
---

<objective>
Deploy the Pi runtime changes, then prove the whole phase on the real rig in ONE user session.

Purpose: this is the phase's single synchronization point. Everything Pi-side has been
edit-in-mirror only; every Pi test written since plan 23-01 is still unrun (`autopilot` is
unimportable on the dev host). CMP-16 in particular can only be proven with live ES and a real
task run. Bundling it here means the user is asked to touch the rig once, not six times.
Output: `23-HARDWARE-VALIDATION.md` recording exactly what is proven and what is not.

HARD RULES (`.claude/skills/pi-deploy/SKILL.md` — these OVERRIDE anything in this file):
- NEVER run git anywhere near the Pi or `/home/ido/pi-mirror`. Not even `status`.
- NEVER start or stop the pilot. NEVER run any Python on the Pi. Hand the user the command
  and wait.
- Deploy ONLY the files edited in this phase, listed explicitly. Never the whole mirror.
  Never `--delete`.
- Pi tests are run BY THE USER from `~/Apps/mice_interactive_home_cage`, not from `~/pi-mirror`.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-compute-primitives-variables/23-CONTEXT.md
@.planning/phases/23-compute-primitives-variables/23-VALIDATION.md
@.planning/phases/23-compute-primitives-variables/23-04-SUMMARY.md
@.planning/phases/23-compute-primitives-variables/23-05-SUMMARY.md
@.planning/phases/23-compute-primitives-variables/23-08-SUMMARY.md
@.planning/phases/23-compute-primitives-variables/23-09-SUMMARY.md
@.planning/phases/24-trigger-assignment-action-lists/24-HARDWARE-VALIDATION.md
@.claude/skills/pi-deploy/SKILL.md

<interfaces>
The canonical verification task (23-CONTEXT `<specifics>`) — build exactly this shape:

```json
{
  "version": 2,
  "initial_state": "trial_onset",
  "variables": { "target": {} },
  "states": {
    "trial_onset": {
      "entry_actions": [
        {"type": "compute", "ref": "COMPUTE", "method": "random_bool",
         "args": [0.5], "output": "target"}
      ]
    },
    "go_branch":   { "entry_actions": [] },
    "nogo_branch": { "entry_actions": [] }
  },
  "transitions": [
    {"from": "trial_onset", "to": "go_branch",
     "condition_tree": {"left": {"view": "target"}, "op": "==", "right": true}},
    {"from": "trial_onset", "to": "nogo_branch",
     "condition_tree": {"left": {"view": "target"}, "op": "==", "right": false}}
  ]
}
```

Read one real saved task definition out of the DB first and mirror its exact operand encoding
rather than trusting this sketch's key names.

Rig facts (from 24-/25-HARDWARE-VALIDATION.md and MEMORY):
- Pi host `132.77.72.28`, key `~/.ssh/pi_mics`, code root `~/Apps/mice_interactive_home_cage/`
- pilot name `pilot_raspberry_lior`; orchestrator `NAME="T"`; pilot log `/tmp/pilot.log`
- live ES is `132.77.73.217:9200`, index `event_log_v2`; isolate a run with
  `subject: bp_s<session>_r<run_id>` (see the `es-query` skill; `.125` is historical only)
- toolkit 100 carries hardware modules 7 (`MPR121`) and 8 (`TOUCH_INT`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: register the compute lib on the test toolkit and build the gonogo translation</name>
  <files>.planning/phases/23-compute-primitives-variables/23-HARDWARE-VALIDATION.md</files>
  <action>
    All backend/API work, agent-driven against the docker compose stack. Record every id you
    create in `23-HARDWARE-VALIDATION.md` as you go — that file is the deliverable. Paste real
    responses; do not summarise them.

    1. Confirm the seed landed: `GET /api/hardware-libs` shows `compute_ops.py` with
       `kind: "compute"` and a non-null `stable_version_id`; `GET /api/hardware-modules` shows
       `COMPUTE` with `lib_kind: "compute"`.

    2. Link the compute lib to toolkit 100 BOTH ways. This doubled ceremony is the phase's
       most likely failure point — `toolkit_hardware_libs` alone does NOT make the class
       reachable as `self.hardware["Modules"]["COMPUTE"]`, because `get_dispatch_spec` never
       reads that table:
       a) `POST /api/toolkits/100/hardware-libs {"hardware_lib_id": <id>}` — versioning and
          the `LOAD_HARDWARE_LIBS` file-write path.
       b) add the `COMPUTE` module id to toolkit 100's `hardware_module_ids` — instantiation
          and op-name validation.

    3. Confirm auto-provisioning: `GET /api/pilots/1/hardware-config` now has a `COMPUTE` row
       whose config is exactly `{"class_name": "ComputeOps"}`.

    4. Create the gonogo-translation task definition against toolkit 100 via
       `POST /api/task-definitions`. It MUST return 201. If it 422s, that is plan 23-03's gate
       working — fix the payload, never weaken the gate.

    5. Assert the negative suite bites. Record each response:
       - same payload with `output` removed → 422 naming `output`
       - `output: "undeclared"` → 422
       - a variable renamed to collide with a toolkit flag → 422
       - a transition reading an undeclared variable → 422

    6. `GET /api/toolkits/100/dispatch-spec?pilot_id=1&task_def_id=<new id>` — confirm
       `hardware.Modules.COMPUTE` carries the seed source AND that MPR121 and TOUCH_INT are
       still present. Their absence would mean plan 23-05's version chain is too strict.

    7. `POST /api/sessions/{sid}/preflight-validate/1` for a session using this def — confirm
       NO spurious `incomplete_config` for COMPUTE and no `variable_never_written`. Then
       temporarily point a transition at an unwritten variable, confirm the issue DOES appear
       with the reader's location, and revert.

    8. Attach the task definition to a protocol step so it is runnable on the rig, using the
       same route the previous phases used (`24-HARDWARE-VALIDATION.md` has the exact calls).
  </action>
  <verify>
    <automated>docker compose exec -T api python -m pytest -q</automated>
  </verify>
  <done>Task definition created (id recorded), all four negative payloads 422 with the
  expected messages, dispatch-spec carries the compute source alongside the existing modules,
  preflight clean and provably able to report an unwritten variable. Full backend suite green.</done>
</task>

<task type="auto">
  <name>Task 2: deploy the Pi files and the rebuilt web UI</name>
  <files>.planning/phases/23-compute-primitives-variables/23-HARDWARE-VALIDATION.md</files>
  <action>
    1. Read `23-04-SUMMARY.md`'s "Next Phase Readiness" section for the authoritative file
       list. It should be exactly:
       - `autopilot/autopilot/tasks/fda_vocabulary.py`
       - `autopilot/autopilot/tasks/mics_task.py`
       - `tools/validate_fda.py`
       - `tests/test_compute_ops.py`
       - `tests/test_fda_vocabulary.py`
       Deploy nothing outside that list. Note that `autopilot/autopilot/core/pilot.py` is NOT
       on it: plan 23-05 deliberately made zero Pi-side changes for the `test_import`
       round-trip, because that branch was already implemented and merely never triggered.

    2. Before pushing, prove mirror-vs-Pi state for every file with the diff-over-ssh pattern
       (never git):
       `diff <(ssh -i ~/.ssh/pi_mics pi@132.77.72.28 'cat ~/Apps/mice_interactive_home_cage/PATH') /home/ido/pi-mirror/PATH`
       Expect a diff ONLY for files this phase edited. If any other file differs, STOP and
       report — someone edited on the Pi and the mirror is stale.

    3. Deploy exactly those files with `--relative`, no `--delete`, one rsync:
       `rsync -avz --relative -e "ssh -i ~/.ssh/pi_mics" /home/ido/pi-mirror/./autopilot/autopilot/tasks/fda_vocabulary.py /home/ido/pi-mirror/./autopilot/autopilot/tasks/mics_task.py /home/ido/pi-mirror/./tools/validate_fda.py /home/ido/pi-mirror/./tests/test_compute_ops.py /home/ido/pi-mirror/./tests/test_fda_vocabulary.py pi@132.77.72.28:~/Apps/mice_interactive_home_cage/`

    4. Re-run the diffs and confirm every one is now empty. Record the md5s in
       `23-HARDWARE-VALIDATION.md`.

    5. Rebuild and restart the web UI and orchestrator containers:
       `docker compose up --build -d web_ui orchestrator`. Confirm the served bundle hashes
       match those recorded in `23-06-`, `23-08-` and `23-09-SUMMARY.md`.

    6. Report to the user exactly which files were pushed. Do NOT restart the pilot.
  </action>
  <verify>
    <automated>docker compose ps --format "table {{.Service}}\t{{.State}}" && docker compose logs --tail=30 orchestrator | grep -ciE "traceback" </automated>
    Per-file sync proof: run the `diff <(ssh ... cat) MIRROR_PATH` command once per deployed
    file and confirm empty output. Do not use git for this.
  </verify>
  <done>All five files byte-identical between mirror and Pi (md5s recorded). web_ui and
  orchestrator containers up, no tracebacks, bundle hashes match the summaries. Pilot NOT
  restarted by the agent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: rig proof — Pi test suite, gonogo run, ES evidence, GUI click-through</name>
  <files>none (checkpoint — no repo file is edited here; `23-HARDWARE-VALIDATION.md` is written after sign-off)</files>
  <action>
Agent side, BEFORE pausing:
1. Confirm the pilot is connected: `curl -s http://localhost:9000/pilots/live | python3 -m json.tool`.
   If `pilot_raspberry_lior` is not `connected: true`, say so in the checkpoint text — do NOT
   start the pilot yourself.
2. Confirm the deployed Pi files are byte-identical to the mirror (task 2's diffs), and that
   `docker compose ps` shows api / web_ui / orchestrator all up.
3. Confirm task 1's task-definition id, session id and protocol step are recorded in
   `23-HARDWARE-VALIDATION.md`, and restate them in the checkpoint text so the user knows
   exactly which session to run.
Then pause and present `<how-to-verify>` verbatim.
  </action>
  <what-built>
    - Pi runtime: a `type:"compute"` entry action that calls an op on a `Hardware`-subclass
      compute module and writes the result into a declared variable, with `output` mandatory
      at FDA-load time.
    - Backend: `hardware_libs.kind`, declared imports, the seed Compute Ops lib + COMPUTE
      module, auto-provisioned per-pilot config, hard 422s for compute FDA shapes, the
      unified pin → toolkit default → stable → validated-active version chain, an activated
      `test_import` round-trip, and three new preflight issue kinds.
    - GUI: a `kind` filter chip on the Hardware Libraries page, exactly one new "compute"
      action-type entry rendering `[output] = [op](args)` with ops grouped by lib, an output
      field that auto-declares a variable, and a read-only variables inspector.
    - Registered and ready: the gonogo-translation task definition on toolkit 100, attached to
      a protocol step, with the compute lib linked to the toolkit both ways.
  </what-built>
  <how-to-verify>
    Please run these in order and report back. The agent will not run any of them.

    **1. Pi test suite** (still outstanding from phase 24 and larger now):
    ```
    cd ~/Apps/mice_interactive_home_cage && python3 -m pytest tests/ -q
    ```
    Report the pass/fail count and paste any failure. `tests/test_compute_ops.py`,
    `tests/test_load_fda_from_json.py`, `tests/test_validate_fda.py` and
    `tests/test_fda_vocabulary.py` are the ones this phase cares about.

    **2. Restart the pilot** (your call, your command — the agent must not do this), then tell
    the agent so it can confirm reconnection via `curl -s http://localhost:9000/pilots/live`.

    **3. Run the gonogo translation on the rig** for at least 20 trials, from the session UI,
    on `pilot_raspberry_lior`. Note the session id and run id.

    **4. In the pre-run modal**, confirm before starting:
       - no "incomplete config" warning appears for COMPUTE (that would be the false positive
         plan 23-07 fixed)
       - no issue renders as a blank row

    **5. Browser click-through** while the run is up:
       - `/react/hardware-libs`: the filter chips show All / Hardware / Compute and picking
         Compute shows the Compute Ops lib and nothing else. There is NO new nav entry.
       - Task editor, state body: the action-type dropdown has exactly ONE new entry,
         `compute`. Selecting it shows ONE row reading `[output] = [op](args)` with the ops
         grouped under a `COMPUTE` heading, at the same visual weight as a hardware action.
       - Type a brand-new output name (e.g. `probe`). It should appear immediately in a
         transition condition's operand dropdown WITHOUT saving first.
       - Open the variables inspector: it lists `target` with its writers and readers, and is
         read-only (no editable fields, no delete buttons).
       - Judgement call, and the one thing only you can answer: **does the editor feel more
         cluttered than before?** One chip row and one dropdown entry was the agreed budget.

    **6. Report what happened on the rig:**
       - Did BOTH branches fire across trials (some trials went to `go_branch`, some to
         `nogo_branch`)? Roughly what split?
       - Any error in `/tmp/pilot.log`? In particular anything mentioning `release`,
         `_semantic_hw`, `KeyError`, or `output`.
       - Did the run END cleanly (no `The release method was not overridden` exception)?

    The agent will then query ES itself for the CMP-16 evidence (both a `Hardware_Event`
    carrying `random_bool` with its args, and the `Tracker` set carrying the result, on the
    same trial) and write `23-HARDWARE-VALIDATION.md`.
  </how-to-verify>
  <resume-signal>Paste the Pi test result, the session/run ids, the branch split, any pilot-log
  errors, and your click-through verdict. Or describe what broke.</resume-signal>
</task>

</tasks>

<verification>
After the checkpoint returns, the agent (not the user) completes:
- ES query for the run, via the `es-query` skill against `132.77.73.217:9200/event_log_v2`,
  filtered to `subject: bp_s<session>_r<run>`. CMP-16 requires BOTH per compute call:
  a `Hardware_Event` naming the op and its args, and the `Tracker` set carrying the result.
  Finding only one is a FAILURE, not a partial pass — that gap is the entire reason CONTEXT
  requires the `Hardware` subclass rather than a module of pure functions.
- Confirm `HARDWARE_LIB_TEST_RESULT` was received for the compute lib version: check the
  orchestrator log and that the version's `validation_error` is null / its state advanced
  (CMP-19b — first time this round-trip has ever fired).
- Write `23-HARDWARE-VALIDATION.md` following `24-HARDWARE-VALIDATION.md`'s structure: what is
  proven, what is NOT, every defect found with its commit, and the exact deployed/registry/DB
  state (lib id, version id, module id, toolkit links, task def id, md5s, bundle hashes).
- Update `.planning/STATE.md`'s "Phase 23 status" prose section and Next Actions.
</verification>

<success_criteria>
The gonogo translation runs on the real rig with a compute action; both branches fire; both
event types are visible in ES for the same trial; the Pi suite passes; the user confirms the
GUI gained exactly one action-type entry and one filter chip and does not feel cluttered.
Anything not proven is written down as not proven.
</success_criteria>

<output>
After completion, create `.planning/phases/23-compute-primitives-variables/23-10-SUMMARY.md`
and `.planning/phases/23-compute-primitives-variables/23-HARDWARE-VALIDATION.md`.
</output>
