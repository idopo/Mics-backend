---
phase: 30-pi-repo-cleanup
plan: 09
type: execute
wave: 6
depends_on: ["30-08"]
files_modified:
  - /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-HARDWARE-VALIDATION.md
autonomous: false
requirements: [HYG-02, HYG-01, HYG-13]
must_haves:
  truths:
    - "The ExtlinkDemo fixture is off pilot 1, so a failed session means the cleanup failed and nothing else"
    - "The pilot starts from the cleaned tree and reaches HANDSHAKE on the rig"
    - "A real sourceless-toolkit session runs to completion with CONTINUOUS events in ES and the run reaches `completed`, not a stuck `running`"
    - "The Gmail app password is revoked and absent from the new repo's history"
  artifacts:
    - path: "/home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-HARDWARE-VALIDATION.md"
      provides: "Final evidence log with HYG-01 and HYG-02 verdicts, run number and md5 manifest"
  key_links:
    - from: "the cleaned tree on .28"
      to: "the backend HANDSHAKE endpoint"
      via: "orchestrator ZMQ, tasks: [] payload"
      pattern: "tasks_received"
---

<objective>
Clear the standing blocker off pilot 1, then take the phase's acceptance test: a live session
from the cleaned tree.

Purpose: the acceptance test is deliberately a live session, not an import check. The Pi has
**no `TASK_ERROR` emitter at all** — `pilot.py:609` catches a failed START, sets state IDLE and
reports nothing, so the backend marks the run `running` indefinitely. Every removal failure mode
in this phase is silent by default, and only a real session surfaces it. And because `.28` will
eventually receive the new repo onto a *preserved* OS rather than a fresh image, this proof
carries more weight than it would if every target were a clean image.
Output: the HYG-02 verdict with a run number and md5 manifest, plus the HYG-01 revocation and
history-grep results, recorded in `30-HARDWARE-VALIDATION.md`.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-pi-repo-cleanup/30-CONTEXT.md
@.planning/phases/30-pi-repo-cleanup/30-VALIDATION.md
@.planning/phases/30-pi-repo-cleanup/30-08-SUMMARY.md
@.planning/phases/30-pi-repo-cleanup/30-PUBLISH.md
@.planning/phases/18-external-hardware/18-HARDWARE-VALIDATION.md

<pi_rules>
ABSOLUTE (STATE.md:1727-1745). Override anything below:
- **Never start or stop the pilot. Never run Python on the Pi.** Hand the user the command and
  wait. This applies to every step of the rig proof.
- **No git command in `/home/ido/pi-mirror` — not even `status`.** The branch cut and the
  deploy-by-checkout are the user's, on the user's machine.
- Never `rsync --delete`.
- Pi tests are USER-RUN, from `~/Apps/mice_interactive_home_cage` **on the device** — not from
  `~/pi-mirror`, which is the dev host.
- SSH for reading/grepping the Pi is fine (`ssh -i ~/.ssh/pi_mics pi@132.77.72.28`), and is how
  the agent confirms what landed without touching the process.
</pi_rules>

<the_blocker>
## `ExtlinkDemo` must come off pilot 1 before the session, or HYG-02 fails for the wrong reason.

The Phase 18 demo fixture is still live: module **62** (`role: router_bind`, `required: true`),
assigned to toolkit **100** and configured on pilot **1**. Every real session there
preflight-fails or hangs the full 30 s `wait_timeout_s`. A second standing dependency: a TCP echo
listener on the dev host at `132.77.73.125:5597`, without which `demo.alive` drops false ~3 s
into every run.

DB rows to remove: **module 62, lib 177, pilot config 21, task def 434.** Teardown procedure is
in `18-HARDWARE-VALIDATION.md` §3 — read it before acting; the exact column names and the
correct removal order are there, not here.

This is exactly the kind of false signal that discredits a destructive phase: a session that
fails because of an unrelated Phase 18 fixture would be read as "the cleanup broke the rig".
Clear it first, and record the before/after state so the removal itself is auditable.
</the_blocker>

<what_the_agent_does_vs_the_user>
**Agent (Task 1):** the DB teardown. It is an API/SQL operation on the backend, not a Pi
operation, so the automation-first rule applies — the agent does it rather than asking.

**User (Task 2):** revoke, publish, deploy the branch to `.28`, start the pilot, start the
session, run the Pi test suite. All four are either third-party actions, git on the user's
machine, or starting/stopping the pilot — every one of which the standing rules reserve to the
user.

**Agent (Task 3):** read-only confirmation over SSH and against ES/the DB, and the write-up.
</what_the_agent_does_vs_the_user>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tear down the ExtlinkDemo fixture on pilot 1</name>
  <files>
    /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-HARDWARE-VALIDATION.md
  </files>
  <action>
    Read `18-HARDWARE-VALIDATION.md` §3 first for the authoritative teardown procedure and the
    correct removal order. Do not infer the order from this plan.

    1. **Snapshot before.** Record the current rows for module 62, lib 177, pilot config 21 and
       task def 434, plus the toolkit-100 ↔ module-62 assignment, so the teardown is reversible
       from the log if the fixture is ever wanted again.
    2. **Remove** the four rows using the API endpoints where they exist (prefer
       `DELETE /api/hardware-modules/{id}`, `DELETE /api/hardware-libs/{id}`, and the pilot
       hardware-config delete route) and direct SQL only where no endpoint exists. Using the API
       keeps any cascade/validation logic in play; raw SQL bypasses it.
    3. **Confirm** pilot 1 has no `required: true` module left whose `role` is `router_bind`, and
       that no surviving task definition references module 62.
    4. **Do not** stand up the TCP echo listener on `132.77.73.125:5597` — with the fixture gone
       it is no longer needed, and starting a listener to satisfy a fixture you just removed
       would be backwards.
    5. Append a **§5.0 Blocker teardown** subsection to `30-HARDWARE-VALIDATION.md`: the before
       snapshot, the commands run, the after state, and a note that HYG-02's proof is now
       measuring the cleanup and nothing else.
    6. Run the backend suite afterwards — removing rows can trip a fixture-dependent test:
       `docker compose exec -T api python -m pytest -q tests/`.
  </action>
  <verify>
    <automated>docker compose -f /home/ido/mics-backend/docker-compose.yml exec -T db psql -U mics_user -d mics_db -tAc "select count(*) from hardware_modules where id=62;" | tr -d ' ' | grep -qx '0' &amp;&amp; docker compose -f /home/ido/mics-backend/docker-compose.yml exec -T db psql -U mics_user -d mics_db -tAc "select count(*) from hardware_libs where id=177;" | tr -d ' ' | grep -qx '0' &amp;&amp; docker compose -f /home/ido/mics-backend/docker-compose.yml exec -T api python -m pytest -q tests/ &amp;&amp; grep -q 'Blocker teardown' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-HARDWARE-VALIDATION.md</automated>
  </verify>
  <done>
    Module 62, lib 177, pilot config 21 and task def 434 are gone. Pilot 1 has no `required`
    `router_bind` module. Backend suite green. §5.0 records the before snapshot, the commands and
    the after state.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rig proof — publish, deploy the branch, run a live session</name>
  <files>none — this task performs no edits; it hands control to the user</files>
  <action>
    STOP. Do not proceed to Task 3 until the user has reported back.

    This task performs no automated work. It presents the completed sweep, hands the user the
    four actions the standing rules reserve to them (revoke the credential, publish via a fresh
    `git init`, cut and deploy the `.28` branch, start the pilot and a live session), and waits.

    The agent must NOT, in this task: start or stop the pilot, run Python on the Pi, run any git
    command in `/home/ido/pi-mirror`, or run the publication commands on the user's behalf.
  </action>
  <what-built>
    The full Phase 30 sweep is complete and verified as far as this host can verify it:
    - `check_tree_integrity.py --final` exits 0 — closure intact, no dangling references,
      survival manifest byte-identical, all five completion assertions pass.
    - `compileall` clean over `autopilot/autopilot`, `tools`, `tests`.
    - Pi suite: **no new failing node ids** against the Wave 0 baseline recorded in
      `30-PYTEST-BASELINE.json`. That baseline is **179 failed / 181 passed** over 19 collectable
      modules, with `tests/test_compute_ops.py` and `tests/test_log_action_values.py`
      `collect_ignore`d because they import `autopilot` at top level, which pulls
      `setup_autopilot` → `npyscreen`, absent on the dev host. **Those 179 failures are
      pre-existing debt that predates Phase 30** — the suite has never been run to green anywhere
      — so "no new failures" here means the failure *set* did not grow, not that the suite is
      green. Do not read the number as damage from the cleanup.
    - Backend suite: 352 pass (this one is genuinely green).
    - `hardware_libs` id 9's active version now matches the edited `i2c.py` on disk.
    - The `ExtlinkDemo` fixture is off pilot 1 (Task 1), so a failed session now means the
      cleanup failed and nothing else.
    - `30-PUBLISH.md` holds the exact publication procedure.

    What this host **cannot** verify: `autopilot` cannot be imported here (`npyscreen` absent),
    so nothing above instantiates a `Task`, a `Hardware` or the registry. Only the rig can.
  </what-built>
  <how-to-verify>
    Work through `30-PUBLISH.md` in order, then run the session. Every step below is yours to
    run — the agent will not start or stop the pilot and will not run git in `pi-mirror`.

    **1. Revoke the credential (first, before publishing).**
    Google account → Security → App passwords → revoke the password used by the deleted
    `AssociationLearning.py`. Note the date.

    **2. Publish.** Follow `30-PUBLISH.md` Step 2: `rsync -a --exclude='.git'` the cleaned tree
    into a new directory, `git init`, commit, push to the same git account.
    Never `git clone`, never a history rewrite.

    **3. Prove HYG-01.**
    ```
    git -C /home/ido/<new-repo> log -p --all | grep -c -F -f /home/ido/.hyg01-probe.txt
    git -C /home/ido/<new-repo> log --oneline | wc -l
    ```
    Expect `0` and `1`. Report both numbers.

    **4. Cut the `.28` branch** on the old repo, on your machine, and check it out on `.28`.
    `.28` keeps its OS — it is not reimaged and not migrated.

    **5. Start the pilot** on `.28`:
    ```
    python3 -m autopilot.core.pilot -f pilot/prefs.json
    ```
    Remember `pilot/prefs.json` now ships as a **template** — set `TERMINALIP` and `NAME` for
    this cage before first boot, or restore the device's own prefs (which `sync_pi.sh` never
    overwrote).

    **6. Confirm HANDSHAKE reaches the backend.** Expect `tasks: []` and `tasks_received: 0` —
    that is the HYG-04-proven no-op, not a fault. Existing toolkit rows will still be listed in
    the UI; the backend never prunes, and that staleness is accepted.

    **7. Start a real session** on a `source_less_toolkit` protocol (56, 57 or 58).

    **8. Watch for the failure mode that matters.** Confirm CONTINUOUS events land in ES and the
    run reaches **`completed`**. A run that sits at `running` forever is the signature of a
    silent START failure — `pilot.py:609` catches it, sets IDLE and reports nothing. If you see
    that, stop and report; do not restart into it.

    **9. Run the Pi test suite**, from `~/Apps/mice_interactive_home_cage` **on the device**:
    ```
    python3 -m pytest -q tests/
    ```
    This suite has never been run anywhere — it is the project's largest standing verification
    debt and predates this phase. On the dev host it collects only 19 of its 21 modules and
    reports 179 failed / 181 passed; on the Pi, all **21** should collect (Python 3.7 +
    `npyscreen` are present there) and many of those 179 are expected to **pass**, because a
    large share of them are dev-host environment artefacts rather than real defects. Whatever the
    numbers are, they are a finding about the pre-existing debt, **not** about this cleanup —
    this is the first time anyone has measured it. Report the full pass/fail counts and the
    collected-module count either way.

    **Report back:** the two HYG-01 numbers, the run number, whether the run reached `completed`,
    a sample of CONTINUOUS events seen in ES, and the Pi suite's counts.
  </how-to-verify>
  <resume-signal>
    Type "approved" with the run number and the Pi suite counts, or describe what failed.
  </resume-signal>
  <done>
    The user has reported: the revocation date, the two HYG-01 numbers (`grep -c -F -f` and
    `log --oneline | wc -l`), the run number, whether the run reached `completed`, evidence of
    CONTINUOUS events in ES, and the Pi test suite's pass/fail counts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the rig verdicts and close the phase evidence log</name>
  <files>
    /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-HARDWARE-VALIDATION.md
  </files>
  <action>
    Confirm what the user reported, read-only, then write it up.

    1. **Independent confirmation** (read-only, no process control):
       - `ssh -i ~/.ssh/pi_mics pi@132.77.72.28 'ls ~/Apps/mice_interactive_home_cage/pilot/plugins'`
         → should show only `.gitkeep`, confirming the branch checkout actually removed the
         plugins on the device.
       - `ssh ... 'ls ~/Apps/mice_interactive_home_cage/autopilot/autopilot/hardware/'` →
         no `cameras.py`, no `usb.py`, no `unreal.py`.
       - `ssh ... 'md5sum ~/Apps/.../autopilot/autopilot/hardware/i2c.py'` and compare with the
         mirror's — they must match, and the value must be the post-edit md5 from plan 05.
       - Query the DB for the reported run: its `status` must be `completed`, not `running`.
       - Query ES for CONTINUOUS events on that run (`subject: bp_s<session>_r<run_id>`) and
         record the document count.

    2. **Write §5, the rig session record**, in the house format of
       `18-`/`23-`/`24-`/`25-HARDWARE-VALIDATION.md`: date, run number, protocol, toolkit,
       the md5 manifest of the deployed files confirmed over SSH, the ES event count, and the
       Pi suite's pass/fail counts.

    3. **Write §3's HYG-01 record**: revocation date, the `grep -c -F -f` result (expect 0), the
       `log --oneline | wc -l` result (expect 1), and the probe file's sha256 — **never the
       credential values**.

       **Before trusting either grep, re-verify the probe file's integrity**, exactly as plan 01
       Task 3 did:
       ```
       test "$(grep -c . /home/ido/.hyg01-probe.txt)" -ge 2      # at least 2 literals
       ! grep -qc '^$' /home/ido/.hyg01-probe.txt                 # no blank line
       test "$(wc -l < /home/ido/.hyg01-probe.txt)" = "$(grep -c . /home/ido/.hyg01-probe.txt)"
       ```
       A blank line in a `-f` pattern file is an **empty pattern, which matches every line**. If
       one crept in (a stray trailing newline is enough), then the negative assertion at the end
       of this task's verify — `! grep -rq -F -f <probe> .planning/` — fails unconditionally,
       *and* the user's positive history proof (`git log -p | grep -c -F -f <probe>` → expect 0)
       can never return 0, so HYG-01 would be marked UNPROVEN for a reason that has nothing to do
       with the credential. `grep -c .` alone does **not** catch a trailing blank line, which is
       why all three checks are needed. If the probe is malformed, stop: it must be recaptured
       from `/home/ido/.hyg01-probe.txt`'s recorded sha256 or from the user, **not** regenerated
       from the tree — `AssociationLearning.py` was deleted in plan 04.

    4. **Flip the verdict table**: HYG-02 and HYG-01 to PROVEN if and only if the evidence
       supports it. If the run stuck at `running`, or the grep returned non-zero, mark UNPROVEN,
       record exactly what was observed, and stop — a partially-proven destructive phase must not
       be written up as complete.

    5. **Update `.planning/STATE.md`** with the phase outcome, and add to its Roadmap Evolution
       section the two findings worth carrying: the HYG-05 `MLX90640(Camera)` correction, and the
       fact that the Pi test suite now has a first-ever recorded result.

    6. **Carry the deferred items forward** as GSD todos, unchanged from 30-CONTEXT.md's Deferred
       list — do not silently drop them: `LOAD_HARDWARE_LIBS` failing silently mid-run
       (`mics_task.py:1589`), the missing `TASK_ERROR` emitter, `i2c.py:819`'s `except(e):`,
       `pilot.py:887`'s undefined `get_hardware_class`, the `hardware_libs` ↔ disk drift having
       no sync process, and the in-place conversion of `.28`.
  </action>
  <verify>
    <automated>grep -q 'HYG-02' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-HARDWARE-VALIDATION.md &amp;&amp; grep -qE 'HYG-01.*(PROVEN|UNPROVEN)' /home/ido/mics-backend/.planning/phases/30-pi-repo-cleanup/30-HARDWARE-VALIDATION.md &amp;&amp; test "$(grep -c . /home/ido/.hyg01-probe.txt)" -ge 2 &amp;&amp; ! grep -qc '^$' /home/ido/.hyg01-probe.txt &amp;&amp; test "$(wc -l &lt; /home/ido/.hyg01-probe.txt)" = "$(grep -c . /home/ido/.hyg01-probe.txt)" &amp;&amp; ! grep -rq -F -f /home/ido/.hyg01-probe.txt /home/ido/mics-backend/.planning/ &amp;&amp; docker compose -f /home/ido/mics-backend/docker-compose.yml exec -T api python -m pytest -q tests/</automated>
  </verify>
  <done>
    §5 records the rig session with run number, deployed-file md5 manifest confirmed over SSH, ES
    event count and Pi suite counts. §3 records the revocation date and both HYG-01 numbers
    without the credential values. All 14 verdict rows carry a PROVEN or UNPROVEN with evidence.
    STATE.md updated. The deferred items are carried forward, not dropped. The credential
    literals appear nowhere under `.planning/`.
  </done>
</task>

</tasks>

<verification>
- Every one of HYG-01..HYG-14 has a PROVEN or UNPROVEN verdict with cited evidence in
  `30-HARDWARE-VALIDATION.md`.
- The run reported by the user is `completed` in the DB, with CONTINUOUS events in ES.
- The deployed `i2c.py` md5 on `.28` matches the mirror's post-edit value.
- `/home/ido/.hyg01-probe.txt` still has ≥2 literals, no blank line, and line count == non-blank
  line count. A blank line would make every `-f` pattern match everything and silently invalidate
  both HYG-01 greps.
- `grep -rq -F -f /home/ido/.hyg01-probe.txt /home/ido/mics-backend/.planning/` finds nothing —
  the credential never entered the committed docs.
- The agent started nothing, stopped nothing, ran no Python on the Pi and no git in `pi-mirror`.
</verification>

<success_criteria>
1. The `ExtlinkDemo` fixture is off pilot 1 before the session, with an auditable before/after.
2. The pilot starts from the cleaned tree on `.28`, reaches HANDSHAKE with `tasks_received: 0`,
   and a `source_less_toolkit` session runs to `completed` with CONTINUOUS events in ES.
3. The Gmail app password is revoked and `git log -p --all | grep -c -F -f <probe>` returns 0
   over a one-commit history.
4. The Pi test suite has its first recorded result on the device, whatever it is — reported as
   collected/passed/failed and explicitly framed against the dev-host Wave 0 baseline of
   179 failed / 181 passed over 19 collectable modules, so nobody later reads the pre-existing
   debt as cleanup damage.
5. `30-HARDWARE-VALIDATION.md` is complete and the deferred items are carried forward as todos.
</success_criteria>

<output>
After completion, create
`.planning/phases/30-pi-repo-cleanup/30-09-SUMMARY.md`.
</output>
