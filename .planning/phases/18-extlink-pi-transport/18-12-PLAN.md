---
phase: 18-extlink-pi-transport
plan: 12
type: execute
wave: 5
depends_on: [18-04, 18-07, 18-09, 18-11]
files_modified:
  - .planning/phases/18-extlink-pi-transport/18-HARDWARE-VALIDATION.md
autonomous: false
requirements: [EXTLINK-01, EXTLINK-02, EXTLINK-05, EXTLINK-06, EXTLINK-07, EXTLINK-08, EXTLINK-09, EXTLINK-10, EXTLINK-11, EXTLINK-13, EXTLINK-14, EXTLINK-15, EXTLINK-16, EXTLINK-17, EXTLINK-18]
must_haves:
  truths:
    - "A DEALER on the dev machine pushes a signal and an FDA transition fires on the real rig"
    - "A PUB on the dev machine is dialled into by the Pi, decoded by a lib's @decoder, and lands in a view key"
    - "A control-only module (`role: \"none\"`, zero signals, NO socket) binds, registers `.alive`, and gates the task start"
    - "The readiness pre-state's three exits all behave as specified on a real task start"
    - "Lifecycle hooks fire on normal completion, STOP, and an induced task exception"
    - "A killed pilot's device lease is released by reconciliation"
  artifacts:
    - path: ".planning/phases/18-extlink-pi-transport/18-HARDWARE-VALIDATION.md"
      provides: "what is PROVEN on hardware vs. what remains unproven, with the exact commands and outputs"
      contains: "PROVEN"
  key_links:
    - from: "the deployed Pi"
      to: "extlink_smoke.py on the dev machine"
      via: "the single consolidated rig checkpoint — every manual-only row of 18-VALIDATION.md"
      pattern: "extlink_smoke"
---

<objective>
The single consolidated rig checkpoint. Every manual-only row of `18-VALIDATION.md` is exercised
in ONE session, and the result is written down.

Purpose: seven behaviours in this phase cannot be proven anywhere but the rig — they need a live
bound Tornado IOLoop inside the real `pilot.py` process, a real `pigpio` clock, or a real foreign
publisher. Per the project's Pi rules the agent never deploys, never restarts the pilot, and never
runs Python on the Pi, so these are collected into one checklist handed to the user rather than
scattered across the phase.
Output: three reference libs registered, one deploy, one checkpoint, one validation record.
</objective>

<execution_context>
@/home/ido/.claude/get-shit-done/workflows/execute-plan.md
@/home/ido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-extlink-pi-transport/18-CONTEXT.md
@.planning/phases/18-extlink-pi-transport/18-VALIDATION.md
@.planning/phases/18-extlink-pi-transport/18-04-SUMMARY.md
@.planning/phases/18-extlink-pi-transport/18-10-SUMMARY.md
@.planning/phases/18-extlink-pi-transport/18-11-SUMMARY.md
@.planning/phases/24-trigger-assignment-action-lists/24-HARDWARE-VALIDATION.md

<interfaces>
Rig access (read-only for the agent):
```
ssh -i ~/.ssh/pi_mics pi@132.77.72.28
Pi app root: ~/Apps/mice_interactive_home_cage/
Mirror:      /home/ido/pi-mirror/
```
**Absolute rules — a violation invalidates the plan:** never run git on the Pi, never
start/stop the pilot process, never run any Python file on the Pi, never `rsync --delete`.
Reading over SSH (`cat`, `grep`, `md5sum`, `diff`) is fine. Every command below that touches the
rig is handed to the USER.

Existing backend endpoints the agent uses for setup (all agent-driven, all in docker):
```
POST   /api/hardware-libs                            upload a lib (Phase 09)
POST   /api/hardware-modules                         register an instance (Phase 10)
PUT    /api/pilots/{pilot_id}/hardware-config/{name} per-pilot config JSON (Phase 17)
POST   /api/sessions/{sid}/preflight-validate/{pid}  preflight (Phase 13, + this phase's step 10)
GET    /api/device-leases                            plan 18-09
DELETE /api/device-leases/{host}                     plan 18-09
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the three reference `ExternalHardware` libs and their pilot configs</name>
  <files>api (via HTTP only — no repository file is modified by this task)</files>
  <action>
    Author two minimal reference libs as source strings and upload them through
    `POST /api/hardware-libs`, then register a `hardware_modules` row for each and a
    `pilot_hardware_config` row on the rig pilot. These are SUBSTRATE FIXTURES, not the
    OpenEphys lib (Phases 26/27) and not the DeepLabCut integration (deferred) — keep them
    trivial and say so in their docstrings.

    **Lib A — `extlink_demo_dealer.py`, role `router_bind`, with signals:**
    ```python
    class DemoDealer(ExternalHardware):
        @signal(default=0.0, stale_after_ms=200, stale_policy="return_default")
        def left_paw_x(self) -> float: ...
        @signal(default=0.0, stale_after_ms=200, stale_policy="hold_last")
        def right_paw_x(self) -> float: ...
        @event(payload={"object": str, "confidence": float})
        def object_detected(self): ...
    ```
    Two signals with DIFFERENT stale policies, so the checkpoint can prove EXTLINK-06 with one
    source. Config: `{"class_name": "DemoDealer", "role": "router_bind", "listen_port": 5599,
    "source_id": "dlc_cam1", "stale_ms": 3000, "required": true, "wait_timeout_s": 30,
    "egress_fail_threshold": 3}`.

    **Lib B — `extlink_demo_control.py`, role `none`, ZERO signals, NO socket (EXTLINK-18):**
    a `@command`, an overridden liveness predicate, and NO `@signal` and NO `@decoder` at all —
    exactly the control-only shape OpenEphys's HTTP side will have. The pre-gap-fix hedge ("give
    it one signal if a zero-signal source cannot demonstrate the decoder path") is GONE: with the
    third role there is nothing to hedge, because the decoder path is INAPPLICABLE in
    `role: "none"` and is proven by Lib C instead. Config:
    `{"class_name": "DemoControl", "role": "none", "host": "<dev machine IP>",
    "source_id": "oe_ctl", "stale_ms": 3000, "required": true, "wait_timeout_s": 30,
    "egress_fail_threshold": 3}` — note the deliberate absence of BOTH `listen_port` and
    `connect_port`. `required: true` is deliberate too: EXTLINK-18's claim is that a control-only
    module participates FULLY in the readiness gate, and only a required module proves it.

    Lib B's liveness override returns True unconditionally, with a docstring saying a real lib
    polls its device (OE polls `GET /api/status`). Without an override the module would be
    permanently dead — the base default is "a message arrived within `stale_ms`" and a module
    with no inbound socket never receives one — which plan 18-10 makes a construction-time
    error. This lib is the fixture proving the intended path.

    **Lib C — `extlink_demo_sub.py`, role `sub_connect`, ONE decoder-fed signal (EXTLINK-14):**
    a `@decoder` that parses the smoke script's synthetic foreign frame plus one `@signal` fed
    exclusively by it. EXTLINK-14's decoder path still needs proving on the rig and
    `role: "none"` cannot prove it, so this lib carries that half. Config:
    `{"class_name": "DemoSub", "role": "sub_connect", "host": "<dev machine IP>",
    "connect_port": 5598, "source_id": "oe_demo", "stale_ms": 3000, "required": false,
    "wait_timeout_s": 30}`.

    Assign all THREE modules to ONE toolkit on ONE task definition, so the checkpoint proves
    ROADMAP success criterion 5 (multiple external modules, one toolkit, no cross-talk) in the
    same run as everything else — and proves all three roles coexist on one pilot.

    Verify server-side, before the user is asked to do anything:
    - both uploads returned `ast_metadata.extlink` populated (EXTLINK-09 live proof);
    - `POST /api/sessions/{sid}/preflight-validate/{pid}` for the target session is clean, or
      names exactly the issues you expect;
    - deliberately break one config (`wait_timeout_s: null`) and confirm
      `extlink_config_invalid` appears, then restore it (EXTLINK-10 live proof);
    - confirm Lib B's config — `role: "none"` with NEITHER port — passes preflight cleanly
      (EXTLINK-18 live proof that validation does not demand a port); then temporarily drop its
      `host` and confirm `extlink_config_invalid` names `host`, and restore it;
    - `GET /api/device-leases` is `[]`.

    Record every id (lib, version, module, toolkit, task_definition, session, pilot) — the
    checkpoint's commands reference them.
  </action>
  <verify>
    <automated>docker compose exec -T api python -m pytest -q && curl -s -H "Authorization: Bearer $MICS_API_TOKEN" http://localhost:8000/api/device-leases</automated>
  </verify>
  <done>All three libs uploaded with a populated `extlink` block (Lib B's shows `signals: {}`
  and `decoder: null`), all three modules registered, all three pilot configs written, preflight
  behaves as expected in the clean case and in both deliberately-broken cases,
  `GET /api/device-leases` returns `[]`, and the full backend suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble the deploy manifest and the checkpoint command sheet</name>
  <files>.planning/phases/18-extlink-pi-transport/18-HARDWARE-VALIDATION.md</files>
  <action>
    Create `18-HARDWARE-VALIDATION.md`, modelled on
    `.planning/phases/24-trigger-assignment-action-lists/24-HARDWARE-VALIDATION.md`. Populate the
    "to be filled by the checkpoint" sections with the exact commands, and leave the RESULT
    columns empty for now.

    **Deploy manifest** — the exact rsync list from plan 18-11's SUMMARY, at minimum:
    ```
    autopilot/autopilot/hardware/external_hardware_wire.py
    autopilot/autopilot/hardware/external_hardware_runtime.py
    autopilot/autopilot/hardware/external_hardware.py
    autopilot/autopilot/tasks/mics_task.py
    requirements.txt
    autopilot/requirements.txt
    ```
    Plus whatever splits plans 18-05/18-06/18-10/18-11 recorded. **Never `--delete`.**

    **Pre-deploy verification the agent performs itself (read-only SSH):** `md5sum` each target
    file on the Pi and diff against the mirror BEFORE the user syncs, so any pre-existing drift
    is caught before it is overwritten. Record the result.

    Include the `msgpack` version plan 18-04 resolved, and the check that it is importable in
    `~/.venv/autopilot`.

    Write out the seven manual-only rows from `18-VALIDATION.md` § Manual-Only Verifications as a
    numbered checklist with concrete commands and concrete expected outputs, using the ids from
    Task 1. Every command must be copy-pasteable.

    Also write the "Explicitly NOT an acceptance criterion" section verbatim into this document:
    **do not assert that a CONTINUOUS event exists in ES for `on_run_stop()` itself.**
    `event_dispatcher.stop()` runs before `task.end()`/`release()` in `pilot.py`'s teardown, so
    that event provably races a sender thread that may already have exited. Stop is verified by
    EFFECT — device idle, lease released, log line present — never by an ES record.
  </action>
  <verify>
    <automated>test -f .planning/phases/18-extlink-pi-transport/18-HARDWARE-VALIDATION.md && grep -c "rsync" .planning/phases/18-extlink-pi-transport/18-HARDWARE-VALIDATION.md</automated>
  </verify>
  <done>The document exists, carries the full deploy manifest, the resolved msgpack pin, the
  pre-deploy md5 comparison result, all seven numbered checks with copy-pasteable commands, and
  the explicit non-criterion. `grep -c "rsync"` >= 1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The consolidated rig checkpoint</name>
  <files>none — this task changes no file; Task 4 records its result</files>
  <action>Present the <how-to-verify> checklist below to the user verbatim and STOP.
  Deploying, restarting the pilot, and running Python on the Pi are all USER actions per
  the project's absolute Pi rules. The agent may read the rig over SSH while waiting, but
  must not act on it.</action>
  <verify>
    <automated>USER-RUN — see &lt;how-to-verify&gt;. The agent runs nothing on the rig.</automated>
  </verify>
  <done>All ten numbered steps have pasted output, or the first failing step is reported
  with its output.</done>
  <what-built>
    The full `ExternalHardware` substrate: three transport roles (including the no-socket
    control-only one), a per-lib `@decoder`, per-signal
    stale policy, a liveness hook split from staleness, a non-blocking egress queue, run
    lifecycle hooks, a readiness pre-state, and a backend device lease. Everything provable
    without the rig is already green: all agent-runnable Pi-mirror tests and the full backend
    suite. Three reference libs are registered against the rig pilot and the deploy manifest
    is written into `18-HARDWARE-VALIDATION.md`.
  </what-built>
  <how-to-verify>
    Work through these in order. Paste the output of each. Stop and report at the first failure —
    do not push past a red step.

    **0. Deploy and restart (you, not the agent).**
    ```bash
    # sync ONLY the files listed in 18-HARDWARE-VALIDATION.md's manifest. NEVER use --delete.
    # then, in the venv on the Pi:
    #   pip install -r ~/Apps/mice_interactive_home_cage/requirements.txt
    # then restart the pilot process your usual way.
    ssh -i ~/.ssh/pi_mics pi@132.77.72.28 'source ~/.venv/autopilot/bin/activate && python -c "import msgpack; print(msgpack.version)"'
    ```
    Expected: the version plan 18-04 resolved. Then confirm the pilot came up and appears
    connected in the pilot grid.

    **1. `router_bind` end-to-end (EXTLINK-01/02/05/11).**
    ```bash
    python3 ~/pi-mirror/scripts/dev/extlink_smoke.py probe \
        --pi-host 132.77.72.28 --listen-port 5599 --source-id dlc_cam1 \
        --signal left_paw_x --value 0.7
    ```
    Expected: PASS, and the FDA transition gated on `left_paw_x > 0.5` advances the state
    machine. Then re-run with `--identity-mismatch` and confirm the frame is DROPPED (view key
    unchanged, no traceback in the pilot log).

    **2. `sub_connect` end-to-end (EXTLINK-14).**
    ```bash
    python3 ~/pi-mirror/scripts/dev/extlink_smoke.py publish --bind-port 5598 --interval-ms 200
    ```
    Expected: the Pi dials out, Lib C's `@decoder` translates the synthetic foreign frame, and
    the resulting view key updates. This is the single most important step — `sub_connect` has
    no precedent anywhere in the codebase, and this is the only thing that proves the IOLoop
    `add_callback` registration actually works on the real process.

    **3. Stale policy + liveness split (EXTLINK-06/07).** Stop the `sustain`/`publish` script
    mid-run. Expected: the `return_default` signal returns its declared default after
    `stale_after_ms` while the `hold_last` signal keeps its last value — AND, for the source
    whose liveness hook still reports reachable, `<source_id>.alive` stays `true` with stale
    signals. Then kill the source entirely and confirm `alive` flips to `false` with a CONTINUOUS
    event visible in ES.

    **4. Malformed input (EXTLINK-08).** Send a garbage frame (`probe --value` with a value that
    fails the declared dtype, plus a raw non-msgpack blob). Expected: dropped and logged, pilot
    keeps running, **no traceback in the pilot log**.

    **5. Readiness gate, all three exits (EXTLINK-13).** With the required `router_bind` source:
    (a) start a task with the source already running → it proceeds into the declared initial
    state; (b) start with the source DOWN, then send `EXTLINK_SKIP_WAIT` → it proceeds with a
    warning; (c) start with the source down and wait out `wait_timeout_s` (30s) → clean abort
    with a `CONTINUOUS ExtlinkTimeout` event listing the offline source. Confirm the STOP button
    works during the wait.

    **6. Control-only module, `role: "none"`, NO socket (EXTLINK-18).** Confirm Lib B
    instantiated, "bound" without creating any socket, registered `oe_ctl.alive`, and — because
    it is `required: true` — gated the task start until its liveness override reported ready.
    Then:
    ```bash
    ssh -i ~/.ssh/pi_mics pi@132.77.72.28 'ss -tln'
    ```
    Expected: port 5599 (Lib A's `router_bind`) is LISTENING and there is NO listening socket
    for Lib B on any port. Also confirm the pilot log shows no port/role error for a module that
    declares neither `listen_port` nor `connect_port`. The mechanism itself is already
    agent-tested (`-k role_none`, `-k control_only`); this step proves only the end-to-end
    wiring.

    **7. Lifecycle on all three teardown paths (EXTLINK-16).** Run to normal completion; press
    STOP mid-run; induce a task exception. Expected in all three: `on_run_stop` ran (visible in
    the Pi's LOCAL log file) and the device returned to idle.
    **Do NOT look for an ES event for the stop itself** — `event_dispatcher.stop()` runs before
    `task.end()`, so that event provably races and may never be dispatched. Verify by effect.

    **8. Egress under real latency (EXTLINK-15).** Point the egress at an endpoint you then hang
    (e.g. a `nc -l` that never responds). Expected: FDA timing visibly unaffected, the queue
    bounds, the drop is RECORDED as a CONTINUOUS event naming what was lost, and `alive` flips
    after the configured consecutive failures.

    **9. Lease from a genuine pilot disconnect (EXTLINK-17).** Start a run on the pilot, confirm
    `GET /api/device-leases` shows the lease, then KILL the pilot process (power/network, not a
    clean stop). Within the reconcile interval, expected: the lease is released. Then re-acquire
    and confirm `DELETE /api/device-leases/{host}` force-releases it. Finally, with a lease held,
    run preflight for a SECOND pilot configured against the same host and confirm the
    `device_held` issue renders in the hardware-check modal naming the holding pilot, subject
    and run.

    **10. Pi test suite (the standing gap since Phase 24).**
    ```bash
    ssh -i ~/.ssh/pi_mics pi@132.77.72.28 'cd ~/Apps/mice_interactive_home_cage && python3 -m pytest tests/ -q'
    ```
    Report the counts.
  </how-to-verify>
  <resume-signal>Paste the output of each numbered step, or type "approved" if every step passed
  and you have nothing to add. Report failures as-is — a documented red step is far more useful
  than a skipped one.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Record the result</name>
  <files>.planning/phases/18-extlink-pi-transport/18-HARDWARE-VALIDATION.md</files>
  <action>
    Fill in `18-HARDWARE-VALIDATION.md` from the checkpoint output: what is PROVEN on hardware,
    what FAILED, and what remains UNPROVEN and why. Follow
    `24-HARDWARE-VALIDATION.md`'s structure — a proven/unproven table per requirement, a defect
    list with the commit that fixed each, and an "exact deployed/registry/DB state" section.

    Be honest about coverage. Any step the user could not complete stays UNPROVEN — do not
    upgrade it on the strength of a passing unit test. In particular, restate the known residual
    risk: Phase 18's lease safety net releases the lease row and lets the run be marked errored,
    but does NOT command the foreign device to stop. A crashed pilot can leave an external
    recorder running until a human notices. Phase 26 decides whether OpenEphys needs its own
    additional reconciliation.

    If any step failed, fix it under this plan (Rule 3 — a defect found during verification is
    owned by the finder), re-verify what can be re-verified without the rig, and add whatever
    still needs a rig re-run to the document's outstanding list rather than silently closing it.
  </action>
  <verify>
    <automated>grep -c "PROVEN\|UNPROVEN" .planning/phases/18-extlink-pi-transport/18-HARDWARE-VALIDATION.md</automated>
  </verify>
  <done>Every one of the ten checkpoint steps has an explicit PROVEN / FAILED / UNPROVEN verdict
  with the evidence pasted. The residual-risk statement is present.</done>
</task>

</tasks>

<verification>
```bash
docker compose exec -T api python -m pytest -q
cd /home/ido/pi-mirror && python3 -m pytest -q tests/test_extlink_wire.py tests/test_extlink_decoder.py \
  tests/test_extlink_liveness.py tests/test_extlink_egress.py tests/test_extlink_lifecycle.py \
  tests/test_wait_extlink_ready_transitions.py tests/test_mics_task_attrs.py
cat .planning/phases/18-extlink-pi-transport/18-HARDWARE-VALIDATION.md
```
Phase gate per `18-VALIDATION.md` § Sampling Rate: full backend suite green + every agent-runnable
Pi-mirror test green + this checkpoint confirmed, before `/gsd:verify-work`.
</verification>

<success_criteria>
- Every manual-only row of `18-VALIDATION.md` has a verdict backed by pasted evidence.
- All three transport roles are demonstrated on the real rig, including the no-socket one.
- The readiness gate's three exits and the lifecycle hooks' three teardown paths are exercised.
- The device lease releases from a genuine pilot disconnect and from a manual force-release.
- `18-HARDWARE-VALIDATION.md` states plainly what is not proven.
</success_criteria>

<output>
After completion, create `.planning/phases/18-extlink-pi-transport/18-12-SUMMARY.md` and update
`.planning/STATE.md`'s Phase 18 section with the proven/unproven split and any outstanding items.
</output>
