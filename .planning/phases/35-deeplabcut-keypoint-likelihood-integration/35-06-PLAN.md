---
phase: 35-deeplabcut-keypoint-likelihood-integration
plan: 06
type: execute
wave: 4
depends_on: ["35-03", "35-04"]
files_modified:
  - dlc_link/RUNBOOK.md
  - dlc_link/README.md
  - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-FIXTURE-INVENTORY.md
  - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/dlc_cam1_lib.py
  - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/dlc_cam1_signals.py
autonomous: true
requirements: [DLC-01, DLC-08, DLC-13]

must_haves:
  truths:
    - "A generated DLC hardware lib travels the ordinary hw_lib to hw_module to pilot-config to toolkit to preflight pipeline with no new backend endpoint and no substrate change."
    - "The FDA editor's operand picker has real DLC signals to offer, proven against the live API before any human opens a browser."
    - "The demo task definition ships WITHOUT its gated transition, so the browser authoring step actually proves something."
    - "The runbook names all six steps and states which one is the obstacle, in writing."
    - "A researcher who uploads a lib and goes straight to the editor sees an empty picker, and the runbook says why before they hit it."
    - "Graduation is not on the demo task's path."
    - "The demo is gated on an operator-controllable unique bodypart, so the rig proof does not depend on an animal cooperating."
    - "The lib is declared PROVISIONAL on the pose-order probe rather than presented as final."
  artifacts:
    - path: "dlc_link/RUNBOOK.md"
      provides: "The numbered researcher-facing path from a working sender to an authored transition"
      min_lines: 120
    - path: ".planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-FIXTURE-INVENTORY.md"
      provides: "Every row this phase created, with its id, its exact payload, and the teardown command"
      min_lines: 60
    - path: ".planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/dlc_cam1_lib.py"
      provides: "The generated lib source actually uploaded, kept for byte comparison against what is deployed"
  key_links:
    - from: "pilot_hardware_config row for the DLC module on pilot 3"
      to: "the FDA editor's operand picker"
      via: "derive_extlink_keys reading config.source_id, then module_extlink_signals onto the toolkit payload"
      pattern: "extlink_signals"
    - from: "dlc_link/RUNBOOK.md"
      to: "D-36's forced ordering"
      via: "the runbook states lib then module then config row then author, and says the picker is silently empty otherwise"
      pattern: "silently empty"
---

<objective>
Stand up the DLC fixture as an ORDINARY customer of the existing hardware-lib pipeline (DLC-01), and
write the researcher-facing runbook that DLC-13(d) makes a deliverable in its own right.

Purpose: DLC-01, DLC-08's prerequisites, DLC-13(d). This plan creates DATA, not code: a hardware lib
version, a hardware module, pilot config rows, a toolkit and a task definition. No new backend
endpoint, no seeded lib, no substrate change.

Output: a standing DLC fixture on pilot 3, a picker proven to have real signals to offer BEFORE a
human opens a browser, and a runbook that names the obstacle rather than papering over it.
</objective>

<the_deliberately_missing_piece>
**The demo task definition ships WITHOUT its gated transition, on purpose.**

DLC-08 requires the transition be authored entirely in the browser. Phase 18 recorded EXTLINK-19 with
a SPLIT VERDICT precisely because task def 434's two transitions were authored via the API by the
coordinator and not through the picker, and plan 18-15 deliberately shipped its demo FDA with the
gated transitions MISSING for the same reason. Authoring the DLC transition here by API would prove
nothing about the picker and would burn the only chance this phase has to exercise it - the first
real human click through that picker happens in plan 35-07 and nowhere else.

So: create the task definition with its states and its ungated transitions, leave the
likelihood-ANDed-coordinate transition absent, and record in the fixture inventory that its absence
is deliberate and which plan fills it in.
</the_deliberately_missing_piece>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-CONTEXT.md
@.planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-LIVENESS-FINDINGS.md
@.planning/phases/34-mics-link-sdk-client-package/34-HARDWARE-VALIDATION.md
@api/extlink_keys.py
@api/routers/toolkit_dispatch.py
@sdk/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the demo lib and create every row the pipeline needs</name>
  <files>.planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/dlc_cam1_lib.py, .planning/phases/35-deeplabcut-keypoint-likelihood-integration/fixtures/dlc_cam1_signals.py, .planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-FIXTURE-INVENTORY.md</files>
  <read_first>
    - dlc_link/src/dlc_link/generate.py and dlc_link/src/dlc_link/config_read.py (the `from_explicit_list` provenance path this task uses while the real config.yaml is not yet in hand)
    - api/routers/hardware_libs.py lines 255-300 (POST /hardware-libs, the multipart upload form: name, file, kind, declared_imports) and lines 657-691 (POST/DELETE /toolkits/{id}/hardware-libs)
    - api/routers/hardware_modules.py lines 20-100 (HardwareModuleCreate and `_validate_class_name`, which checks `ast_metadata["classes"]` - a DIFFERENT structure from `ast_metadata["extlink"]`, so the emitted class must be visible to both)
    - api/routers/pilot_hardware_config.py lines 26-60 (PUT /api/pilots/{pilot_id}/hardware-config/{name} - config stored as-is, caller includes class_name)
    - api/routers/toolkits.py lines 339-460 (POST /toolkits) and lines 597-694 (POST /task-definitions)
    - api/routers/toolkit_dispatch.py lines 270-300 (config rows are matched by NAME against the toolkit's module set, which is why a new toolkit keeps this fixture off every existing task definition's path)
    - .planning/phases/34-mics-link-sdk-client-package/34-HARDWARE-VALIDATION.md section 1 (the pilot-1 fixture inventory shape to mirror) and section 3b caveat 2 (pilot 3's row 33 is `required: false`)
    - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-CONTEXT.md decisions D-14, D-25, D-27, D-28, and the whole section "The actual model — config.yaml READ 2026-08-31" (D-37 through D-42), which SUPERSEDES D-10's "config.yaml not yet in hand"
  </read_first>
  <action>
Generate the demo lib with `dlc-link-generate` from the REAL project config. Bodyparts:
**`LED_on` and `LED_off`, coordinates enabled on `LED_on` only** - four signals
(`led_on_likelihood`, `led_on_x`, `led_on_y`, `led_off_likelihood`), which at the decimator's 10 Hz
cap is 40 messages per second, inside Phase 18's proven ~60 envelope.

**Why the LEDs and not a body part (D-38).** They are `uniquebodyparts`: single-instance by
construction, so D-37's identity ambiguity cannot apply to them at all. And they are
**operator-controllable and deterministic** - the rig proof does not depend on an animal cooperating,
which makes it repeatable. It still exercises D-19's likelihood-ANDed-with-coordinate pattern
honestly, because `LED_on` has a real position in the arena.

Use `--source-id dlc_cam1`, `--stale-after-ms 200` and `--liveness-hook clock-consistent` (plan
35-02's analysis; note in the inventory that plan **35-09** is the rig confirmation and that if it
refutes the hook, plan 35-08 Task 1 regenerates with `--liveness-hook substrate-default`).

**Do NOT declare `NW`, `NE`, `SE`, `SW`.** They are recorded in D-38 as a deferred normalisation
idea only: corner-based normalisation needs the corners to be reliably detected and adds a failure
mode, so D-18's divide-by-frame-dimensions stands for v1. Declaring them would also spend budget on
signals no transition reads.

**Config source.** D-10 is superseded: the real `config.yaml` has been read and its verbatim key
table is in `35-CONTEXT.md`. If the actual file is reachable from the dev host, pass it to
`--config` and record its real sha256. If it is not (it lives on the Windows vision box), use the
transcription plan 35-03 committed at `dlc_link/tests/fixtures/dlc3_multianimal_config.yaml`, and
record in the inventory that the provenance sha256 is the TRANSCRIPTION's, naming the real path and
the fact that it is a transcription of the table in `35-CONTEXT.md`. Do not silently present a
transcription's hash as the model's.

**Pose order.** Generate WITHOUT `--pose-order` at this stage, so the emitted map records
`POSE_ORDER_SOURCE = config-declared-UNVERIFIED`, and record that value in the inventory. D-42 is an
open empirical question - DLC-Live's documentation says only `(num_bodyparts, 3)` and does not state
whether `single_animal=True` returns the 10 multianimal parts or all 32 including the unique ones,
nor in what row order. **State explicitly in the inventory that this lib is provisional on plan
35-07's probe**, and that if the probe reports a different membership or order, the lib and map must
be regenerated with `--pose-order` and re-uploaded before the live proof means anything. Do not
present the provisional lib as final.

Save both emitted files under `.planning/phases/.../fixtures/` so the exact bytes uploaded are kept
for later byte-comparison against what is deployed.

Then create, in this order, recording every returned id in `35-FIXTURE-INVENTORY.md`:
1. `POST /api/hardware-libs` with `name` `dlc_cam1`, the generated lib source as the uploaded file,
   `kind` `hardware`, `declared_imports` `[]`. A `422` here means the version came back
   `unvalidated` - report the `validation_error` verbatim and fix the generator rather than the row.
2. `POST /api/hardware-modules` with `name` `dlc_cam1`, `hardware_lib_id` from step 1 and
   `class_name` matching the generated class. A 422 saying the class is not in the lib AST means the
   emitted class is invisible to `_validate_class_name`, which reads `ast_metadata["classes"]` - that
   is a generator bug, not a payload bug.
3. `POST /toolkits` creating a NEW toolkit named `dlc_demo`, cloned from toolkit 100's source so no
   existing task definition's dispatch changes. Do NOT link the DLC lib onto toolkit 100: config rows
   are matched by name against the toolkit's module set, so linking there would put a socket-binding
   module on the path of real task definitions 186 and 434. If cloning proves impossible, STOP and
   report rather than falling back to toolkit 100 silently.
4. `POST /toolkits/{dlc_demo_id}/hardware-libs` linking the new lib, and link whatever else toolkit
   100 links that the demo task actually needs. Pin lib 8 to version id 174, never version id 25 -
   run 583 established v4/id 25 crashes on `.213`'s numpy with `AttributeError: module 'numpy' has no
   attribute 'int'`, and `hardware_libs.stable_version_id` for lib 8 still points at it.
5. `PUT /api/pilots/3/hardware-config/dlc_cam1` with config
   `{"class_name": "<generated class>", "role": "router_bind", "listen_port": 5601,
   "host": "132.77.73.125", "source_id": "dlc_cam1", "stale_ms": 3000, "required": false,
   "wait_timeout_s": 30, "egress_fail_threshold": 3}`.
   Port 5601, NOT 5599: pilot 3's row 33 already binds 5599 for `source_id` `demo`, and two
   `router_bind` modules on one pilot cannot share a port. `required: false` matches row 33's posture
   on this pilot, which per Phase 34 means the correct start order here is RUN FIRST, then sender -
   the opposite of pilot 1. Before writing, `GET /api/pilots/3/hardware-config` and assert no existing
   row already uses `source_id` `dlc_cam1` or `listen_port` 5601 (threat T-35-13).
6. `POST /api/task-definitions` creating `dlc_demo` on the new toolkit, with an FDA of at least
   `wait`, `armed` and `fired` states, an unconditional `fired -> wait` transition, and
   **NO transition referencing any `dlc_cam1.*` view key** (and therefore no threshold value baked in
   anywhere - the threshold comes from plan 35-07's measured likelihood distribution, per D-41, and
   the transition itself is authored in the browser) - see this plan's
   `<the_deliberately_missing_piece>` block. It must contain no `INC_TRIAL_COUNTER` and no graduation
   condition anywhere: from a GUI-built task definition that special is a silent no-op (it sends an
   empty dict and the orchestrator drops it on the missing `subject` key, proven on run 562), so
   putting graduation on the demo's path would produce a mystery, not a test (D-28). State that
   omission in the inventory as deliberate.
7. Pin the DLC lib version on the new task definition via
   `PUT /api/task-definitions/{id}/hw-lib-versions/{lib_id}` so the demo is pinned to an exact
   version rather than resolving through `active_version_id`.

Write `35-FIXTURE-INVENTORY.md` in the shape of `34-HARDWARE-VALIDATION.md` section 1: a table of
every entity with its id, its exact payload, what created it, and a numbered teardown procedure that
removes them in reverse dependency order. Add a line stating what is deliberately absent and which
plan adds it.
  </action>
  <verify>
    <automated>TOKEN=$(python3 -c "import json;print(json.load(open('orchestrator/orchestrator/prefs.json'))['MICS_API_TOKEN'])") && curl -sf -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/pilots/3/hardware-config | python3 -c "
import json,sys
rows=json.load(sys.stdin)
by={r['name']:r['config'] for r in rows}
c=by['dlc_cam1']
assert c['source_id']=='dlc_cam1' and c['role']=='router_bind' and c['listen_port']==5601, c
assert c['required'] is False and c['stale_ms']==3000, c
ports=[r['config'].get('listen_port') for r in rows if r['config'].get('role')=='router_bind']
assert len(ports)==len(set(ports)), ('router_bind port collision', ports)
sids=[r['config'].get('source_id') for r in rows]
assert len(sids)==len(set(sids)), ('source_id collision', sids)
print('pilot 3 config row ok, no port or source_id collision')
"</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/pilots/3/hardware-config` contains a row named `dlc_cam1` with exactly the config keys and values listed in the action, `listen_port` 5601 and `required` false.
    - No two `router_bind` rows on pilot 3 share a `listen_port`, and no two rows share a `source_id`.
    - `GET /api/hardware-modules` contains a module named `dlc_cam1` whose `class_name` equals the generated class name and whose `hardware_lib_id` is the newly uploaded lib.
    - The uploaded lib's active version source is byte-identical to `fixtures/dlc_cam1_lib.py` (compare with `hashlib.sha256`).
    - The generated lib declares exactly four signals - `led_on_likelihood`, `led_on_x`, `led_on_y`, `led_off_likelihood` - and declares none of `NW`, `NE`, `SE`, `SW` and no multianimal bodypart.
    - The emitted map records `POSE_ORDER_SOURCE = config-declared-UNVERIFIED`, and `35-FIXTURE-INVENTORY.md` states in words that the lib is PROVISIONAL on plan 35-07's D-42 probe and must be regenerated with `--pose-order` if the probe disagrees.
    - The inventory states whether the config provenance sha256 is the real file's or the transcription's, and names the real path either way.
    - `GET /api/task-definitions/{dlc_demo_id}` returns an `fda_json` containing zero occurrences of the substring `dlc_cam1.` and zero occurrences of `INC_TRIAL_COUNTER`.
    - Toolkit 100's linked-lib set is unchanged: `GET /api/toolkits/100/hardware-libs` returns the same lib id set as before this plan ran, recorded before and after in the inventory.
    - `35-FIXTURE-INVENTORY.md` lists every created id and a reverse-order teardown procedure.
  </acceptance_criteria>
  <done>The DLC lib is a versioned hardware lib, a registered module, a pilot config row and a toolkit selection, created entirely through existing endpoints, on a new toolkit that leaves every real task definition's dispatch untouched, with the gated transition deliberately absent.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the editor picker has real signals to offer, before anyone opens a browser</name>
  <files>.planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-FIXTURE-INVENTORY.md</files>
  <read_first>
    - api/extlink_keys.py (`derive_extlink_keys` lines 52-62 and `module_extlink_signals` lines 82-167 - the per-pilot provenance and the exact payload shape)
    - api/routers/toolkits.py lines 145-162 and 210-211 (`extlink_signals_by_module` wiring onto the toolkit payload) and lines 171-216 (`GET /toolkits/by-name/{name}`, which is the call TaskEditor.tsx line 94 makes)
    - api/fda_validation.py (the save gate that returns 422 for an undeclared view key)
    - web_ui/react-src/src/components/ConditionBuilder.tsx line 50 and web_ui/react-src/src/components/ArgInput.tsx line 38 (both pass `toolkit.extlink_signals` into `buildViewOptions`; DLC-08 requires BOTH be exercised)
    - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-CONTEXT.md, the "Reviewed Todos" entry about the stale React bundle trap
  </read_first>
  <action>
Every browser observation in plan 35-07 is worthless if the picker had nothing to offer or if the
browser is running a stale bundle. Rule both out here, from the API and the filesystem, and record the
evidence.

1. `GET /api/toolkits/by-name/dlc_demo` (the exact call the task editor makes) and assert the
   response's `extlink_signals` contains an entry for the `dlc_cam1` module listing all five generated
   signal names plus `alive`, each with dtype `float` (and `alive` as bool), with pilot-3 provenance.
   Record the response's `extlink_signals` block verbatim in the inventory - that block is what the
   picker renders, and having it on record turns any later "the picker was empty" report into a
   diagnosable difference rather than a guess.
2. Demonstrate D-36's ordering constraint against the LIVE system rather than in prose: temporarily
   read the `dlc_cam1` config row, confirm `derive_extlink_keys` returns `[]` for a config with the
   `source_id` key removed and the full dotted key list with it present. Do this as a read-only
   computation against the fetched config - do NOT delete and recreate the live row to demonstrate it.
3. Confirm the save gate: `PUT` the demo task definition with a transition referencing
   `dlc_cam1.made_up_signal` and assert a 422; then restore the definition to exactly its prior
   `fda_json`. Record both the 422 body and the restoration. This proves the gate is live for THIS
   module, which is the half of DLC-08 that does not need a human.
4. Rule out the stale-bundle hazard: compare the modification time and content hash of
   `web_ui/static/react/main.js` against a fresh `cd web_ui/react-src && npm run build` output, or
   confirm from the container that the served bundle matches the current source. Record which check
   was performed and its result. If the bundle is stale, rebuild it and say so - a stale bundle makes
   a working picker look broken and would send plan 35-07 chasing a nonexistent frontend bug.

Do not modify any file under `api/` or `web_ui/react-src/src/`. This task reads and, at most,
rebuilds the bundle.
  </action>
  <verify>
    <automated>cd /home/ido/mics-backend && TOKEN=$(python3 -c "import json;print(json.load(open('orchestrator/orchestrator/prefs.json'))['MICS_API_TOKEN'])") && curl -sf -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/toolkits/by-name/dlc_demo | python3 -c "
import json,sys
tk=json.load(sys.stdin)
sig=tk.get('extlink_signals') or {}
blob=json.dumps(sig)
for name in ['led_on_likelihood','led_on_x','led_on_y','led_off_likelihood','alive']:
    assert name in blob, ('missing from picker payload', name, blob[:400])
print('picker payload carries all declared signals')
" && grep -q "extlink_signals" .planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-FIXTURE-INVENTORY.md && echo "recorded"</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/toolkits/by-name/dlc_demo` returns an `extlink_signals` block containing `led_on_likelihood`, `led_on_x`, `led_on_y`, `led_off_likelihood` and `alive`, and that block is recorded verbatim in `35-FIXTURE-INVENTORY.md`.
    - `derive_extlink_keys` is shown returning `[]` for the same config with `source_id` removed and the full dotted list with it present, computed read-only against the live config row and recorded.
    - A `PUT` of the demo task definition referencing `dlc_cam1.made_up_signal` returns 422, the body is recorded, and a subsequent `GET` shows the definition's `fda_json` byte-identical to its pre-test value.
    - The React bundle freshness check is recorded with its method and result; if a rebuild was performed, the inventory says so.
    - `git status --porcelain api/ web_ui/react-src/src/` is empty.
  </acceptance_criteria>
  <done>The picker's data path is proven end to end from the database to the exact API call the editor makes, the empty-picker ordering constraint is demonstrated against the live system, the save gate is shown rejecting an undeclared key for this module, and the stale-bundle hazard is ruled out before any human looks at a screen.</done>
</task>

<task type="auto">
  <name>Task 3: Write the runbook, and name the obstacle</name>
  <files>dlc_link/RUNBOOK.md, dlc_link/README.md</files>
  <read_first>
    - sdk/README.md (the structure and tone precedent; it is device-neutral by construction so it cannot be copied verbatim, but its shape is the target)
    - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-FIXTURE-INVENTORY.md (the six steps this runbook describes, as actually performed in Task 1, with the real payloads)
    - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-CONTEXT.md decisions D-04, D-05, D-06, D-09, D-27, D-35, D-36, and the whole section "The actual model — config.yaml READ 2026-08-31" (D-37 identity, D-38 unique bodyparts and the deferred corners, D-39 the 72 candidates, D-40 the three sources, D-41 pcutoff, D-42 the pose-order probe)
    - .planning/REQUIREMENTS.md DLC-13 (the ergonomics bar, and the clause making an un-collapsible runbook itself the deliverable)
    - api/extlink_keys.py lines 52-62 (the mechanical reason the picker is silently empty without a config row)
  </read_first>
  <action>
Write `dlc_link/RUNBOOK.md`: the researcher-facing path from "my model runs" to "I authored a
transition on my keypoint in the browser". Numbered, every step named, nothing collapsed (D-35).

Vision-box half:
1. Clone the DeepLabCut environment before installing anything - `conda create -n <name> --clone DEEPLABCUT`.
   State why: `DEEPLABCUT` holds the working DLC 3.0 training setup that produced the model, and
   installing into it gambles that setup (D-04).
2. Install `deeplabcut-live[pytorch]`, `mics-link` and `dlc-link`. State that on the verified target
   environment the mics-link install is a dependency NO-OP (pyzmq 27.1.0 and msgpack 1.2.1 both
   exceed the floors) and that the only package `deeplabcut-live` adds is `colorcet`. State that if
   pip proposes to UPGRADE pyzmq, the researcher must stop and report it rather than proceed -
   Jupyter, IPython, Spyder and napari all depend on pyzmq.
3. Run `python -m mics_link.selfcheck` and state plainly why it is not optional: something in the
   DeepLabCut 2.2.3 import chain patches `msgpack` globally, and a sender that imports DLC 2.x would
   have every frame silently numpy-extended and counted `malformed` on the Pi with nothing visible
   from the sender (D-06, D-09d). The target DLC 3.0 environment does not carry that hazard, and the
   selfcheck is how the researcher knows theirs does not either.
4. Export the model: `deeplabcut.export_model(...)`. State that for the PyTorch engine `model_path` is
   the resulting `.pt` FILE, not a directory and not the project directory, that a DLC 3.0 project
   typically has no `exported-models/` until this is run, and that DeepLabCut-live issue 137 and
   image.sc report DLC 3.0 exports emitting a single `.pt` while DLC-Live still complains about a
   missing pose_cfg - so budget for this being the first real obstacle (D-09).
5. Measure the achieved frame rate before choosing a decimation rate, and say why: GPU inference on
   the target RTX 3060 is proven, so real fps is high, which makes decimation MORE load-bearing, not
   less (D-09b, D-22).
6. **Probe the pose array before generating anything**: `dlc-link-live --probe-pose --video <a project video> --model-path <the .pt>`.
   State why this step exists and why it is not optional. DLC-Live's documentation says only that
   `single_animal=True` yields `(num_bodyparts, 3)`; it does NOT say whether that is the 10
   multianimal parts or all 32 including the 22 unique ones, nor in what row order (D-42). The
   adapter indexes `pose[i]` positionally, so a wrong order sends the wrong keypoint's numbers under
   the right name - the worst failure mode available, because nothing looks broken. State the
   contingency plainly: **if the unique bodyparts are ABSENT under `single_animal=True`, the LED demo
   needs `single_animal=False` plus explicit detection selection, which changes the adapter's shape** -
   that is a design change to be made deliberately, not discovered mid-run.
7. Generate the lib and the signal map with
   `dlc-link-generate --config <config.yaml> --source-id ... --bodyparts ... --coords ... --pose-order-file <from step 6>`.
   Show the `--config` form as the normal path and the explicit-list form as the fallback. State that
   `--bodyparts` is REQUIRED and that there is no flag that emits everything: this project alone
   offers 72 candidates (10 multianimal parts x 5 individuals + 22 unique), and at 10 Hz even the 22
   unique parts would be 220 messages per second against a proven ~60 envelope (D-39).
8. **Measure the likelihood distribution before choosing a threshold.** State the trap in the
   researcher's own terms: this project sets `pcutoff: 0.01`, two orders of magnitude below
   DeepLabCut's 0.6 default, so a condition authored as `> 0.9` may fire never, or always, and either
   way it will not mean what the author thought. The threshold is read off the measured distribution
   of the DECLARED bodyparts on a real video (D-41), and plan 35-07 records the measurement.

**A section titled "What you can and cannot declare", stated as fact rather than preference:**
- **Per-individual signals are technically UNAVAILABLE live, not merely deferred (D-37).** This
  project sets `identity: false` with `default_track_method: ellipse`, so individual identity is
  assigned **post-hoc** by `convert_detections2tracklets` and `stitch_tracklets` - a batch step
  DLC-Live does **not** run. In live inference the detection index is unstable frame to frame, so a
  `mouse3_nose_x` signal would silently change which animal it refers to between frames. Write this
  as the REASON, so nobody adds per-individual signals later without re-reading it. A model trained
  with `identity: true` is a different situation and would need its own evaluation.
- **`uniquebodyparts` are the ideal v1 signals (D-38)** - single-instance by construction, so the
  ambiguity above cannot apply. This project's 22 include `LED_on`/`LED_off`, which the demo uses
  because an operator can drive them deterministically, and the four arena corners `NW`/`NE`/`SE`/`SW`.
- **The corners are a deferred idea, not a v1 feature.** Using them as a normalisation reference
  frame would make a condition a property of the arena rather than of the frame buffer, which is what
  D-18 is reaching for - but it requires the corners to be reliably detected and adds a failure mode.
  v1 divides by the frame's own width and height. Recorded so it is not mistaken for an oversight.

Backend half - the six steps D-35 counts, each with the exact call from `35-FIXTURE-INVENTORY.md`:
upload the lib version, register the hardware module, create the `pilot_hardware_config` row, select
the toolkit, run preflight, author the transition.

**Then a section titled "The obstacle, named" - this is a deliverable, not an aside.** State, in the
researcher's own terms:
- The ordering is FORCED and non-obvious. `derive_extlink_keys` returns an empty list unless
  `config["source_id"]` is a non-empty string in a `pilot_hardware_config` row. Signal NAMES are
  pilot-invariant (they come from the lib's AST) but the KEYS are not. So a researcher who uploads a
  lib and goes straight to the FDA editor sees a **silently empty picker with no error to explain
  it** - nothing tells them a config row is missing (D-36).
- The count is six steps, three of which have no UI affordance connecting them, and none of which
  tells the researcher what the next one is.
- Name what would fix it, and name that it is deliberately NOT fixed here: a single
  `POST /api/dlc/register` collapsing lib, module and config row into one atomic call, or an empty-
  picker message that says "no pilot config row for this module". Both are recorded as deferred in
  `35-CONTEXT.md` and are revisited only now that the obstacle has a name.

Also document, with reasons: the sender is started BEFORE the run when the pilot's config row has
`required: true` and AFTER the run when it has `required: false`, and pilot 3's DLC row is
`required: false`, so the order there is run first then sender (D-27 plus Phase 34's run-582 burst
caveat); coordinates are opt-in per keypoint and every coordinate condition must be ANDed with that
keypoint's likelihood, with a worked condition shown; and the replay path for reproducing a run
without the camera.

Update `dlc_link/README.md` to link the runbook and to state the one-line install.

Write nothing in this runbook that claims a latency, and claim no rig result that has not yet been
observed - plan 35-07 and 35-08 supply those, and the runbook's rig sections must read as
instructions, not as results.
  </action>
  <verify>
    <automated>cd /home/ido/mics-backend && python3 - <<'PY'
import re
t = open('dlc_link/RUNBOOK.md').read()
low = t.lower()
for needle in ["--clone", "selfcheck", "export_model", "dlc-link-generate", "pilot_hardware_config",
               "silently empty", "the obstacle, named", "required: false", "colorcet",
               "--probe-pose", "stitch_tracklets", "uniquebodyparts", "pcutoff",
               "what you can and cannot declare", "72"]:
    assert needle.lower() in low, "runbook missing: " + needle
assert "single_animal=True".lower() in low
assert not re.search(r"\blatenc", low), "runbook must claim no latency"
steps = re.findall(r"^\s*\d+\.", t, re.M)
assert len(steps) >= 12, ("too few numbered steps", len(steps))
print("runbook ok:", len(steps), "numbered steps")
PY</automated>
  </verify>
  <acceptance_criteria>
    - `dlc_link/RUNBOOK.md` contains at least 12 numbered steps and section headings containing `The obstacle, named` and `What you can and cannot declare`.
    - It contains the literal strings `--clone`, `mics_link.selfcheck`, `export_model`, `dlc-link-generate`, `--probe-pose`, `pilot_hardware_config`, `silently empty`, `required: false`, `colorcet`, `single_animal=True`, `stitch_tracklets`, `uniquebodyparts`, `pcutoff` and `72`.
    - The declaration section states per-individual signals as technically unavailable live and gives `identity: false` plus the post-hoc `stitch_tracklets` step as the reason, not a preference.
    - The corners `NW`/`NE`/`SE`/`SW` are described as a deferred normalisation idea and the runbook does not instruct anyone to declare them.
    - The probe step names the contingency that absent unique bodyparts under `single_animal=True` would require `single_animal=False` plus explicit detection selection.
    - The threshold step states `pcutoff: 0.01` versus DeepLabCut's 0.6 default and instructs the reader to measure rather than assume.
    - It contains no occurrence of the word `latency` in any form.
    - The obstacle section states the count of six steps, names `derive_extlink_keys` and the empty-`source_id` behaviour, and names both deferred fixes as deliberately not implemented in this phase.
    - It states the start-order rule in both directions (`required: true` sender first, `required: false` run first) and identifies which applies to pilot 3.
    - `dlc_link/README.md` links to `RUNBOOK.md`.
    - Every rig instruction is phrased as an instruction; no sentence asserts an observed rig result.
  </acceptance_criteria>
  <done>A numbered runbook covers both halves of the path with nothing collapsed, and the ordering trap that makes the picker silently empty is written down as a named finding with its mechanical cause and its two possible fixes, rather than absorbed as folklore.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated lib source -> `hardware_lib_versions.source_code` -> Pi `exec` | Generated Python is uploaded and later executed on the pilot. |
| new pilot config row -> a socket bound on the pilot | A `router_bind` row makes the pilot listen on a port for the duration of a run. |
| shared live database | This plan writes rows to the same database real experiments use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-25 | Elevation of Privilege | uploaded generated lib source executed on the Pi | mitigate | The source is generated by plan 35-03's validated generator, whose identifier gate and post-render AST re-parse are the code-execution control; the exact uploaded bytes are archived under `fixtures/` and hash-compared against what the API stored. |
| T-35-26 | Denial of Service | port and `source_id` collision on pilot 3 | mitigate | Port 5601 is chosen because row 33 already binds 5599; the plan asserts no duplicate `listen_port` and no duplicate `source_id` across all of pilot 3's rows before and after the write. |
| T-35-27 | Tampering | contaminating real task definitions 186 and 434 | mitigate | A NEW toolkit is created rather than linking onto toolkit 100; config rows are matched by name against the toolkit's module set, so the DLC module stays off every existing definition's dispatch. Toolkit 100's linked-lib set is recorded before and after and asserted unchanged. |
| T-35-28 | Spoofing | an unauthenticated sender claiming `source_id` `dlc_cam1` | accept | The substrate's `identity_ok` check is an identity match, not authentication, and this phase adds no new ingress (DLC-01). Lab-internal network. Recorded so the limit is explicit; authenticating the extlink channel is a Phase 18 amendment. |
| T-35-29 | Repudiation | a browser observation that cannot be diagnosed | mitigate | The picker payload is recorded verbatim before any human looks, and the React bundle's freshness is checked and recorded, so a later "the picker was empty" report is a diagnosable difference rather than a guess. |
| T-35-SC | Tampering | package-manager installs | mitigate | This plan installs nothing; it documents installs the user performs in plan 35-07, where the blocking legitimacy checkpoint lives. |
</threat_model>

<verification>
- Every created id is recorded in `35-FIXTURE-INVENTORY.md` with a reverse-order teardown.
- Toolkit 100's linked libs are unchanged.
- The demo task definition contains no `dlc_cam1.` view key and no `INC_TRIAL_COUNTER`.
- `git status --porcelain api/ sdk/ web_ui/react-src/src/` is empty.
- No file under `/home/ido/mics_core/` or `/home/ido/pi-mirror/` modified.
</verification>

<success_criteria>
- The DLC lib travels the ordinary pipeline with no new backend endpoint (DLC-01).
- The picker is proven to have real signals to offer before a human opens a browser (DLC-08's prerequisites).
- The gated transition is deliberately absent so the browser step proves something.
- The runbook names the obstacle in writing (DLC-13).
</success_criteria>

<output>
Create `.planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-06-SUMMARY.md` when done.
</output>
