---
phase: 38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi
plan: 04
type: execute
wave: 3
depends_on: ["38-03"]
files_modified:
  - dlc_link/pyproject.toml
  - .planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-HARDWARE-VALIDATION.md
autonomous: false
requirements: [CAM-01, CAM-03, CAM-05, CAM-07, CAM-08, CAM-16]

must_haves:
  truths:
    - "The camera's TOPOLOGY (D-65 T1-T5, D-75 T5a/T5b/T6) is established from the LAB COMPUTER before the wheel is built, and the answer is recorded as one of the named branches rather than inferred from whether a command happened to work."
    - "The topology verdict is already T5 (D-75: DMK 33GP1300, GigE Vision, mono) and the session's job is to resolve T5a vs T5b with ONE dshow probe, not to rediscover the make and model."
    - "T5b — no DirectShow wrapper — is recognised as a STOP with a named next step (a vendor-SDK shim behind plan 38-01's seam, preferably under T6, and only after the vision box has been probed for the vendor's own DirectShow driver), not retried with more flags."
    - "Whether the lab computer is also recording from the camera is established, because under T4 exclusive device access makes a relay and a local recording mutually exclusive."
    - "A camera filming the rig reaches a trained DeepLabCut model, and the keypoints it produces reach pilot 3 as declared signals, on the same proven chain Phase 35 exercised with a file."
    - "The researcher watches the annotated frames while the task runs, and can see whether the model is following the animal."
    - "The camera's own delivered rate is MEASURED with no model loaded, before any keep-up claim is made."
    - "The keep-up criterion is a number that came off that measurement, recorded in writing, not a threshold anyone guessed."
    - "Whether the orchestrator and ElasticSearch are reachable from the vision box is established by a command, not assumed."
    - "Whether Jupyter exists in the DLC environment is established before anything depends on it, and no install proceeds that would touch pyzmq, numpy or torch."
    - "Every rig and vision-box action is performed by the USER; the agent runs no git, no Python and no process control on any Pi."
    - "The researcher's DLC project directory is provably unchanged: a recursive manifest before and after differs in nothing."
    - "Every command handed to the user states what it writes and where, or says it writes nothing."
    - "No number produced by this session is a latency, and none is presented as one."
  artifacts:
    - path: ".planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-HARDWARE-VALIDATION.md"
      provides: "The session log: preconditions, the exact commands, the write-footprint table, the discovery answers, the measured rates, the derived keep-up criterion, and a per-truth verdict ledger"
      min_lines: 120
  key_links:
    - from: "the vision box's camera"
      to: "an ES state_transition document"
      via: "dlc-link-live --source -> DLCProcessor -> mics-link -> pilot 3 :5601 -> dlc_cam1 lib 189 -> FDA"
      pattern: "state_transition"
    - from: "the measured capture-only baseline rate"
      to: "the --min-rate value written into RUNBOOK.md by plan 38-05"
      via: "the keep-up criterion is derived, then recorded, then used"
      pattern: "min-rate"
---

<objective>

> ## ⚠ RUN STEP 1a-0 BEFORE WAVE 1. It is one command and it can end the phase.
>
> D-67 ("topology is discovered FIRST, **before the wheel is built**") and D-75 ("it **is the first
> thing to run**") both put this ahead of everything, but the wave graph puts this plan in wave 3,
> behind eight code tasks and behind this plan's own Task 1 wheel build. That ordering was set when
> the camera was unknown. It no longer holds.
>
> **On the lab computer, before any of 38-01/38-02/38-03 is executed:**
>
>     ffmpeg -list_devices true -f dshow -i dummy
>
> It writes nothing, needs no wheel, needs nothing from any other plan, and its two outcomes need
> different work: **T5a** and the phase proceeds as planned; **T5b** and Task 2 terminates the phase
> at its own stop condition, making three waves of preceding code speculative.
>
> Running it early is a pure saving and costs nothing. If it has already been run, its verdict is in
> `38-HARDWARE-VALIDATION.md` §1 and the rest of this plan is read against that cell.
>
> *(Recorded rather than restructured: extracting this into a wave-0 discovery plan is the fuller
> fix the plan-checker recommends, and it changes the phase's wave graph — a decision for the
> researcher, not a doc edit.)*

Point the installed camera at the rig and prove the whole thing: frames in, keypoints to pilot 3,
FDA transitions in ElasticSearch, and the researcher watching it happen. Then measure what the
camera and the model actually do together, and derive the keep-up criterion from that measurement
rather than from anyone's expectation.

Purpose: CAM-01, CAM-03, CAM-05, CAM-07, CAM-08 — proven on hardware — plus CAM-16's discovery
half. This is the plan the 2026-09-08 deadline is about.

**On the T5b branch this plan delivers CAM-16 only.** CAM-01, CAM-03, CAM-05, CAM-07 and CAM-08 are
then recorded as `NOT EXERCISED — no camera could be opened (T5b)` in the per-truth ledger, and that
is an honest terminal state, not a failure to retry. Do not mark them satisfied on the strength of
the file-based Phase 35 evidence; the point of this plan is the camera.

Output: `38-HARDWARE-VALIDATION.md` with the discovery answers, the measured rates, the derived
`--min-rate`, and an honest per-truth ledger in the shape of `35-HARDWARE-VALIDATION.md` §7.
</objective>

<standing_rules_this_plan_operates_under>
1. **Never run git, Python or process control on any Pi.** Every rig action below is USER-RUN: the
   plan supplies the exact command, the user runs it, the agent waits. The agent's own actions are
   confined to this repository and to the backend HTTP API.
2. **Never write into the researcher's DLC project directory.** Reading is always fine. Phase 35
   violated this (`35-HARDWARE-VALIDATION.md` §8) and could not make the claim afterwards because no
   manifest existed. A before/after manifest is mandatory here (D-64). This phase runs **no
   `export_model` at all** — the model is already exported to `C:\MICS\dlc_test\exported-models-pytorch\`
   — so the expected diff is empty by construction.
3. **The vision box is Windows, driven by a human, and long single-line commands break on paste**
   (`35-HARDWARE-VALIDATION.md` §9e). Every command below uses the `set VAR=...` then `%VAR%` idiom
   and no line exceeds roughly 70 characters.
4. **Every command states what it writes and where, or says it writes nothing** (D-48).
5. **No latency claims.** Counts and rates only; the vision box's clock and the Pi's clock are
   different domains and Phase 28 owns timing.
6. **`opencv-python` is never installed.** `opencv-python-headless` stays; both ship the same `cv2`
   module and the second one to land clobbers the first, taking the DLC stack with it.
</standing_rules_this_plan_operates_under>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-CONTEXT.md
@.planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-DECISIONS.md
@.planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-HARDWARE-VALIDATION.md
@.planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-FIXTURE-INVENTORY.md
@dlc_link/RUNBOOK.md
</context>

<the_standing_fixture_this_session_uses>
Nothing about the backend changes in this plan. The Phase 35 fixture is still live and is reused
exactly as it stands:

| Object | Value |
|---|---|
| Hardware lib | 243 `dlc_cam1` v2 (**version id 189**) — `nose_likelihood`, `nose_x`, `nose_y` |
| Task definition | 626 `dlc_demo`, toolkit 157, pins `{"45": 41, "243": 189}` |
| Pilot config row | 41 `dlc_cam1`, `router_bind`, port **5601**, `required: false`, `stale_ms` 3000, `egress_fail_threshold` 3 |
| Pilot | 3 / `RecordingBox` (`132.77.73.213`) |
| Authored transition (run 588's retuned set) | `wait -> armed`: likelihood > 0.6 AND x > 0.50 AND y > 0.50; `armed -> fired`: x < 0.40 OR y < 0.40 |

`required: false` means **start the run FIRST, then the sender** (D-27). Starting the sender first
produces a flushed DEALER backlog delivered in a burst on connect, which looks like a working run
and is not live streaming.

Port **5601**, not 5599. Pilot 3 also binds 5599 for `ExtlinkDemo`, and a wrong-but-open port
succeeds into the wrong fixture.

**The thresholds above were measured against a VIDEO FILE of five mice.** A camera pointed at this
rig sees a different scene, so the likelihood distribution may differ and the transition may fire
constantly or never. That is an expected outcome of this session, not a failure: it is recorded, and
re-deriving the threshold from a fresh distribution is plan 38-05's input, not a mid-session
improvisation.
</the_standing_fixture_this_session_uses>

<tasks>

<task type="auto">
  <name>Task 1: Bump to 0.2.0, release it to PyPI, and write the session log before the session</name>
  <files>dlc_link/pyproject.toml, .planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-HARDWARE-VALIDATION.md</files>
  <read_first>
    - dlc_link/pyproject.toml (the version line and the extras — nothing but the version changes)
    - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-HARDWARE-VALIDATION.md (the document shape this one copies: headline, what was run, findings, measurements, a per-truth ledger, defects, rollback)
    - .planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-DECISIONS.md D-63, D-64
    - dlc_link/RUNBOOK.md, the "Ports" and "Start order" sections (the commands in this log must be byte-identical to their runbook counterparts, never paraphrased)
  </read_first>
  <action>
**Version.** Set `version = "0.2.0"` in `dlc_link/pyproject.toml`. Nothing else in that file changes.
`mics-dlc-link` **0.1.0 is already installed on the vision box**; republishing 0.1.0 is not merely a
no-op, it is impossible — PyPI releases are immutable and an upload of an existing version is
rejected. The bump is also what makes `pip show mics-dlc-link` usable as evidence of which build is
running. Add a one-line comment above the version recording that (D-63).

**Release.** The package is published to PyPI by tag; there is no wheel to build by hand and nothing
to stage on a share (**D-86**, 2026-09-10 — supersedes D-63's wheel mechanics, not its version-bump
reasoning). Commit the bump, then:

    cd /home/ido/mics-backend
    git tag mics-dlc-link-v0.2.0
    git push origin mics-dlc-link-v0.2.0

Then WAIT for the `publish clients` workflow to go green and confirm the release is actually on the
index before the rig session starts:

    gh run watch $(gh run list --limit 1 --json databaseId --jq '.[0].databaseId') --exit-status
    curl -s https://pypi.org/pypi/mics-dlc-link/json | python3 -c "import sys,json; print(json.load(sys.stdin)['info']['version'])"

That must print `0.2.0`. See `RELEASING.md` for the full procedure and its traps.

*Write footprint: one commit and one tag in this repository, and one immutable release on PyPI.
Writes nothing on any Pi and nothing on the vision box.* `mics-link` is unchanged by this phase — it
is neither rebumped nor re-released, and the vision box's existing `mics-link` install stays.

If the workflow fails, report the failing step and stop. Do NOT hand-build a wheel and copy it over
as a workaround: an install whose provenance differs from every other install defeats the point of
recording which build ran.

**Session log.** Write
`.planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-HARDWARE-VALIDATION.md`
with every section present and every result cell reading `pending`:

- **§0 Preconditions** — the fixture table above, the standing rules, and the released version
  (`mics-dlc-link 0.2.0`) with the URL of the workflow run that published it (D-86).
- **§0a WRITE FOOTPRINT table** — one row per command that appears anywhere in this document, naming
  what it writes and where, or the words `writes nothing`. A command appearing in the log without a
  row here is a defect in the log (D-48). Fill this in for every command as you write it, not
  afterwards.
- **§0b Package legitimacy** — one row per package the session might install, initially
  `[ASSUMED] - unverified`. On the current plan the only candidates are `jupyterlab`, `notebook` and
  `ipywidgets`, and only if the discovery in Task 2 finds them absent AND the researcher wants the
  notebook sink rather than the browser sink. State plainly that Task 2's checkpoint is BLOCKING on a
  human opening the public index page for each, that this is never auto-approvable, and that the
  MJPEG sink exists precisely so that the correct answer can be "install nothing".
- **§1 Discovery answers** — every question Task 2 asks, each with a `pending` cell. The first
  block is TOPOLOGY (D-65, D-75): the camera's make and model (**already known — `DMK 33GP1300`,
  GigE Vision, mono, serial `5810436`; recorded as a fact with its source, not as a `pending`
  cell**), what it plugs into, the lab computer's NIC count, whether that machine is also recording,
  whether the camera's Ethernet can reach the vision box (T6), and the resulting verdict. The
  verdict cell reads **`T5a`** or **`T5b`** — not a bare `T5`, which no longer distinguishes
  "proceeds with no code change" from "stops the phase". It is a single named cell because
  everything after it branches on that one value.
- **§2 Camera bring-up** — the commands, with a `pending` result for each.
- **§3 Rate measurement** — an empty table with columns: run, mode, duration_s, frames_read,
  frames_inferred, frames_skipped, rate_read_per_s, rate_inferred_per_s. Above it, one sentence
  stating that every figure in the table is a count or a count-over-duration on the vision box's own
  clock and that **no cell in it is a latency**.
- **§4 The derived keep-up criterion** — `pending`, with the arithmetic written out so the number
  can be checked: baseline from the capture-only run, achieved from the inference run, the ratio, and
  the `--min-rate` value chosen with the sentence explaining why that value and not a rounder one.
- **§5 The live run** — run id, session id, the sender summary, the state-transition sequence, what
  the researcher saw in the viewer.
- **§6 Manifest** — before/after counts and the `Compare-Object` output verbatim.
- **§7 Per-truth ledger** — one row per `must_haves.truths` entry in this plan, each to be marked
  exactly one of `PROVEN` / `PARTIAL` / `UNPROVEN` / `NOT EXERCISED` / `NOT DONE` / `HELD`. Copy the
  scoring line format from `35-HARDWARE-VALIDATION.md` §7. **Do not mark the plan done on a green
  subset** — that instruction is written into the document itself, because Phase 35 needed it.
- **§8 Defects found** — empty, to be filled.
- **§9 Rollback** — nothing to roll back on the backend (this plan creates no rows and changes no
  task definition); on the vision box, `pip install <the 0.1.0 wheel>` returns the previous build.

Every command in §1, §2 and §5 must be written in the `set VAR=...` / `%VAR%` idiom, with no line
over roughly 70 characters, and must be byte-identical to its `RUNBOOK.md` counterpart where one
exists.
  </action>
  <verify>
    <automated>grep -q 'version = "0.2.0"' dlc_link/pyproject.toml && curl -sf https://pypi.org/pypi/mics-dlc-link/0.2.0/json >/dev/null && D=.planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-HARDWARE-VALIDATION.md && for s in "## 0" "WRITE FOOTPRINT" "Package legitimacy" "keep-up criterion" "Per-truth ledger" "Rollback"; do grep -q "$s" $D || { echo "MISSING: $s"; exit 1; }; done && test $(grep -c "pending" $D) -ge 10 && test $(grep -ci "latency" $D) -le 3 && echo "log skeleton complete"</automated>
  </verify>
  <acceptance_criteria>
    - `dlc_link/pyproject.toml` reads `version = "0.2.0"` and differs from its previous state in the version line and one comment only.
    - `mics-dlc-link` 0.2.0 is live on PyPI (`https://pypi.org/pypi/mics-dlc-link/0.2.0/json` returns 200) and the publishing run's URL is recorded verbatim in §0 of the validation log.
    - `git status --porcelain dlc_link/` shows only `pyproject.toml`, and the tag `mics-dlc-link-v0.2.0` exists on `origin`.
    - Every section §0 through §9 exists with at least one `pending` cell where a result belongs.
    - Every command appearing anywhere in the document has a row in §0a naming what it writes or saying `writes nothing`.
    - No command in the document exceeds roughly 70 characters on one line, and every multi-flag command uses `set VAR=` / `%VAR%`.
    - The only occurrences of the word `latency` in the document are in the sentences PROHIBITING latency claims.
    - The §0b legitimacy rows read `[ASSUMED] - unverified` and the document states the checkpoint is blocking on a human and is never auto-approvable.
  </acceptance_criteria>
  <done>The wheel the vision box will install exists and is hashed, and the session has a log with its questions written down before any of them is answered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: CHECKPOINT (USER-RUN, BLOCKING) — discovery: the camera's address, the environment, and what is reachable</name>
  <read_first>
    - .planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-HARDWARE-VALIDATION.md §0, §0a, §0b, §1
    - .planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-DECISIONS.md D-50, D-57, D-59, D-63, D-64
    - dlc_link/RUNBOOK.md step 2 (the standing STOP condition on a proposed `pyzmq` upgrade)
  </read_first>
  <what-built>
    `dlc-link` 0.2.0: camera source addressing, source-derived pacing, the newest-frame reader with a
    skipped-frame count, four deliberate stops, and the annotated viewer with two sinks. All of it
    unit-tested offline on a host with no cv2, no dlclive, no GPU and no camera. Nothing has yet
    touched a real camera.
  </what-built>
  <how-to-verify>
**Everything below is run by YOU. The agent runs none of it. Nothing here touches any Pi.**

**WHICH MACHINE — read this before the first command.** This checkpoint spans two hosts and running
a step on the wrong one produces a confident wrong answer:

| Steps | Machine |
|---|---|
| **Step 1, Step 1a-0, Step 1a** | the **LAB COMPUTER** (the one the camera is attached to and that runs the acquisition software) |
| Steps 0, 1b, 2, 3, 4, 5, 6 | the **vision box** (`YizharGPU12`), in the `mics-dlc` conda environment |

This matters most at Step 1a-0: `ffmpeg -list_devices` run on the vision box enumerates the vision
box's own devices, finds no `DMK 33GP1300` because the camera is not attached to it, and returns a
**false T5b** that terminates the phase on a one-command test. Each step below names its machine
again; if a step does not, it is the vision box.

---

**Step 0 — BASELINE MANIFEST of your DLC project. Run this first, before anything else.**
*Write footprint: writes one text file, outside your project.*

In PowerShell:

    $P = 'C:\Users\YizharGPU12\Desktop\Gili\MultiMice-Gili-2026-06-21'
    Get-ChildItem -Recurse -File $P |
      Select-Object FullName,Length,LastWriteTimeUtc |
      Sort-Object FullName |
      Out-File -Encoding utf8 C:\dlc-work\manifest-before-38.txt

Report the file count and the total size. This phase runs no `export_model`, so the after-manifest
should differ in nothing at all — which is exactly why it is cheap to prove here.

---

**Step 1 — TOPOLOGY. Answer these BEFORE anything else, about the LAB COMPUTER.**
*Write footprint: writes nothing. These are questions, not commands.*

D-65/D-67. The vision box is not in the rig room and the camera is; nothing below works until we
know what stands between them. One of these three answers can end the phase early, which is exactly
why they come first and why none of them needs the 0.2.0 wheel.

1. ~~**What make and model is the camera?**~~ **ANSWERED 2026-09-06 — do not re-ask. See D-75.**

   The acquisition software reports `DMK 33GP1300  BR2_UP  5810436`, live at 30 fps. That decodes
   to: The Imaging Source, **monochrome** (`DMK`), **GigE Vision** (`33G`), 1280×1024 global
   shutter (`P1300`), serial `5810436`.

   **The verdict is T5.** `cv2.VideoCapture` cannot open it by any `--source` value, locally or
   remotely — it speaks GVCP/GVSP over UDP, not RTSP. Transcribe this into §1 as a recorded fact
   with its source ("reported by the acquisition software on the lab computer, 2026-09-06"), not as
   something this session discovered. **Do not run `arp -a`; the label is already in hand.**

   T5 is no longer a single outcome. Step 1a-0 below resolves **T5a** (a DirectShow wrapper exists →
   collapses to T4, no new code) against **T5b** (no wrapper → a capture shim is required, and this
   plan stops). That one command is now the load-bearing question.

2. **What does the camera physically plug into, and how many NICs does the lab computer have?**

       ipconfig /all

   Report the adapter list and each adapter's IPv4 address.

   **Do NOT use the generic T1-T4 mapping here.** It reads an IP address as evidence of T2 or T3,
   and a GigE Vision camera always holds an IP — so that mapping routes this camera into two
   topologies D-75 has already declared dead (GVCP/GVSP are UDP; `portproxy` is TCP-only). The
   verdict for **this** camera is fixed at **T5** and is resolved by Step 1a-0, not by `ipconfig`.

   What `ipconfig /all` is for here is the **arrangement**, which the T5b branch needs:
   - which adapter the camera's subnet is on, and whether it is a **dedicated** NIC or shared with
     the lab LAN
   - that adapter's MTU (jumbo frames, 9000, are the GigE Vision norm — report it even if it is 1500)
   - whether a switch sits between the camera and the NIC

   The generic mapping below is retained only for a future rig with a different camera:
   *vision box by USB/CSI → T1; routable on the lab LAN → T2; two NICs, camera on the private
   range → T3; one NIC, camera USB into it → T4.*

3. **Is the lab computer also recording from that camera?** This decides the architecture:
   DirectShow devices are typically exclusive-access, so a relay and a local recording cannot both
   hold the device. If yes, say which software is doing the recording and whether it can itself emit
   a stream.

   **Do not treat "yes" as a stop (D-75).** This camera is GigE Vision, which unlike DirectShow
   defines **multicast**: the recording application stays the primary/control client and further
   clients attach **read-only as monitors**, which both the vendor SDK and aravis support. So report, in preference order,
   which of these the recorder can do: (a) stream to a **multicast** group so a monitor can attach;
   (b) emit a stream, or accept a **single capture with two sinks**; (c) neither. Only (c) is a
   conflict, and even then it is the researcher's call which wins, not the agent's.

4. **Could the camera's Ethernet cable reach the vision box, or could a NIC be added there?**
   (D-75 **T6**.) GigE means the camera travels over a cable, which USB never did — so this question
   only became askable once the model was known. Report the physical distance, whether a switch sits
   between them, and whether `YizharGPU12` has a spare Ethernet port. A "yes" here removes the lab
   computer from the frame path entirely and is the preferred arrangement if the verdict is T5b.

**Report the T-number.** Everything below branches on it, and it is recorded as one cell in §1.

---

**Step 1a-0 — THE DSHOW PROBE. One command; it decides whether any new code is needed.**
*Write footprint: writes nothing. `-list_devices` enumerates and exits.*

D-75. The camera is GigE Vision, which normally means `cv2.VideoCapture` is out of the question —
but The Imaging Source is one of the few machine-vision vendors that ships a **Windows DirectShow
wrapper**. If it is installed, the camera already presents as an ordinary capture device and
everything below proceeds as **T4** with no change to `dlc_link` whatsoever. On the **lab
computer**:

    ffmpeg -list_devices true -f dshow -i dummy

> ## ✅ ANSWERED 2026-09-10 — the verdict is **T5a**. Do not re-litigate; re-run only to confirm.
>
> `ffmpeg` is **not installed on the lab computer**, so the probe was run against the same source
> ffmpeg's dshow enumerator itself walks — the DirectShow *Video Capture Sources* registry category
> `{860BB310-5D01-11d0-BD3B-00A0C911CE86}`, 64-bit view — in PowerShell, which writes nothing and
> installs nothing:
>
>     Get-ChildItem 'HKLM:\SOFTWARE\Classes\CLSID\{860BB310-5D01-11d0-BD3B-00A0C911CE86}\Instance' | Get-ItemProperty | Select-Object -ExpandProperty FriendlyName
>
> It returned, verbatim:
>
>     DMK 33GP1300 [BR2_UP]
>
> **The camera IS a DirectShow capture source. T5a. T5 collapses to T4, no shim is needed, and this
> plan does not stop here.** The T5b branch below is dead for this camera — keep it only for a
> future rig with a different one.
>
> **The device name is `DMK 33GP1300 [BR2_UP]`** — square brackets included. That is the string
> `-i video="..."` needs in Step 1a. Note ffmpeg's own printed name may differ in whitespace from
> the registry FriendlyName; if Step 1a fails to open it, install ffmpeg
> (`winget install Gyan.FFmpeg`) and re-run the canonical probe to get ffmpeg's exact spelling
> before assuming the camera is at fault.
>
> **Still open, and cheaper than anything else in this phase:** the SECOND probe from D-75's T6.
> This one was run on the LAB COMPUTER. Run the same registry query on the **vision box**
> (`YizharGPU12`) after moving the camera's Ethernet cable and installing the vendor driver there —
> if `DMK 33GP1300` enumerates on that machine, T6 collapses to **T1** (`--source 0`, the
> device-index path 38-01 already builds), the lab computer leaves the frame path entirely, and no
> relay is needed at all.
>
> **Not established by this probe:** whether the 32-bit view also carries the filter (that command
> was mangled in the terminal and never ran cleanly). It does not block anything — the 64-bit
> registration is the one a 64-bit Python needs — but if a 32-bit tool ever has to open this camera,
> check `HKLM\SOFTWARE\Classes\WOW6432Node\CLSID\{860BB310-...}\Instance` before assuming.

Report the **full** device list verbatim — both the video and audio sections — and read it as:

- a device resembling `DMK 33GP1300` (or `The Imaging Source`) in the **video** list → **T5a**.
  Record the device name **exactly as printed, including spaces and punctuation**; it is what
  `-i video="..."` needs in Step 1a, and a name transcribed approximately produces a
  device-not-found that reads like a driver fault. Proceed to Step 1a's T4 half.
- no such device → **T5b**. Stop at the stop condition below. Do not try `--source 0`, do not try
  URLs; there is nothing for either to open.

If `ffmpeg` is not on that machine, a GUI application that lists capture devices answers the same
question (OBS's *Video Capture Device* source, or any app's webcam picker) — report what it lists.
That is a weaker answer than the command and is recorded as such.

**This step is `pending` as of 2026-09-06:** the researcher does not have the lab computer to hand.
It is the first thing to run when they do, and nothing else in this plan should be attempted before
it, because its two outcomes need different work.

---

**Step 1a — MAKE THE FRAME REACHABLE (T3 and T4 only).**
*Write footprint: T3 writes a persistent portproxy rule on the lab computer; T4 writes nothing (a
foreground `ffmpeg` process, stopped with Ctrl-C). Neither touches the vision box.*

Skip entirely for T1 and T2. Per D-66:

> **The T3 half below does not apply to this camera (D-75).** It is retained because the taxonomy is
> general and a future rig may need it. GigE Vision puts **both** its control channel (GVCP, UDP
> 3956) and its stream (GVSP, UDP) on UDP, with broadcast discovery; `netsh portproxy` carries
> **TCP only**. There is no RTSP endpoint to forward and no `rtsp_transport;tcp` to set. **Under
> T5a, go straight to the T4 half.**

**T3 — forward one TCP port. Do not route the subnet.** On the lab computer, as Administrator:

    set CAM=<camera-private-ip>
    set LAN=<lab-computer-lan-ip>
    netsh interface portproxy add v4tov4 listenaddress=%LAN%
      listenport=8554 connectaddress=%CAM% connectport=554

Then on the **vision box**, and this line is not optional:

    set OPENCV_FFMPEG_CAPTURE_OPTIONS=rtsp_transport;tcp

`portproxy` carries **TCP only** while RTSP puts its media on UDP by default, so without that
variable the capture opens and then never delivers a frame — a silent failure that looks like a
broken camera. Report whether you set it.

To undo: `netsh interface portproxy delete v4tov4 listenaddress=%LAN% listenport=8554`.

**T4 — relay MJPEG. This is the CONFIRMED path (D-75 T5a + D-87), not one branch of several.**

> ### ⚠ PREREQUISITE, established 2026-09-10: `ffmpeg` is NOT installed on the lab computer.
>
> The relay below IS ffmpeg, so this is a hard prerequisite of the settled architecture, not an
> optional convenience. Step 1a-0's fallback ("a GUI application answers the same question") covered
> only the *probe*; nothing covers the relay. On the **lab computer**, before anything else in this
> step:
>
>     winget install Gyan.FFmpeg
>
> Then **close and reopen the terminal** so `PATH` picks it up, and confirm:
>
>     ffmpeg -version
>
> *Write footprint: installs ffmpeg on the lab computer and adds it to PATH. This is the first
> thing in this phase that installs software on that machine — say so in the write-footprint table.*
>
> Use a **full** build (Gyan's is): the `-f mpjpeg -listen 1` HTTP listener below is absent from
> minimal/essentials builds, and its absence presents as an unknown-format error, not as a missing
> feature.
>
> Once ffmpeg is present, **run the canonical probe** and prefer ITS device name over the registry
> FriendlyName recorded in Step 1a-0 — ffmpeg matches on its own spelling:
>
>     ffmpeg -list_devices true -f dshow -i dummy

On the lab computer, with the device name from the probe above:

    set DEV=<device name as ffmpeg prints it>
    ffmpeg -f dshow -i video="%DEV%" -c:v mjpeg -q:v 5
      -f mpjpeg -listen 1 http://0.0.0.0:8080/

MJPEG and not H.264 is a decision, not a default (D-66): per-frame coding has no GOP buffering.
If the lab computer must keep its own recording, add a second output to the **same** invocation
rather than starting a second process — one capture, two sinks.

**For both: prove the URL in VLC on the vision box before `dlc-link-live` ever sees it.** Media →
Open Network Stream; for T3 also tick *Use RTP over RTSP (TCP)*. VLC is a ten-second yes/no on the
same FFmpeg backend OpenCV uses, and it separates "the network path is wrong" from "the tool is
wrong". Report whether VLC showed a picture.

You may also need a Windows Firewall rule on the lab computer for the listening port. That is a
local rule; it does not register anything with lab IT.

---

**Step 1b — THE ZERO-CODE SOURCE TEST. Do this BEFORE installing the new wheel.**
*Write footprint: writes nothing.*

Whatever step 1/1a produced — a camera stream URL (T2), a forwarded URL (T3), a relay URL (T4) —
**it is a string, and a string already works with the `dlc-link` 0.1.0 you have installed**. That is
D-65's point: a forward and a relay both terminate in a URL, so no code in this phase is what makes
them work. Try it, in `cmd.exe`:

    set SRC=<the URL from step 1 or 1a>
    set MODEL=C:\MICS\dlc_test\exported-models-pytorch\<your>.pt
    set SMAP=C:\MICS\dlc_test\dlc_cam1_signals.py

    dlc-link-live --video "%SRC%" --model-path "%MODEL%" ^
      --signal-map "%SMAP%" --probe-pose

Report: whether it read a frame at all, and the full printed output.

~~**For T1 there is no URL — skip this step and say so**; step 3's `--source 0` is your first
test.~~ **T1 is unreachable (D-87):** the camera is in the rig room, `YizharGPU12` is in another
room, and the vision box has no DirectShow capture devices at all. There is no local-device path on
this rig — this step ALWAYS applies, with the relay URL.
**Under T5a the URL is the relay's** (`http://<lab-lan-ip>:8080/`) and this step applies unchanged —
the relay re-serves MJPEG over HTTP, which is a plain string to `cv2.VideoCapture`, so 0.1.0 can
already read it.
**If Step 1a-0 landed on T5b, stop here** and report that instead: no value of `--source` will help
*from the lab computer*. The next step is T6 — the camera moved to the vision box with the vendor's
driver installed there, which may make `--source 0` work outright — and only failing that, a shim on
the vendor's Windows SDK. Both are separate work.

---

**Step 2 — UPGRADE TO 0.2.0 FROM PyPI.**
*Write footprint: writes into the `mics-dlc` conda environment's site-packages only.*

Task 1 released `mics-dlc-link` 0.2.0 to PyPI and confirmed the index serves it. There is no wheel
to fetch and no share to reach (**D-86**). With the conda env active:

    python -m pip install --no-deps --upgrade mics-dlc-link
    python -m pip show mics-dlc-link

`--no-deps` is deliberate and unchanged in intent: every dependency is already present and this
install must change nothing else in the environment. Report `pip show`'s `Version` line — it must
read **0.2.0**, which is how we know which build the rest of this session exercised.

**If `pip` cannot reach pypi.org from the vision box**, that is a finding to report, not something
to work around by copying a file — record it and use the documented offline path (`pip download` on
a connected machine, then `pip install --no-index --find-links`), which fetches the same artifact
the index serves. Then:

    python -m pip check

Report its output verbatim. Then confirm the environment is still intact:

    python -c "import cv2, torch; print(cv2.__version__, torch.__version__)"

Report both version strings. If `cv2` is anything other than the headless build you had before, or
`torch` fails to import, **stop and report** — that is a broken environment, not a step to continue
through.

---

**Step 3 — DOES THE CAMERA OPEN AT ALL?**
*Write footprint: writes nothing.*

With the new build. **`%SRC%` is whatever step 1 decided**: `0` for T1, and the URL you proved in
VLC for T4 and **T5a** — which, on this rig, is the only live URL branch, since T2/T3 cannot carry a
GigE Vision camera (D-75). For a forwarded RTSP source (T3, not this rig),
`OPENCV_FFMPEG_CAPTURE_OPTIONS` must be set in this same `cmd.exe` window (step 1a) — a new window
does not inherit it. **Under T5b this step does not run at all.**

    set SRC=<0 for T1, else the relay/stream URL>
    dlc-link-live --source "%SRC%" --capture-only --max-seconds 10

`--capture-only` loads no model, connects to no Pi and sends nothing. Report the whole printed
summary, including the `source:` line (which says how your argument was read), the reported
`CAP_PROP_FPS` (which the tool prints but does not trust), the `CAP_PROP_BUFFERSIZE` line, and
`frames_read` with `rate_frames_read_per_s`.

**T1 only: if `--source 0` finds nothing, try 1, then 2.** Report which index worked. For T4 and
**T5a** the URL is the source and there is no index to try — if the URL opened in VLC but not here,
that is a tool problem worth reporting, not a reason to go hunting for device indices. **Under T5a
do not fall back to `--source 0`**: the relay is the only thing that can serve this camera to
`cv2`, and a device index on the vision box addresses some other device entirely.

**This number is the phase's baseline.** It is what the camera actually delivers to this machine
with no model in the way, and every keep-up statement later is measured against it.

---

**Step 4 — IS JUPYTER PRESENT?**
*Write footprint: writes nothing. The `--dry-run` in step 4b writes nothing either.*

    python -c "import jupyterlab, ipywidgets; print('jupyter present')"

Report the output, whether it is the success line or a `ModuleNotFoundError` naming which module.

**If it printed the success line, you are done with this step.** If it did NOT:

*Step 4b, and only if you actually want the notebook rather than the browser view:*

    python -m pip install --dry-run jupyterlab ipywidgets

**Report the resolver's whole plan verbatim and install NOTHING yet.** If it proposes to install,
upgrade or downgrade **`pyzmq`, `numpy` or `torch`**, the answer is no: `mics-link`'s transport is
`pyzmq`, `numpy` is pinned below 2 by `deeplabcut-live`, and `torch` is the whole GPU stack. In that
case we use `--view-sink mjpeg`, which is a browser tab on this machine and needs nothing installed
at all. That sink exists for exactly this outcome and choosing it costs you nothing.

---

**Step 5 — WHAT CAN THIS MACHINE REACH?**
*Write footprint: writes nothing.*

    curl -s -m 5 http://132.77.73.125:9000/pilots/live

Report whether it returned JSON, and if so, whether `RecordingBox` appears in it and what its
`state` and `active_run` are. (`curl` ships with Windows 10+; if it is missing, use
`python -c "import urllib.request;print(urllib.request.urlopen('http://132.77.73.125:9000/pilots/live',timeout=5).read()[:400])"`.)

    curl -s -m 5 http://132.77.73.217:9200

Report whether it returned an ElasticSearch banner, an authentication challenge, or nothing.

Both are optional for the viewer: unreachable simply means the status block says
`unavailable - <reason>` and the picture still works. We need to know which world we are in before
plan 38-05 writes it down as instructions.

---

**Step 6 — PACKAGE LEGITIMACY. Blocking, and not auto-approvable.**

Only if step 4b's plan was acceptable AND you want the notebook sink: before installing, open each
of these and report the maintainer shown, the latest version, the release date, and whether the page
links to the project's own GitHub organisation:

- `https://pypi.org/project/jupyterlab/`
- `https://pypi.org/project/ipywidgets/`

An unfamiliar maintainer, a brand-new project, or a name that is a near-miss of the one you expected
is a STOP.

**This rule CHANGED on 2026-09-10 (D-86).** `mics-link` and `mics-dlc-link` are now published by us
on PyPI and ARE installed from the public index — the old instruction ("never installed from a
public index") is obsolete and must not be followed. The names to trust are exactly
`mics-link` and `mics-dlc-link`, both owned by this lab and uploaded only by
`.github/workflows/publish-clients.yml` via Trusted Publishing. Note the distribution was renamed:
`dlc-link` (unprefixed) is NOT ours — if a project by that name appears on PyPI it is somebody
else's, and installing it is the near-miss STOP this paragraph is about.

**If you chose the mjpeg sink, skip this step entirely and say so.** Installing nothing is a
legitimate and preferred outcome.

---

**Report back, numbered:**

1. Step 0's manifest file count and total size.
2. **Step 1's TOPOLOGY verdict.** The camera is known to be T5 (D-75) — so what this reports is the
   `ipconfig /all` adapter list, whether the lab computer is also recording from the camera, and
   whether the camera's Ethernet could physically be moved to the vision box (**T6**).
3. **Step 1a-0's full `-list_devices` output verbatim, and the T5a/T5b verdict.** If T5a, the
   device name exactly as printed.
4. Step 1a's outcome if it applied: the `ffmpeg` relay command you ran and **whether VLC showed a
   picture**. (The portproxy/`OPENCV_FFMPEG_CAPTURE_OPTIONS` half does not apply to this camera —
   say `N/A (T5a relay, not T3)` rather than leaving it blank.)
5. Step 1b's result: the full printed output; or `N/A (T1 — no URL exists)`; or `N/A (T5b — session
   stopped at step 1a-0)`.
6. Step 2's `pip show` Version line, `pip check` output, and the cv2/torch version strings.
7. Step 3's whole summary, including which `--source` value worked, and the baseline
   `rate_frames_read_per_s`.
8. Step 4's answer, and step 4b's dry-run plan verbatim if you ran it.
9. Step 5's two answers.
10. Step 6's two pages, or the statement that you chose the mjpeg sink and installed nothing.
  </how-to-verify>
  <files>.planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-HARDWARE-VALIDATION.md</files>
  <action>
Present the checkpoint, wait for the user's report, then transcribe it into §0b, §1 and §2 of
`38-HARDWARE-VALIDATION.md`. Record each of the nine answers as its own row with a verdict, not as
prose. Specifically:

- **The topology verdict is recorded first, as a single named cell — `T1`..`T4`, `T5a`, `T5b` or
  `T6`.** A bare `T5` is not an acceptable value (D-75): it does not distinguish the branch that
  proceeds unchanged from the branch that stops the phase. Every later row is read against this
  cell: a `--source` value means something different under T1 than under T5a, and a reader six
  months from now must not have to reconstruct which world the numbers came from.
- **The camera's make and model is transcribed as a pre-existing fact with its provenance**
  (`DMK 33GP1300 BR2_UP 5810436`, reported by the lab computer's acquisition software, 2026-09-06),
  not as something this session discovered. The `-list_devices` output that decided T5a vs T5b is
  recorded verbatim beneath it, because that output IS the evidence for the verdict.
- If a hop was built (T3/T4), the exact `netsh` or `ffmpeg` command is recorded verbatim in §2, and
  the fact that a hop exists is carried into §5 — the live run's numbers are the hop's numbers too.
- The baseline `rate_frames_read_per_s` goes into §3 as the `capture-only` row. **It is the only
  number the keep-up criterion may be derived from**, so it is recorded before the inference run
  happens, where nobody can be tempted to adjust it afterwards.
- The working `--source` value is recorded verbatim in §2 and used unchanged in Task 3.
- The reachability answers decide which `--pilot` / `--orchestrator-url` / `--es-url` flags Task 3
  passes. If the orchestrator is unreachable, Task 3 runs without them and the status block says so;
  that is a recorded outcome, not a blocker.
- The sink choice is recorded, and if it is `mjpeg` the §0b legitimacy rows are marked
  `NOT APPLICABLE - nothing installed`, which is a better result than an approved install.

**Stop conditions — do not proceed to Task 3, record the finding and stop:**
- `pip show` reports anything other than 0.2.0.
- `cv2` is no longer the headless build, or `torch` fails to import.
- **Step 1a-0 returns T5b** — the `DMK 33GP1300` does not appear as a DirectShow device. It is a
  GigE Vision camera (D-75); `cv2.VideoCapture` cannot open it, no `--source` value exists, and no
  flag changes that. Record the full device list verbatim as the evidence, record that the next step
  is **T6 first, then a vendor-SDK capture shim** behind plan 38-01's injected-capture seam, and stop.
  Do not spend the session trying indices and URLs against a camera that speaks neither.

  **This whole stop condition is UNREACHABLE for this rig as of 2026-09-10 and is retained only
  for a future camera.** Step 1a-0 returned `DMK 33GP1300 [BR2_UP]`, so the verdict is T5a, not T5b.
  Ignore the ladder below unless a different camera puts you back here.

  ~~**Before assuming a shim is needed, record that T6 has not been tried.**~~ **T6 was tried and is
  IMPOSSIBLE (D-87): different rooms, the Ethernet cannot be moved.** Step (1) below is struck out
  for this rig; if a future camera lands on T5b, step (2) is where you start.
  ~~(1) move the camera's Ethernet to the vision box (or add a NIC), install the vendor driver
  there, and run the SAME `-list_devices` probe on the vision box — if the camera appears, T6
  collapses to T1, `--source 0` works, and no new code is needed at all;~~ (2) a shim on
  **The Imaging Source's IC Imaging Control 4 Python bindings (`imagingcontrol4`)** — the vendor's
  supported Windows API. **Not aravis**, which is a Linux/GObject stack and the wrong choice next to
  this conda environment's `torch`; it is the fallback only if the vision box ever becomes Linux.

  Carry into that shim's brief, from D-75: the frames are **Mono8** and need a 3-channel expansion
  before DLC; the shim must **not** go through `cv2.CAP_GSTREAMER` (the headless wheel has no
  GStreamer and a second `cv2` install is prohibited by D-58); and the device handle is the serial
  `5810436`.
- **T4 with the lab computer already recording from the camera, and no way to share it.** Exclusive
  device access means one of the two loses; that is the researcher's call, not the agent's.
- No `--source` value opens a camera at all on a T1-T4 topology. That is a hardware or driver
  problem, and the honest next step is the camera's own vendor tooling, not more flags.
  </action>
  <verify>
    <automated>python3 - <<'PY'
import re
t = open(".planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-HARDWARE-VALIDATION.md").read()
low = t.lower()

# --- assertions that hold on EVERY branch -------------------------------------
s1 = t.split("## 3")[0]
assert "pending" not in s1.split("## 1")[1], "section 1 still has a pending cell"
assert "DMK 33GP1300" in t, "the known camera model was not transcribed"
assert "dshow" in low, "the dshow probe that decides T5a vs T5b was not recorded"
assert "make" in low or "model" in low, "the camera make/model was not recorded"
for bad in ("latency of", "ms latency", "round trip"):
    assert bad not in low, "a latency claim reached the validation log: " + bad

# The verdict must be a DECLARED cell, not a T-string appearing anywhere in prose.
mv = re.search(r"(?im)^\s*(?:[|*-]\s*)?topology[^|\n]*[|:]\s*`?(T[0-9a-z]*)`?", t)
assert mv, "no declared TOPOLOGY verdict cell was found in section 1"
verdict = mv.group(1)
assert verdict != "T5", "the verdict cell reads a bare T5; D-75 requires T5a (relay works) or T5b (stop)"
assert verdict in ("T1", "T2", "T3", "T4", "T5a", "T5b", "T6"), \
    "unrecognised topology verdict %r; expected T1-T4, T5a, T5b or T6" % verdict

# --- branch: T5b is a legitimate, complete terminal outcome -------------------
if verdict == "T5b":
    assert "list_devices" in low, "T5b was declared without the device-list evidence that decides it"
    assert "mono8" in low or "3-channel" in low or "three-channel" in low, \
        "T5b stop did not carry D-75's Mono8 -> 3-channel constraint into the next step"
    assert "gstreamer" in low, "T5b stop did not carry the no-cv2.CAP_GSTREAMER constraint"
    assert "5810436" in t, "T5b stop did not record the serial as the device handle"
    print("discovery transcribed (T5b: phase stops here, and the stop is fully recorded)")
else:
    assert "0.2.0" in t, "the installed version was not recorded"
    assert "pip check" in t, "pip check output was not recorded"
    assert ("NOT APPLICABLE - nothing installed" in t) or ("[ASSUMED]" not in t), \
        "package legitimacy rows are still [ASSUMED] and nothing was recorded as installed-free"
    assert "rate_read_per_s" in t, "the capture-only baseline rate was not recorded"
    print("discovery transcribed (verdict %s)" % verdict)
PY</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the ten report items is transcribed into the validation log as its own row with a verdict; none is left `pending` and none is inferred from a different step. **Items 5-10 do not apply on the T5b branch** and are recorded as `N/A (T5b — session stopped at step 1a-0)`, which is a complete answer, not a gap.
    - §1 carries a TOPOLOGY cell reading exactly one of `T1`..`T4`, `T5a`, `T5b` or `T6` — never a bare `T5` — plus the camera's make and model recorded as a pre-existing fact with its provenance, and every later row that depends on the topology names it rather than assuming it.
    - Step 1a-0's `ffmpeg -list_devices` output is recorded verbatim as the evidence for the T5a/T5b verdict, and if T5a the DirectShow device name is recorded exactly as printed.
    - If the topology is T4 or T5a, the relay command is recorded verbatim and VLC's verdict on the URL is recorded. The T3 portproxy row reads exactly `N/A (GigE Vision is UDP; D-75)` — that one string, used in both the report and the log, rather than being left blank.
    - If the verdict is T5b, the log records the stop, the named next step in order (T6 + the vendor driver probed on the vision box first; a shim on The Imaging Source's `imagingcontrol4` only if that fails; aravis explicitly NOT the Windows answer), and the three carried constraints from D-75 (Mono8 → 3-channel, no `cv2.CAP_GSTREAMER`, serial `5810436` as the handle).
    - **On any branch that reaches step 3**, the baseline read rate is recorded in §3 BEFORE any inference run is performed, under §3's own column name `rate_read_per_s`. On the T5b branch there is no baseline and §3 reads `N/A (T5b)`.
    - **On any branch that reaches step 2**, the `pip show` Version line recorded is exactly `0.2.0`. On the T5b branch step 2 never runs and §0b/§2 say so.
    - `pip check`'s output is recorded verbatim, and the cv2/torch import line is recorded verbatim.
    - If a `--dry-run` was performed, its full resolver plan is recorded verbatim, and the log states explicitly whether it proposed to touch `pyzmq`, `numpy` or `torch`.
    - If anything was installed, §0b carries a human-verified legitimacy row for each package with the maintainer and release date the user reported; if nothing was installed, §0b says `NOT APPLICABLE - nothing installed`.
    - Every command handed to the user in this checkpoint has a stated write footprint; the user is instructed to refuse any that does not.
    - No number recorded from this step is described as, or converted into, a latency.
  </acceptance_criteria>
  <done>The camera's working address, the environment's real state, and what this machine can reach are all established by commands the user ran, recorded as rows with verdicts, before anything depends on any of them.</done>
  <resume-signal>Report the ten numbered items in full, including the verbatim outputs where asked. There are TWO approvable outcomes and they are not the same shape:

**(a) The camera works.** Type "approved" only after step 1a-0 has returned `T5a` (or the generic mapping has returned T1-T4), step 3 has actually opened a camera and produced a baseline read rate, and — if anything was installed — after the PyPI pages in step 6 were actually opened and their maintainers reported.

**(b) The camera cannot be opened.** Step 1a-0 returned `T5b`. This is a COMPLETE and approvable result, not a failure to report and not a reason to keep trying flags: type "approved — T5b" once the verbatim `-list_devices` output, the arrangement answers from step 1 questions 2-4 (including the T6 reachability answer), and the three carried constraints from D-75 are recorded. Steps 2 through 6 are marked `N/A (T5b)` and Task 3 does not run.

Do not type "approved" on branch (a)'s wording for a branch (b) outcome; the two are distinguished in the validation log and in the verify script.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CHECKPOINT (USER-RUN) — the live camera run, the annotated view, and the measured keep-up criterion</name>
  <read_first>
    - .planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-HARDWARE-VALIDATION.md §1, §2, §3 (Task 2's answers — the working `--source` value, the baseline rate, the reachability answers and the sink choice all come from there)
    - .planning/phases/35-deeplabcut-keypoint-likelihood-integration/35-HARDWARE-VALIDATION.md §5b and §5c (the measured likelihood distribution the authored thresholds came from, and the hysteresis defect — the reason a fresh scene may need fresh thresholds)
    - dlc_link/RUNBOOK.md, "Start order" (required: false means run first, then sender)
  </read_first>
  <what-built>
    A camera that opens (Task 2), a build that is provably 0.2.0 on the box, a baseline capture rate,
    and a viewer whose sink has been chosen. The backend fixture is Phase 35's, unchanged: lib 243
    v189, task def 626, pilot 3 config row 41 on port 5601.
  </what-built>
  <how-to-verify>
**All of this is run by YOU. The agent starts no run, stops no run, and touches no Pi.**

Substitute the `--source` value that worked in Task 2 step 3 wherever `%SRC%` appears, and drop the
`--pilot` / `--orchestrator-url` / `--es-url` flags if Task 2 step 5 found those unreachable.

---

**Part A — THE MEASUREMENT, with no run and no Pi involved.**
*Write footprint: both commands write nothing.*

Run A1, capture only, for a full minute:

    set SRC=<the value that worked>
    dlc-link-live --source "%SRC%" --capture-only --max-seconds 60

Run A2, the same source, the same minute, with the model loaded and `--dry-run` so nothing reaches
the Pi:

    set MODEL=C:\MICS\dlc_test\exported-models-pytorch\<your>.pt
    set SMAP=C:\MICS\dlc_test\dlc_cam1_signals.py

    dlc-link-live --source "%SRC%" --model-path "%MODEL%" ^
      --signal-map "%SMAP%" --dry-run --max-seconds 60

Report **both** whole summaries. The four numbers that matter are A1's `rate_frames_read_per_s`,
A2's `rate_frames_inferred_per_s`, A2's `frames_skipped`, and A2's `processor` snapshot.

*What these mean, so the numbers are read correctly:* A1 is what the camera delivers. A2 is what the
model can consume from it. `frames_skipped` is the difference, counted rather than inferred. A large
`frames_skipped` is **not** a fault — the decimator caps the outbound signal rate at 10 Hz per signal
regardless, so skipped frames cost the FDA nothing until the inference rate approaches the rate at
which the animal's position meaningfully changes. What we are establishing is where that line is for
THIS camera and THIS model, which nobody has measured.

---

**Part B — THE LIVE RUN.**
*Write footprint: the sender writes nothing; starting the run writes to the MICS database through
the web UI, and nothing on your filesystem.*

Pilot 3's `dlc_cam1` row is `required: false`, so the order is **run first, then sender**. The
reverse produces a flushed backlog delivered in a burst on connect, which looks like a working run
and is not one.

1. Start a run on pilot 3 / `RecordingBox` with task definition 626 `dlc_demo`. Record the run id
   and the session id.
2. Then start the sender **with the view on**:

       set SRC=<the value that worked>
       set MODEL=C:\MICS\dlc_test\exported-models-pytorch\<your>.pt
       set SMAP=C:\MICS\dlc_test\dlc_cam1_signals.py
       set OVL=nose_likelihood>0.6,nose_x>0.50,nose_y>0.50

       dlc-link-live --source "%SRC%" --model-path "%MODEL%" ^
         --signal-map "%SMAP%" --host 132.77.73.213 --port 5601 ^
         --source-id dlc_cam1 --view --view-sink <notebook|mjpeg> ^
         --view-min-likelihood 0.6 --overlay "%OVL%" ^
         --pilot RecordingBox ^
         --orchestrator-url http://132.77.73.125:9000 ^
         --es-url http://132.77.73.217:9200 ^
         --max-seconds 300

   Port **5601**, not 5599 — pilot 3 binds both and the wrong one succeeds into the wrong fixture.
   For the mjpeg sink add `--view-port <a free port>` and open the `http://127.0.0.1:<port>/` URL the
   tool prints. For the notebook sink, open `notebooks/live_view.ipynb`, fill the parameters cell
   with these same values, and run it.
3. Watch it for the whole five minutes, with an animal in the arena if one is available. If none is,
   say so — the run still proves the camera path; it just cannot prove anything about tracking an
   animal, and the log will say that.
4. Stop the run.

**What to report about what you SAW, in your own words** — this is the deliverable the researcher
asked for and no counter substitutes for it:

- Did the keypoint marker follow the mouse? Did it lag visibly behind it, sit still, or jump around?
- Were the likelihood numbers on the labels plausible — high when the mouse was clearly visible, low
  when it was occluded?
- Did the threshold lines appear where you expected them in the arena?
- Did the `condition: HOLDS` line change at moments that made sense?
- Did the FDA state line show a real state name, and did it change during the run?
- Did the view keep up, or did it visibly stutter while the counters kept climbing?
- Anything that looked wrong, however small.

---

**Part C — AFTER-MANIFEST. Run this once the run is finished.**
*Write footprint: writes one text file, outside your project.*

    $P = 'C:\Users\YizharGPU12\Desktop\Gili\MultiMice-Gili-2026-06-21'
    Get-ChildItem -Recurse -File $P |
      Select-Object FullName,Length,LastWriteTimeUtc |
      Sort-Object FullName |
      Out-File -Encoding utf8 C:\dlc-work\manifest-after-38.txt

    Compare-Object (Get-Content C:\dlc-work\manifest-before-38.txt) `
                   (Get-Content C:\dlc-work\manifest-after-38.txt)

Report the comparison output verbatim. **Empty is the expected and required result.** Any difference
at all — including a changed `LastWriteTimeUtc` on a file whose size is unchanged — is reported as-is
and not explained away.

---

**Report back, numbered:**

1. Part A's two whole summaries.
2. The run id and the session id.
3. Part B's whole sender summary: the loop counts, `frames_skipped`, the viewer counters,
   `processor`, and `link.stats.snapshot()` (`enqueued`, `sent`, `dropped`, `abandoned`,
   `send_failed`). `stats.dropped` is the only end-to-end drop accounting that exists; a frame lost
   at ZMQ's high-water mark is counted nowhere on the Pi.
4. Your account of what you saw, per the bullets above.
5. From ElasticSearch on `132.77.73.217`, index `event_log_v2`, isolated with
   `subject: bp_s<session>_r<run>`: every `state_transition` document with its timestamp and target
   state, and the `nose_likelihood` / `nose_x` / `nose_y` `value_raw` values seen immediately before
   each transition. Read `value_raw`, not `event_data.value` — the latter is an int-cast and shows 0
   for 0.7.
6. Roughly how many `Tracker` documents per second appeared for the `dlc_cam1` signals, as a count
   per second.
7. Anything in the pilot journal that looked wrong, including tracebacks, and whether the
   `single_animal=True` guard ever fired.
8. Part C's `Compare-Object` output verbatim, and whether it was empty.

Do not compute, report or infer any latency from any timestamp in any of this. The vision box's
clock and the Pi's clock are different domains; comparing them measures NTP skew.
  </how-to-verify>
  <files>.planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-HARDWARE-VALIDATION.md</files>
  <action>
Present the checkpoint, wait for the user's report, and transcribe it into §3 through §8 of
`38-HARDWARE-VALIDATION.md`.

**Then derive the keep-up criterion in §4, showing the arithmetic**, from A1 and A2 and nothing
else:

- `baseline = A1.rate_frames_read_per_s`
- `achieved = A2.rate_frames_inferred_per_s`
- `ratio = achieved / baseline`
- `--min-rate` = a value set BELOW the observed `achieved` with a stated margin, so an ordinary run
  does not trip it and a genuine degradation does. State the margin and why that margin. **Do not
  round to a familiar number** and do not reach for 30, 10 or 1 because they look tidy — Phase 35
  chose a threshold that way once (§5c) and produced 4 ms state dwells.
- State explicitly what the number does NOT mean: it is not a latency, it is not a guarantee about
  any individual frame, and it is not transferable to a different camera, a different model or a
  different GPU. It is this rig's number.

Record the researcher's account from item 4 **verbatim, in their own words**, as its own subsection.
It is the only evidence about whether the marker actually followed the mouse, and paraphrasing it
into counters destroys exactly the thing it proves.

Fill the §7 per-truth ledger: every truth in this plan's `must_haves` becomes exactly one of
`PROVEN` / `PARTIAL` / `UNPROVEN` / `NOT EXERCISED` / `NOT DONE` / `HELD`, with the evidence named.
Then write the score line. **If a truth is not proven, say so** — Phase 35's §7 is the model, and its
value came entirely from being willing to write `IMPOSSIBLE AS WRITTEN` next to its own plan's demo.

Record in §6 whether `Compare-Object` was empty. Empty is recorded as
`RESEARCHER PROJECT UNCHANGED`; anything else is recorded verbatim as a `D-64 VIOLATION` together
with which command is suspected of having caused it. Do not summarise a non-empty diff as "just a
timestamp"; paste it.

**Expected outcomes that are results, not failures, and must be recorded as such:**
- The transition fires constantly, or never. The thresholds came off a five-mouse video file
  (`35-HARDWARE-VALIDATION.md` §5b), and this camera sees a different scene. Record the fresh
  distribution; re-deriving the threshold is plan 38-05's input.
- `frames_skipped` is large. Record it and the ratio; that is what the measurement is for.
- The FDA state line reads `unavailable`. Record the reason string; the frame view is the deliverable
  and it is independent of that.
- **The model tracks the animal poorly, or the likelihoods sit low.** Before attributing this to
  plumbing, record that this camera is **monochrome** (D-75): if the exported model was trained on
  colour footage, its input distribution has changed and the degradation is the model's, not the
  capture path's. Record the likelihood distribution and say which explanation the evidence
  supports; do not tune thresholds around a mismatch that a re-train or a colour camera would fix.
  </action>
  <verify>
    <automated>python3 - <<'PY'
import re
t = open(".planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-HARDWARE-VALIDATION.md").read()
assert "capture-only" in t and "with-inference" in t, "section 3 is missing one of the two runs"
assert "--min-rate" in t, "the derived keep-up criterion was not recorded"
assert re.search(r"ratio", t, re.I), "section 4 does not show the ratio arithmetic"
assert ("RESEARCHER PROJECT UNCHANGED" in t) or ("D-64 VIOLATION" in t), \
    "the manifest comparison was not classified"
verdicts = len(re.findall(r"\b(PROVEN|PARTIAL|UNPROVEN|NOT EXERCISED|NOT DONE|HELD)\b", t))
assert verdicts >= 10, "the per-truth ledger is incomplete: %d verdicts found" % verdicts
assert "pending" not in t.split("## 7")[1], "the ledger still has a pending cell"
low = t.lower()
for bad in ("latency of", "ms latency", "round trip"):
    assert bad not in low, "a latency claim reached the validation log: " + bad
print("rig session transcribed, ledger complete")
PY</automated>
  </verify>
  <acceptance_criteria>
    - §3's table has both the capture-only and the with-inference rows filled with real numbers, and §4 shows the ratio arithmetic and the chosen `--min-rate` with its stated margin and the sentence saying what the number does not mean.
    - The researcher's account of what they saw is recorded verbatim, in their own words, in its own subsection — not compressed into counters.
    - At least one `state_transition` document is recorded with the `value_raw` values immediately preceding it, OR the log states explicitly that none fired and records the observed likelihood distribution instead.
    - The sender's `link.stats.snapshot()` is recorded verbatim.
    - §7 assigns exactly one verdict to every truth in this plan's `must_haves.truths`; none is left `pending` and none is inferred from a different run. The score line is present.
    - §6 records the `Compare-Object` output verbatim and classifies it as `RESEARCHER PROJECT UNCHANGED` or as a `D-64 VIOLATION`.
    - No number anywhere in §3, §4 or §5 is described as a latency, and no timestamp from the vision box is compared against one from the Pi.
    - If any truth is `UNPROVEN` or `NOT EXERCISED`, §7 states what the next step for it is. The plan is not reported complete on a green subset.
  </acceptance_criteria>
  <done>A camera filming the rig drove the model into pilot 3, the researcher watched it in their own words, the keep-up criterion came off a measurement with its arithmetic shown, and every truth in this plan carries exactly one verdict.</done>
  <resume-signal>Report the eight numbered items in full, including Part A's two summaries and Part C's comparison output verbatim. Item 4 — what you actually saw on screen — is required in your own words and cannot be substituted by the counters. Type "approved" only after a live run was performed with the view open, or state which part could not be done and why.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the vision box -> pilot 3 `:5601` | Keypoints from a live camera now drive an FDA on a device actuating hardware in an animal's cage, with no file in between to inspect first. |
| the researcher's DLC project directory | A read-only asset belonging to a collaborator, which a previous phase wrote into. |
| PyPI -> a conda env holding a working GPU stack | A public index reached over the network. Narrower than the SMB path it replaced (D-86): the artifact is served over TLS from an index that records a signed provenance attestation, is uploaded only by this repo's workflow through Trusted Publishing with no token that can be stolen, and `pip` verifies its hash against the index. The residual risk moves from "unverifiable file" to "right name, wrong project". |
| a listening MJPEG socket on the vision box | A new service running while an experiment is live. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-30 | Tampering | writing into the collaborator's DLC project | mitigate | A recursive manifest before and after, compared with `Compare-Object` and recorded verbatim (D-64). This phase runs no `export_model`, so the expected diff is empty by construction. Phase 35's §8 is why this is a task and not an intention. |
| T-38-31 | Spoofing | the sender reaching pilot 3's OTHER bound port (5599, `ExtlinkDemo`) | mitigate | `--port` has no default and 5601 is stated in the command, in the fixture table, and in the runbook. A wrong-but-open port succeeds into the wrong fixture and is far more expensive to debug than a closed one. |
| T-38-32 | Tampering | the vision box running a build nobody can identify | mitigate | D-63's version bump to 0.2.0 plus a recorded `pip show` Version line. Under D-86 this is stronger, not weaker: PyPI is immutable, so 0.2.0 names exactly one artifact for all time and cannot be quietly re-cut. Republishing 0.1.0 is now rejected by the index rather than merely inadvisable. |
| T-38-33 | Tampering | installing a same-named package that is not ours | mitigate | The SMB transit risk is RETIRED with the share (D-86); this replaces it. The distribution is `mics-dlc-link` — the unprefixed `dlc-link` is not ours and installing it is the near-miss failure §0b exists to catch. `pip` verifies the index-published hash, the upload path is Trusted Publishing with no token to steal, and `--no-deps` bounds what the install can change. |
| T-38-34 | Elevation of Privilege | an install disturbing `pyzmq`, `numpy` or `torch` and breaking the DLC stack | mitigate | `--dry-run` first, a blocking human review of the resolver plan, an explicit refusal list, and the MJPEG sink as the path that installs nothing at all. `pip check` and a `cv2`/`torch` import are run afterwards as the proof. |
| T-38-35 | Repudiation | a keep-up threshold that nobody can trace to a measurement | mitigate | §4 records the arithmetic, both source numbers and the chosen margin, and states what the number does not mean. `--min-rate` has no default so no unmeasured value can be inherited silently. |
| T-38-36 | Information Disclosure | live footage of the lab served by the MJPEG sink | mitigate | Bound to `127.0.0.1` only (plan 38-03, grep-gated); nothing outside the vision box can reach it. |
| T-38-37 | Denial of Service | the sender's rate starving the Pi's shared Tornado IOLoop, degrading FDA timing and STOP | mitigate | Unchanged from Phase 35 and NOT re-opened here: the decimator's per-signal 10 Hz cap governs the wire, and 3 signals x 10 Hz = 30 msg/s against a proven ~60 msg/s envelope. A faster camera increases INFERENCE load, not ZMQ load — the two limits are different and this plan does not conflate them. |
| T-38-38 | Repudiation | reporting a rig session as successful on a green subset | mitigate | §7's per-truth ledger requires exactly one verdict per truth and the document itself carries the instruction not to mark the plan done on §1 alone — copied from `35-HARDWARE-VALIDATION.md`, which needed it. |
| T-38-SC | Tampering | npm/pip/cargo installs | mitigate | The only candidate installs are `jupyterlab` and `ipywidgets`, and only if Jupyter is absent AND the notebook sink is preferred over the browser sink. `--dry-run` first, a BLOCKING human legitimacy check on `pypi.org` for each, never auto-approvable regardless of `workflow.auto_advance`, and a hard refusal if the resolver proposes `pyzmq`/`numpy`/`torch`. The `dlc-link` wheel is first-party, installed `--no-deps` from our own build with a recorded sha256, and is never fetched from a public index. |
</threat_model>

<verification>
- `38-HARDWARE-VALIDATION.md` has no `pending` cell left in §1 through §7 after Task 3, or states explicitly why one could not be filled.
- `git status --porcelain dlc_link/` shows only `pyproject.toml`; `git status --porcelain api/ sdk/ orchestrator/ web_ui/` is empty.
- No document produced by this plan contains a latency, jitter or drift figure.
- The word `pending` appears in the final document only inside sections deliberately left for a future session, each with a stated reason.
</verification>

<success_criteria>
- A camera filming the rig drives the model, and the keypoints reach pilot 3 as declared signals.
- The researcher watched the annotated frames while the task ran and said, in their own words, whether the marker followed the mouse.
- The camera's delivered rate and the model's achieved rate are both measured, and `--min-rate` is derived from them with the arithmetic shown.
- The collaborator's DLC project directory is provably unchanged.
- Every truth carries exactly one verdict, and the unproven ones are named as unproven.
</success_criteria>

<output>
Create `.planning/phases/38-live-camera-source-and-annotated-live-view-for-dlc-driven-mi/38-04-SUMMARY.md` when done.
</output>
